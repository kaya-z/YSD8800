**emu23 デバイス設計書**

YSD8002 MMIO化 / YSD8003 ストレージ / YSD8004 割り込みコントローラ / SYSCALL設計
**／ MMIOアドレスデコード層（v1.6 新設・改良2）**

~~Version 1.5  /  2026-07-13~~  **Version 1.6  /  2026-08-11**

| **項目** | **内容** |
| --- | --- |
| ファイル名 | ~~emu23_device_design_v1_5.docx~~ → **emu23_device_design_v1_6.md**（★v1.6 で拡張子を是正★ v1.5 までは拡張子 `.docx` でありながら実体は UTF-8 プレーンテキスト（Markdown）であった。KY41 4点整合の観点から実体に合わせた。過去版は版数がファイル名に含まれるため参照解決に支障なし） |
| 対象ツール | emu23 v1.11 → **v1.12**（本版で改良2 を追加。版数確定は B-1 と同時＝Phase B' 完了時に一括発番） |
| ISAバージョン | YSD8800 ISA2.3 |
| 工程 | Phase B' / **B-C**（emu23 大規模改修・改良2 バス・プルアップ） |
| ステータス | **v1.6 レビュー待ち**（実装未着手・原則43 によりレビュー承認前は実装しない） |
| 関連設計書 | emu23_interactive_mode_design_v1_6.md（B-1・完了）／yuios_memmap_design_v2_4.md §15（MMIO空間定義）／YSD8800_MMU_Design_v1_2_0.docx |

| **版** | **日付** | **変更内容** | **担当** |
| --- | --- | --- | --- |
| v1.0 | 2026-04-17 | 初版作成 | Claude |
| v1.1 | 2026-04-18 | レビュー対応: IRQ優先順位・ネスト仕様追加 / IRQ_STAT Write-to-Clear変更 / BUSY挙動明確化 / SW_BUSY状態遷移追加 / BUF_PTRオーバーラン仕様追加 / Aレジスタ退避ABI明記 / MMU特権性明記 | Claude |
| v1.2 | 2026-04-19 | 追加レビュー対応: ①IRQ3をNMI扱いと明文化 ②BUSYラッチ方式（最低1回BUSY=1返却） ③IRQ_STATビット独立性明記 ④SW_START再発行時カウンタリセット明記 ⑤SYSCALL ABI改定（戻り値=Bレジスタ・A=caller-saved） | Claude |
| v1.3 | 2026-06-22 | Step 8-I対応: §3.4新設（IRET時タイマー再設定はタイマーIRQ復帰時のみ＝ysd8002_timer_design §5.3準拠の明文化・emu23 v1.07実装）／§3.5新設（IRQ2優先選択を将来課題として記録） | Claude |
| v1.4 | 2026-07-12 | FPGA V3.7（YSD8004 RTL実装）完了に伴う是正: ①§3.1「IRQ2＝予備」行を削除（★ISA2.3にIRQ2は存在しない★・ISA2.3 §8.1/§8.2実源照合済／irq_id=3はアラインメント例外）②§3.5「IRQ2優先選択」将来課題をクローズ（前提そのものが誤りだったため。責務の帰属誤りの訂正を含む）③§3.2に実質序列 timer/align > IRQ1 > SYSCALL を明記（emu23 ysd8004_raise() pending保護の実装事実）④§3.3構成図からIRQ2を削除。根拠: v3_7_design_memo_v0_3.md §2 | Claude |
| v1.5 | 2026-07-13 | FPGA V4（YSD8001 UART RTL実装）S5(c) CPU結合検証完了に伴う是正: ①§3.3 注3を訂正（★割込入力規約は「一律1クロックパルス」ではない★。RX/STOR＝1クロックパルス、TX(TDRE)＝レベル。ysd8001 `assign irq_tx_o = stat_r[BIT_TX_READY]` 実源照合）②§3.2.2 新設: irq_pending のラッチは IE を見ない（emu23 `ysd8004_raise()` / RTL `S_IRQCHK` とも FL_IE 非参照）。その帰結として【割込ハンドラは冪等に書かねばならない】という OS 設計上の必須要件を明文化。根拠: S5(c) c6 実測（cnt=2）／emu23 L314-329・L1176／RTL L590・L1223-1225 | Claude |
| **v1.6** | **2026-08-11** | **【改良2：バス・プルアップ／MMIOアドレスデコード層の新設（Phase B' B-C）】**①**§2.3 新設**：MMIO アドレスデコード層の位置づけを定義。**現行 emu23 にはデコード層が存在せず**、`rd8`/`rd16`/`wr8`/`wr16` が `if` チェーンで個別判定し、非一致アドレスは**通常メモリ `mem[]` にフォールスルー**していた（＝未接続 MMIO が RAM として振る舞う）実装事実を明記。②**§4.1 是正**：`$FC86` を「（予備）将来拡張用」から **UART_BAUD（実装済）** に是正（`emu23_v111.c` L599 実源照合。v1.2 で実装されながら設計書が追従していなかった）。③**§4.2 新設**：**未接続 MMIO 領域の応答仕様**を定義（プルアップ模倣＝読出 `$FF`/`$FFFF`・書込は捨てる）。④**§4.3 新設**：**未接続アクセスの検知機構**（α＝警告ログ既定・β＝`--strict-mmio` で停止）。`$FFFF` が IPC 番兵 `IDX_NIL` と衝突し誤アクセスが正常系に化ける問題への対処。⑤**§5.5／§6.6 新設**：**8bit アクセス経路のデバイス被覆漏れの解消**（現行 `rd8`/`wr8` は YSD8002 全8本・YSD8003 の7本・YSD8004 の2本を未処理で `mem[]` に落としていた）。⑥**§9 新設**：**RTL 追随要件**（`ysd8800_mmio_stub_v0_7.sv` L458 の `8'h00` は実機のプルアップと矛盾する捏造値。`8'hFF` への是正を設計として規定し、実装はキャッシュRTL工程＝軸A 冒頭で実施＝案Q）。v1.5 までの記述は削除せず保持。 | Claude |

# **1. 概要**

本設計書はemu23（YSD8800 ISA2.3エミュレータ）に実装するデバイス群の設計を定める。

| **デバイス** | **チップ名** | **説明** |
| --- | --- | --- |
| タイマー（MMIO化） | YSD8002 | 既存タイマーを関数直接呼び出しからMMIO方式に変更 |
| ストレージ | YSD8003 | SDカード相当ブロックデバイス（emu23ではファイルイメージ） |
| 割り込みコントローラ | YSD8004 | 複数デバイスの割り込みを集約しIRQ1に接続 |

# **2. デバイス設計方針**

## **2.1 デバイス制御はMMIO**

全てのハードウェアデバイス制御はMMIOレジスタへの読み書きで行う。FPGA実装との対応関係が明確になり、デバイスドライバの移植性が高まる。

## **2.2 OSサービスはSYSCALL**

カーネルが提供するOSサービス（プロセス管理・ファイルシステム等）はSYSCALL命令を使用する。従来のSYSCALL 0x0010/0x0011（Dhrystone計測）はYSD8002 MMIOに移行し廃止する。

| **項目** | **変更前（emu23 v1.00）** | **変更後（emu23 v1.01以降）** |
| --- | --- | --- |
| SYSCALL処理 | emu23 Cコードでif/else判定 | 単純にIRQ4を発火するだけ |
| デバイス制御 | SYSCALLフック経由 | MMIO直接アクセス |
| ディスパッチ | emu23側で処理 | YSD8800側ディスパッチテーブル |

## **2.3 ★MMIOアドレスデコード層（v1.6 新設・改良2）★**

### 2.3.1 現行実装の事実（v1.5 まで未記載）

**現行 emu23（v1.11）には「アドレスデコーダ」という層が存在しない。** `rd8()`／`rd16()`／`wr8()`／`wr16()` の4関数がそれぞれ独立した `if` チェーンで個別レジスタのアドレスを逐次判定し、**いずれにも一致しなかったアドレスは通常メモリ経路へフォールスルーする**（`emu23_v111.c` L610-679 ほか実源照合済）。

その帰結として、**未接続 MMIO 空間は「読めば `mem[]` の実体値を返し、書けば `mem[]` に書き込まれて読み返せる」＝ RAM として振る舞う**。

| | 従来の想定 | **実装の実際（v1.6 で判明）** |
| --- | --- | --- |
| 未接続 MMIO 読出 | `$0000` を返す | **`mem[]` の実体値（不定）** |
| 未接続 MMIO 書込 | 捨てられる | **`mem[]` に格納され読み返せる** |

これは `yuios_memmap_design_v2_4.md` L225 の規約「**$FC80 以降は MMIO 専用領域。RAM として使用してはならない**」に対し、**エミュレータ側が規約違反を検出できない**状態であることを意味する。

### 2.3.2 デコード層の責務

改良2 は「既定値の変更」ではなく、**デコード層の新設**である。本層は次の責務を負う。

1. **アドレスが MMIO 空間（`$FC80`〜`$FFFF`）に属するかを判定する**（空間の定義は memmap v2.4 §15 に従う）
2. MMIO 空間内であれば、**実装済みデバイスへ振り分ける**（8bit／16bit を統一的に扱う。§5.5／§6.6）
3. MMIO 空間内で**どのデバイスにも該当しなければ「未接続」**として扱い、§4.2 の応答仕様を適用する
4. 未接続アクセスを**検知して記録する**（§4.3）
5. MMIO 空間外であれば従来どおり通常メモリ経路へ渡す

### 2.3.3 設計原則

- **本層は既存の `if` チェーンを置き換えるのではなく、その外側に設ける。** 既存のデバイス判定ロジックは無改変のまま残し、差分を最小化する（退行時の切り戻しを容易にするため）。
- **実機忠実性と検証容易性を両立させる。** 返す値は実機（プルアップ）と同一にしつつ（§4.2）、**実機にはできない「誤アクセスの記録」をエミュレータの責務として担う**（§4.3）。両者は排他ではない。
- **段階導入とする。** デコード層の新設・被覆漏れ解消（B-C-1a）と、プルアップ既定値の適用（B-C-1b）は**実装・回帰とも分離**する。混在させると回帰差分の由来が切り分け不能になる。

# **3. IRQ仕様**

## **3.1 IRQ優先順位**

| **優先順位** | **IRQ番号** | **用途** | **種別** | **備考** |
| --- | --- | --- | --- | --- |
| 0（最高） | IRQ3 | アラインメント例外 | NMI | IE=0中でも即時発生（v1.2確定） |
| 1 | IRQ0 | YSD8002 タイマー | マスク可 | 外部デバイス最高位・YSD8004経由なし |
| 2 | IRQ1 | YSD8004経由（複数デバイス） | マスク可 | YSD8001 UART / YSD8003 ストレージ完了等 |
| ~~3~~ | ~~IRQ2~~ | ~~予備~~ | ~~マスク可~~ | **★v1.4削除★ ISA2.3にIRQ2は存在しない（ISA2.3 §8.1/§8.2 実源照合済）。ベクタ$0006 は irq_id=3＝アラインメント例外に割り当て済みであり、「IRQ2」という割込源は本設計書の記載誤りであった** |
| 3（最低・旧4） | IRQ4 | SYSCALL | マスク可 | 実質最低位（§3.2実質序列を参照） |

**【v1.4 重要訂正】ISA2.3 に「IRQ2」という割込源は存在しない。** v1.3以前の本表は誤りであった。ISA2.3のベクタ割当は `$0000`=RESET / `$0002`=IRQ0(timer) / `$0004`=IRQ1(YSD8004) / `$0006`=IRQ3(align) / `$0008`=IRQ4(SYSCALL) であり、空きベクタ枠は `$000A` / `$000C` / `$000E` の3枠である。将来の専用ベクタ割込はこの空き3枠を使用する（FPGA V3.7 レビュー確定事項）。

**【重要】IRQ3（アラインメント例外）はNMI（Non-Maskable Interrupt）扱いとする。IE=0中（割り込み禁止中）であっても即時発生する。アラインメント違反はプログラムバグによる例外であり、無視して処理を続けることは危険なためである。**

## **3.2 同時発生時の動作**

| **ルール** | **説明** |
| --- | --- |
| NMI優先 | IRQ3はIE状態に関わらず最優先で即時受理する |
| 優先処理 | IE=1のときpending中で最も優先順位の高いIRQを受理する |
| 低優先IRQ保持 | 低優先IRQはpendingとして保持し、IRET後に再評価して受理する |
| ネスト禁止 | IRQ受理時にIE=0となるため、ハンドラ実行中は原則ネスト禁止 |
| ★IE非依存ラッチ（v1.5追記）★ | 上記「ネスト禁止」は**受理**の抑止であり、**`irq_pending` のラッチは IE=0 中も継続する**。その結果ハンドラは「余分に1回」起動しうる。**ハンドラは冪等に書くこと**。詳細は §3.2.2 参照 |
| ネスト許可 | ハンドラ内でEI命令を実行した場合のみ高優先IRQのネスト割り込みを許可する |
| IRET後再評価 | IRET実行時にpending中IRQがあれば次命令フェッチ前に再評価・受理する |

### **3.2.1 実質序列（v1.4新設・FPGA V3.7で確定）**

emu23 の実装事実として、`irq_pending` の調停は **2段構え**であり、その結果生じる実質的な序列は次のとおりである。

```
timer(IRQ0) / align(IRQ3)  >  IRQ1(YSD8004経由)  >  SYSCALL(IRQ4)
```

**【第1段】pending保護**（`ysd8004_raise()` 内）
後着の IRQ1 は、先着の timer(pending=1) / align(pending=3) を**蹴落とさない**。一方 SYSCALL(pending=4) は IRQ1 に**譲る**。

**【第2段】IRQ_STAT 再評価**（毎命令）
第1段の保護によって後回しにされた IRQ1 は、`IRQ_STAT != 0` である限り毎命令再評価され、IRET後に復活する。したがって IRQ1 は**取りこぼされない**。

**【FPGA実装への写像】**
第2段は、YSD8004 の `irq1_o` を**レベル出力**（`IRQ_STAT != 0`）とすることで自動的に成立する（V3.7 で実証済）。第1段（優先度比較）は V5（Timer）実装時の設計論点として残る。詳細は `v3_7_design_memo_v0_3.md` §4.4b を参照。

### **3.2.2 ★IE 非依存ラッチとハンドラ冪等性要件（v1.5新設・FPGA V4 S5(c)で確定）★**

#### **【事実】`irq_pending` のラッチは IE を見ない**

emu23・FPGA RTL のいずれも、`irq_pending` への**ラッチ**（取り込み）は `IE`（`FL_IE` / `flags_ie`）を**参照しない**。`IE` を見るのは**受理**（accept）のみである。

| 段階 | emu23 実源 | FPGA RTL 実源 | IE を見るか |
| --- | --- | --- | --- |
| **ラッチ**（取り込み） | `ysd8004_raise()` L314-329<br>`IRQ_STAT`再評価 L1576-1578 | L1223-1225<br>`if (state == S_IRQCHK && irq_in != 3'd0) irq_pending <= irq_in;` | ★**見ない**★ |
| **受理**（accept） | L1176<br>`if (cpu.irq_pending >= 0 && (cpu.flags & FL_IE))` | L590<br>`if (irq_pending != 3'd0 && flags_ie)` | **見る** |

#### **【帰結】ハンドラ実行中（IE=0）でも pending は積まれ続ける**

§3.2 の「ネスト禁止」（受理時 `IE=0`）は**受理の抑止**であって、**ラッチの抑止ではない**。したがって:

> **ハンドラが割込源を沈黙させても、沈黙させる「前」に既にラッチされた `irq_pending` は消えない。`IRET` で `IE=1` に戻った瞬間、その pending が受理され、ハンドラが「もう1回だけ」起動する。**

これは異常ではなく、**emu23 と FPGA RTL が一致した正しい挙動**である。

#### **【MC6809 との対応】**

これは MC6809 の `IRQ`/`FIRQ` でも起きる古典的挙動である。6809 において `CC` の `I` ビットを立てても、既に内部でラッチされた要求は `RTI` 後に処理される。

**OS-9 の IRQ ポーリングルーチンが「デバイスの STAT を読み、自分の仕事が無ければ何もせず即 RTI」という構造になっているのは、まさにこの「余分な1発」を吸収するためである。** YSD8800 でも同じ規律が必要となる。

#### **【★OS設計上の必須要件★】割込ハンドラは冪等（idempotent）に書くこと**

YUI OS を含む YSD8800 上のすべての割込ハンドラは、次を満たさねばならない:

1. **冒頭でデバイス STAT を読み、自分に処理すべき仕事があるかを判定する。**
2. **仕事が無ければ、何もせず直ちに `IRET` する。**（＝ 空振り起動を許容する）
3. **2回以上起動しても状態が壊れないこと。**（カウンタの二重加算・バッファの二重取り出し等を起こさない）

★**「ハンドラが必ず1回だけ呼ばれる」ことを前提にしたコードを書いてはならない。**★

#### **【実証】FPGA V4 / S5(c) c6（UART_TX_IRQ_TDRE）**

TX(TDRE) はレベル（§3.3 注3）であるため、この性質が最も顕著に現れる。

- ハンドラが**1回目の起動で**即座に `IRQ_MASK` bit2 をセットし `irq1_o` を落とす。
- にもかかわらず、ハンドラ実行中（`IE=0`）に `irq_pending ← 2` が再ラッチされている。
- `IRET` 後にそれが受理され、ハンドラは**合計2回**起動する（実測 `cnt=2`）。

したがって「ハンドラ起動回数 = 2」が、**TDRE がレベル信号であることの証明**として機能する（パルスであれば `cnt=1` となる）。

#### **【V5（Timer）への申し送り】**

`v3_7_design_memo_v0_3.md` §4.4b に記載の「第1段（timer/align の pending 保護）」を FPGA RTL に実装する際、この IE 非依存ラッチの性質を前提とすること。**保護ロジックを `flags_ie` で条件付けてはならない**（emu23 と乖離する）。

## **3.4 IRET時のタイマー再設定（v1.3新設・Step 8-I）**

タイマー（YSD8002）の次回IRQ発火サイクル再設定（YSD8002_iret相当）は、
**タイマーIRQ（IRQ0）から復帰するIRETでのみ実行する。** 他のIRQ
（IRQ1=YSD8004経由・IRQ3=アラインメント・IRQ4=SYSCALL）のIRETでは
タイマー再設定を行ってはならない。これは ysd8002_timer_design_v1_0.docx §5.3
「IRQ0（タイマー）のみを対象とする。他のIRQのIRETでは呼び出さない」の明文化である。

**背景（既知不具合の是正）**: emu23 v1.06以前はIRET命令（case 0x04）が
IRQ種別を問わず無条件にYSD8002_iret()を呼んでいた（§5.3違反の実装バグ）。
このためIRQ1等のハンドラがIRETするたびタイマーの次回発火が「現在cycle+period」へ
先送りされ、タイマーとIRQ1が高頻度衝突する負荷下でタイマーIRQが大量欠落していた。
emu23 v1.07で「タイマーIRQ実行中フラグ（timer_in_service）」を導入し、
タイマーIRQ受理時にセット・IRET時にこのフラグが真の場合のみYSD8002_iret()を
呼んでクリアする方式で是正した。

**検証**: タイマー(IRQ0)とIRQ1を高頻度衝突させる専用テスト（理論タイマーIRQ=983）で、
v1.06=1回受理（欠落）→ v1.07=983回受理（欠落ゼロ）を実証。通常動作（衝突なし）の
回帰は、yuios 56416バイト一致・Dhrystone 826/48405/P:20一致・v1.06/v1.07同一
バイナリ出力完全一致で確認済み。

**FPGA実装への含意**: timer_in_serviceフラグは逐次実行エミュレータ固有の足場であり、
ハードのRTLには写像されない。ハードのタイマーは自走カウンタ＋オートリロードで
CPUのIRETと無関係に時を刻むため、本不具合はハードには元来存在しない。
詳細は step8i_irqfix_design_v0_2.md §3.3 を参照。

## **3.5 ~~IRQ2優先選択（将来課題・v1.3新設・Step 8-I）~~ → ★v1.4 クローズ★**

### **【v1.4】本項目はクローズする（前提そのものが誤りであったため）**

**~~§3.2「優先処理: pending中で最も優先順位の高いIRQを受理する」の厳密実装のうち、IRQ1とIRQ2の優先選択ロジックは現状未実装である。再評価機構は irq_stat 非0時に一律IRQ1を再pendingする。IRQ2（§3.1で「予備」＝現状デバイス割当なし）が将来使用される時点で、IRQ1/IRQ2の優先選択を§3.1の固定優先順位に従って実装する必要がある。現状はIRQ2未使用のため実害なし。本項目を将来課題として記録する。~~**

**クローズ理由（2026-07-12・FPGA V3.7）**:

1. **前提が誤り**: 本課題は「IRQ2 という割込源が将来使われる」ことを前提としていたが、**ISA2.3 に IRQ2 は存在しない**（§3.1 v1.4訂正を参照）。したがって「IRQ1/IRQ2 の優先選択」という課題は**そもそも成立しない**。

2. **責務の帰属誤りの訂正**: v1.3 は「再評価機構が irq_stat 非0時に一律IRQ1を再pendingする」ことを**未実装の手抜き**と記述していたが、これは誤読であった。実際には、

   - 「一律IRQ1を再pending」する第2段（IRQ_STAT再評価）は、**IRQ1がpending保護に敗れて後回しにされた場合の救済機構**であり、正しく機能している。
   - 優先選択そのものは第2段ではなく、**第1段（`ysd8004_raise()` の pending保護）が担っている**。すなわち責務は別の場所にあり、「未実装」ではなかった。

   詳細は §3.2.1（v1.4新設）および `v3_7_design_memo_v0_3.md` §2 を参照。

3. **真に残る課題は別物**: 実際に未解決なのは「IRQ1 vs IRQ2」ではなく、**FPGA RTL 側での第1段（timer/align の pending保護）の実装**である。これは V5（Timer）の設計論点として `v3_7_design_memo_v0_3.md` §4.4b に移管済み。本項目とは別課題であるため、本§はここでクローズする。

## **3.3 IRQ構成図（v1.4更新）**

```
IRQ3（アラインメント例外） ─── NMI（最優先・IE=0でも即時）

YSD8002 ──────────────────── IRQ0（マスク可・外部最高位）

YSD8001(UART RX) ─┐  IRQ_STAT bit0
YSD8003(Storage)  ─┼─▶ YSD8004 ─── IRQ1（マスク可・レベル出力）  IRQ_STAT bit1
YSD8001(UART TX)  ─┘  IRQ_STAT bit2

                      ★IRQ2 は存在しない（v1.4削除）★

IRQ4 ─────────────────────── SYSCALL（マスク可・実質最低位）
```

**注1**: `IRQ_STAT` ビット割当は bit0=UART_RX / bit1=STOR / bit2=UART_TX（FPGA V3.7 確定）。
**注2**: `IRQ_MASK` のリセット値は **`0x04`**（bit2=TXマスク）であり `0x00` ではない。
**注3（v1.5訂正）**: ~~デバイス→YSD8004 の割込入力規約は **1クロックパルス**。保持は YSD8004 の責務。~~
→ ★訂正★ 割込入力規約は**一律ではない**。デバイスごとに次のとおり:

| 割込源 | IRQ_STAT bit | 入力形式 | 根拠 |
| --- | --- | --- | --- |
| YSD8001 UART **RX** | bit0 | **1クロックパルス** | 受信は事象。保持は YSD8004 の責務 |
| YSD8003 **STOR** | bit1 | **1クロックパルス** | 完了/エラーは事象。同上 |
| YSD8001 UART **TX**（TDRE） | bit2 | ★**レベル**★ | `ysd8001` 実源: `assign irq_tx_o = stat_r[BIT_TX_READY];` |

TX(TDRE) がレベルであるのは、**「送信バッファが空である」という状態そのもの**が割込要因だからである（MC6809 系の ACIA/PIA における TDRE と同型）。
`IRQ_STAT` への `|=` は冪等であるため、レベル入力でも YSD8004 は破綻しない。
ただし**ハンドラが `IRQ_STAT` を W2C しても `TX_READY=1` の間は即座に再セットされる**。
沈黙させるには `IRQ_MASK` bit2 をセットするか、TX データを書いて `TX_READY` を落とすしかない。
（FPGA V4 / S5(c) c6 にて実証済）
**注4**: 空きベクタ枠 `$000A` / `$000C` / `$000E` は温存し、将来の専用ベクタ割込に充てる。

# **4. MMIOアドレスマップ全体**

## **4.1 実装済みレジスタ一覧**

| **アドレス** | **デバイス** | **用途** |
| --- | --- | --- |
| $FC80 | YSD8001 | UART TX データレジスタ |
| $FC82 | YSD8001 | UART RX データレジスタ |
| $FC84 | YSD8001 | UART ステータス（bit0=TX ready, bit1=RX ready） |
| **$FC86** | **YSD8001** | **★v1.6 是正★ UART_BAUD ボーレート分周値（予約定義・emu23 v1.02 で実装済）。**~~（予備）将来拡張用~~ ← v1.5 までの記載は実装に追従していなかった（`emu23_v111.c` L599 実源照合）。**なお本レジスタは 16bit 経路（`rd16`/`wr16`）のみ実装され、8bit 経路は意図的に対象外**（本設計書 §3.5 既述の方針）。§5.5 の被覆漏れ解消においても 8bit 対象外を維持する |
| $FC88〜$FC8F | （予備・**未接続**） | 将来拡張用。**未接続領域として §4.2 の応答仕様を適用する（v1.6）** |
| $FC90 | YSD8002 | TCR タイマー制御レジスタ |
| $FC92 | YSD8002 | PERIOD_HI IRQ周期設定 上位16bit |
| $FC94 | YSD8002 | PERIOD_LO IRQ周期設定 下位16bit |
| $FC96 | YSD8002 | CYCLE_LO サイクルカウンタ 下位16bit（読み出しでHIラッチ） |
| $FC98 | YSD8002 | CYCLE_HI サイクルカウンタ 上位16bit |
| $FC9A | YSD8002 | SW_RUNS ストップウォッチ用 Number_Of_Runs |
| $FC9C | YSD8002 | SCORE_LO 経過サイクル数 下位16bit（読み出しでHIラッチ） |
| $FC9E | YSD8002 | SCORE_HI 経過サイクル数 上位16bit |
| $FCA0 | YSD8003 | SD_CMD コマンドレジスタ |
| $FCA2 | YSD8003 | SD_STAT ステータスレジスタ |
| $FCA4 | YSD8003 | LBA_LO セクタアドレス 下位16bit |
| $FCA6 | YSD8003 | LBA_HI セクタアドレス 上位16bit |
| $FCA8 | YSD8003 | BUF_PTR バッファポインタ（0〜511） |
| $FCAA | YSD8003 | DATA PIOデータレジスタ（8bit有効） |
| $FCAC | YSD8003 | IRQ_CTRL 割り込み制御 |
| $FCAE | YSD8003 | DISK_LO 総セクタ数 下位16bit |
| $FCB0 | YSD8003 | DISK_HI 総セクタ数 上位16bit |
| $FCB2 | YSD8004 | IRQ_STAT 割り込み要因レジスタ（Write-to-Clear） |
| $FCB4 | YSD8004 | IRQ_MASK 割り込みマスクレジスタ |
| $FCB6〜$FEFF | （将来拡張） | 予備 |
| $FF00〜$FF0F | MMU（特権） | PTR[0〜15] ページテーブルレジスタ（特権モード専用） |
| $FF10 | MMU（特権） | MCR MMU制御レジスタ（特権モード専用） |
| $FF11〜$FFFF | （予約） |  |

*【注】$FF00〜$FF1Fはカーネル特権領域。将来のKRN_PROT実装時にユーザモードからのアクセスを禁止する（MMU設計書参照）。*

> **★v1.6 追記：MMU レジスタ領域（`$FF00`〜`$FF10`）の扱い★**
> **`--mmu` オプションの有無にかかわらず、本領域は常にデバイス（MMU レジスタ）として応答する。** 未接続扱いとはしない。
> 根拠は3点。①**RTL がそうなっている**：`ysd8800_mmio_stub_v0_7.sv` L237-238 の `hit_ptr`／`hit_mcr` は **MCR.EN を一切参照しない**。レジスタファイルは常に存在する。②**「MMU 無効」とはレジスタの不在ではなく MCR bit0 = 0 という状態である**（`ysd8800_mmu_v0_1.sv` L15）。そうでなければ MCR 自身が読めず MMU を有効化できない（MMU 設計書 L23 が MMIO を変換対象から外す理由として同旨を述べる）。③**emu23 の `--mmu` はエミュレータ都合のオプションであって、ハードウェア構成の記述ではない。**
> **これは既定動作の変更である**（v1.11 では `--mmu` 非指定時に本領域は `mem[]` へフォールスルーしていた）。§10 の回帰で影響を確認すること。

## **4.2 ★未接続 MMIO 領域の応答仕様（v1.6 新設・改良2 の中核）★**

### 4.2.1 対象範囲

**MMIO 空間 `$FC80`〜`$FFFF`（896 B）のうち、§4.1 の実装済みレジスタを除く全アドレス**を「未接続」と定義する。空間の定義は `yuios_memmap_design_v2_4.md` L202/L221-237 に従う。

現時点の未接続領域は次のとおり（§4.1 より導出）。

| アドレス | 規模 | 備考 |
| --- | --- | --- |
| $FC88〜$FC8F | 8 B | UART と YSD8002 の間 |
| $FCB6〜$FEFF | 約 1.1 KB | **最大の未接続領域** |
| $FF11〜$FFFF | 239 B | memmap v2.4 L237 の予約領域 |

### 4.2.2 応答仕様

| 操作 | 応答 | 根拠 |
| --- | --- | --- |
| **8bit 読出** | **`$FF`** | 実機のデータバスはプルアップされる。未接続デバイスへのアクセス時、バスは開放状態となり全ビット 1 が読める |
| **16bit 読出** | **`$FFFF`** | 同上 |
| **書込（8/16bit）** | **捨てる**（`mem[]` に書かない） | 駆動先が存在しないため。**v1.11 の「書けば読み返せる」挙動は誤り**であり、これを是正する |

### 4.2.3 設計判断の根拠：なぜ `$00` ではなく `$FF` か

**現行 RTL（`ysd8800_mmio_stub_v0_7.sv` L458）は未接続 MMIO に `8'h00` を返しているが、これは誤りである。** GND に落ちている回路が存在しないにもかかわらず 0 を返すのは、**デコーダが値を捏造している**ことに等しい。V3 スタブ時代の暫定実装が残置されたものと判断する。

一方 **`$FF` は「プルアップ抵抗という実在の回路をモデル化した値」**であり、性格が異なる。`emu23_kairyo.txt` 項番2 のご指示（実機のバスはプルアップするのでハイインピーダンス時は 1 としたい）に合致する。

なお **FPGA 内部では「誰も駆動しない状態」を作れない**。内部配線に三態バッファは存在せず、`z` 記述は合成時にマルチプレクサへ変換されるため、RTL では必ず default 値を書かざるを得ない。したがって RTL における選択肢は実質「`8'h00`（誤）」か「`8'hFF`（プルアップ模倣）」の二択であり、後者を採る（§9 参照）。

### 4.2.4 段階導入（B-C-1a / B-C-1b の分離）

| 段 | 内容 | 既定動作 |
| --- | --- | --- |
| **B-C-1a** | デコード層の新設＋8bit 経路の被覆漏れ解消（§5.5／§6.6） | **未接続応答は現行維持**（`mem[]` フォールスルー） |
| **B-C-1b** | 未接続応答のプルアップ化（本節） | **オプション（`--bus-pullup`）で有効化**。既定は現行維持 |
| **B-C-2** | 既定切替 | 上記2段の回帰通過を確認後、**既定をプルアップに切り替える**（独立ステップとして判断） |

**B-C-1a を先に単独で回帰させることにより、以降に生じた差分はすべてプルアップ由来と断定できる。** 混在させると切り分け不能になるため、この順序を守ること。

## **4.3 ★未接続アクセスの検知機構（v1.6 新設）★**

### 4.3.1 必要性：`$FFFF` は YUI OS の番兵と衝突する

未接続読出値をプルアップ模倣（`$FFFF`）とすると、**誤アクセスの結果が YUI OS の予約値と完全に一致する**という問題が生じる。

| 値 | 意味 | 出典 |
| --- | --- | --- |
| **`$FFFF`** | **`IDX_NIL`（IPCキューの終端番兵）** | `kernel_v12_11.asm` L292 |
| `$FFFE` | `ERR_IPC_NOSLOT`（プール枯渇） | 同 L293 |
| `$FFFF` | `rx_pop` の異常センチネル | 同 L1904-1918 |

IPC キュー処理は `CMPI A, #$FFFF`（L1042・L1086）で終端を判定する。**壊れたポインタが未接続領域を指した場合、読み出した `$FFFF` は「キューが空」という完全に正常な状態として受理され、タスクは正常に `WAIT_IPC` で寝る。**例外もログも出ない。症状は「なぜかタスクが起きてこない」という形で、**発生地点から遠く離れた場所・時刻に現れる**。

現行の `mem[]` フォールスルー（不定値）は、多くの場合 `$FFFF` にならないため**派手に落ちてくれる**。プルアップ化はこの「親切さ」を失わせる。

**ただしこれはプルアップを採らない理由にはならない。** 実機のバスがプルアップされている以上 `$FFFF` が返るのが物理的に正しく、ここは譲らない。論点は「実機忠実性を取ったときに失われるデバッグ可能性を、エミュレータ側でどう補うか」である。**emu23 は実機ではないため、実機と同じ値を返しつつ同時に記録できる。これは実機にはできない仕事である。**

### 4.3.2 α：警告ログ（既定で有効）

未接続 MMIO へのアクセスを検知した場合、**アクセスアドレスと当該アクセスを行った PC を 1 行で記録する**。

- 出力先は診断ログ（`stderr`）とし、**`quiet_mode`（`-q`／`-it`）では抑制する**（§2.4 の A方針・対話画面と自動テストの汚染防止）
- 読出・書込の別、8bit/16bit の別を含める

### 4.3.3 ログ長大化の抑制（必須要件）

**未接続アクセスがループ内で発生すると、警告が毎命令出力されログが長大化する。** 本プロジェクトではログ長大化がツール動作の不安定化を招く実害として認識されている。したがって抑制機構を**必須要件**とする。

| 要件 | 内容 |
| --- | --- |
| 同一 PC の重複抑止 | **同一 PC からの警告は初回のみ**出力する |
| 総数上限 | **N = 16** 件で打切る（既定値） |
| 打切り時の明示 | 上限到達時に「以降抑制」を 1 行出力する |
| 総数サマリ | **実行終了時に未接続アクセスの総件数を 1 行で報告**する（打切り後も計数は継続する。件数が消えると検知機構の意味が失われるため） |

### 4.3.4 β：`--strict-mmio`（オプション）

`--strict-mmio` 指定時、**未接続 MMIO アクセスを検出した時点で実行を停止**し、アドレス・PC・アクセス種別を報告する。既定では停止しない（実機忠実性を既定とし、厳格検査は明示的に選択させる）。

本オプションは、`$FFFF` が正常値に化けて症状が遠方に現れる問題（§4.3.1）に対し、**発生地点そのものを押さえるための手段**である。

## **4.4 ★8bit アクセス経路の被覆漏れ解消（v1.6 新設・B-C-1a）★**

### 4.4.1 現行実装の欠陥

**`rd8()`／`wr8()` が扱うレジスタ集合は、`rd16()`／`wr16()` より大幅に狭い。** 未処理のレジスタに 8bit アクセスすると、デバイスに届かず `mem[]` へフォールスルーする（`emu23_v111.c` L823-842／L844-870 実源照合済）。

| レジスタ | 16bit 経路 | **8bit 経路（現行）** | v1.6 での扱い |
| --- | --- | --- | --- |
| UART_STAT／RX／TX | ✅ | ✅ | 現状維持 |
| UART_BAUD（$FC86） | ✅ | ❌（**意図的に対象外**・§3.5） | **意図的対象外を維持**（欠陥ではない） |
| **YSD8002 全8本**（TCR／PERIOD_HI/LO／CYCLE_LO/HI／SW_RUNS／SCORE_LO/HI） | ✅ | **❌ 未処理** | **★対応する★** |
| YSD8003 SD_DATA（$FCAA） | ✅ | ✅ | 現状維持 |
| **YSD8003 の7本**（SD_CMD／SD_STAT／LBA_LO/HI／BUF_PTR／IRQ_CTRL／DISK_LO/HI） | ✅ | **❌ 未処理** | **★対応する★** |
| **YSD8004 の2本**（IRQ_STAT／IRQ_MASK） | ✅ | **❌ 未処理** | **★対応する★** |
| MMU レジスタ | ✅ | ✅ | 現状維持（ただし `--mmu` 依存を解消・§4.1 注記） |

**現状これが露見していないのは、全ドライバが 16bit アクセスを行っているためにすぎない。** 潜在的な欠陥として残置されている状態である。

### 4.4.2 対応方針（案b＝同時解消）

**デコード層で 8bit／16bit を統一的に扱い、被覆漏れごと解消する。**

デコード層を新設する以上、8bit と 16bit で別々の経路判定を残すのは新設層の意味を損なう。また、被覆漏れを残したままプルアップを適用すると、上表の ❌ 群への 8bit アクセスが `mem[]` フォールスルー経由で `$FF` 固定に変わるだけであり、**「デバイスに届かない」という誤りは是正されない**。

### 4.4.3 バイトアクセスの規定

16bit レジスタへの 8bit アクセスに対する規定を以下のとおり定める。

| 項目 | 規定 |
| --- | --- |
| アドレス対応 | **リトルエンディアン**に従う。偶数アドレス＝下位バイト、奇数アドレス＝上位バイト |
| 読出 | 対応する 16bit レジスタ値の該当バイトを返す |
| 書込 | 対応する 16bit レジスタの該当バイトのみを更新する |
| **副作用を伴うレジスタ** | **★要注意★** `CYCLE_LO`／`SCORE_LO` は**読出時に上位16bitをラッチする**副作用を持つ（§5.2）。`SD_DATA` は読み書き後に `BUF_PTR` を自動インクリメントする（§6）。**これらの副作用は「下位バイト読出時に発動」とし、上位バイト読出では発動させない**（16bit 読出時と副作用の発生回数を一致させるため） |

> **★実装上の危険（KY）★** 副作用付きレジスタのバイトアクセス規定を誤ると、**ラッチ／自動インクリメントが 1 回のはずが 2 回発動する**（下位・上位の各バイト読出で発動してしまう）。これは Dhrystone のサイクル計測値（`CYCLE_LO`/`SCORE_LO` 経由）を直接狂わせる。**回帰ゲートの 48,785 サイクル一致で検出可能**であるため、B-C-1a の回帰で必ず確認すること。

# **5. YSD8002 タイマーMMIO化設計**

## **5.1 レジスタ仕様**

| **アドレス** | **R/W** | **名称** | **説明** |
| --- | --- | --- | --- |
| $FC90 | R/W | TCR | タイマー制御レジスタ（詳細は下表） |
| $FC92 | R/W | PERIOD_HI | IRQ周期設定 上位16bit（cycles_per_irq[31:16]） |
| $FC94 | R/W | PERIOD_LO | IRQ周期設定 下位16bit（cycles_per_irq[15:0]） |
| $FC96 | R | CYCLE_LO | 現在サイクルカウンタ 下位16bit（読み出し時に上位をラッチ） |
| $FC98 | R | CYCLE_HI | 現在サイクルカウンタ 上位16bit（CYCLE_LO読み後に読む） |
| $FC9A | R/W | SW_RUNS | ストップウォッチ用 Number_Of_Runs 設定値 |
| $FC9C | R | SCORE_LO | 経過サイクル数 下位16bit（SW_STOP後確定・読み出しでHIラッチ） |
| $FC9E | R | SCORE_HI | 経過サイクル数 上位16bit（SCORE_LO読み後に読む） |

### **5.1.1 TCRビット定義**

| **ビット** | **名称** | **R/W** | **説明** |
| --- | --- | --- | --- |
| bit0 | TIMER_EN | R/W | 1=タイマーIRQ有効 |
| bit1 | IRQ_EN | R/W | 1=IRQ0発火有効 |
| bit2 | SW_START | W | 1書き込みで計測開始トリガ（自動クリア・読み出しは常に0） |
| bit3 | SW_STOP | W | 1書き込みで計測停止トリガ（自動クリア・読み出しは常に0） |
| bit4 | SW_BUSY | R | 1=計測中（状態遷移は下表参照） |
| bit7〜5 | （予約） | — | 常に0 |

*【注】bit2/bit3はMC6809系PIA/ACIAと同様の方式。書き込みでトリガが発生し即座に0に戻る。*

### **5.1.2 SW_BUSY状態遷移（v1.2更新）**

| **状態** | **SW_BUSY値** | **遷移条件** | **次状態** | **カウンタ動作** |
| --- | --- | --- | --- | --- |
| IDLE | 0 | SW_START書き込み | MEASURING | 計測カウンタをリセットして開始 |
| MEASURING | 1 | SW_STOP書き込み | IDLE | カウンタ停止・SCORE確定 |
| MEASURING | 1 | SW_START再書き込み | MEASURING | カウンタをリセットして再スタート（v1.2追加） |

**【重要】SW_START再発行（計測中にSW_STARTを書き込む）時は、カウンタをリセットして計測を再開始する。直前のSCOREは上書きされる。**

## **5.2 64bitカウンタのラッチ方式**

64bitカウンタを16bitレジスタで2回に分けて読み出す際の一貫性保証：CYCLE_LO読み出し時に上位16bitをラッチする。SCORE_LOも同様。

LDW  A, [$FC96]      ; CYCLE_LO読み出し → この瞬間CYCLE_HIもラッチ

LDW  B, [$FC98]      ; CYCLE_HI（ラッチ値）を読み出す

## **5.3 使用例（アセンブラ）**

### **5.3.1 タイマーIRQ設定（100Hz @4MHz = 40,000サイクル/IRQ）**

LDW  A, #0

STW  A, [$FC92]          ; PERIOD_HI = 0

LDW  A, #40000

STW  A, [$FC94]          ; PERIOD_LO = 40000

LDW  A, #0x0003          ; TIMER_EN | IRQ_EN

STW  A, [$FC90]          ; TCR設定

### **5.3.2 ストップウォッチ計測**

; 計測開始（SW_BUSY=0→1、カウンタリセット）

LDW  A, #10

STW  A, [$FC9A]          ; SW_RUNS = 10

LDW  A, #0x0004          ; SW_START bit

STW  A, [$FC90]          ; トリガ発行

; ... 計測対象処理 ...

; 計測停止（SW_BUSY=1→0、SCORE確定）

LDW  A, #0x0008          ; SW_STOP bit

STW  A, [$FC90]          ; トリガ発行

; 結果読み出し（LO先読み必須）

LDW  A, [$FC9C]          ; SCORE_LO（この瞬間HIもラッチ）

LDW  B, [$FC9E]          ; SCORE_HI

## **5.4 emu23実装方針**

| **変更箇所** | **変更内容** |
| --- | --- |
| wr16($FC90) SW_START | sw_busy=1・dhry_start_cycle=cpu.cycleをセット（再発行時もリセット） |
| wr16($FC90) SW_STOP | sw_busy=0・elapsed_cycles確定・SCOREラッチ |
| wr16($FC92/$FC94) | PERIOD_HI/LO書き込み → cycles_per_irqを再計算 |
| rd16($FC96) | CYCLE_LOラッチ → CYCLE_HI返却 |
| rd16($FC9C) | SCORE_LOラッチ → SCORE_HI返却 |
| SYSCALL case 0x05 | if/else chain削除 → cpu.irq_pending=4のみ |
| IRET case 0x04 | YSD8002_iret()はMMIO管理（PERIOD設定値で次回発火計算）に移行 |

# **6. YSD8003 ストレージ設計**

## **6.1 概要**

| **項目** | **内容** |
| --- | --- |
| セクタサイズ | 512バイト固定 |
| アドレッシング | LBA（Logical Block Address）32bit |
| 転送方式 | PIO方式（8bit幅・8bitCPU対応） |
| emu23バックエンド | ホストファイルシステム上の.imgファイル |
| emu23起動オプション | --disk <imagefile.img> |
| 割り込み | YSD8004経由IRQ1（IRQ_EN有効時） |

## **6.2 レジスタ仕様**

| **アドレス** | **R/W** | **名称** | **説明** |
| --- | --- | --- | --- |
| $FCA0 | R/W | SD_CMD | コマンドレジスタ（詳細は下表） |
| $FCA2 | R | SD_STAT | ステータスレジスタ（詳細は下表） |
| $FCA4 | R/W | LBA_LO | セクタアドレス 下位16bit（LBA[15:0]） |
| $FCA6 | R/W | LBA_HI | セクタアドレス 上位16bit（LBA[31:16]） |
| $FCA8 | R/W | BUF_PTR | バッファポインタ（0〜511）。EXEC発行時に自動リセット |
| $FCAA | R/W | DATA | PIOデータレジスタ（8bit有効）。読み書き後BUF_PTRが自動インクリメント |
| $FCAC | R/W | IRQ_CTRL | 割り込み制御（詳細は下表） |
| $FCAE | R | DISK_LO | ディスク総セクタ数 下位16bit |
| $FCB0 | R | DISK_HI | ディスク総セクタ数 上位16bit |

### **6.2.1 SD_CMDコマンド値**

| **値** | **名称** | **説明** |
| --- | --- | --- |
| 0 | READ_SETUP | 読み込み準備（LBA設定後に発行） |
| 1 | WRITE_SETUP | 書き込み準備（LBA設定後に発行） |
| 2 | EXEC | 転送実行。BUF_PTRリセット・BUSY=1セット後にfread()/fwrite()実行 |

### **6.2.2 SD_STATビット定義**

| **ビット** | **名称** | **説明** |
| --- | --- | --- |
| bit0 | BUSY | BUSY挙動はBUSYラッチ方式参照（v1.2） |
| bit1 | ERROR | 1=直前のコマンドがエラー |
| bit2 | READY | 1=デバイス準備完了（--diskでイメージが開けた） |
| bit3〜15 | （予約） | 常に0 |

### **6.2.3 BUSYラッチ方式（v1.2確定）**

emu23はシングルスレッドのためEXEC発行後即座にfread()/fwrite()が完了するが、実機との乖離を防ぐため以下のラッチ方式を採用する。

| **タイミング** | **BUSY値** | **sd_busy_latch** | **説明** |
| --- | --- | --- | --- |
| EXEC発行直後 | 1 | 1 | 転送処理完了後もBUSY=1を維持 |
| 1回目のSD_STAT読み出し | 1 | 0→クリア | BUSY=1を返しラッチをクリア |
| 2回目以降のSD_STAT読み出し | 0 | 0 | BUSY=0を返す（転送完了） |

これによりポーリングコードは最低2回SD_STATを読み出す動作となり、実機との挙動差異が最小化される。

*【注】これはemu23固有の擬似実装であり、FPGA実機では実際の転送時間に応じてBUSYが維持される。*

### **6.2.4 IRQ_CTRLビット定義**

| **ビット** | **名称** | **説明** |
| --- | --- | --- |
| bit0 | IRQ_EN | 1=転送完了時にYSD8004経由でIRQ1を発火 |
| bit1 | ERR_EN | 1=エラー時にYSD8004経由でIRQ1を発火 |
| bit2〜15 | （予約） | 常に0 |

### **6.2.5 BUF_PTRオーバーラン仕様**

| **条件** | **BUF_PTR動作** |
| --- | --- |
| BUF_PTR = 0〜510 のとき DATA 読み書き | BUF_PTR += 1 |
| BUF_PTR = 511 のとき DATA 読み書き | BUF_PTR → 0（ラップアラウンド・エラーなし） |
| EXEC発行 | BUF_PTR → 0（自動リセット） |
| BUF_PTRへの直接書き込み | 任意値（0〜511）に設定可能 |

*【注】将来的に16bit DATAレジスタへの拡張を考慮した設計とする。現バージョンは8bitCPU互換性のため8bit固定。*

## **6.3 転送シーケンス**

### **6.3.1 セクタREAD（LBA=5、読み込み先=$5000）**

; ① LBA設定

LDW  A, #0

STW  A, [$FCA6]          ; LBA_HI = 0

LDW  A, #5

STW  A, [$FCA4]          ; LBA_LO = 5

; ② READ_SETUP

LDW  A, #0

STW  A, [$FCA0]          ; SD_CMD = READ_SETUP

; ③ EXEC発行（BUF_PTR=0・BUSY=1・fread()実行・latch=1）

LDW  A, #2

STW  A, [$FCA0]          ; SD_CMD = EXEC

; ④ 完了待ち（1回目: BUSY=1返却・latchクリア / 2回目: BUSY=0）

_wait_read:

    LDW  A, [$FCA2]      ; SD_STAT読み出し

    ANDI A, #0x0001      ; BUSY bit

    BNE  _wait_read      ; BUSY=1なら再読み出し

; ⑤ PIOで512バイト読み出し

LDW  X, #$5000

LDW  B, #512

_pio_read:

    LDB  A, [$FCAA]      ; DATA読み出し（BUF_PTR自動インクリメント）

    STB  A, [X]

    ADDI X, #1

    SUBI B, #1

    BNE  _pio_read

### **6.3.2 セクタWRITE（LBA=5、書き込み元=$5000）**

; ① LBA設定・WRITE_SETUP

LDW  A, #0 / STW  A, [$FCA6]  ; LBA_HI = 0

LDW  A, #5 / STW  A, [$FCA4]  ; LBA_LO = 5

LDW  A, #1 / STW  A, [$FCA0]  ; SD_CMD = WRITE_SETUP

; ② BUF_PTRリセット＋PIOで512バイト書き込み

LDW  A, #0 / STW  A, [$FCA8]  ; BUF_PTR = 0

LDW  X, #$5000 / LDW  B, #512

_pio_write:

    LDB  A, [X] / STB  A, [$FCAA]  ; DATA書き込み

    ADDI X, #1 / SUBI B, #1 / BNE  _pio_write

; ③ EXEC発行（BUSY=1・fwrite()実行・latch=1）

LDW  A, #2 / STW  A, [$FCA0]  ; SD_CMD = EXEC

; ④ 完了待ち

_wait_write:

    LDW  A, [$FCA2] / ANDI A, #0x0001 / BNE  _wait_write

## **6.4 emu23実装方針**

| **項目** | **内容** |
| --- | --- |
| 内部バッファ | uint8_t sd_buf[512]（emu23グローバル変数） |
| BUSYラッチ | int sd_busy_latch（EXEC発行で1セット・SD_STAT初回読み出しでクリア） |
| ディスクイメージ | FILE *disk_fp（--diskオプションで開く） |
| READ処理 | EXEC: sd_busy_latch=1 → fseek(lba*512) + fread(sd_buf,512) → IRQ発火 |
| WRITE処理 | EXEC: sd_busy_latch=1 → fseek(lba*512) + fwrite(sd_buf,512) → IRQ発火 |
| BUF_PTR | EXEC発行時に0リセット・DATA読み書き毎にインクリメント（511→0ラップ） |
| IRQ発火 | EXEC完了時にIRQ_EN=1ならysd8004_raise(bit1)を呼ぶ |

# **7. YSD8004 割り込みコントローラ設計**

## **7.1 レジスタ仕様**

| **アドレス** | **R/W** | **名称** | **説明** |
| --- | --- | --- | --- |
| $FCB2 | R/W | IRQ_STAT | 割り込み要因レジスタ。Write-to-Clear方式 |
| $FCB4 | R/W | IRQ_MASK | 割り込みマスク。1=マスク（禁止）、0=許可 |

### **7.1.1 IRQ_STAT / IRQ_MASKビット定義**

| **ビット** | **デバイス** | **説明** |
| --- | --- | --- |
| bit0 | YSD8001 UART RX | UART受信完了（将来実装） |
| bit1 | YSD8003 ストレージ | 転送完了またはエラー |
| bit2〜14 | （将来拡張） |  |
| bit15 | （予約） | 常に0 |

### **7.1.2 IRQ_STATビット独立性（v1.2追加）**

| **規則** | **説明** |
| --- | --- |
| 独立セット | 各ビットは対応デバイスの割り込み発生時に独立してセットされる |
| 独立クリア | Write-to-Clearで指定したビットのみクリアされ、他ビットは影響を受けない |
| 同時保持 | 複数ビットが同時にセットされても全て保持される |
| 処理順序 | 複数ビット同時発生時の処理順序はハンドラ実装側の責務とし、本仕様は規定しない |

### **7.1.3 IRQ1ハンドラの処理フロー**

_irq1_handler:

    PUSH A

    LDW  A, [$FCB2]      ; IRQ_STAT読み出し（クリアされない）

    PUSH A               ; 要因を保存

    ; bit1: YSD8003ストレージ完了チェック

    ANDI A, #0x0002

    BEQ  _irq1_check_uart

    LDW  A, #0x0002

    STW  A, [$FCB2]      ; Write-to-Clearでbit1のみクリア

    JSR  _handle_storage ; 処理中に他ビットがセットされても保持される

_irq1_check_uart:        ; bit0: UART RX（将来）

    ; ...

_irq1_ret:

    POP  A               ; 要因を破棄

    POP  A

    IRET

# **8. SYSCALLディスパッチテーブル設計**

## **8.1 SYSCALL番号体系**

| **番号範囲** | **カテゴリ** | **説明** |
| --- | --- | --- |
| $0000〜$00FF | プロセス管理 | fork/exit/wait等 |
| $0100〜$01FF | ファイルシステム | open/close/read/write等 |
| $0200〜$02FF | メモリ管理 | malloc/free等 |
| $0300〜$03FF | 時刻・タイマー | gettimeofday等 |
| $0400〜$04FF | デバッグ | printk等 |
| $0500〜$FEFF | （将来拡張） |  |
| $FF00〜$FFFF | （予約） |  |

| **番号** | **名称** | **説明** |
| --- | --- | --- |
| $0000 | SYS_EXIT | プロセス終了 |
| $0001 | SYS_FORK | プロセス生成（将来） |
| $0002 | SYS_WAIT | プロセス待機（将来） |
| $0100 | SYS_OPEN | ファイルオープン（将来） |
| $0101 | SYS_CLOSE | ファイルクローズ（将来） |
| $0102 | SYS_READ | ファイル読み込み（将来） |
| $0103 | SYS_WRITE | ファイル書き込み（将来） |
| $0200 | SYS_MALLOC | メモリ確保（将来） |
| $0201 | SYS_FREE | メモリ解放（将来） |
| $0300 | SYS_GETTIME | 時刻取得（将来） |
| $0400 | SYS_PRINTK | カーネルデバッグ出力 |

## **8.2 SYSCALL ABI（v1.2改定）**

SYSCALL命令の呼び出し規約を以下にABIとして定める。

| **項目** | **規約** |
| --- | --- |
| 入力: SYSCALL番号 | Aレジスタ |
| 入力: 引数 | スタック渡し（事前にPUSHしておく） |
| 出力: 戻り値 | Bレジスタ（成功時: >= 0 / 失敗時: < 0） |
| A: caller-saved | ハンドラはAレジスタを保存・復元する責務を持つ |
| B: caller-saved | 戻り値として使用。呼び出し元はIRET後にBを参照する |

*【注】AとBの役割を明確に分離することで、ネスト時のスタック破壊リスクをなくす。*

## **8.3 スタック状態（IRQ4受理後）**

| **SPオフセット** | **内容** | **備考** |
| --- | --- | --- |
| SP+0 | FLAGS | IRQ4受理時にpush |
| SP+2 | 戻りPC | IRQ4受理時にpush |
| SP+4 | 引数1 | 呼び出し側が事前にpush |
| SP+6 | 引数2 |  |
| SP+N | 引数N/2 | Aレジスタ = SYSCALL番号（スタック外） |

## **8.4 SYSCALLハンドラのABI規約フレーム**

; 標準的なSYSCALLハンドラ（戻り値あり）

_syscall_handler:

    PUSH A               ; ★caller-saved: SYSCALL番号を退避（必須）

    PUSH X               ; 作業レジスタ退避

    ; ... ディスパッチ処理 ...

    ; Bレジスタに戻り値をセット（成功:>=0 / 失敗:<0）

    POP  X               ; 作業レジスタ復元

    POP  A               ; ★caller-saved: Aレジスタ復元

    IRET                 ; 呼び出し元へ復帰（BはIRET後も保持）

*【注】POP AはIRET前に必ず実行する。BはPOP Aで変化しないためIRET後の呼び出し元でBレジスタから戻り値を取得できる。*

## **8.5 ディスパッチ実装（案A→案B移行方式）**

_syscall_disp_ptr:       ; 間接ポインタ変数（startup_harness23.asm）

    .word _harness_dispatch

| **段階** | **ディスパッチ先** | **移行手順** |
| --- | --- | --- |
| 案A（初期） | _harness_dispatch（harness内） | デフォルト状態 |
| 案B（YUI OS導入後） | _kernel_dispatch（カーネル内） | OS起動時に_syscall_disp_ptrを書き換え |

# **9. 将来拡張**

| **項目** | **説明** | **優先度** |
| --- | --- | --- |
| YSD8001 UART割り込み | UART RX完了時にYSD8004経由でIRQ1発火 | 中 |
| YSD8002 --freqオプション | emu23起動時にcpu_freqを指定可能にする | 低 |
| YSD8003 16bit DATA | $FCAAを16bit幅に拡張（8bitCPU互換性確認後） | 低 |
| YSD8003 複数ドライブ | --disk2等で複数イメージを指定 | 低 |
| ファイルシステム層 | FAT32相当の簡易実装（YUI OS側） | 高 |
| SYSCALLフルセット | SYS_OPEN/READ/WRITE等をYUI OSカーネルに実装 | 高 |

# **10. ★RTL 追随要件（v1.6 新設・改良2 に伴う）★**

## **10.1 現状：3者が食い違っている**

改良2 の設計にあたり実機想定・RTL・emu23 を照合した結果、**未接続 MMIO の扱いが三者三様であることが判明した**。

| | 未接続 MMIO 読出値 | `$FF00`-`$FF10`（MMU 無効時） |
| --- | --- | --- |
| **実機（想定・プルアップ）** | `$FF` / `$FFFF` | レジスタ応答 |
| **現行 RTL** | **`8'h00`** ← 誤り | レジスタ応答（常時） |
| **現行 emu23（v1.11）** | **`mem[]` フォールスルー（不定）** | **`--mmu` 無しなら `mem[]`** |

**すなわち改良2 は「emu23 を RTL に合わせる」作業ではない。RTL 自身が実機と食い違っており、RTL 側にも同じ是正が必要である。**

## **10.2 是正内容**

| 対象 | 現行 | 是正後 |
| --- | --- | --- |
| `ysd8800_mmio_stub_v0_7.sv` **L458** | `else mmio_rdata = 8'h00;  // V3スタブ挙動` | `else mmio_rdata = 8'hFF;  // 未接続=プルアップ` |

**修正規模は 1 行**である。ただし RTL 修正は RTL の検証工程（iverilog 回帰・V8-b 等）を要求する。

## **10.3 実施工程：案Q（軸A 冒頭で追随）**

**本設計書では設計として規定するにとどめ、実装はキャッシュ RTL 工程（軸A）の冒頭タスクとして実施する。**

| 案 | 内容 | 採否 |
| --- | --- | --- |
| P | 改良2 と同時に RTL も修正 | ❌ Step 8-Sim の完了状態を再オープンし、Phase B' が emu23／RTL の2軸作業になる。回帰が崩れたとき原因層を切り分けられない（B-1／B-C を分離した理由と同一の論理） |
| **Q** | **emu23 先行。RTL はキャッシュ RTL 工程の冒頭で追随** | ✅ **採用**（2026-08-10 決定） |
| R | 台帳に記録して保留 | ❌ 「いつやるか」の無い記録は忘れられる。乖離を残したまま実機実装に入ると、実機だけ挙動が違うという最悪の切り分け困難を招く |

**採用理由**：①RTL 修正は 1 行でも RTL 検証を伴い、emu23 工程に混ぜると切り分け不能になる。②キャッシュ RTL 工程は CCR レジスタ追加のため**どのみち `mmio_stub` に触る**。同じファイルに触る工程でまとめるのが自然。③**実機はまだ焼かれていない**（F1〜F8 未着手）ため、乖離が実害になる前に間に合う。

## **10.4 軸A への申し送り（着手条件）**

**キャッシュ RTL 工程の着手時、先頭タスクとして以下を実施すること。**

1. `ysd8800_mmio_stub` の未接続応答を `8'h00` → `8'hFF` に是正（版数繰上げ）
2. V8-b を含む既存 TB による回帰（`yuios_road2.bin` の RTL 起動確認）
3. `fpga_source_version_ledger` の更新

**回帰への影響は軽微と予測される。** 実コード（`kernel_v12_11.asm`／`kernel_forth_v0_10_18.fs`／C ソース）に MMIO 空間を RAM として参照する箇所は**皆無**であることを実源 grep で全数確認済（2026-08-10）。ただし予測であり、**実測で確認すること**。

## **10.5 emu23 と RTL の等価性に関する注記**

案Q を採る間、**emu23 と RTL の間に「未接続 MMIO 応答値」の既知の乖離が残る**（emu23=`$FF` / RTL=`$00`）。Step 8-Sim で確立した等価性は、**未接続アクセスに関してのみ**成立しない状態となる。実コードが未接続領域にアクセスしないため既存の回帰テストでは差が出ないが、**潜在的乖離として本節に記録し、軸A での解消をもってクローズとする**。

# **11. ★回帰計画（v1.6 新設）★**

## **11.1 絶対ゲート**

改良2 は `rd8`／`rd16`／`wr8`／`wr16` の**全メモリアクセス経路**に及ぶ。B-1（`-it` 指定時のみ通る 1 関数）とは異なり、**非回帰を構造的に担保できない。実測でのみ確認可能**である。

| ゲート | 期待値 | 備考 |
| --- | --- | --- |
| **Dhrystone** | **819 / 48,785 / P:20** | **3値すべて一致で判定する。**完走したことを PASS の根拠にしてはならない（kaizen 原則115） |
| **`yuios.bin`** | **56,416 B / md5 `599a7f9d1ebf103f81f58450ea1b6491`** | `build_road2.sh` |
| **`-q` 出力** | v1.11 と **byte-exact 一致** | `yuios_road2.bin -q --disk disk.img` |

## **11.2 段階回帰（★必須★）**

**4経路を一度に書き換えてはならない。経路ごとに回帰を回すこと（1変更1検証）。**

| 段 | 内容 | 回帰 |
| --- | --- | --- |
| B-C-1a-1 | デコード層の骨格を追加（既存 `if` チェーンは無改変・応答も現行維持） | 絶対ゲート3種 |
| B-C-1a-2 | 8bit 経路の被覆漏れ解消（YSD8002／8003／8004） | 絶対ゲート3種。**特に Dhrystone 48,785 サイクル一致で §4.4.3 の副作用二重発動を検出する** |
| B-C-1a-3 | MMU レジスタの `--mmu` 非依存化（§4.1 注記） | 絶対ゲート3種＋`--mmu` 指定時の動作 |
| B-C-1b | 未接続応答のプルアップ化（`--bus-pullup` オプション） | 絶対ゲート3種（**オプション無し**＝既定で一致すること）＋オプション有効時の動作 |
| B-C-2 | 既定切替 | 絶対ゲート3種（**オプション無しで既定プルアップ**の状態で一致すること） |

## **11.3 検知機構（§4.3）の検証**

| ID | 内容 | 期待結果 |
| --- | --- | --- |
| **BC-N** | **ネガティブコントロール**：未接続領域を読むテストコードを用意し、**改修前**の emu23 で実行 | 警告が出ない（`mem[]` の値が返る）＝現行挙動の確認。**これを先に実施する** |
| BC-1 | 同テストを改修版（`--bus-pullup`）で実行 | `$FFFF` が返り、警告が 1 行出力される |
| BC-2 | 未接続読出をループで多数回実行 | **同一 PC は初回のみ出力**・**上限 16 件で打切り**・**終了時に総件数サマリ**（§4.3.3） |
| BC-3 | `--strict-mmio` 指定で同テストを実行 | 初回検出時点で停止し、アドレス・PC・種別を報告 |
| BC-4 | 未接続領域への書込後に同アドレスを読出 | `$FFFF` が返る（**書込が捨てられている**ことの確認。v1.11 では書いた値が読み返せた） |
| BC-5 | 通常の YUI OS 起動（`-q`） | **警告が 1 件も出ないこと**（実コードは未接続領域にアクセスしないため。1 件でも出れば、それ自体が実コードの欠陥か本層の実装誤りを示す） |

**BC-N を先に実施すること。**症状（現行挙動）が再現しない環境で改修版を試しても、それは改修の効果を証明しない（原則43 の否定制御運用・kaizen 原則115）。

**BC-5 は本改修の副次的価値が最も現れる項目である。** 従来は検出不能だった「MMIO 空間の誤アクセス」を、以後は起動するだけで検知できるようになる。

---

~~emu23 デバイス設計書 v1.3  /~~ **emu23 デバイス設計書 v1.6 / 2026-08-11**
