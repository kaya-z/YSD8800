# emu23 マシンサイクル対応（`-mc`）設計書 v0.2

| 項目 | 内容 |
|---|---|
| 文書名 | `emu23_mc_design_v0_2.md` |
| Version | **v0.2**（陰性対照 実測反映・レビュー未了） |
| 作成日 | 2026-08-18 |
| 対象 | emu23 Phase C（改良3: マシンサイクル対応） |
| 起点ソース | `emu23_v1_14.c`（2,625 行） |
| 目標版数 | **emu23 v2.00**（CPI=1 前提を条件付きで崩すため v2 系へ昇格） |
| 上位文書 | `emu23_kairyo.txt` 改良3 ／ `HANDOVER_CHAT136.md` v1.0 §2, §6 |
| 参照文書 | `ysd8800_cycle_count_table_v1_0.md` v1.0 ／ `emu23_argsym_design_v1_0.md`（EMU-A） |
| 状態 | ⬜ レビュー待ち（原則43：承認まで実装着手しない） |

---

## 1. 目的と適用範囲

### 1.1 目的

emu23 は現在 **CPI=1 固定**であり、`cpu.cycle` は実質「実行命令数」である。
本改修により、オプション `-mc` 指定時に限り **命令ごとに固有サイクル数で歩進**させ、
RTL 実装（`ysd8800_cpu` v0.5.x）に近いサイクル挙動を扱えるようにする。

### 1.2 適用範囲外（本 Phase では扱わない）

- **メモリウェイト**（PSRAM 待ち）：要求 R6 により理想メモリ前提。対象外
- **キャッシュ挙動**：改良4（Phase C 後）
- **RTL 実測との自動突合**：D-6 の決定により第1段では行わない（§9.3）

---

## 2. 要求と設計方針の対応

| # | 要求（`emu23_kairyo.txt`） | 本設計での実現 |
|---|---|---|
| R1 | 既定は 1 クロック 1 命令 | `mc_enable=0` が既定。歩進経路は既存コードと**同一の式**に落ちる（§5.1） |
| R2 | `-mc` 指定時は命令ごとのサイクルで動作 | `mc_table[]` による 2 段表引き（§4） |
| R3 | `-mc <file>` で外部定義を読む | 独自簡易テキスト形式（§6） |
| R4 | ファイル名省略時はデフォルト表 | 引数消費数の動的判定（§7） |
| R5 | RTL のリアルなサイクルを扱う | デフォルト表を `ysd8800_cycle_count_table_v1_0.md` v1.0 から転記 |
| R6 | メモリウェイトは無いものとする | 理想メモリ前提の表をそのまま使用。ウェイト機構は実装しない |

---

## 3. 用語と前提（★KY：ここを取り違えると全体が狂う★）

### 3.1 `S_IRQCHK` の扱い（D-3 決定）

サイクル表 §1.3 は、**命令間で必ず通る `S_IRQCHK` の 1 サイクルを「命令固有 CPI」に含めていない**。
本設計では **emu23 側で毎命令 +1 する**方式を採る。

```
1命令あたりの歩進量 = mc_table[opcode]（表の命令固有CPI） + 1（S_IRQCHK）
```

**採用理由**：RTL の実クロック数と突合することが R5 の目的であり、
RTL では `S_IRQCHK` が実在の 1 サイクルを消費するため。

**★設計書明記事項★**：`mc_table[]` に格納する値は**表の値そのもの**であり、
`+1` は加算時に行う。外部定義ファイル（§6）に書く値も**表と同じ意味（IRQCHK 非込み）**とする。
これを混同すると「ファイルの値をそのまま使ったつもりが全命令 1 ずれる」事故になる。

### 3.2 HALT の CPI

表 §2 の HALT `(4)` は「停止遷移検出コスト」であり命令実行コストではない。
本設計では **HALT は 2**（FETCH, OPFETCH）として扱う。
括弧付きの値をそのまま使わないこと。

> ⚠ **レビュー確認事項 C-1**：HALT を 2 とする判断の可否。
> 停止後は歩進しないため実害は最終 cycle 値の ±2 のみ。

### 3.3 表に無い命令

サイクル表は悉皆ではない。**表に載っていない opcode に遭遇した場合の既定値**を定める（§4.3）。

---

## 4. サイクル表のデータ構造

### 4.1 2 段表（★0x1F はプリフィックス★）

実源確認（`emu23_v1_14.c` L1521-1534）により、**`0x1F` は拡張プリフィックス**であり、
直後の 1 バイトがサブオペコードである。したがって単純な 256 エントリ表では引けない。

```c
#define MC_UNDEF 0xFFFF                 /* 未定義マーカ */
static uint16_t mc_table[256];          /* 主 opcode 用 */
static uint16_t mc_table_ext[256];      /* 0x1F サブオペ用 */
static uint16_t mc_irq_accept;          /* 割込受理コスト（既定 9） */
static int      mc_enable = 0;          /* 0=CPI固定1（既定） 1=マシンサイクル */
```

### 4.2 引き方

```
opcode = cpu.ir
if (opcode == 0x1F)  cpi = mc_table_ext[ mem[cpu.pc] ]   /* pc は IR の次を指す */
else                 cpi = mc_table[opcode]
```

**★実装上の注意★**：`cpu.cycle++` は `fetch8(cpu.pc++)` の**直後**（L1665-1666）にある。
この時点で `cpu.pc` は既にサブオペコードを指しているため、`mem[cpu.pc]` で peek 可能。
ただし **`cpu.pc` を進めてはならない**（実行部が改めて読む）。

> ⚠ **レビュー確認事項 C-2**：MMU 有効時（`--mmu`）に `mem[cpu.pc]` 直読が正しいか。
> 実行部が使う読み出し経路と一致させる必要がある。実装前に実源で要確認（KY34）。

### 4.3 未定義 opcode の扱い

| 状況 | 動作 |
|---|---|
| `mc_table[op] == MC_UNDEF` | **既定値 4** を使用し、**初回のみ** stderr に警告を1行出す |
| 警告文言 | `emu23: [-mc] no cycle entry for opcode 0xNN, using default 4` |

**★禁止事項★**：警告文言に `Dhrystones/sec` / `cycles` / `P:` の文字列を含めてはならない
（HANDOVER §7.1：`2>&1` 合流後に grep するため誤検出する）。

既定値 4 の根拠：表 §3 の CPI 分布で最頻はレジスタ ALU 系の 4。

### 4.4 デフォルト表の内容

`ysd8800_cycle_count_table_v1_0.md` v1.0 §2 から転記する。**転記時は表の値をそのまま**（IRQCHK 非込み）。

| 主 opcode | CPI |
|---|---:|
| 0x00 NOP / 0x02 EI / 0x03 DI | 2 |
| 0x01 HALT | 2（§3.2） |
| 0x04 IRET | 7 |
| 0x05 SYSCALL | 3 |
| 0x20 MOV / 0x40,0x42,0x44 / 0x50,0x52,0x54,0x56,0x57,0x58,0x59 | 4 |
| 0x21 / 0x41,0x43,0x45 / 0x51,0x53,0x55 | 6 |
| 0x22 / 0x26 | 8 |
| 0x23 / 0x27 / 0x24 | 7 |
| 0x25 | 6 |
| 0x60-0x64 (JMP/Bcc) | 5 |
| 0x68 JSR / 0x69 RET | 7 |
| 上記以外 | `MC_UNDEF` |

| 0x1F サブオペ | CPI |
|---|---:|
| 0x00-0x05（PUSH/POP A,B,X） | 6 |
| 0x10,0x12（LDB [imm16]） | 7 |
| 0x14,0x16（STB [imm16]） | 7 |
| 0x11,0x13（LDB [X]） | 6 |
| 0x15,0x17（STB [X]） | 6 |
| 上記以外 | `MC_UNDEF` |

| その他 | 値 |
|---|---:|
| 割込受理 | 9 |

---

## 5. 歩進ロジックの変更

### 5.1 命令サイクル（★R1 死守の要★）

現行（L1665-1666）：

```c
cpu.ir = fetch8(cpu.pc++);
cpu.cycle++;
```

変更後：

```c
cpu.ir = fetch8(cpu.pc++);
cpu.cycle += mc_step();     /* mc_enable==0 のとき必ず 1 を返す */
```

`mc_step()` は `mc_enable==0` なら**表を一切参照せず即座に 1 を返す**。
これにより **既定動作は現行と完全に同一**であり、G-1〜G-5 は不変であるべきである。

### 5.2 割込受理（D-5 決定：反映する）

L1655-1662 の IRQ 受理ブロック末尾（`cpu.pc = vec;` の後）に追加：

```c
if (mc_enable) cpu.cycle += mc_irq_accept;   /* 既定 9 */
```

**★注意★**：割込受理は `S_IRQCHK` を含むシーケンス（表 §割込関連）である。
一方 §3.1 で毎命令 +1 している。受理サイクルでは IRQCHK が二重計上されないか、
**実装前に RTL FSM の遷移で要確認**。

> ⚠ **レビュー確認事項 M-1**：二重計上の有無。表の「割込受理 9」の状態連鎖には
> `IRQCHK` が明記されている。§3.1 の毎命令 +1 と重なるなら、受理時は **+8** が正しい可能性がある。
> **本件が未決のまま実装してはならない。**

### 5.3 タイマーへの影響（D-4）

`YSD8002` / `ysd8001` は `cpu.cycle` を基準に発火判定する（L948, L2027, L2052）。
`-mc` 時は `cpu.cycle` の意味が変わるため、**タイマー発火が実命令数ベースで早くなる**
（同じ命令数でより多くの cycle が進むため）。

**これは仕様として正しい**。RTL 実機では 4MHz の実クロックでタイマーが動くため、
`-mc` の方がむしろ実機に近い。**追加の補正は行わない**。

---

## 6. サイクル定義ファイル形式（D-1 決定）

### 6.1 方針

`ysd8800_cycle_count_table_v1_0.md` は Markdown 表であり機械可読でない（HANDOVER §6 D-1）。
**Markdown をパースしない。** 独自の簡易テキスト形式を新規定義する。

### 6.2 書式

```
# 行頭 '#' はコメント。空行は無視。
# 形式: <キー> <CPI>
#   キー: 0xNN        … 主 opcode
#         X:0xNN      … 0x1F サブオペコード
#         IRQ         … 割込受理コスト
0x00 2
0x20 4
X:0x00 6
IRQ 9
```

- 数値は 10 進 / `0x` 付き 16 進の双方を受理
- **記載されなかった opcode はデフォルト表の値を保持する**（差分上書き方式）
- 同一キーが複数回現れた場合は**後勝ち**

### 6.3 差分上書きとする理由

全数記載を強制すると、1 命令だけ実験したいときに 40 行以上書く必要が生じる。
デフォルト表を土台に差分を当てる方が実用的である（改良4 のキャッシュ実験でも有用）。

### 6.4 エラー方針

| 事象 | 動作 |
|---|---|
| ファイルが開けない | stderr にエラー出力し **exit(1)**（明示指定の黙殺は EMU-A の教訓に反する） |
| 行が解釈不能 | stderr に行番号付き警告を出し、その行を無視して継続 |
| CPI が 0 または 1000 超 | 同上（範囲外として無視） |

---

## 7. 引数解析（D-2 決定）★最重要リスク箇所★

### 7.1 問題の所在（実源確認済）

`emu23_v1_14.c` L2422-2466 の EMU-A 前置パスは、**オプションごとに `extra` が固定値**である
（0 / 1 / 2 のいずれか）。`-mc` は**引数を取ることも取らないこともある初のオプション**であり、
この方式にとって未経験のパターンである。

誤判定すると、`cyc.txt` が未消費のまま残り **argv[2] として `.dbg` と誤認される**
——EMU-A で解消したはずの症状の再来（HANDOVER §6 D-2）。

### 7.2 消費数決定表（★実装の唯一の参照★）

`-mc` に続く argv を `nxt = argv[i+1]` とする。

| 条件 | `extra` | `-mc` の値 |
|---|---:|---|
| `i+1 >= argc`（次が無い） | 0 | デフォルト表 |
| `nxt[0] == '-'`（オプション様） | 0 | デフォルト表 |
| 上記以外 | **1** | `nxt` をファイル名として採用 |

**`-` 始まりで判定する理由**：ファイルの存在チェック（`access()`）で判定すると、
**タイプミスしたファイル名が黙ってデフォルト動作に落ちる**（サイレント失敗＝EMU-A と同じ病）。
`-` 始まりでない以上はファイル名として扱い、開けなければ §6.4 により exit(1) とする。

> ⚠ **既知の制限**：`-` で始まるファイル名は指定できない。実用上問題ないと判断する。
> 設計書・マニュアル双方に明記する。

### 7.3 実装位置

L2425-2459 のオプション表に `-mc` を追加する。値の回収は `--dbg`/`--sym` と同じく
**前置パス内で行う**（`mc_file_opt` に保存）。実際の表読み込みは `load_bin()` の後、
CPU 初期化の前に行う。

**★横展開確認（原則122）★**：EMU-A のオプション表はコメント L2408-2410 で
「本表が実装の唯一の参照」と明記されている。`-mc` 追加時にこの表を更新すれば足りる。
ただし **`--help` / usage 表示があれば同時に更新**すること（実装前に grep で要確認）。

---

## 8. 版数と KY41 整合（7 点）

| # | 対象 | 変更内容 |
|---|---|---|
| 1 | ファイル名 | `emu23_v2_00.c` |
| 2 | ソース冒頭のファイル名＋版数コメント | v2.00 |
| 3 | ヘッダ日付 | 2026-08-18 |
| 4 | ソース冒頭の改版履歴 | v2.00 の行を追記 |
| 5 | 起動バナー1行目 | v2.00 |
| 6 | 起動バナー内の版数履歴リスト | v2.00 を追記 |
| 7 | REPL 起動メッセージ | v2.00 |

**★着手前に `grep -n "v1\.14" emu23_v1_14.c` を実行し全数を列挙すること★**（原則124：例示≠全数）。

---

## 9. 検証計画

### 9.1 陰性対照（KY54・実装前に実施）

**★実施済み（2026-08-18・v1.14 基準バイナリ `emu23_v114_ref` にて実測）★**

| ID | コマンド | 実測結果 | 判定 |
|---|---|---|---|
| N-1 | `emu23_v114_ref t.bin -q -mc` | `[SYM-NOTFOUND] cannot open symbol file: -mc` | ★症状再現★ `-mc` が argv[3] に残り **`.sym` と誤認** |
| N-2 | `emu23_v114_ref t.bin -q -mc cyc.txt` | 同上（`cyc.txt` は argv[4] のため無視） | ★症状再現★ |
| N-2b | `emu23_v114_ref t.bin -mc cyc.txt` | `[DBG-NOTFOUND] cannot open dbg file: -mc`。**`cyc.txt` については何も出ない** | ★最悪★ `cyc.txt` が実在するため**エラーも出ずシンボルファイルとしてサイレント誤読**された |

**考察**：N-2b が示すとおり、現行では `-mc` 導入時に
「サイクル定義ファイルがシンボルテーブルとして黙って読まれる」という**完全にサイレントな誤動作**が起きる。
これは EMU-A で解消したはずの病の再来であり、§7.2 の消費数決定表が必須であることを裏付ける。
また、この症状は **stderr に何も出ない**ため、テストで能動的に検出しない限り発見できない
（→ C-08 を検証項目として必須とする根拠）。

### 9.2 機能検証（実装後）

| ID | 内容 | 期待 |
|---|---|---|
| C-01 | `-mc` なしで Dhrystone | **819 / 48,785 / P:20**（G-1 と完全一致） |
| C-02 | `-mc` ありで Dhrystone | 完走し `P:20`。cycles は増加（**参考値として記録**） |
| C-03 | NOP のみ N 個のプログラムを `-mc` 実行 | cycle = N×(2+1) + 起動分。手計算と一致 |
| C-04 | `0x1F` 系（PUSH/POP）を含むプログラム | 2 段表引きが効き、6+1 で歩進 |
| C-05 | `-mc cyc.txt` で `0x00 99` を指定 | NOP が 99+1 で歩進（差分上書きの確認） |
| C-06 | `-mc` の後に別オプション（`-mc -q`） | `-q` がファイル名として食われない |
| C-07 | `-mc nonexist.txt` | stderr にエラー、exit(1)。**黙殺しない** |
| C-08 | `-mc` と位置引数併用（`prog.bin p.dbg p.sym -mc`） | `.dbg`/`.sym` が正しく読まれる（EMU-A 非退行） |
| C-09 | 未定義 opcode 遭遇 | 既定 4 で継続、警告は 1 回のみ |
| C-10 | `-mc` ありで YUIOS 起動 | `YUIOS Booted!` → `YUI>`（タイマー周期変化で破綻しないこと） |

### 9.3 RTL 突合の範囲（D-6 決定）

**第1段は表の値との一致確認に留める**（C-03/C-04 が該当）。
`tb_cpi_probe_poc.sv` による RTL 実測との突合は**別チケットとして起票**し、
本 Phase の完了条件には含めない。

### 9.4 絶対ゲート（不変・死守）

| ID | 対象 | 期待値 |
|---|---|---|
| G-1 | Dhrystone（**`-mc` なし**） | 819 / 48,785 / P:20 |
| G-2 | `yuios_road2.bin` | 56,416 B / md5 `599a7f9d1ebf103f81f58450ea1b6491` |
| G-3 | `dhry_final.bin` | 21,846 B |
| G-4 | `-q` stdout（**`-mc` なし**） | v1.14 と byte-exact 一致 |
| G-5 | YUIOS 起動（**`-mc` なし**） | `YUIOS Booted!` → `YUI>` |
| G-6 | 起動バナー | v2.00 が正しく出る |

**★`-mc` 時の値はゲート化しない（D-4）。参考値として §11 に記録するのみ。★**

---

## 10. 実装手順（1変更1検証）

| 段 | 内容 | 検証 |
|---|---|---|
| 0 | KY54 陰性対照（N-1/N-2）を実施し記録 | 観測結果を v0.2 に記載 |
| 1 | `mc_*` 変数とデフォルト表の埋め込み（`mc_enable` は常に 0） | ビルド通過・G-1 不変 |
| 2 | `mc_step()` 導入と L1666 の置換 | **G-1〜G-5 全 PASS**（`mc_enable=0` ゆえ不変であるべき） |
| 3 | 引数解析に `-mc` 追加（§7.2 決定表） | C-06 / C-07 / C-08 |
| 4 | 定義ファイル読込（§6） | C-05 |
| 5 | 割込受理コスト（§5.2、M-1 解決後） | C-10 |
| 6 | 版数 7 点更新 | G-6 |
| 7 | 波及文書改版 | — |

---

## 11. 波及文書（実装完了後に改版）

| 文書 | 予定 |
|---|---|
| `tool_version_ledger` | v1.18 → v1.19（emu23 v2.00） |
| `emu23_debug_manual` | v1.5 → v1.6（`-mc` の節を新設。既存節番号は動かさない＝原則121） |
| `ysd8800_cycle_count_table` | **改版不要**（参照するのみ）。ただし §3.2 HALT / §5.2 IRQCHK の解釈を本設計書側に記録 |
| `emu23_kairyo.txt` | 改良3 完了マーク |
| `kaizen.txt` | 得られた教訓を追記 |
| `yuios_build_procedure` | **要判定**（ビルド手順に `-mc` は不要のはず） |

---

## 12. 未決事項（レビューで判断を仰ぐ）

| ID | 内容 | 影響 |
|---|---|---|
| **M-1** | 割込受理 9 サイクルと毎命令 +1（IRQCHK）の**二重計上**の有無 | 大：解決前に §5.2 は実装不可 |
| **C-1** | HALT を 2 とする判断（表は括弧付き 4） | 小：最終 cycle 値 ±2 |
| **C-2** | MMU 有効時の `mem[cpu.pc]` 直読（サブオペ peek）の妥当性 | 中：`--mmu` 併用時に誤引き |
| **C-3** | `-` 始まりファイル名を許容しない制限の可否 | 小 |
| **C-4** | 未定義 opcode の既定値を 4 とする判断 | 小 |

---

## 13. 改版履歴

| 版 | 日付 | 内容 | 作成 |
|---|---|---|---|
| **v0.1** | 2026-08-18 | 初版。D-1〜D-6 のユーザー方針を反映し設計化。実源確認（EMU-A 前置パス L2422-2466 / cycle 歩進 L1665-1666 / 0x1F プリフィックス L1521-1534）に基づき、①2 段表引きの必要性 ②`-mc` が初の可変消費オプションである事実 ③IRQCHK 二重計上疑義（M-1）を摘出 | Claude |

| **v0.2** | 2026-08-18 | §9.1 の陰性対照（KY54）を**実測に置換**。N-1 / N-2 / N-2b を `emu23_v114_ref` で実施し、`-mc` が `.sym`/`.dbg` として誤認される症状を再現。特に **N-2b でサイレント誤読**（`cyc.txt` が実在するためエラーすら出ない）を確認し、§7.2 消費数決定表の必要性を実証した。実装段0（陰性対照）は**消化済み**となり、承認後は段1から着手可能 | Claude |

---

*— 以上 `emu23_mc_design_v0_2.md` v0.2 —*
