# emu23 改良4（キャッシュ下地）設計書

| 項目 | 内容 |
|---|---|
| 文書名 | `emu23_cache_base_design_v0_4.md` |
| Version | **v0.4**（理想メモリ TB 存在調査の結果を反映・段10 新設） |
| 作成日 | 2026-08-19 |
| 対象ツール | emu23 |
| 起点ソース | **`emu23_v2_00.c`** |
| 目標版数 | **emu23 v2.10**（`emu23_v2_10.c`） |
| 前提文書 | `HANDOVER_CHAT137.md` v1.0 ／ `emu23_mc_design_v0_6.md` v0.6 ／ `cache_study_handover_v1_0.md` v1.0 ／ `ysd8800_cycle_count_table_v1_0.md` v1.0 |
| レビュー指摘書 | v0.1 用 `..._v0_1_review_v1_0.md`（条件付き差戻し）／**v0.2 用 `..._v0_2_review_v1_0.md`（条件付き承認・再レビュー不要）** |
| 状態 | **実装着手可**（§7 段1 から）。★ただし **M-5（§9 手順 0）は §9 の校正実行前に必ず消化**すること★ |
| 実装の実施 | **別チャットで実施**。起点は `HANDOVER_CHAT138.md` |

---

## 0. v0.2 からの変更概要（差分再レビュー対応表）

| ID | 指摘 | 対応 | 反映先 |
|---|---|---|---|
| **M-5** | 校正手順の大前提（RTL 理想メモリ実測＝351,761）が未検証 | ✅ **是正**。**手順 0** を新設し、校正前の必須確認とした | §9 |
| **M-6** | T-05 が定性的で PASS/FAIL 判定不能 | ✅ **是正**。数式で期待値を与え、**§3.5 の結論（バースト非対応ではライン拡大に利得なし）の直接証明**となるよう再設計 | §8.1 |
| **C-5** | ack 同期段数が 2 段ではなく 3 段の可能性 | ✅ **実源で決着**。**3 段が正しい**（＋`req_hold` 1）。CPU ドメイン計 **4.00**、机上推定 **4.75 → 5.75** に修正 | §3.1.3 |
| **C-6** | `LATENCY_NORMAL - 2` 補正の意味 | ✅ **実源で決着**。**12 は FSM 全体（req 受理〜ack 成立）**であり現行換算で正しい | §3.1.3 |
| **C-7** | 16bit のライン境界跨ぎが未定義 | ✅ **明記**。判定は必ずバイトごと。跨ぎで充填 2 回。**T-07** 追加 | §3.2.2 / §8.1 |
| **C-8** | 改版履歴 v0.1 行から情報削除（KY41） | ✅ **是正**。**v0.1 行を原文のまま復元**し、誤りであった旨は v0.2 行に記す | §12 |
| **C-9** | `--trace-addr` 単独時の「判定する・記録する・課金しない」が未設計 | ✅ **是正**。`charge_enable` を `cache_enable` と分離 | §5.4 / §6.6 |
| N-5 | インデックス計算式の明記 | ✅ 採用 | §5.5 |
| N-6 | 全オプションの引数消費数表 | ✅ 採用 | §6.7 |
| N-7 | 損益分岐ヒット率の定量化 | ✅ **採用**。M-6 の定量化により式が立ったため数値で記載 | §3.5 |
| E-4 | T-03 の意図変更が未記載 | ✅ 採用 | §8.1 |

---

## 1. 目的とスコープ

### 1.1 目的

emu23 は現在 CPI=1 固定（既定）または命令固有 CPI（`-mc`）で動作するが、
いずれも **理想メモリ前提**であり、PSRAM アクセス待ちを含まない。

本改良は **メモリアクセスのサイクル課金**と**物理アドレストレース出力**を追加し、
将来のキャッシュ RTL 開発（工程②）に必要な定量的基盤を整備する。

### 1.2 スコープ（H-2 決定）

| # | 項目 | 本改良 |
|---|---|---|
| 1 | メモリアクセスのサイクル課金モデル | ✅ **対象** |
| 2 | 物理アドレストレース出力 | ✅ **対象** |
| 3 | キャッシュシミュレータ本体 | ❌ 対象外（Python 後処理に分離＝H-1） |
| 4 | CCR（キャッシュ制御レジスタ）の MMIO モデル | ❌ 対象外（別段） |

### 1.3 非目標

- 実機性能の絶対予測（本モデルは相対評価＝キャッシュ効果の測定が目的）
- キャッシュ方式（容量・ライン長・連想度）の決定
  ※ ただし**その決定に用いるデータを産出する**のが本改良である。
    したがって**ラインサイズを評価できるモデルであることは必須要件**（M-1 の教訓）

---

## 2. 決定事項一覧（2026-08-19 ユーザー承認済）

| ID | 論点 | 決定 |
|---|---|---|
| H-1 | キャッシュシミュレータの配置 | **Python 後処理に分離**。確定後に emu23 へ取り込む二段構え |
| H-2 | スコープ | **項目1＋項目2 を先行** |
| H-3 | オプション名 | **`--mem-latency`**（課金）／**`--trace-addr`**（トレース）。`-mc` と併用可 |
| H-4 | MMIO の扱い | **キャッシュ対象外・課金しない**。`mmio_classify()` 流用 |
| H-5 | I/D 構成 | **統合（unified）**。※アーキテクチャは von Neumann のまま |
| H-6 | 目標版数 | **v2.10**（2 桁表記維持・KY41 7 点整合） |
| 追加 | 課金の粒度 | **(a) アクセス種別ごと**（命令オペコードごとではない＝案 A 回帰の回避） |

---

## 3. ★課金モデル（本設計の中核）★

### 3.1 前提の確定：先行課題および実源調査の結果

#### 3.1.1 RTL 版数差（段0）

`ysd8800_cycle_count_table_v1_0.md` の対象は CPU コア **v0.5.6**、現行 RTL は **v0.5.8**。
差分 2 件を実源確認した結果、**いずれも FSM 状態遷移に影響しない**。

| 版 | 変更内容 | 修正箇所 | 状態遷移への影響 |
|---|---|---|---|
| v0.5.7 | `S_SUBOP` に `mem_rd=1'b1` 追加 | L869（バス出力 `always_comb`） | **無し**（`next_state` は L684-686 で無変更） |
| v0.5.8 | `S_IRQCHK` ラッチ条件に `irq_pending==0` 追加 | L1263（`always_ff`） | **無し**（受理可否のみ変更） |

> **判定：CPI 差分なし。サイクル表 v1.0 は v0.5.8 に対しても有効。**
>
> ★ただしこれは**個々の命令の CPI が表と一致する**ことを意味するにすぎず、
> **Dhrystone 全体の総サイクル数が emu23 `-mc` と一致することの証明ではない**（M-5）。
> §9 手順 0 で別途検証すること。

#### 3.1.2 ★PSRAM レイテンシ（段0'）— 机上推定であることの明示（M-4 是正）★

`cache_study_handover_v1_0.md` §2 の **「約 60 CPU cyc/byte」は前提が誤っている**。
同文書の換算表は `cpu_clk=100MHz : psram_clk=25MHz`（比 4:1）を前提としていたが、
**現行 TB は `cpu_clk=4MHz : psram_clk=32MHz`（比 1:8）に修正済**。

| 出典 | cpu_clk | psram_clk |
|---|---|---|
| `tb_cpu_v8b_prod` **v0.1**（旧・ハンドオーバーの出典） | 100MHz (#5) | 25MHz (#20) |
| **v0.2（現行・実源 L38-40 / L166 / L174）** | **4MHz (#125)** | **32MHz (#15.625)** |

**★v0.1 の重大な誤り（自己申告・保持）★**
v0.1 §3.1.2 は「実測逆算 約 5.0 CPU cyc/byte」と記したが、**これは実測ではない**。
実際には「1 命令あたり平均 4 バイト」という**筆者の仮定**を置いた推定であり、
総アクセスバイト数 B は測定していない。B は §9 手順 1（本改良の実装後）で初めて得られる。
**推定を実測と称することは KY34 に反する。**以後、本文書では**机上推定**と明記する。

#### 3.1.3 ★机上推定の計算過程（実源根拠付き・C-5 / C-6 決着版）★

**実源：`ysd8800_psram_ctrl_v0_2.sv`**

| 項目 | 実源の値 | 行 |
|---|---|---|
| データ幅 | **8bit**（`wdata`/`rdata` とも `logic [7:0]`） | L53 / L57 |
| インタフェース | req/ack 4 相ハンドシェイク | L55-56 |
| FSM | `PS_IDLE` → `PS_BUSY` → `PS_WAIT_REQ_LOW` | L66 |
| **連続転送（バースト）モード** | **存在しない** | FSM に該当状態なし |
| `LATENCY_NORMAL` | **12** psram_clk | L43 |
| `LATENCY_REFRESH` | 15 psram_clk（発生率 0.05%） | L44-45 |

**★C-6 の決着：`12` は FSM 全体の値である★**

L110 の `(... ) - 2` はカウンタ初期化時の補正だが、L107-108 のコメントが
「**req 受理サイクルから ack 成立サイクルまでを丁度 LATENCY に揃えるための補正。
単体 TB の実測で LATENCY 一致を確認済**」と明記している。

> したがって **12 psram_clk は `PS_IDLE` / `PS_WAIT_REQ_LOW` を含む FSM 全体の値**であり、
> **現行の換算（12 ÷ 8 = 1.50 CPU cyc）で正しい**。追加加算は不要。

**実源：`ysd8800_cdc_bridge_v0_2.sv`**

**★C-5 の決着：ack 側は 3 段である（レビュー指摘が正しい）★**

L89-90 / L94-96 を追うと、`psram_ack` 立上りから `cpu_mem_ready` アサートまでは次のとおり。

| サイクル | 動作 |
|---|---|
| 1 | `ack_sync[0] <= psram_ack`（=1） |
| 2 | `ack_sync[1] <= 1`／`ack_sync_d <= ack_sync[1]`（旧値 0） |
| 3 | エッジ検出 `ack_sync[1] & !ack_sync_d` が成立 → `cpu_mem_ready <= 1'b1` |

すなわち **3 CPU サイクル**。さらに L98 の `req_hold` が次トランザクションの req を
1 サイクル強制的に下げるため、**CPU ドメイン計 4.00 CPU cyc**。

**換算（`cpu:psram = 1:8`）**

| 要素 | 生値 | ドメイン | CPU cyc 換算 |
|---|---|---|---|
| PSRAM アクセス本体（FSM 全体） | 12 psram cyc | psram | 12 ÷ 8 = **1.50** |
| req 2FF 同期 | 2 psram cyc | psram | 2 ÷ 8 = **0.25** |
| **ack 同期（2FF ＋ エッジ検出段）** | **3 cpu cyc** | **cpu** | **3.00** |
| req_hold ワンショット | 1 cpu cyc | cpu | **1.00** |
| **計** | | | **約 5.75 CPU cyc/byte（机上推定）** |

> ★この 5.75 は**机上推定であり、設計判断の根拠に用いてはならない**。★
> 確定は §9 の校正手順（**手順 0 を経たうえで**）による。

**C-1 への回答：律速の所在（v0.1 の断定を撤回・保持）**

v0.1 は「CPU が 4MHz なので律速は PSRAM 側にない」と断じたが、**これは誤りである**。
メモリ待ちは実行時間の **73%**（965,640 / 1,317,401）を占め、**律速はメモリ側にある**。

ただし内訳を分解すると、支配項は次のとおりである（C-5 決着により比率を更新）。

| 分類 | CPU cyc | 比率 |
|---|---|---|
| PSRAM 本体＋req 同期（**psram_clk に依存**） | 1.75 | **30%** |
| **CDC ack 同期＋req_hold（cpu_clk に依存・昇圧しても不変）** | **4.00** | **70%** |

> C-5 の決着により **CDC 側の支配がさらに強まった**。
> §3.1.3 の結論（psram_clk 昇圧の効果は限定的）は**より強く成立する**。

psram_clk を 32→81MHz（約 2.5 倍）としても削減できるのは 1.75→0.7 程度で、
全体は 5.75→4.7、すなわち **約 1.22 倍**にとどまる（旧試算の 3.2 倍には遠く及ばない）。

> ⚠ **v0.1 の「無意味化」という断定も撤回する**。効果は皆無ではないが限定的である。

**より効果が大きいと推定される施策（工程②への申し送り）**

| 施策 | 見込み | 備考 |
|---|---|---|
| **PSRAM のバースト転送対応** | **大** | §3.5 参照。キャッシュ導入の前提条件になりうる |
| **CDC の削減**（ack 同期段数・cpu_clk と psram_clk の関係見直し） | **大** | 支配項 4.00 cyc（70%）の削減 |
| psram_clk 昇圧 | 小（約 1.22 倍） | 上記のとおり |

### 3.2 ★課金式（本設計の心臓部）★

#### 3.2.1 サイクル表に含まれるメモリアクセス

`ysd8800_cycle_count_table_v1_0.md` の内訳欄より、
**メモリアクセスは 1 バイトあたり 1 サイクル**として既に計上されている。

| FSM 状態 | 表での計上 | アクセスバイト数 |
|---|---|---|
| `FETCH` / `OPFETCH` | 各 1 | 各 1 |
| `IMML` / `IMMH` | 各 1 | 各 1 |
| `SUBOP` | 1 | 1 |
| `MEMR8` / `MEMW8` | 1 | 1 |
| `MEMR_LO/HI` / `MEMW_LO/HI` | **2** | **2** |
| `PUSH_LO/HI` / `POP_LO/HI` | **2** | **2** |
| `DECODE` / `EXEC_*` / `WRITEBACK` | 1 | **0** |

根拠例（同表 L54, L62, L91）：

```
NOP            0x00  CPI=2  FETCH, OPFETCH
LDW rD,[imm16] 0x22  CPI=8  FETCH,OPFETCH,IMML,IMMH,DECODE,MEMR_LO/HI,WRITEBACK
LDB A,[imm16]  1F/10 CPI=7  FETCH,OPFETCH,SUBOP,DECODE,IMML,IMMH,MEMR8
```

#### 3.2.2 ★課金式（4 分岐）★

> **★取り違え厳禁★**
> 課金は**バイト単位**で行い、末尾の `−1` は**表に既に含まれる 1 サイクルの相殺**である。
> **同時に、課金漏れ（過少課金）も等しく危険である**（M-1 の教訓）。

| 事象 | 追加課金 | 根拠 |
|---|---|---|
| **読みヒット** | **0** | PSRAM へ到達しない。表の値がそのまま正しい |
| **読みミス** | **(LINE_SIZE × LAT_FILL) − 1** | ライン全体を充填する（M-1） |
| **書きヒット** | **LAT_WR − 1** | ★ライトスルーゆえヒットでも PSRAM へ到達する★（M-2） |
| **書きミス** | **LAT_WR − 1** | ノーライトアロケートゆえライン充填は無い（M-2） |

**★実装者への注意 1（M-2 の再発防止）★**

> **ライトスルー／ノーライトアロケートでは、書き込みのヒット判定は課金額に影響しない。**
> 「書きもヒット判定して 0 にする」のは誤りである。
> ヒット判定は**タグ更新の要否**にのみ関係する（ヒット時はキャッシュ側も更新する）。

**★実装者への注意 2（C-7・ライン境界跨ぎ）★**

> **判定は必ずバイトごとに行うこと。「16bit は 1 回だけ判定する」実装は誤りである。**
>
> `rd16()` / `wr16()` は `pa` と `pa+1` の 2 バイトを扱うが、
> `pa % LINE_SIZE == LINE_SIZE − 1` のとき **2 バイトは異なるラインに属する**。
>
> | 状況 | 課金 |
> |---|---|
> | 同一ライン・1 バイト目ミス | 2 バイト目は**充填済ゆえヒット** → 充填 **1 回** |
> | **ライン境界跨ぎ・両バイトともミス** | **充填 2 回** ＝ `2 × ((LINE_SIZE × LAT_FILL) − 1)` |
>
> 検証は §8.1 **T-07**。

**クリティカルワードファースト：無しとする。**
ミスしたバイトを先に返す最適化は現行 RTL に存在しない（§3.5）。
したがってライン充填完了まで待つモデルとし、課金は上表のとおりミス時に一括計上する。

#### 3.2.3 なぜ「アクセス回数ごと」ではなく「バイトごと」か

現行 RTL は 16bit アクセスをバイト 2 回に分割し、表側も `MEMR_LO/HI` で 2 サイクル計上している。
**バイト単位でのみ対応が取れる。**

### 3.3 アクセス種別と課金パラメータ

| 種別 ID | 発生元 | 1 回あたりバイト数 | 既定値 | 備考 |
|---|---|---|---|---|
| `LAT_FETCH` | `fetch8()` | 1 | **6**（仮） | 命令フェッチ |
| `LAT_RD8` | `rd8()` | 1 | **6**（仮） | データ読み 8bit |
| `LAT_WR8` | `wr8()` | 1 | **6**（仮） | データ書き 8bit |
| `LAT_RD16` | `rd16()` | 2 | **6**（仮） | **1 バイトあたりの値** |
| `LAT_WR16` | `wr16()` | 2 | **6**（仮） | **1 バイトあたりの値** |
| `LAT_FILL` | ライン充填 | LINE_SIZE | **6**（仮） | **1 バイトあたりの値**（M-1） |

> ★**既定値 6 は §9 で確定するまでの仮値であり、設計判断の根拠に用いてはならない**★
> §3.1.3 の机上推定 5.75 を整数に丸めた値である（v0.2 の 5 から更新。C-5 決着による）。

**C-3 への回答：(a) を採用する。**
種別は分析用途のため分けるが、**既定では全種別同値**とする。
`RD8` と `RD16` に異なる値が設定された場合、**物理的にありえない条件**であるため
起動時に stderr へ警告を出す（実行は継続する。分析目的の意図的設定を許すため）。

> **`LAT_FILL` は `LAT` と別パラメータとする**（レビュー M-1 の要求）。
> ただし**現行 RTL では両者は同値である**（§3.5）。
> 別パラメータとするのは、工程②でバースト対応した場合の評価を可能にするためである。

### 3.4 レイヤの役割分担（`-mc` との関係）

```
実効サイクル = 命令実行の内部コスト（-mc デフォルト表 ＝ 理想メモリ＝全ヒット相当）
             + メモリ課金（本改良の新レイヤ ＝ §3.2.2 の 4 分岐）
```

| レイヤ | 担当 | 値の出所 |
|---|---|---|
| 命令内部 | `mc_step()`（既存・**無改造**） | サイクル表 v1.0 |
| メモリ課金 | **`mem_charge()`（新規）** | §9 で校正した LAT 値 |

`--mem-latency` は **`-mc` 指定時のみ有効**。単独指定時は警告して無効化する（§6.6）。

### 3.5 ★現行 RTL はバースト非対応である（M-1 の重要な帰結）★

§3.1.3 の実源調査より、`ysd8800_psram_ctrl_v0_2.sv` は
**8bit 幅・req/ack 1 バイト 1 トランザクション**であり、**連続転送モードを持たない**。

> **したがって現行 RTL では `LAT_FILL = LAT` である。バースト償却は存在しない。**

**帰結（工程②への最重要の申し送り）**

32 バイトラインの充填コストは `32 × 5.75 ≈ 184 CPU cyc` に達する。
**ライン充填コストがキャッシュの利得を食い潰す可能性がある。**

**★損益分岐ヒット率（N-7・定量化）★**

キャッシュ無しの課金は `N × (LAT − 1)`、
キャッシュ有りの課金は `N × (1−h) × ((S × LAT_FILL) − 1)`（`h`＝ヒット率、`S`＝ライン長）。
両者が等しくなる条件は：

```
(1 − h) = (LAT − 1) ÷ ((S × LAT_FILL) − 1)
```

`LAT = LAT_FILL = 6`（仮値）を代入すると：

| ライン `S` | 充填コスト | **損益分岐ヒット率 `h`** |
|---|---|---|
| 8 B | 47 cyc | **89.4%** |
| 16 B | 95 cyc | **94.7%** |
| **32 B** | **191 cyc** | **97.4%** |
| 64 B | 383 cyc | **98.7%** |
| 128 B | 767 cyc | **99.3%** |

> **ライン 32B では、ヒット率 97.4% を超えて初めてキャッシュが得になる。**
> ダイレクトマップ・統合キャッシュでこの水準を安定して出すのは容易ではない。
>
> ⚠ **「キャッシュ RTL 開発（工程②）」より先に「PSRAM バースト対応」を検討すべき**
> という結論すらあり得る。**この判定こそが本改良のトレース解析の主目的**である。
> §1.3 の非目標（方式の決定）には触れないが、**判定材料は本改良が産出する**。

---

## 4. キャッシュモデル

### 4.1 構成（H-5）

| 項目 | 値 |
|---|---|
| 方式 | ダイレクトマップ |
| I/D | **統合（unified）** |
| 容量 | 16 KB（`--cache-size` で可変） |
| ラインサイズ | 32 バイト（`--cache-line` で可変） |
| 書き込み | **ライトスルー / ノーライトアロケート** |
| **ライトバッファ** | **無しを仮定する**（M-2） |
| クリティカルワードファースト | **無し**（§3.2.2） |
| 配置 | **MMU 物理アドレスの後段** |

> ★**ライトバッファ無しの仮定について**★
> ライトバッファがあれば書き込みは非ブロッキングとなり課金は 0 に近づく。
> 現行 RTL に存在しないため**無しを仮定**する。
> 工程②で採用する場合は**別途モデル化が必要**であり、本設計の課金式は使えない。

**配置理由**：物理アドレスでインデクスすることで、
コンテキストスイッチ時のフラッシュが不要になる（OS-9 の MMU 運用と同様の思想）。

### 4.2 統合とする理由（設計判断の記録）

1. CPU コアは**シングルイシューの多サイクル FSM** であり、
   命令フェッチとデータアクセスが同一サイクルに重なる構造が無い
   → 分離型（modified Harvard）の最大の利点である並行アクセスが機能しない
2. Tang Nano 9K（GW1NR-9）の BSRAM が有限であり、2 系統に割る利得が無い
3. YUI OS はプログラムローダを持つ＝実質的に自己書き換え相当の動作をする。
   分離型では I/D コヒーレンス制御（キャッシュ無効化命令）が必要になり、
   **ISA2.3 に無い命令の追加を要求してしまう**（プロジェクト規約に抵触）

> ※ アーキテクチャそのものは **von Neumann**（命令・データ同一アドレス空間）で不変。
>   本項はマイクロアーキテクチャ（キャッシュ段）の話である。

### 4.3 MMIO の扱い（H-4）

MMIO 領域は**キャッシュ対象外**とし、**課金もしない**。
実装は既存の `mmio_classify()` を流用する。

| 分類 | キャッシュ | 課金 |
|---|---|---|
| `MMIO_NOT_IN_SPACE`（＝通常メモリ） | 対象 | **する** |
| `MMIO_MAPPED` / `MMIO_MAPPED_UNSUPPORTED` / `MMIO_UNMAPPED` | 対象外 | **しない** |

---

## 5. 実装設計

### 5.1 フック位置（案 B・**MMIO 分岐行に統一**）

課金関数は **1 本に集約**する（kaizen「内部状態変数の変更箇所を一か所に集約」）。

| 関数 | **分岐行（フック挿入の基準）** | フック内容 |
|---|---|---|
| `fetch8()` | **L256**（MMIO 分岐なし。関数本体） | `mem_charge(pa, ACC_FETCH)` |
| `rd8()` | **L1300**（`mmio_classify` 分岐） | 分岐後に `mem_charge(pa, ACC_RD8)` |
| `wr8()` | **L1402**（同上） | 分岐後に `mem_charge(pa, ACC_WR8)` |
| `rd16()` | **L1050-1051**（バイト粒度 2 回分類） | 各バイトで `mem_charge()` を**計 2 回** |
| `wr16()` | **L1149-1150**（同上） | 同上 |

> ★フックは **MMIO 分岐の後**に置くこと★（§4.3。分岐前に置くと MMIO も課金される）

**★配置位置の注意★**（Phase C の KY 再発防止）
`mem_charge()` は `cpu` 構造体宣言より**後**に置くこと。

### 5.2 新規関数

```c
/* アクセス種別 */
typedef enum {
    ACC_FETCH = 0, ACC_RD8, ACC_WR8, ACC_RD16, ACC_WR16
} acc_kind_t;

/* 課金＋トレース（★必ず1バイト単位で呼ぶ★）
 *  pa   : MMU変換後の物理アドレス
 *  kind : アクセス種別
 *  戻り : 追加課金サイクル数（§3.2.2 の 4 分岐）
 *         ただし charge_enable==0 のときは常に 0（判定・記録のみ行う）
 * ★R1: cache_enable==0 なら何もせず即0を返す★
 * ★書きは write-through ゆえヒットでも課金する（設計書 §3.2.2）★
 * ★16bit はライン境界を跨ぎうるためバイトごとに判定する（§3.2.2 注意2）★ */
static unsigned mem_charge(uint32_t pa, acc_kind_t kind);
```

### 5.3 ★R1 死守（既定経路の非退行）★

> **`cache_enable == 0`（既定）なら `mem_charge()` はキャッシュ配列を参照せず即座に `0` を返す。**
> このとき emu23 の挙動は **v2.00 と完全に同一**でなければならない。

段ごとに G-1〜G-6 を確認する（§8.2）。

### 5.4 状態変数（C-9 対応）

| 変数 | 型 | 用途 |
|---|---|---|
| `cache_enable` | `int` | **キャッシュ判定を行うか**（既定 0） |
| **`charge_enable`** | `int` | **課金を行うか**（既定 0）★`cache_enable` と分離★ |
| `cache_size` / `cache_line` | `unsigned` | 容量・ラインサイズ（既定 16384 / 32） |
| `cache_tag[]` | `uint32_t*` | タグ配列（要素数＝**ライン数**） |
| `cache_valid[]` | `uint8_t*` | 有効ビット（同上） |
| `lat[6]` | `unsigned` | 種別ごとのレイテンシ（`LAT_FILL` 含む） |
| `trace_fp` | `FILE*` | トレース出力先（NULL=無効） |
| `stat_hit` / `stat_miss` / `stat_charged` | `uint64_t` | 統計 |

**★C-9：2 変数に分離する理由★**

`--trace-addr` 単独時は「**判定はする・記録はする・課金はしない**」状態が必要である。
`cache_enable` 1 本では表現できない。

| 指定 | `cache_enable` | `charge_enable` |
|---|---|---|
| 既定（無指定） | 0 | 0 |
| `--trace-addr` のみ | **1** | **0** |
| `--cache-stats` のみ | **1** | **0** |
| `-mc` ＋ `--mem-latency` | **1** | **1** |

### 5.5 インデックス計算（N-5）

```
ライン数   LINE_COUNT = cache_size / cache_line
インデックス idx = (pa / cache_line) % LINE_COUNT
タグ         tag = pa / (cache_line * LINE_COUNT)
```

実装はマスク演算で行う（`cache_size` / `cache_line` がともに 2 の冪であることが前提）。

> ★これが §6.4 で **2 の冪検査を必須とする理由**である。★
> 非 2 冪ではマスク演算が成立せず、インデックスが破綻する。

---

## 6. オプション仕様

### 6.1 `--mem-latency`

```
--mem-latency N              全種別を N に設定
--mem-latency FILE           種別ごとの定義ファイルを読む
```

定義ファイル書式（`-mc` の `.cyc` に倣う）：

```
# emu23 mem latency definition
FETCH  6
RD8    6
WR8    6
RD16   6
WR16   6
FILL   6
```

**引数消費は可変**（`-mc` と同じ EMU-A パターン）。
`-` で始まる次引数は消費しない（typo によるサイレント誤読の防止）。
未知のキーは**エラーとして `exit(1)`**（サイレント破棄の禁止＝Phase C M-5 の教訓）。

### 6.2 `--trace-addr`

```
--trace-addr             stderr へ出力
--trace-addr FILE        FILE へ出力
```

**★出力の先頭に必ずヘッダ行を出す★**

```
# emu23 v2.10 trace / mc=1 lat=F6,R6,W6,FILL6 cache=16384/32 unified wt-nwa nowb
cyc,pa,kind,hit
1234,0x01A00,F,1
1240,0x0C120,R,0
```

| 列 | 意味 |
|---|---|
| `cyc` | 発生時点の `cpu.cycle`。**`-mc` 無しでは実行命令数を意味する**（単位が異なる） |
| `pa` | **MMU 変換後の物理アドレス** |
| `kind` | `F`=fetch / `R`=read / `W`=write |
| `hit` | 1=ヒット / 0=ミス |

> ヘッダ行により、Python 後処理（H-1）が**前提の取り違えを検出できる**。

### 6.3 `--cache-stats`

トレースを出力せず、ヒット/ミスの計数のみ行う集計モード。
ラインサイズ掃引時に同一実行を繰り返すため、6MB 規模のトレース出力を回避する。
**引数を取らない単独フラグ**（§6.7）。

### 6.4 `--cache-size` / `--cache-line`

```
--cache-size N        キャッシュ総容量（バイト。既定 16384）
--cache-line N        ラインサイズ（バイト。既定 32）
```

**検査規則（サイレント丸めの禁止）**

| 条件 | 動作 |
|---|---|
| 2 の冪でない | stderr へエラー出力し **`exit(1)`** |
| `cache_size < cache_line` | stderr へエラー出力し **`exit(1)`** |
| いずれも 0 以下 | 同上 |

**`cache_enable` との関係**

> `--cache-size` / `--cache-line` は **`cache_enable` を立てない**（構成の指定のみ）。
> サイズだけ指定した場合は**構成が変わるだけで課金もトレースも起きず、
> 挙動は v2.00 と完全に同一**である（R1 死守）。

### 6.5 統計出力

**終了時に stderr へ 1 行サマリを出力する。**

```
[CACHE] hit=123456 miss=7890 rate=93.98% charged=31560
```

> ★**警告文言規約（Phase C §4.3 で確立）を遵守すること**★
> Makefile が `2>&1` 合流後に grep するため、
> **`Dhrystones/sec` / `cycles` / `P:` の語を含めてはならない。**
> 上記例が `charged`（`cycles` ではない）としているのはこのためである。

出力は `cache_enable != 0` のときのみ。既定では出力しない（R1 死守）。

### 6.6 併用規則

| 指定 | 動作 | `cache_enable` | `charge_enable` |
|---|---|---|---|
| `-mc` のみ | 従来どおり（v2.00 相当） | 0 | 0 |
| `-mc` ＋ `--mem-latency` | **本改良の本命**。課金が加算される | 1 | 1 |
| `--mem-latency` のみ（`-mc` 無し） | **警告を出して課金を無効化** | 1 | **0** |
| `--trace-addr` のみ | 課金せずトレース出力（ヘッダに `mc=0` を記録） | 1 | **0** |
| `--cache-stats` のみ | 課金せず計数のみ | 1 | **0** |
| `--cache-size` / `--cache-line` のみ | **何も起きない**（§6.4） | 0 | 0 |

### 6.7 ★引数消費数の一覧（N-6）★

Phase C §7.2 の消費数決定表に相当する。**全オプションについて明示する。**

| オプション | 引数消費 | 判定規則 |
|---|---|---|
| `-mc` | **可変（0 または 1）** | 次引数が `-` で始まらなければ消費 |
| `--mem-latency` | **可変（0 または 1）** | 同上。数値なら全種別、それ以外はファイル名 |
| `--trace-addr` | **可変（0 または 1）** | 同上。無ければ stderr |
| `--cache-size` | **固定 1** | 数値必須。無ければエラー `exit(1)` |
| `--cache-line` | **固定 1** | 同上 |
| `--cache-stats` | **固定 0** | 単独フラグ |

> 固定 1 のオプションで引数が欠落した場合、**次のオプションを値と誤読してはならない**。
> `-` で始まる場合はエラーとする。

---

## 7. 実装段階

| 段 | 内容 | 完了条件 |
|---|---|---|
| **0** | **KY54 陰性対照**：課金無しでの Dhrystone / トレース不在を実測記録 | 記録取得 |
| 1 | キャッシュ配列と `mem_charge()` の骨格（常に 0 を返す） | G-1〜G-6 PASS（完全不変） |
| 2 | 5 箇所のフック挿入（`cache_enable==0` のため挙動不変） | G-1〜G-6 PASS（完全不変） |
| 3 | 引数解析（§6.7 の消費数表に従う） | typo・非 2 冪で `exit(1)` を確認 |
| 4 | キャッシュ判定と**4 分岐課金**の実装 | §8.1 T-01〜T-07 PASS |
| 5 | `--trace-addr`（ヘッダ行含む）／`--cache-stats` | 書式確認・行数一致 |
| 6 | 定義ファイル読込 | 不正キーでエラー |
| 7 | 統計出力（警告文言規約の遵守確認） | `make regress` が誤検出しないこと |
| 8 | 版数 7 点整合（v2.10） | KY41 7 点確認 |
| **9** | **★M-5 手順 0 の実施★**：`tb_cpu_v9_idealmem_poc_v0_1.sv` を派生作成し、理想メモリ Dhrystone のサイクル数を実測。`emu23 -mc` の 351,761 と突合（§9.0） | 一致、または乖離の原因を特定 |
| 10 | §9.1 の校正を実行し LAT を確定 | 確定値を v1.0 に反映 |
| 11 | 波及文書改版 | §11 参照 |

> ★**段の順序を守ること**★
> 段9（手順 0）→ 段10（校正）→ 段11（波及文書改版）の順序は**逆転させてはならない**。
> 段9 を飛ばして段10 を実行すると、誤った LAT が段11 の文書改版を駆動し、
> **v0.1 で犯した「未確定値が他文書の改版を駆動する」誤りを繰り返す**（§10 リスク4）。

---

## 8. 検証項目

### 8.1 ★手計算検算テスト（4 分岐＋境界を網羅）★

| ID | 対象分岐 | 内容 | 期待値 |
|---|---|---|---|
| **T-01** | **読みミス** | 容量 32B（1 ライン）・ライン 32B とし、32B 離れた 2 アドレスを交互に読む | `(-mc のみ) + N × ((32 × LAT_FILL) − 1)` |
| **T-02** | **読みヒット** | ライン内に収まる連続読みのみ | **追加課金 0**（`-mc` のみと完全一致） |
| **T-03** | **書きの 2 バイト課金** | `LDW` を全ヒットさせ、`STW` で書く | 読み 0／書きは **2 × (LAT_WR − 1)** |
| **T-04** | **MMIO 非課金** | UART レジスタ（`$FC80` 系）へアクセス | **追加課金 0** |
| **T-05** | **ラインサイズ掃引** | §8.1.1 参照 | §8.1.1 の式と実測が一致 |
| **T-06** | **書きヒット** | ライン内に収まる同一アドレスへ連続書き込み | **追加課金 = N × (LAT_WR − 1)**。★0 ではない★ |
| **T-07** | **ライン境界跨ぎ**（C-7） | `LDW` を `pa % LINE_SIZE == LINE_SIZE − 1` のアドレスに対し全ミスで実行 | **`2 × ((LINE_SIZE × LAT_FILL) − 1)`**（充填 2 回） |

> **T-01 の意図**：容量 32B＝1 ラインの構成では**全アドレスが同一インデックスに落ちる**ため、
> 異なるタグのアドレスを交互にアクセスすれば必ず衝突ミスとなる。
> ライン数を増やした構成ではこの前提が崩れるため、テスト改変時は注意すること。

> **★T-03 の意図変更について（E-4）★**
> v0.1 の T-03 は「`LDW` を**全ミス**させ、追加課金が `2 × (LAT−1)` であることを確認」だった。
> **M-1 により読みミスの課金がライン単位（`(S × LAT_FILL) − 1`）に変わった**ため、
> 読み側では 2 バイト課金を観測できなくなった。
> よって T-03 は**書き側の 2 バイト課金の確認**に役割を変更した。
> 読みミスの検証は T-01、境界跨ぎは T-07 が担う。

#### 8.1.1 ★T-05 の定量設計（M-6 是正）★

**手順**：ライン境界に整列した連続領域を `N` バイト、順に 1 回ずつ読む
（各ラインの先頭バイトでのみミスし、以降はヒットする）。

**期待値の導出**：

```
ミス回数 = N ÷ S                       （S = LINE_SIZE）
追加課金 = (N ÷ S) × ((S × LAT_FILL) − 1)
         = N × LAT_FILL − (N ÷ S)
```

**判定**：`S = 32` と `S = 64` の 2 条件で実行し、実測が上式と一致すること。

`N = 2048`, `LAT_FILL = 6`（仮値）の場合の予測値：

| `S` | ミス回数 | 1 ミスあたり充填 | **追加課金（予測）** |
|---|---|---|---|
| 32 | 64 | 191 | **12,224** |
| 64 | 32 | 383 | **12,256** |

> ★**この結果自体が §3.5 の主張の直接証明になる**★
>
> ライン長を 2 倍にすると **ミス回数は半減**するが **1 ミスあたり充填コストは倍増**し、
> 総課金は `N × LAT_FILL − (N ÷ S)` すなわち **ほぼ不変（むしろ微増）** となる。
>
> これは「**バースト非対応の現行 RTL ではライン拡大に利得が無い**」という
> §3.5 の結論を、手計算で検算可能な形で実証するものである。
>
> 逆に、将来バースト対応して `LAT_FILL < LAT` となれば、
> 同じテストで**ライン拡大が有利に転じる**ことが観測できるはずである。
> **T-05 は改良4 の成果を工程②へ橋渡しする中核の検証項目**と位置づける。

### 8.2 絶対ゲート（各段で確認）

| ID | 対象 | 期待値 |
|---|---|---|
| G-1 | Dhrystone（**`-mc` なし・課金なし**） | **819 / 48,785 / P:20** |
| G-2 | `yuios_road2.bin` | 56,416 B / md5 `599a7f9d1ebf103f81f58450ea1b6491` |
| G-3 | `dhry_final.bin` | 21,846 B |
| G-4 | `-q` stdout | **v2.00 と byte-exact 一致** |
| G-5 | YUIOS 起動 | `YUIOS Booted!` → `YUI>` |
| G-6 | 起動バナー | v2.10 が正しく出る |

---

## 9. ★レイテンシ値の実測校正手順★

> ★**手順 1 の B が得られるまで、LAT の確定値は存在しない。**★
> §3.3 の既定値 6 および §3.1.3 の机上推定 5.75 は**いずれも仮値**であり、
> **他文書の改版や設計判断を駆動してはならない。**

### 9.0 ★手順 0（M-5・校正の前に必ず実施）★

> **RTL を理想メモリ構成**（`mem_ready` 常時 1、PSRAM/CDC をバイパス）**で Dhrystone を走らせ、
> 総サイクル数が emu23 `-mc` の 351,761 と一致することを確認する。**
>
> **不一致の場合、その差分の原因を先に特定すること。**
> 差分を残したまま `965,640` を分子として用いてはならない。

**なぜ必要か**：手順 2 の引き算 `1,317,401 − 351,761` が「メモリ待ちの実費」を与えるためには、
**引き算の両辺が同じものを測っている**必要がある。
§3.1.1 が示すのは「個々の命令の CPI が表と一致する」ことであり、
**Dhrystone 全体の総サイクル数が一致することの証明ではない**。

想定される乖離要因：

| 要因 | 内容 |
|---|---|
| 割込受理コスト | emu23 は実加算 8（`emu23_mc_design_v0_6.md` §3.1.1）。RTL 実測との一致は未確認 |
| MMIO アクセスのサイクル | emu23 は MMIO 非課金だが、RTL は `mmio_stub` 経由で固有サイクルを要する |
| リセットシーケンス | emu23 は非計上（同 §3.4）。RTL は 2 サイクル |
| 表に無い命令 | emu23 は既定 4 で代替。RTL の実際の値と一致する保証はない |

#### 9.0.1 ★理想メモリ TB は存在しない（2026-08-19 調査結果）★

手順 0 の実施にあたり既存 TB を調査した結果、
**Dhrystone を理想メモリで完走させる TB は存在しない**ことが判明した。

| 分類 | 該当ファイル | 用途 | Dhrystone 完走 |
|---|---|---|---|
| 理想メモリ TB（`mem_ready` 常時 1） | `tb_cpu_fetch/mem/byte/memalign/iret/irq/align_irq_v0_1.sv`、`tb_cpu_v2a/v2c/v2d/v2e_v0_1.sv` | **V1/V2 期の命令検証用** | ❌ 不可 |
| Dhrystone 完走 TB | `tb_cpu_v8b_prod_v0_2.sv`、`tb_cpu_v8d_char_fix_v0_1.sv` | 統合検証 | ✅ 可（**PSRAM/CDC 経由**） |

**経緯の記録**：`ysd8800_v5_membus_v0_2.sv` L63-65 は
「Dhrystone の `SW_SCORE` はクロック数を返す。実機では別値になる。
**実機判定は『完走＋Dhrystone 結果値(826)』のみ**」と述べている。
すなわち**サイクル数の一致を要求してこなかった**方針が、M-5 の未検証状態を生んだ。
手順 0 は、その前提を**改良4 のためだけに一段深く踏み込む**作業である。

#### 9.0.2 ★TB の作成方針（KY38 準拠）★

> **V1/V2 期の理想メモリ TB を流用してはならない。**
> 64KB フラットメモリ・MMU 無しの構成であり、
> `yuios_road2.bin`（56KB、MMU/PSRAM 前提）を載せられない。

**採用する方式：`tb_cpu_v8b_prod_v0_2.sv` からの派生**

```
tb_cpu_v9_idealmem_poc_v0_1.sv
  = tb_cpu_v8b_prod_v0_2.sv から
    PSRAM/CDC ブリッジをバイパスし mem_ready を常時 1 にしたもの
```

| # | 利点 |
|---|---|
| 1 | メモリマップ・MMU・ロード手順が**動作実績のあるものをそのまま使える** |
| 2 | 差分が**メモリ応答部のみに局所化**され、比較の妥当性が担保される（**KY54 の陰性対照の構図そのもの**） |
| 3 | **KY38（_poc 先行）**に従い、本番 TB を変更せず実験ファイルで先行確認できる |

> ★**V8-b 確定構成（14 ファイルセット＋SD モデル）には手を触れないこと。**★
> ファイル名は `_poc` を付し、実験ファイルであることを明示する。

> **分子が 1% 狂えば LAT も 1% 狂う。** LAT は改良4 の全成果物
> （トレース解析・ラインサイズ評価・工程②の判断材料）の基礎である。
>
> 未消化のまま校正すると、誤った LAT が §11-1 を駆動し、
> **v0.1 で犯した「未確定値が他文書の改版を駆動する」誤りを、値を変えて繰り返す**ことになる。

### 9.1 校正手順

| # | 手順 |
|---|---|
| **0** | **★上記 §9.0 を実施し、前提の一致を確認する★** |
| 1 | Dhrystone を `-mc --trace-addr` で走らせ、**総アクセスバイト数 B** を実測する（トレース行数−ヘッダ行） |
| 2 | RTL 実測との差分 **965,640 cycles**（＝1,317,401 − 351,761）を分子とする ※手順 0 で補正済の値を使う |
| 3 | `LAT − 1 = (補正済分子) ÷ B` から **LAT を逆算**する |
| 4 | 種別ごとの内訳は、トレースの `kind` 列で分類集計して求める |
| 5 | 机上推定 5.75 と実測値の乖離を評価する。**乖離が大きい場合はモデルの誤りを疑う**（KY54 の精神） |
| 6 | 確定値を定義ファイルの既定として本設計書 v1.0 に反映する |

> この手順は**全ミス条件での校正**である。RTL 側にキャッシュは無いため、
> 全アクセスが PSRAM に到達しており、前提として妥当。
>
> ★ただし手順 3 は `LAT_FILL` を分離できない★（RTL にキャッシュが無くライン充填が存在しないため）。
> `LAT_FILL` は §3.5 のとおり**現行 RTL では `LAT` と同値**として扱い、
> バースト対応後の値は工程②で別途取得する。

---

## 10. リスクと KY

| # | リスク | 方向 | 防止策 |
|---|---|---|---|
| **1** | **メモリ課金の二重計上** | 過大 | §3.2.2 に「バイトごとに (LAT−1)」を明記。T-02/T-03 で検算 |
| **2** | **★課金漏れ（未計上）★**（M-1 の教訓） | **過少** | ライン充填・書きヒットを課金式に明記。T-05/T-06 で検算 |
| **3** | **★未確定値を確定値として扱う★**（M-4 の教訓） | — | §3.1.2/§3.3/§9 に「仮値」「机上推定」と明記 |
| **4** | **★引き算の両辺が同じものを測っていない★**（M-5 の教訓） | — | **§9.0 手順 0** を校正前の必須確認とする |
| 5 | `mem_charge()` を `cpu` 宣言より前に置きビルドエラー | — | §5.1 に配置位置を明記 |
| 6 | フックを MMIO 分岐の**前**に置き MMIO を課金 | 過大 | §5.1 に分岐行を明記 |
| 7 | 16bit を 1 回だけ判定しライン境界跨ぎを取り逃す | 過少 | §3.2.2 注意2 に明記。T-07 で検算 |
| 8 | `LAT_RD16` / `LAT_FILL` を「全体の値」と誤解 | 過少 | §3.3 に「1 バイトあたり」と明記 |
| 9 | 統計出力が `make regress` の grep を誤爆 | — | §6.5 の警告文言規約を遵守 |
| 10 | トレース出力の巨大化 | — | 既定無効。`--cache-stats` を用意 |

> ★**KY 活動の反省（記録・保持）**★
> v0.1 の KY は「二重計上＝**課金しすぎ**」の一方向のみを予測していた。
> その結果、**課金しなさすぎ**（M-1／M-2）を見落とした。
> **KY は危険を両方向で予測すべき**という教訓であり、kaizen 原則への登録を推奨する（§11-7）。

---

## 11. 派生起票（本改良では扱わない）

| # | 内容 | 起票先 |
|---|---|---|
| 1 | **`cache_study_handover_v1_0.md` の v1.1 改版**（60 cyc/byte は誤り。**正しい値は §9 の校正まで未確定**であり、改版は校正後） | §3.1.2 |
| 2 | **★PSRAM バースト転送対応の検討★**（§3.5）。キャッシュ RTL より優先度が高い可能性 | **優先度高** |
| 3 | **CDC 同期コスト（4.00 cyc・70%）の削減検討**。ack 同期段数の見直しを含む | **新規・優先度高**（C-5 決着により重要度上昇） |
| 4 | psram_clk 昇圧の効果再評価（約 1.22 倍と推定） | C-1 |
| 5 | CCR（キャッシュ制御レジスタ）の MMIO モデル | 別段（H-2） |
| 6 | Python キャッシュシミュレータ | 別ツール（H-1） |
| 7 | **kaizen 原則追加：「KY は危険を両方向で予測する」** | §10 の反省より |
| 8 | `instr_size()` が `mem[]` 直読で `--mmu` 時に表示が狂う | `emu23_mc_design_v0_6.md` **§13 表 項番 1** |
| 9 | RTL CPU FSM に `ID_BRK` 処理が無い | 同 **§13 表 項番 2** |
| 10 | EMU-D（シンボル名 31 文字超で BP 不能） | 未着手 |
| **11** | **`tb_cpu_v9_idealmem_poc_v0_1.sv` の派生作成**（§9.0.2）。段9 で必要。**V8-b 確定構成には手を触れない** | **§9.0.2・段9** |
| 12 | 手順 0 で乖離が判明した場合、`emu23 -mc` 自体の精度問題として別途起票すること | §9.0 |

---

## 12. 改版履歴

| 版 | 日付 | 内容 | 作成 |
|---|---|---|---|
| **v0.1** | 2026-08-19 | 初版。改良4（キャッシュ下地）の設計。案 B（アクセス単位課金）を前提に、課金粒度を**アクセス種別ごと**と決定。先行課題 2 件の検証結果を §3.1 に収録（**RTL v0.5.6→v0.5.8 は CPI 差分なし**／**PSRAM は約 60 ではなく約 5 CPU cyc/byte**）。サイクル表の内訳を実源確認し、**メモリアクセスが 1 バイトあたり 1 サイクル計上**であることを確定、**ペナルティは「バイトごとに (LAT−1)」**と §3.2 に明記。統合キャッシュとする根拠を §4.2 に記録 ※**本行は v0.1 の原文を保持している（KY41 削除禁止）。うち「約 5 CPU cyc/byte」は v0.2 で誤り（実測ではなく仮定を含む推定）と判明し、v0.3 現在の机上推定は 5.75 である** | Claude |
| **v0.2** | 2026-08-19 | **レビュー指摘書 v1.0（条件付き差戻し）への対応版。**①**M-1 是正**：ライン充填コスト未計上を修正し課金式を**4 分岐**に再構成。`LAT_FILL` を新設。`ysd8800_psram_ctrl_v0_2.sv` を実源調査し、**現行 RTL がバースト非対応ゆえ `LAT_FILL = LAT`** であることを §3.5 に記録。**バースト対応がキャッシュより優先されうる**旨を工程②へ申し送り。②**M-2 是正**：ライトスルーゆえ**書きヒットでも課金**。ライトバッファ無しの仮定を明記。③**M-3 是正**：`--cache-size` / `--cache-line` 新設。2 冪検査・エラー処理・`cache_enable` との関係を規定。④**M-4 是正**：v0.1 の「実測逆算 5.0」が**筆者の仮定を含む推定**であったことを自己申告し「**机上推定**」にラベル訂正。⑤**C-1 対応**：「律速は PSRAM 側にない」を撤回し内訳分解。⑥**C-2**：トレースにヘッダ行追加。⑦**C-3**：(a) 採用。⑧**C-4**：統計出力を新設し**警告文言規約の横展開漏れ**を是正。⑨N-1/N-2/N-4 採用、N-3 不採用。⑩**E-1**：フック行番号を MMIO 分岐行に統一。**E-3**：参照先を §13 表 項番 1/2 に訂正。⑪**T-05/T-06** 新設。⑫§10 に**課金漏れ**・**未確定値の確定値扱い**を追加し、**KY を一方向でしか予測しなかった反省**を記録 | Claude |
| **v0.3** | 2026-08-19 | **差分再レビュー（条件付き承認・再レビュー不要）への対応版。**①**M-5 是正**：§9 に**手順 0** を新設。校正の引き算 `1,317,401 − 351,761` が成立するには「**RTL 理想メモリ実測＝351,761**」という未検証の前提が必要であることを明示し、**校正実行前の必須確認**とした。乖離要因 4 件（割込受理・MMIO・リセット・表に無い命令）を列挙。②**M-6 是正**：T-05 を定量化。`追加課金 = N × LAT_FILL − (N ÷ S)` の式を導出し、`N=2048` で `S=32→12,224` / `S=64→12,256` という予測値を提示。**ライン拡大でミス回数半減と充填コスト倍増が相殺し総課金がほぼ不変**となることを示し、これが **§3.5 の結論（バースト非対応ではライン拡大に利得なし）の直接証明**になると位置づけた。③**C-5 決着**：`ysd8800_cdc_bridge_v0_2.sv` L89-96 を実源確認し、**ack 側は 2FF＋エッジ検出段の計 3 サイクル**（レビュー指摘が正しい）と確定。`req_hold` を加え CPU ドメイン計 **4.00**。机上推定を **4.75 → 5.75 CPU cyc/byte** に、内訳比率を **30%/70%** に、32B 充填コストを **184 cyc** に、仮値を **5 → 6** に更新。**CDC 支配がさらに強まり §3.1.3 の結論は強化された**。④**C-6 決着**：`ysd8800_psram_ctrl_v0_2.sv` L107-108 のコメントより **`LATENCY_NORMAL=12` は req 受理〜ack 成立の FSM 全体の値**と確定。現行換算のままで正しく、追加加算不要。⑤**C-7 対応**：ライン境界跨ぎを §3.2.2 注意2 に明記し **T-07** を新設。⑥**C-8 是正**：**v0.1 の改版履歴行を原文のまま復元**し（KY41 削除禁止）、誤りであった旨を注記の形で追加。⑦**C-9 対応**：`charge_enable` を `cache_enable` と分離し、状態の対応表を §5.4 / §6.6 に明記。⑧**N-5**：インデックス計算式を §5.5 に追加し、2 冪検査の必要性と結び付けた。⑨**N-6**：全オプションの**引数消費数一覧**を §6.7 に新設。⑩**N-7**：損益分岐ヒット率を定量化し、**ライン 32B では 97.4% が損益分岐**であることを §3.5 に明記。⑪**E-4**：T-03 の意図が「読みの 2 バイト課金確認」から「書きの 2 バイト課金確認」へ変更された理由を明記。⑫§10 リスクに **M-5 の教訓（引き算の両辺が同じものを測っていない）**と **C-7 の教訓**を追加。⑬§11 に **CDC 削減検討**を優先度高で新規起票 | Claude |

| **v0.4** | 2026-08-19 | **理想メモリ TB 存在調査（M-5 手順 0 の前提確認）の結果を反映。**①**§9.0.1 新設**：既存 TB を調査し、**Dhrystone を理想メモリで完走させる TB は存在しない**ことを確定。V1/V2 期の理想メモリ TB（`tb_cpu_v2a〜v2e` 等）は命令検証用で 64KB フラット・MMU 無しのため `yuios_road2.bin`（56KB）を載せられず流用不可。あわせて `ysd8800_v5_membus_v0_2.sv` L63-65 の「**実機判定は完走＋Dhrystone 結果値(826)のみ**」という記述を根拠に、**サイクル数の一致を要求してこなかった方針が M-5 の未検証状態を生んだ**経緯を記録。②**§9.0.2 新設**：TB 作成方針を **`tb_cpu_v8b_prod_v0_2.sv` からの派生**（`tb_cpu_v9_idealmem_poc_v0_1.sv`）と決定。差分をメモリ応答部のみに局所化することで**比較の妥当性を担保**し、**KY38（_poc 先行）**に従い本番 TB を変更しない。**V8-b 確定構成（14 ファイルセット＋SD モデル）には手を触れない**旨を明記。③**§7 に段9（M-5 手順 0 の実施）を新設**し、旧段9（波及文書改版）を段11 へ繰り下げ。段10（校正実行）を新設。**段9→段10→段11 の順序を逆転させてはならない**旨を警告として追加（逆転すると誤った LAT が文書改版を駆動し v0.1 の誤りを繰り返す）。④**§11 に起票11**（TB 派生作成）**・起票12**（手順 0 で乖離した場合は `emu23 -mc` 自体の精度問題として別途起票）を追加。⑤ヘッダに「**実装は別チャットで実施・起点は `HANDOVER_CHAT138.md`**」を追記 | Claude |

---

*— 以上 `emu23_cache_base_design_v0_4.md` v0.4 —*
