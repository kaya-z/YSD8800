# 工程② キャッシュRTL開発 ②-A：PSRAM バースト対応 設計書

| 項目 | 値 |
|---|---|
| 文書名 | `v10_psram_burst_design_v0_4.md` |
| 版数 | **v0.4** |
| 作成日 | 2026-08-27（v0.4 改版日 **2026-08-28**） |
| 工程 | ② キャッシュRTL開発 → **②-A（バースト先行）** |
| 状態 | ★**実装完了（段0〜段5）／ゲート全通過**★ — 実測結果を反映 |
| レビュー | `review_v10_psram_burst_design_v0_1_v1_0.md`（M×6／C×4／N×3／E×2）<br>`review_v10_psram_burst_design_v0_2_v1_0.md`（★承認★／M×3／C×3／N×2） |
| 前提文書 | `v9_psram_perf_baseline_v1_2.md`／`v9_psram_perf_design_memo_v0_4.md`／`emu23_cache_base_design_v1_0.md` §3.5／`HANDOVER_CHAT144.md` |
| 対象 | `ysd8800_psram_ctrl_v0_2.sv` → **v0.3**（実装済 md5 `78d386e9…`）<br>`ysd8800_v5_membus_v0_5.sv` → **v0.6**（実装済 md5 `c9e77f67…`） |

> ⚠️ `cache_study_handover_v1_0.md` は**単独参照禁止**（クロック比の向きが逆）。
> 是正文書 `cache_study_numbers_audit_v1_0.md` は**ナレッジに不在**のため、
> 本設計は同文書を数値根拠として一切使用しない。

---

## 1. 目的と、なぜバーストが先なのか

### 1.1 目的

`ysd8800_psram_ctrl` に**連続転送（バースト）能力**を追加する。
ただし本工程では**誰もその能力を使わない**。②-B のキャッシュが接続されて初めて機能する。

### 1.2 ★キャッシュ単独では成立しない（本工程の存在理由）★

現行 `psram_ctrl_v0_2.sv` は 3 状態 FSM（`PS_IDLE`/`PS_BUSY`/`PS_WAIT_REQ_LOW`・L66）で
**req/ack 1 バイト 1 トランザクション**であり、連続転送モードを持たない。
したがって `LAT_FILL = LAT` であり、**バースト償却が存在しない**。

**★C-2 対応：損益分岐式の出所を明示する★**

厳密式（`emu23_cache_base_design_v1_0.md` §3.5）：

```
(1 − h) = (LAT − 1) ÷ ((S × LAT_FILL) − 1)
```

簡約式は `S × LAT − 1 ≫ 1` として `−1` を落としたものである：

```
h ≒ 1 − 1/S          ← ★近似★
```

~~`h ≒ 1 − 1/S` と書きつつ表に 97.4% を掲げていた（v0.1）~~
→ ★**両者は別の値である**★。`LAT = LAT_FILL = 5` を代入すると：

| ライン `S` | ★厳密式★ | 近似式 `1−1/S` | 差 |
|---|---|---|---|
| 32 B | **97.4%**（`1 − 4/159`） | 96.9% | +0.5 pt |
| 64 B | **98.7%**（`1 − 4/319`） | 98.4% | +0.3 pt |

**表の値は厳密式であり、近似式は説明用**である。以後、判断には**厳密式**を用いる。

**★C-5 対応：どの `LAT` に基づく値かを明示する★**

| `LAT` | 由来 | S=32 の `h` | S=64 の `h` |
|---|---|---|---|
| **5** | `emu23_cache_base_design_v1_0.md` 段10 校正（★A-2q **以前**★） | **97.4%**（`1−4/159`） | **98.7%**（`1−4/319`） |
| **4** | ★A-2q 適用後の推定値（未校正）★ | **97.6%**（`1−3/127`） | **98.8%**（`1−3/255`） |

> 結論（バースト無しでは成立しない）はどちらでも揺るがない。
> ★**再校正（Q2 承認済）の対象に「損益分岐ヒット率の再計算」も含める**★こと。②-B で漏らさない。

> ★★ダイレクトマップ統合キャッシュで 97.4% は現実的でない。
> **バースト無しでキャッシュを載せると性能が悪化する。**★★

バースト有りでは構造が一変する（**試算・②-B で実測により確定**）：

| 方式 | 32B ライン充填コスト | 損益分岐 `h` |
|---|---|---|
| 現行 | 約 **128 cpu cyc**（32 × 4 ※） | **97.4%** |
| バースト有り | 約 **10 cpu cyc**（CDC 1 回 ＋ 12+31 psram cyc） | 約 **60%** |

> ※ **E-2 対応**：この **4** は★**A-2q 適用後**の 1 バイトあたり cpu cyc★である。
> BASE-01（`baseline v1.2` §4）時点は **5〜6** であり、①.5 の CDC −1 cpu cyc を反映した値。
> baseline v1.2 と直接突合すると一致しないので注意。**再校正（Q2 承認済）で確定させる。**

根拠は `baseline v1.2` §3.6 の非対称性：

> ★**キャッシュのライン転送内部は量子化の影響を受けない**★（CPU 可視トランザクションが 1 回のみ）

### 1.3 ★なぜ「同時設計」ではなく「バースト先行」なのか★

②-A は**システムレベルで効果が構造的にゼロ**である。
キャッシュが無い以上、2 バイト目以降の**置き場所が無く**、バースト経路を誰も叩かない。

- 実際にロジックを触る（ビルド成果物が変化する）
- しかし論理的に結果が変わらないことが**構造的に自明**

**★M-5 対応：②-A が担保するのは「特異性」のみである★**

~~★②-B の測定系の信頼性を、先に買っておく工程★である。~~
→ ★**過大な主張であった。以下に限定する。**★

| 概念 | 定義 | ②-A が担保するか |
|---|---|---|
| **特異性** | 変えていないものを「変わった」と言わない（偽陽性の否定） | ★**する**★ |
| **感度** | 変えたものを「変わった」と言える（偽陰性の否定） | ★**しない**★ |

②-A で G-0 バイト完全一致が得られても、それは測定系が**偽陽性を出さない**ことの証拠にしかならない。
★**②-B で「効果ゼロ」が出たとき、真にゼロなのか測定系が鈍いのかは、②-A の結果からは何も言えない。**★
→ ②-B では **KY54 の両方向検証（感度確認）が依然として必須**である（R-6 起票）。

**★C-4 対応：KY73 の引用を是正する★**

~~「①.5 では A-2 単独版をネガコンに選んで −7.3% 動いてしまい失敗した（KY73）」~~
→ KY73 の本文（`memo v0.4` L151）は
★「**単体 TB の負荷モデルが実ワークロードを代表しない**場合〜」★であり、
ネガコン選定の失敗ではなく**負荷モデルの代表性**を教訓としている。

正しい記述：**同一インシデント（A-2 単独版が単体 TB で 0・システムで −7.3%）から、
KY73 は「負荷モデルの代表性」を、本設計 §1.3 は「ネガコンの選定条件」を、
それぞれ別の教訓として抽出している。**

★重要な帰結★：KY73（感度側の失敗）は**負方向対照では検出できない**。
この点は M-5 の限定と整合しており、②-A の論拠を KY73 に寄りかからせてはならない。
②-A の論拠は★「経路が実行されないことによる構造的不変」★のみで自足する。

### 1.4 非目標

| 項目 | 扱い |
|---|---|
| キャッシュ本体 RTL | ②-B |
| CCR（キャッシュ制御レジスタ） | ②-B（`mmio_stub` v0_9）。★リセット値 disable・invalidate ビット必須★ |
| `LATENCY_NORMAL` のパラメータ化（N-3） | ②-B で一括整理 |
| CDC ブリッジのライン転送経路 | ②-B。★下記の構造的制約あり★ |
| 実機 PSRAM の物理タイミング | ③。本 FSM は**挙動モデル**である |
| CPU コア改修 | ★行わない（10 フェーズ連続無改修を維持）★ |

**★N-3 対応：②-B への構造的制約（申し送り）★**

`cdc_bridge_v0_4.sv` **L149** `assign cpu_mem_rdata = psram_rdata;` は★**組合せ**★である。
ブリッジ経由でバーストを流すと `psram_rdata` が psram クロック毎に変化し、
`cpu_mem_rdata` が beat 毎に揺れる。

> ★★**ブリッジ経由のライン転送は成立しない。
> ②-B のキャッシュは `psram_ctrl` に直結して `beat_valid` を受ける構成でなければならない。**★★

**★C-3 対応：モデル前提が崩れた場合の再評価条件★**

§1.2 の「バースト有り 約 10 cpu cyc」および B-4 の「総サイクル = LAT+31」は、
★`PS_BURST` が **1 psram cyc につき 1 バイト**を出せる★という前提に立つ。
この前提が②-B のライン長選択と損益分岐（約 60%）を直接決めている。

> ★**工程③で実機 PSRAM（HyperBus 等）の物理タイミングが判明し、
> 「1 バイト/psram cyc」が成立しないと判明した場合は、
> ②-B のライン長 `S` と損益分岐ヒット率を再評価する。**★
> 具体的には、実効ビート間隔を `k` psram cyc として
> 充填コストを `LAT + (S−1)×k` で再計算し、`S` を選び直す。

---

## 2. インタフェース設計

### 2.1 追加ポート（`ysd8800_psram_ctrl_v0_3`）

| 方向 | 信号 | 幅 | 意味 |
|---|---|---|---|
| input | `burst_len` | `BLEN_W` | 転送バイト数（**1 = 従来動作**） |
| output | `beat_valid` | 1 | ★**そのサイクルの `rdata` が有効な新規バイトであることを示す**★（M-9） |

### 2.2 追加パラメータ

| パラメータ | 既定 | 意味 |
|---|---|---|
| `BURST_MAX` | **32** | 最大バースト長（②-B のライン長上限。★②-B で最終確定★＝C-4 回答） |
| `BLEN_W` | `$clog2(BURST_MAX)+1` ＝ **6** | 32 を表現するため +1 |

### 2.3 ★req/ack 4 相契約★

| 信号 | 契約 |
|---|---|
| `req` | レベル。要求中 1。ack を見て要求側が下げる |
| `ack` | レベル。★**最終バイト確定時のみ** 1★。`req` が下がるまで維持。★`PS_BURST` 中は 0 を維持★ |
| `beat_valid` | ★**バースト中は `blen_r` サイクル連続で 1 となる（パルス列ではない）**★（M-9） |
| `rdata` | `beat_valid` と**同一サイクル**に有効（両者とも同じ `always_ff` で更新） |
| ★`we`★ | ★**`we=1` のとき `burst_len` は 1 でなければならない（バースト書きは未サポート）**★（M-3） |
| ★`burst_len`★ | ★`req` 受理サイクルでのみ参照され、`blen_r` にラッチされる。以後の変化は無視★（M-1） |
| ★`burst_len` の値域★ | ★**1 以上 `BURST_MAX` 以下の 2 の冪**でなければならない（1/2/4/8/16/32）★（M-8）<br>非 2 冪はアラインメント判定が意味を持たないため assertion で検出する |

**★M-2 対応：CDC ブリッジ側の根拠を是正★**

~~`cdc_bridge_v0_4` は `ack` の**レベル 4 相のみ**を見ており（L155 `req_sync`）~~
→ ★**誤り。L155 は req 経路（`assign psram_req = req_sync[1];`）であり ack とは無関係。**★

正しい実源：

| 行 | 記述 | 意味 |
|---|---|---|
| **L139** | `suppress_r <= ack_sync[0] & ~ack_sync[1];` | ★`ack_sync[0]` の**立上りエッジ**検出★ |
| **L148** | `assign cpu_mem_ready = suppress_r;` | 1 cpu cyc パルス |

> ★**ブリッジは `ack` のレベルではなく立上りエッジを 1 cpu cyc パルス化して使用する。**★
> `burst_len=1` では `ack` 波形が現行と 1 サイクルも違わないため、
> ★エッジ検出前提でも無改修で成立する★（結論は v0.1 から不変）。
>
> ⚠️ ②-B への警告：**「レベルを見ている」と誤解すると、
> 各 beat で `ack` を立てる設計変更が安全に見えるが、実際は挙動が変わる。**

### 2.4 アドレス生成

```
mem_idx(n) = (addr + n)[MEM_AW-1:0]     n = 0 .. blen_r-1
```

**ラップしない**。ライン境界跨ぎの防止は**呼び出し側の責務**（＋ assertion で検出）。
②-B のキャッシュはライン先頭アドレス（下位 `log2(S)` ビットが 0）で発行する。

---

## 3. ★レジスタレベル設計（M-1 対応・本版の中核）★

### 3.1 追加する内部レジスタ

| 信号 | 幅 | ★`rst_n` リセット値★ | req 受理時 | 更新 | 役割 |
|---|---|---|---|---|---|
| `beat_cnt` | `BLEN_W` (6) | ★`'0`★ | **0** | `PS_BUSY` の beat0 発行時に **1**／`PS_BURST` で毎 beat **+1** | 現在のビート番号 |
| `blen_r` | `BLEN_W` (6) | ★`BLEN_W'(1)`★ | ラッチ（飽和付） | 転送中は不変 | 確定バースト長 |
| ★`beat_valid`★ | 1（**出力**） | ★`1'b0`★ | — | `PS_BUSY`/`PS_BURST` で 1、`PS_IDLE`/`PS_WAIT_REQ_LOW` で 0 | `rdata` 有効表示 |

> ★**M-7：`rst_n` 非同期リセット節に必ず入れる。**★ 実源 L91-98 は既存 6 信号を全て明示初期化しており、
> 追加 3 信号だけ初期化しないのは不統一である。特に ★`beat_valid` は**出力ポート**★であり、
> 未初期化だと `1'bx` となって②-B のキャッシュ FSM が誤ってビートを取り込む。
> ②-A では未接続のため顕在化せず、★**②-B で「リセット直後の 1 ビートだけ誤る」という最も追いにくい形**★で出る。
> `blen_r` を **1** に初期化するのは、万一の暴走時も**単バイト動作に縮退**させるためである。

**★`blen_r` にラッチする理由★**（M-1 推奨に従う）
ポート `burst_len` を直接参照し続けると、★呼び出し側が転送中も値を保持する契約★に
依存してしまう。ラッチすれば `req` 受理サイクルの一瞬だけ有効であればよく、
②-B のキャッシュ FSM の設計自由度が上がる。

**★C-1 対応：`burst_len = 0` は 1 に飽和させる★**

```systemverilog
blen_r <= (burst_len == '0) ? BLEN_W'(1) : burst_len;   // ★飽和・破壊しない★
```

案 (a) を採用。アンダーフローで `BLEN_W` 全域（63 バイト）を転送する事故を**構造的に排除**する。

### 3.2 ★`mem_idx` の新定義★

現行（L72-73）は `addr` の**純配線**である：

```systemverilog
assign mem_idx = addr[MEM_AW-1:0];              // ← v0.2（現行）
```

v0.3：

```systemverilog
assign mem_idx = MEM_AW'(addr[MEM_AW-1:0] + MEM_AW'(beat_cnt));   // ← v0.3
```

| 論点 | 扱い |
|---|---|
| 幅整合 | `addr` は `PHYS_AW`(20)、`beat_cnt` は `BLEN_W`(6)。★両者を明示的に `MEM_AW` へキャストしてから加算★し、暗黙拡張に頼らない |
| ★C-7★ | `MEM_AW'()` 等の size cast が Icarus 12.0(`-g2012`) を通らない場合の代替：<br>`assign mem_idx = addr[MEM_AW-1:0] + {{(MEM_AW-BLEN_W){1'b0}}, beat_cnt};`<br>★段 1 の最初のコンパイルで確認する★ |
| 桁上げ | `MEM_AW` 幅で**ラップする**（`MEM_AW=20` なら 1MB 境界で折り返す）。ライン境界跨ぎを禁止しているため実害は生じないが、★仕様として明記★ |
| ★等価性★ | ★**`burst_len=1` では `beat_cnt` が 0 のまま `PS_BURST` を踏まないため、`mem_idx = addr[MEM_AW-1:0]` に縮退し、現行とビット単位で同一**★ |

> ★これが §3.4 の等価性論拠の実体である。v0.1 では**この縮退を明示していなかった**（M-1）。★

### 3.3 FSM 遷移

```
PS_IDLE ──req──▶ PS_BUSY ──busy_cnt==0──┬─(blen_r==1)─▶ PS_WAIT_REQ_LOW
                                         │               ack=1, beat_valid=1
                                         └─(blen_r >1)─▶ PS_BURST
                                                         beat_valid=1, ack=0
PS_BURST ──毎cyc 1バイト──┬─(beat_cnt != blen_r-1)─▶ PS_BURST
                          └─(beat_cnt == blen_r-1)─▶ PS_WAIT_REQ_LOW (ack=1)
```

**擬似コード（副作用を明記・設計原則に準拠）**

```
rst_n == 0:                                              // ★M-7: 非同期リセット節★
    ps_state <= PS_IDLE ; ack <= 0 ; busy_cnt <= 0
    rdata <= 8'h00 ; dbg_refresh_hit <= 0 ; refresh_this_access <= 0   // 現行と同一
    beat_valid <= 1'b0                                   // ★新規・出力のため必須★
    beat_cnt   <= '0                                     // ★新規★
    blen_r     <= BLEN_W'(1)                             // ★新規・単バイトへ縮退★

PS_IDLE:
    ack <= 0
    beat_valid <= 1'b0                                   // ★N-5: 明示クリア★
    if (req):
        refresh_this_access <= (lfsr[19:0] < REFRESH_THRESH)        // ★R-a: 先頭1回のみ★
        busy_cnt <= (refresh? LATENCY_REFRESH : LATENCY_NORMAL) - 2 // 現行と同一
        beat_cnt <= 0                                                // ★新規★
        blen_r   <= (burst_len==0) ? 1 : burst_len                   // ★新規・飽和★
        ps_state <= PS_BUSY

PS_BUSY:
    if (busy_cnt == 0):
        if (we && blen_r == 1): mem[mem_idx] <= wdata   // ★M-3: blen_r==1 に限定★
        rdata      <= mem[mem_idx]                       // beat 0（mem_idx = addr+0）
        beat_valid <= 1                                  // ★新規★
        dbg_refresh_hit <= refresh_this_access
        if (blen_r == 1):
            ack <= 1 ; ps_state <= PS_WAIT_REQ_LOW       // ★現行と完全同一★
        else:
            beat_cnt <= 1 ; ps_state <= PS_BURST
    else:
        busy_cnt <= busy_cnt - 1

PS_BURST:                                                // ★新規状態★
    rdata      <= mem[mem_idx]                            // beat n（mem_idx = addr+beat_cnt）
    beat_valid <= 1
    // ★ack は 0 を維持（明示的に代入しない＝前状態の 0 を保持）★
    if (beat_cnt == blen_r - 1):
        ack <= 1 ; ps_state <= PS_WAIT_REQ_LOW
    else:
        beat_cnt <= beat_cnt + 1

PS_WAIT_REQ_LOW:
    beat_valid <= 0
    if (!req): ack <= 0 ; ps_state <= PS_IDLE            // 現行と同一
```

> ★`beat_valid` は `rdata` と**同一の `always_ff` ブロックでノンブロッキング代入**する。
> 両者とも次サイクルに現れるため、§2.3 の「同一サイクルに有効」が満たされる。★

**サイクル数の検算（B-4）**：`blen_r=32`・`LATENCY_NORMAL=12` のとき
`PS_IDLE`(1) ＋ `PS_BUSY`(`busy_cnt` 10→0 ＝ 11) ＝ 12 サイクルで beat0、
以降 `PS_BURST` を 31 サイクル → ★**総計 LAT + 31 = 43 psram cyc**★。§1.2 の試算と整合。

### 3.4 ★`burst_len=1` の完全等価性（G-0 の根拠）★

| 要素 | 縮退後 |
|---|---|
| `PS_BURST` | ★一度も踏まれない★ |
| `beat_cnt` | 0 のまま不変 |
| `mem_idx` | `addr[MEM_AW-1:0]` に縮退（§3.2） |
| `ack` / `rdata` の立つサイクル | 現行と同一（`PS_BUSY` の `busy_cnt==0`） |
| 書込条件 `we && blen_r==1` | `blen_r==1` のため `we` と等価 |

> ★測定で偶然一致したのではなく、**経路が実行されない**ことによる構造的保証である。★

### 3.5 ★リフレッシュ判定の副作用（R-a 採用）★

現行 L105・L109 はリフレッシュ重畳を **`req` 受理時点で 1 回だけ**確定する。
バースト化するとこの判定が**バースト全体で 1 回**になり、
`REFRESH_PPM=500` は実質**バイトあたり 1/S に希釈**される。

**R-a（現状踏襲）を採用**。実 PSRAM でも行アクティベート時に 1 回であり、
バースト中のリフレッシュ割込は通常起きないため。

**★M-4 対応：観測手段を確定する★**

~~`dbg_refresh_hit` の発生回数を測定記録に残すこと~~
→ ★**現状 `membus_v0_5` L391 で `.dbg_refresh_hit()` は未接続であり、観測できなかった。**★

| 案 | 内容 | 判定 |
|---|---|---|
| ★**(a) TB からの階層参照**★ | `tb_cpu_v8b_prod` 内で<br>`dut.u_membus.u_psram_ctrl.dbg_refresh_hit` を監視しカウント | ★**採用**★。RTL 改修不要のため②-A の「効果ゼロ」性を損なわない |
| (b) membus に診断出力を引き出す | ロジック追加＝Case X-1 の前提が崩れる | ②-A では不採用 |

**②-B の測定時に必須**：リフレッシュ発生回数を BASE と比較し、
★リフレッシュ密度の変化による見かけの改善をキャッシュ効果に算入しない★こと。

---

## 4. `membus` v0.6 の改修

`ysd8800_v5_membus_v0_5.sv` L384-392 に**ポート接続 2 本を追加**（ロジック変更なし）。

```systemverilog
localparam int PS_BLEN_W = $clog2(32) + 1;   // ★E-2: 幅を明示するため membus 側にも定義★

ysd8800_psram_ctrl_v0_3 #(
    .LATENCY_NORMAL(12), .LATENCY_REFRESH(15), .REFRESH_PPM(500),
    .PHYS_AW(PHYS_AW), .MEM_AW(MEM_AW), .BURST_MAX(32)
) u_psram_ctrl (
    .clk(psram_clk), .rst_n(psram_rst_n),
    .addr(psram_addr_w), .wdata(psram_wdata_w), .we(psram_we_w),
    .req(psram_req_w), .ack(psram_ack_w), .rdata(psram_rdata_w),
    .burst_len (PS_BLEN_W'(1)),   // ★②-A: 常に1（休眠）。幅明示で'x混入を防ぐ★
    .beat_valid(),                // ★②-A: 未接続★
    .dbg_refresh_hit()            // ★TB階層参照で観測（§3.5 案a）★
);
```

★**モジュール名 `ysd8800_v5_membus_v0_1` は据置（Case X-1）**★
→ ①.5 で 3 世代を TB 無改修で通した運用を踏襲（HANDOVER §2）。

---

## 5. 検証設計

### 5.1 ★G-0：システム等価性（最上位ゲート）★

本日の KY74 に基づき、**効果測定より先に**等価性を固定する。

**★M-6 対応：出典欄を追加する★**

| 項目 | 期待値 | ★出典★ | 一次／二次 |
|---|---|---|---|
| M-1 `_kstart` | **7** | `HANDOVER_CHAT144.md` **L51** | ★二次★ |
| M-2 初 UART TX | **74,512** | 同 **L52** | ★二次★ |
| M-3 `YUIOS Booted!` | **222,994** | 同 **L53** | ★二次★ |
| M-4 `YUI> ` | **320,156** | 同 **L54** | ★二次★ |
| M-5 `0123MD` | **3,754,937** | 同 **L55** ＋ `memo v0.4` **L137** | ★二次（2 文書一致）★ |
| G-1 Dhrystone | 819 / 48,785 / P:20 | 絶対ゲート | — |
| G-2 `yuios_road2.bin` | 56,416 B / md5 `599a7f9d1ebf103f81f58450ea1b6491` | 絶対ゲート | — |
| AC-1 TCR-ACK | 1 hit @ `0x003d` | `baseline v1.2` §1.2 | — |

> ★★**KY34 の適用：上表は全て引継ぎ文書＝二次情報である。**★★
> ★**段 0（§5.3）で実ログを再取得し、一次情報として確定させてから G-0 の基準に据える。**★
> 二次情報のまま最上位ゲートの基準にしてはならない。
>
> ★参考（差異の説明）★：`baseline v1.2` L160-161 のビン A は
> `74,512 / 222,977 / 320,196 / 3,755,316` であり、M-2 のみ一致し M-3/M-4/M-5 は異なる。
> これは★ビン A（LAT=4〜6）と A-2q（CDC −1 cpu cyc）が**異なる機序**★であるため
> ~~と考えられる。**転記ミスではない。**~~ → ★**C-6：現時点では仮説である。断定しない。**★
> 前者は PSRAM 側レイテンシの短縮、後者は CDC 側の短縮であり、量子化の受け方が違う（`baseline v1.2` §3.6）
> ★**と推定される。段 0 で確定する。**★
>
> ⚠️ **未説明の点（C-6）**：機序が異なるなら M-2 も違う値になるのが自然だが、
> ★**なぜ M-2 だけが 74,512 で完全一致するのか**★は説明できていない。
> 偶然か、M-2 が量子化ビンの内側に収まっているためかのいずれか。**段 0 の一次確定で判断する。**

> ★**KY70 の運用強化（レビュー指摘を受けて）**★
> 「起票するだけでは効かない」ため、防止策に次を追加する：
> ★**新規設計書の基準値表には必ず「出典」欄を設ける**★（→ kaizen 登録候補）。

★ビルド成果物のサイズは**変化してよい**★（むしろ変化しないと FSM 追加が効いていない疑い）。

### 5.2 単体 TB（`tb_psram_ctrl_v0_2`・新規ベクタ）

★バースト経路は結合 TB からは叩けない★（`cdc_bridge` が単バイトのため）。
`tb_psram_ctrl_v0_1.sv` を v0_2 へ拡張し、**DUT を直接駆動**する。

| # | ベクタ | 期待 |
|---|---|---|
| B-1 | `burst_len=1` 読み | ★現行 v0.2 と同一サイクルで `ack`★（回帰） |
| B-2 | `burst_len=1` 書き | 同上 |
| B-3 | `burst_len=4` 読み | `beat_valid` 4 回・`rdata` が `addr`..`addr+3` の順 |
| B-4 | `burst_len=32` 読み | ★**`beat_valid` が 1 であるサイクルを計数して 32**（エッジ計数ではない・M-9）★・中間 31 beat で `ack==0` を能動確認（N-2）・総 = LAT+31 |
| ~~B-5~~ | ~~`burst_len=4` 書き~~ | ★**削除（C-3 承認：バースト書き非実装）**★ |
| B-6 | 4 相確認 | `ack` は `req` が下がるまで維持 |
| B-7 | `burst_len=2` | 最小バーストで `PS_BURST` を 1 回だけ踏む |
| B-8 | 連続バースト | バースト終了後、次の `req` が正常受理される |
| ★**B-9**★ | ★`burst_len=0`★ | ★1 に飽和し、B-1 と同一波形（C-1）★ |
| ★**B-10**★ | ★`we=1, burst_len=4`★ | ★`mem[addr]` のみ更新・`addr+1..+3` は**不変**（M-3）★<br>★**N-4：本ベクタのみ `$error` の発火が期待動作（発火しなければ FAIL）。他ベクタと判定基準が逆である**★ |

### 5.3 実装段階（1 変更 1 検証）

| 段 | 内容 | 検証 |
|---|---|---|
| **0** | ★BASE 再現（フルビルド）★ | ★§5.1 の M-1〜M-5 を**実ログで一次確定**★（KY34） |
| 1 | `psram_ctrl_v0_3`：ポート・パラメータ・レジスタ追加（FSM 遷移は未追加） | 単体 B-1/B-2 が **1 サイクルもずれない**<br>★**C-7：最初のコンパイルで size cast が Icarus 12.0 を通ることを確認**★ |
| 2 | `PS_BURST` 追加 ＋ assertion | 単体 B-3/B-4/B-6/B-7/B-8/B-9/B-10 |
| 3 | `membus_v0_6` 追従 | ★G-0 バイト完全一致★ |
| 4 | 絶対ゲート | G-1／G-2／AC-1 |
| 5 | 文書・台帳改版 | KY41 4 点整合 |

> ★段 1 と段 2 を分ける理由★：段 1 は**レジスタを足しただけで FSM 遷移は不変**。
> 既存ベクタが 1 サイクルもずれないはず。ここでずれたら追加自体に副作用がある。
> **感度ではなく不変を確認する段**である。

> **E-1 対応**：段 0 のフルビルド構成 **16 本**の内訳は
> ★`v9_psram_perf_baseline_v1_2.md` §1.3★ に一覧がある（CPU 系 4／メモリ系 5／MMIO 1／
> ペリフェラル 4／検証 2）。ただし①.5 で 3 本が世代更新されているため、
> 現行版数は `fpga_source_version_ledger_v1_12.md` を正とする。

### 5.4 ★assertion（N-1 採用）★

```systemverilog
`ifndef SYNTHESIS
    always_ff @(posedge clk) begin
        if (ps_state == PS_IDLE && req) begin
            if (burst_len > 1 && we)
                $error("PSRAM: burst write not supported (blen=%0d)", burst_len);
            // ★M-8: 境界条件は BURST_MAX ではなく burst_len に依存する★
            if (burst_len > 1 && ((addr & (PHYS_AW'(burst_len) - 1)) != '0))
                $error("PSRAM: burst must be %0d-byte aligned (addr=%h)", burst_len, addr);
            if (burst_len > BURST_MAX)
                $error("PSRAM: blen exceeds BURST_MAX (%0d)", burst_len);
            // ★M-8: 非2冪は上式が意味を持たないため別途検出★
            if (burst_len > 1 && ((burst_len & (burst_len - 1)) != '0))
                $error("PSRAM: blen must be a power of 2 (%0d)", burst_len);
        end
    end
`endif
```

- Gowin EDA は合成時に `SYNTHESIS` を定義するため**合成対象から確実に外れる**
- ★`// synthesis translate_off` 系のコメントプラグマはツール依存で挙動が割れるため `ifndef` を採用★
- ★**M-8 訂正の経緯**★：v0.2 は `$clog2(BURST_MAX)` 固定であったため、`burst_len=4` で `addr=0x0004` が
  **正常であるのに `$error` が出る**（B-3／B-7 の正常系ベクタが汚染される）。段 2 の切り分け工数を生むため是正した
- §3.3 の RTL 側無害化（`we && blen_r==1`）と合わせ、
  ★**シミュレーションでは顕在化し、実機では破壊しない**二段防御★

---

## 6. リスクと KY

| # | リスク | 対策 |
|---|---|---|
| **KY74** | キャッシュ／バースト導入で絶対ゲートの前提が崩れ、論理バグを構造変更のせいと誤認 | ★G-0（等価性）を効果測定より先に固定★。②-B では CCR=disable |
| R-1 | `burst_len` 未接続で `'x` | membus で ★`PS_BLEN_W'(1)` と幅明示★ |
| R-2 | ライン境界跨ぎで静かに誤データ | §5.4 assertion |
| R-3 | リフレッシュ密度の希釈を効果と誤認 | R-a ＋ ★TB 階層参照で `dbg_refresh_hit` を計数★（§3.5） |
| R-4 | ②-A で「効果ゼロ」を失敗と誤認 | ★本工程の期待値は効果ゼロ★（§1.3） |
| ★**R-5**★ | ★`we=1 && burst_len>1` で**同一 wdata を S バイト書き込みメモリを不可逆破壊**★ | ★RTL 無害化（`we && blen_r==1`）＋ assertion の二段★（M-3） |
| ★**R-6**★ | ★②-A の G-0 合格をもって**測定系全体の信頼性**が得られたと誤認★ | ★②-A は特異性のみ担保。②-B で KY54 両方向検証を必須とする★（M-5） |

---

## 7. レビュー指摘の反映状況

| # | 指摘 | 反映箇所 |
|---|---|---|
| M-1 | レジスタレベル設計が無い | ★§3.1／§3.2 新設★ |
| M-2 | `cdc_bridge` 引用誤り | §2.3（L139／L148 へ是正・エッジ検出と明記） |
| M-3 | バースト書きの未定義動作 | §2.3 契約／§3.3 無害化／§5.4 assertion／B-10／R-5 |
| M-4 | `dbg_refresh_hit` 観測不能 | §3.5（TB 階層参照＝案 a を採用） |
| M-5 | 特異性と感度の混同 | §1.3（限定）／R-6 |
| M-6 | 基準値の出典無し | §5.1（出典欄・KY34 適用・段 0 で一次確定） |
| C-1 | `burst_len=0` | §3.1（1 に飽和）／B-9 |
| C-2 | 損益分岐式の不一致 | §1.2（厳密式と近似式を分離） |
| C-3 | モデル前提の再評価条件 | §1.4 |
| C-4 | KY73 引用意図 | §1.3（是正・別教訓として整理） |
| N-1 | assertion 実装 | §5.4 |
| N-2 | 中間 beat の `ack==0` | B-4 |
| N-3 | ②-B 構造的制約 | §1.4 |
| E-1 | 「16 本」の出典 | §5.3 注記 |
| E-2 | 幅明示・「4」の注記 | §4／§1.2 注記 |
| ★M-7★ | リセット値未定義 | ★§3.1 表に `rst_n` 欄／§3.3 リセット節★ |
| ★M-8★ | assertion 境界条件の誤発火 | ★§5.4（`burst_len` 依存へ是正）／§2.3 に 2 冪契約★ |
| ★M-9★ | `beat_valid` 波形定義の不整合 | ★§2.1／§2.3（連続 High）／B-4 計数方法★ |
| C-5 | `LAT` が 4 と 5 で揺れ | §1.2（両論併記・再校正対象に追加） |
| C-6 | 「転記ミスではない」は仮説 | §5.1（断定を撤回・段 0 で確定） |
| C-7 | size cast の Icarus 対応 | §3.2／§5.3 段 1 |
| N-4 | B-10 は `$error` 発火が期待 | B-10 |
| N-5 | `PS_IDLE` で `beat_valid` 明示クリア | §3.3 擬似コード |

## 9. ★実装結果（v0.4 追記・2026-08-28）★

### 9.1 段別の実測結果

| 段 | 内容 | 結果 |
|---|---|---|
| 段 0 | BASE 再現・基準値の一次確定 | ✅ M-1〜M-5 が二次情報と**全一致**→一次確定 |
| 段 1 | ポート・パラメータ・レジスタ追加（FSM 不変） | ✅ ★等価性 TB で **7,563 サイクル完全一致**★ |
| 段 2 | `PS_BURST` 追加 ＋ assertion | ✅ B-3〜B-10 ALL PASS |
| 段 2' | ★案 B：TB 強化（B-11〜B-13）★ | ✅ 追加 3 ベクタ PASS |
| 段 3 | `membus_v0_6` 追従 | ✅ ★**G-0 バイト完全一致**（md5 `34a5d1b9…`）★ |
| 段 4 | ゲート確認 | ✅ G-2／AC-1／G-5 |
| 段 5 | 文書・台帳改版 | 本版 |

### 9.2 ★成果物（実測 md5）★

| ファイル | 版 | md5 | 行数 |
|---|---|---|---|
| `ysd8800_psram_ctrl_v0_3.sv` | v0.3 | `78d386e909ca503cff95a79081b4efd6` | 252 |
| `ysd8800_v5_membus_v0_6.sv` | v0.6 | `c9e77f675d52fbe3c4fe48703f1b4e0b` | 420 |
| `tb_psram_ctrl_v0_3.sv` | v0.3 | `dc3467693b9a9e25f0568a3fca1fc312` | 354 |
| `tb_psram_equiv_v0_1.sv` | v0.1 | `a8f9dd47ddb453b72bcfeef0daf1ed8e` | 165 |

### 9.3 ★バースト性能の実測（設計仮定の検証）★

**B-11（全長掃引）により、単一点ではなく線形性そのものを実証した。**

| `blen` | 総サイクル（TB 計数） | 増分 |
|---|---|---|
| 1 | 13 | — |
| 2 | 14 | +1 |
| 4 | 16 | +2 |
| 8 | 20 | +4 |
| 16 | 28 | +8 |
| 32 | 44 | +31（対 blen=1） |

★**全長で `total_cyc = 12 + n` に厳密に乗る**★＝「1 バイト/psram cyc」が成立。
コスト式 `LAT + (S−1)` が **S を変えても使える**ことが保証された。
→ ②-B でライン長を 16／64 に変更しても損益分岐の再計算が可能。

バースト無しなら 32 B に `32 × 12 = 384` psram cyc を要するところが **44 cyc**。

### 9.4 ★リフレッシュ希釈の実測（B-13・R-a の帰結）★

同一転送量（1024 B）での `dbg_refresh_hit` 発生回数：

| 転送方法 | 発生回数 |
|---|---|
| 単バイト × 1024 回 | **1 hit** |
| 32 B バースト × 32 回 | **0 hit** |

★§3.5 で予測した希釈が実測で確認された。★
②-B でキャッシュ導入後に性能が改善したとき、その一部は
**キャッシュ効果ではなくリフレッシュ発生率の低下**である。
1 回あたり 3 cyc（12→15）で絶対量は小さいが、機序として存在する。
→ ★②-B の測定では `dbg_refresh_hit` を BASE と比較し、この分を効果から除外する★

> B-13 は**意図的に PASS/FAIL 判定を置いていない**。LFSR の確率的挙動に
> 判定を依存させると不安定な TB になるため、密度の**記録**を目的とする。

### 9.5 ★C-6 の確定（v0.3 で保留していた仮説）★

v0.3 §5.1 で「なぜ M-2 だけがビン A と一致するのか説明できていない」として
段 0 に持ち越した件について、実測により次のとおり確定した。

- 段 0 で M-1〜M-5 の**全 5 点**が HANDOVER 記載値と一致
- M-2（74,512）は★ブート直後で PSRAM アクセスの累積が少ない区間★

CPU:PSRAM = 8:1 の量子化構造により、**アクセス回数が少ない区間では
ビン A（PSRAM 側短縮）と A-2q（CDC 側短縮）が同一ビンに落ちる**。
M-3 以降は PSRAM アクセスが累積し、量子化を受けない CDC 側短縮だけが差として現れる。

→ ★「転記ミスではない」は実測により裏付けられた。仮説を確定に格上げする。★

### 9.6 ★C-7 の確定（size cast の Icarus 対応）★

`BLEN_W'(1)` 等の size cast は **Icarus 12.0 (`-g2012`) をそのまま通った**。
§3.2 に用意した代替案（連接によるゼロ拡張）は**不要**。
ただし `mem_idx` の加算のみ可読性を優先して連接形を採用した：

```systemverilog
assign mem_idx = addr[MEM_AW-1:0] + {{(MEM_AW-BLEN_W){1'b0}}, beat_cnt};
```

### 9.7 ★検証対象ゲートの是正（重要・v0.4 の主要な訂正）★

v0.3 §5.1 は G-1（Dhrystone）を②-A の絶対ゲートに含めていた。
実際に段 4 で実行し **819 / 48,785 / P:20 で PASS** したが、
★**これは②-A の正しさを何ら保証していない**★。

| ゲート | 検証系 | ②-A の改修対象（`psram_ctrl`）を通るか |
|---|---|---|
| G-1 Dhrystone | **emu23** | ★**通らない**★ |
| G-2 md5 | ビルド成果物 | ★**通らない**★ |
| AC-1 | ビルド成果物 | ★**通らない**★ |
| G-0 / G-5 | **RTL** | ✅ 通る |

②-A は★RTL のみを改修し、ツールチェーンは 1 バイトも触っていない★。
Dhrystone 回帰の規約は「**ツール類を改修した時**」の条件付きであり、
②-A は適用条件を満たさない。にもかかわらず機械的に実行し、
「絶対ゲート全通過」として報告した。**通過した事実は正しいが、
証拠能力のない項目を成果として数えた**ものである。

> ★**是正原則（kaizen 登録候補）**★
> 工程開始時に「本工程が改修する層」を宣言し、
> ★**その層を実際に通るゲートのみ**を検証対象とする★。
> RTL のみの改修工程では G-1／G-2／AC-1 は対象外とし、
> RTL 側ゲート（G-0／G-5）で判定する。

### 9.8 ★②-B への申し送り（案 C・最重要）★

**G-0 は `PS_BURST` を一度も実行していない。**

`membus_v0_6` は `burst_len` を `PS_BLEN_W'(1)` に固定しているため、
`blen_r` は常に 1、`PS_BUSY` は常に `blen_r==1` 側へ分岐し、
★`PS_BURST` は 3,754,937 サイクルを通じて一度も踏まれていない★。
G-0 の md5 一致は「バーストが正しく動いた」証拠ではなく、
★「バースト経路が実行されなかった」証拠★である（これは②-A の設計意図どおり）。

したがって現時点で `PS_BURST` を実行しているのは**段 2 の単体 TB のみ**であり、
しかも DUT 直接駆動のため **CDC ブリッジ・MMU・CPU を含むシステム文脈では
一度も動作していない**。

> ★★**②-B 段 0 として、キャッシュ本体を載せる前に
> 「バースト経路をシステムに通す」工程を必ず置くこと。**★★
> `membus` で `burst_len` を有効化し YUI OS を完走させる。
> キャッシュが無いため 2 バイト目以降は捨てられ、
> ★論理結果は単バイトと同一になるはず★。ここで壊れたら
> 原因はバースト経路に確定し、キャッシュ本体との切り分けが不要になる。
>
> ただし §1.4 N-3（`cpu_mem_rdata` が組合せのためブリッジ経由の
> ライン転送は成立しない）との関係上、②-B 段 0 の位置づけは
> 「ライン転送を成立させる」ことではなく
> ★「バーストを流しても最終 `ack` の契約が守られシステムが壊れない」★
> ことの確認に限定される。

**根拠（ユーザ判断・2026-08-28）**：
「実装の途中でシステムが動くことを確認しないと無駄な手戻りが発生する」

### 9.9 環境に関する記録（ビルド手順書 v1.15 への追記候補）

段 0〜段 2 で判明した環境起因の躓き：

| # | 事象 | 対処 |
|---|---|---|
| 1 | `iverilog` がコンテナ初期状態で不在 | `apt-get install -y iverilog`（12.0 stable） |
| 2 | Force は `frontend/`（ir・lexer・parser）と★`backend/`（codegen）★の分離必須 | `codegen_v1_4.h`／`codegen_v1_5.c` を `backend/codegen.h`／`.c` へ改名配置 |
| 3 | `ysd8800_kern_v0_6.tgt` → **`ysd8800_kern.tgt`** への改名が必要 | Makefile・手順書は版数なし名を要求 |
| 4 | ★`iverilog -P` はトップモジュールを `-s` で明示しないと**黙って無視**★ | `-s tb_cpu_v8b_prod_v0_2 -Ptb_...MAX_CYCLES=10000000` |
| 5 | ★**Icarus 12.0 は `break` 文が未サポート**★（`sorry: break statements not supported`） | `while` ＋フラグ変数に書換 |

> #4 は**エラーも警告も出ない**ため最も危険。phase-1 のまま走った結果を
> 一度 FAIL と受け取りかけた。

### 9.10 デバッグ記録（段 2・サイクル数の +1 ずれ）

**現象**：B-4／B-7／B-9 の総サイクル数が期待より一律 +1。

**仮説の絞り込み**：データ内容・beat 数・中間 `ack` は全て正しく、
★差が一律 +1 で増分（31）が完全一致★。RTL の転送ロジックが誤っていれば
増分がずれるはずであるため、**RTL ではなく計数規約を疑った**。

**原因**：`burst_rd` は `@(negedge clk)` で `req` を立てた後、
最初の `@(posedge clk)` から計数するため**req 受理サイクルを 1 回余分に含む**。
等価性 TB（`negedge` 基準）が 11 を返していたのに対し本 TB は 13 と数えていた。

**対処**：★RTL は変更せず★ TB の期待値を計数規約に合わせて是正
（43→44、13→14、12→13）。コメントに「重要なのは絶対値ではなく増分」と明記。

---

## 10. 改版履歴

| 版 | 日付 | 内容 |
|---|---|---|
| v0.1 | 2026-08-27 | 新規作成。②-A（バースト先行）の設計。実源調査により §3.4（リフレッシュ判定の副作用）を発見・記録 |
| v0.2 | 2026-08-27 | ★レビュー指摘 M×6／C×4／N×3／E×2 を全反映★。★§3 にレジスタレベル設計を新設（`beat_cnt`／`blen_r`／`mem_idx` 縮退・M-1）★。M-3（バースト書き無害化・R-5）／M-5（特異性のみ・R-6）／M-6（出典欄・KY34 適用）を追加。B-5 削除・B-9/B-10 追加。§5.4 assertion 新設 |
| v0.3 | 2026-08-27 | v0.2 レビュー（判定＝承認）の **M-7／M-8／M-9** を反映。M-7：`rst_n` リセット節を新設（`beat_valid` は出力のため必須）。M-8：assertion 境界条件を `burst_len` 依存へ是正し 2 冪契約を追加。M-9：`beat_valid` を「連続 High」と再定義し B-4 の計数方法を確定。C-5／C-6／C-7・N-4／N-5 も反映 |
| **v0.4** | **2026-08-28** | ★**実装完了（段0〜段5）**。§9 新設★。段1 等価性7,563cyc完全一致／段2 B-3〜B-10 PASS／★案B B-11〜B-13 追加（全長掃引で `cyc=12+n` の線形性を実証・リフレッシュ希釈を実測）★／段3 ★G-0 バイト完全一致 md5 `34a5d1b9…`★。C-6・C-7 を仮説から確定へ格上げ。★§9.7：G-1/G-2/AC-1 は②-Aの改修層を通らないため検証対象から除外（是正原則を kaizen 登録候補に）★。★§9.8：G-0 は `PS_BURST` を一度も実行していないため、②-B 段0 で「バースト経路をシステムに通す」工程を必須化（案C・ユーザ判断）★。§9.9 環境5件・§9.10 デバッグ記録 |

---

*— 以上 `v10_psram_burst_design_v0_4.md` —*
