# サイクルカウントテーブル v1.0 → v1.1 改版案（レビュー用）

## 0. 文書識別

| 項目 | 値 |
|---|---|
| 文書名 | cycle_table_v1_1_revision_design_v0_3.md |
| バージョン | v0.3 |
| 発行日 | 2026-08-20 |
| 起案 | emu23 改良4（キャッシュ下地）段9・M-5 手順0 の実施中に発見 |
| 対象文書 | `ysd8800_cycle_count_table_v1_0.md`（v1.0・2026-07-11） |
| 対象ソース | `emu23_v2_10.c` `mc_init_default_table()` |
| 判定依頼 | —（**v1.0 レビューにて承認済・再レビュー不要**） |
| 反映元 | `cycle_table_v1_1_revision_design_v0_1_review_v1_0.md`（v1.0・2026-08-20）<br>`cycle_table_v1_1_revision_design_v0_2_review_v1_0.md`（v1.0・2026-08-20・**承認**） |
| 状態 | ★**承認済**★ 表 v1.1 改版および `emu23` 是正実装に着手可 |

> ★**v0.2 の位置づけ**★
> 本書は v0.1 に対する**条件付き差戻し**（M-1〜M-3）を受けた改訂版である。
> **KY41 に従い v0.1 の本文（§1〜§9）は一切削除していない。**
> 誤っていた記述は取り消し線と訂正注記で示し、経緯を追えるようにしてある。
> 新規に確定した内容は **§10 以降**に追記した。
>
> **読む順序**：急ぐ場合は **§10（v0.2 要旨）→ §11（状態連鎖突合）→ §12（区間計数）→ §16（是正案・確定版）** を読めばよい。
> §1〜§9 は v0.1 の記録であり、**§10 以降が優先する**。

---

## 1. 要旨

> ⚠️ **本節は v0.1 の記述である。是正値 2 件（0x68 / 0x69）が誤っていた。**
> **確定版は §10 を参照すること。**

サイクルカウントテーブル v1.0 の **4 エントリが 1 サイクルずつ過大**である。
RTL 実測により確定した。是正しなければ改良4 段10 の校正で
**LAT が約 3.6% 誤る**。

| opcode | 命令 | v1.0 | ~~是正値~~ | 差 | v0.2 での判定 |
|---|---|---:|---:|---:|---|
| 0x24 | `LDW rD,[rS]` | 7 | **6** | −1 | ✅ 維持（承認） |
| 0x25 | `STW rS,[rD]` | 6 | **5** | −1 | ✅ 維持（承認） |
| 0x68 | `JSR imm16` | 7 | ~~6~~ | ~~−1~~ | ❌ **誤り → 7 据置**（§10.2） |
| 0x69 | `RET` | 7 | ~~6~~ | ~~−1~~ | ❌ **誤り → 5**（§10.2） |

> **さらに 2 エントリの是正が漏れていた**（`LDB A/B,[X]` / `STB A/B,[X]`）。§10.3 参照。

---

## 2. 発見の経緯

改良4 設計書 `emu23_cache_base_design_v0_4.md` §9.0（M-5 手順0）は、
**RTL を理想メモリ構成で Dhrystone に通し、総サイクル数が emu23 `-mc` の
351,761 と一致することを確認せよ**と定めている。

§9.0.1 のとおり理想メモリ Dhrystone TB は存在しなかったため新規作成し
（第4章）、実測したところ**一致しなかった**。

| 測定 | 値 |
|---|---:|
| RTL 理想メモリ（YSD8002 `score_r` 区間） | **339,240** |
| emu23 `-mc` | **351,761** |
| 差 | **−12,521（−3.56%）** |

§9.0 は「不一致の場合、その差分の原因を先に特定すること」を求めており、
本書はその特定結果である。

---

## 3. 測定方式と妥当性検証（★本書の中核★）

### 3.1 差分法（表 §1.4）は IRQCHK を相殺してしまう

表 §1.4 の較正式は `CPI = raw(命令) − raw(NOP) + 2` である。
これは NOP との**相対差**であり、全命令に共通して掛かる
S_IRQCHK 分は**引き算で相殺されて見えなくなる**。

したがって差分法は「表の値どうしの整合」は検証できるが、
**表の値そのものが 1 ずれていても検出できない**。
v1.0 の誤りが今日まで残った構造的理由はここにあると考える。

### 3.2 採用した方式：増分法

同一命令を **1 個含むループ**と **3 個含むループ**を用意し、
その傾きから 1 命令あたりの**実コスト**を求める。

```
実コスト = (raw(3個版) − raw(1個版)) / (2 × ループ回数)
表相当値 = 実コスト − 1        （S_IRQCHK 分を除く＝表の規約）
```

ループ回数 100、前置命令列は全条件で同一とした
（前置が異なると非整数値が出る。実際 1 回目の測定で JMP が 4.86 となり、
前置の不一致が原因と判明した）。

### 3.3 ★KY54 陽性対照：方式の妥当性確認★

**疑義のある命令を測る前に、表が正しいと考えられる命令で方式を検証した。**

| 命令 | 実コスト | 表相当（実−1） | 表 v1.0 | 判定 |
|---|---:|---:|---:|---|
| `MOV rD,rS` (0x20) | 5 | **4** | 4 | ✅ 一致 |
| `ADDI rD,#imm` (0x41) | 7 | **6** | 6 | ✅ 一致 |
| `LDW rD,[imm16]` (0x22) | 9 | **8** | 8 | ✅ 一致 |
| `JMP rel16` (0x60) | — | **5** | 5 | ✅ 一致（差分法） |

**「実コスト = 表の値 + 1」が厳密に成立**することを確認した。
これにより増分法の妥当性と、表の規約（S_IRQCHK 非込み）の解釈が裏付けられた。

### 3.4 是正対象 4 エントリの実測

| 命令 | 実コスト | 表相当（実−1） | 表 v1.0 | 判定 |
|---|---:|---:|---:|---|
| `LDW rD,[rS]` (0x24) | 7 | **6** | 7 | ❌ 表が 1 過大 |
| `STW rS,[rD]` (0x25) | 6 | **5** | 6 | ❌ 表が 1 過大 |
| `JSR`+`RET` (0x68+0x69) | 14 | **12**（各 6） | 14（各 7） | ❌ 各 1 過大 |

JSR/RET は複合命令のため対で測定した（表 §1.4 も同様の扱い）。
1 個版 raw=2737 / 3 個版 raw=5537 → 傾き 1400/100 回 = **14.00**。
完全に線形であり、測定の信頼性は高い。

---

## 4. 使用した検証環境（新規作成・KY38 準拠）

| ファイル | 内容 |
|---|---|
| `ysd8800_v9_membus_idealmem_poc_v0_1.sv` | `ysd8800_v5_membus_v0_2.sv` から派生。`cdc_bridge` + `psram_ctrl` を理想メモリ（`mem_ready` 常時 1）に差し替え |
| `tb_cpu_v9_idealmem_poc_v0_1.sv` | `tb_cpu_v8b_prod_v0_2.sv` から派生。`+IMG=` で任意イメージ指定 |
| `build_v9_idealmem_poc.sh` | ビルド手順（`ysd8800_decoder_v0_1.sv` 先頭指定を遵守） |
| `measure_cpi_poc.sh` | 差分法・増分法の測定スクリプト |

- **差分はメモリ応答部のみに局所化**した（KY54 の陰性対照の構図）。
- **V8-b 確定構成（14 ファイル＋SD モデル）には一切手を触れていない**。
- 全ファイルに `_poc` を付し実験ファイルであることを明示（KY38）。
- 対象 RTL は **CPU コア v0.5.8**（`ysd8800_cpu_v0_1_FIXED.sv`・無改修）。

---

## 5. 検算：Dhrystone 全体との整合

Dhrystone 実行中の命令頻度を診断ビルド（`emu23_diag_poc.c`・`_poc`）で実測した。

| opcode | 実行回数 |
|---|---:|
| 0x24 `LDW rD,[rS]` | 5,318 |
| 0x25 `STW rS,[rD]` | 5,950 |
| 0x68 `JSR` | 300 |
| 0x69 `RET` | 300 |
| **合計** | **11,868** |

各 1 サイクル過大 ⇒ **11,868 サイクルの過大計上**。
実測差 12,521 の **95%** を説明する。

`-mc` 定義ファイルで 4 エントリを −1 補正して再実行した結果：

| | 値 |
|---|---:|
| 補正前 emu23 `-mc` | 351,761 |
| **補正後 emu23 `-mc`** | **341,411** |
| RTL 理想メモリ | **339,240** |
| 残差 | **2,171（0.64%）** |

---

## 6. ★残差 0.64% について（未解決・レビュー判断を仰ぐ）★

補正後も 2,171 サイクル（0.64%）の残差がある。原因は未特定である。

想定要因：

| # | 要因 | 備考 |
|---|---|---|
| 1 | 命令頻度カウントの母数差 | 診断ビルドは**プログラム全体**を計数、`score_r` は**ベンチマーク区間のみ**。両者の母数が厳密に一致しない |
| 2 | 未検証の低頻度命令 | `ANDI`(392) `SHL`(495) `SHR`(392) `XOR` 等。同型展開（表 §6-3）で導出された値であり、悉皆実測されていない |
| 3 | リセットシーケンス | emu23 非計上・RTL 2 サイクル（全テストで一定の +2 として観測済み） |

**本書は要因 2 の悉皆実測を行っていない。**
表 §6-3 は「全命令の悉皆実測は V3 の実効値測定時に併せて行う」としているが、
V3 でも実施された形跡が確認できていない。

---

## 7. 是正案

### 7.1 文書側（`ysd8800_cycle_count_table_v1_0.md` → v1.1）

- §2 の 4 エントリの CPI を是正し、根拠欄を「**実測（増分法・2026-08-20）**」とする。
- §1.4 に**増分法の記述を追加**する（差分法が IRQCHK を相殺する旨の注意を明記）。
- §6 に「同型展開エントリは未悉皆実測」の限界を再掲し、残差 0.64% を注記する。
- 対象 RTL 版数を **v0.5.6 → v0.5.8** に更新する（実測は v0.5.8 で実施）。
- ★KY41：v1.0 の記述は削除せず、改版履歴に追記する★

### 7.2 ソース側（`emu23_v2_10.c`）

> ⚠️ **本節のコードは v0.1 の案であり、0x68 / 0x69 が誤っている。**
> **適用してはならない。確定版は §16.2 を参照すること。**

`mc_init_default_table()` の 4 エントリを是正する。

```c
mc_table[0x24] = 6;   /* LDW  rD,[rS]  (v2.10: 7→6 実測是正) */
mc_table[0x25] = 5;   /* STW  rS,[rD]  (v2.10: 6→5 実測是正) */
mc_table[0x68] = 6;   /* JSR  imm16    (v2.10: 7→6 実測是正) */  /* ★誤★ */
mc_table[0x69] = 6;   /* RET           (v2.10: 7→6 実測是正) */  /* ★誤★ */
```

Dhrystone 絶対ゲート（819/48785/P:20）は `-mc` 無指定時の値であり、
本修正の影響を受けない（`mc_step()` は `mc_enable==0` で表を見ない）。

---

## 8. 波及範囲

| 対象 | 影響 |
|---|---|
| emu23 改良4 段10（LAT 校正） | **分子が 3.6% 変わる**。是正必須 |
| 工程②（キャッシュ RTL 開発） | 損益分岐ヒット率の算定根拠に影響 |
| 工程①.5（PSRAM 性能改善） | メモリ待ち実費の見積りに影響 |
| `emu23_mc_design_v0_6.md` | §3.1.1 の値の非対称性の記述に、本件の追記が望ましい |
| 軸C（ISA3.0 / YSD8810） | 将来の性能見積りの基礎データ |

---

## 9. レビューで判断を仰ぎたい点

| # | 論点 |
|---|---|
| **Q-1** | 4 エントリの是正値（6/5/6/6）を承認してよいか。増分法の妥当性検証（§3.3）は十分か |
| **Q-2** | 残差 0.64%（§6）を**未解決のまま** v1.1 を発行し段10 へ進んでよいか。それとも低頻度命令の悉皆実測を先に行うべきか |
| **Q-3** | 「実測」と根拠欄に記載された `JSR` が誤っていた。**他の「実測」エントリも信頼できるか**を再検証すべきか（表 §6-3 の同型展開エントリを含む） |
| **Q-4** | 対象 RTL 版数を v0.5.6 → v0.5.8 に更新する扱いでよいか。CHAT138 では「v0.5.6→v0.5.8 は CPI に影響なし」と結論していたが、**本件はその結論と整合するか**（今回の誤りが版数差由来なのか、当初からの較正誤差なのかは切り分けていない） |

> ★Q-4 は特に重要と考える。★
> もし版数差が原因なら「影響なし」という CHAT138 の結論が誤りであり、
> 他にも版数差由来の乖離が潜在する可能性がある。
> 当初からの較正誤差であれば、v1.0 作成時の測定手順（差分法のみ）に
> 構造的な弱点があったことになる（§3.1）。

---

# ★★ 以下 §10 以降が v0.2 の追記部（確定版）★★

---

## 10. v0.2 要旨（レビュー反映後の確定）

### 10.1 結論

レビュー指摘 M-1〜M-3 を全て実施した結果、**是正対象は 4 エントリではなく 6 エントリ**であり、
うち 1 エントリ（0x68 `JSR`）は**是正不要**であった。
是正後、**emu23 `-mc` と RTL 理想メモリの残差はゼロ**になった。

| opcode | 命令 | v1.0 | **是正値** | 差 | 根拠 |
|---|---|---:|---:|---:|---|
| 0x24 | `LDW rD,[rS]` | 7 | **6** | −1 | 増分法実測 ＋ 状態連鎖突合 ＋ 区間計数（三重） |
| 0x25 | `STW rS,[rD]` | 6 | **5** | −1 | 同上 |
| 0x68 | `JSR imm16` | 7 | **7** | **0** | RTL FSM 直読 ＋ 区間計数（**是正不要**） |
| 0x69 | `RET` | 7 | **5** | **−2** | RTL FSM 直読 ＋ 区間計数 |
| 1F/11,13 | `LDB A/B,[X]` | 6 | **5** | −1 | 状態連鎖突合 ＋ 区間計数 |
| 1F/15,17 | `STB A/B,[X]` | 6 | **5** | −1 | 同上 |

### 10.2 M-1：JSR/RET を等分したのは誤りであった

v0.1 §3.4 は `JSR`+`RET` を**対で測定**して実コスト 14（表相当 12）を得た。
**合計 12 は正しい。誤っていたのは内訳を「各 6」と等分したことである。**

等分には根拠がない。`JSR` は 16bit 即値フェッチ（`IMML`/`IMMH`）を持つが
`RET` は持たないのだから、両者が同一コストであるはずがない。

**RTL の FSM を直接読んで確定した**（`ysd8800_cpu_v0_1_FIXED.sv`・v0.5.8）。

| 命令 | 状態連鎖 | 状態数 | RTL 一次情報 |
|---|---|---:|---|
| `JSR` | FETCH, OPFETCH, IMML, IMMH, EXEC_JSR, PUSH_LO, PUSH_HI | **7** | L821-823 `S_EXEC_JSR: next_state = S_PUSH_LO;`／PC←imm_r は `S_PUSH_HI` 内（L1195） |
| `RET` | FETCH, OPFETCH, EXEC_RET, POP_LO, POP_HI | **5** | L826-828 `S_EXEC_RET: next_state = S_POP_LO;`／PC←{hi,lo} は `S_POP_HI` 内（L1200） |
| **計** | | **12** | 対測定の実コスト 14 −2（IRQCHK 2 命令分）と一致 |

**いずれも PC 反映のための追加状態を持たない。** よって 7 と 5 である。

> **なぜ v0.1 の検算（§5）で露見しなかったか**
> Dhrystone における `JSR` と `RET` の実行回数がほぼ同数であるため、
> 「各 6」でも「7 と 5」でも合計削減量がほぼ同じになる。
> **合計値が合っていることは内訳が正しいことを意味しない。**
> これは §3.1 で指摘した「差分法が共通項を相殺する」のと**同型の錯覚**である。

### 10.3 M-2：`LDB/STB [X]` の是正が漏れていた

表 v1.0 の当該エントリは状態連鎖を 5 状態と記しながら CPI 6 を記載していた。

```
| LDB A/B,[X] | 1F/11,13 | 6 | FETCH, OPFETCH, SUBOP, DECODE, MEMR8 | 実測 |
| STB A/B,[X] | 1F/15,17 | 6 | FETCH, OPFETCH, SUBOP, DECODE, MEMW8 | 実測 |
```

`1+1+1+1+1 = 5` であり **1 過大**。0x24 / 0x25 と**同一のパターン**に属する。

### 10.4 ★誤りの系統性★

是正対象 5 エントリ（0x68 を除く）には明確な共通点がある。

| エントリ | アドレッシング | 即値の有無 |
|---|---|---|
| 0x24 `LDW rD,[rS]` | レジスタ間接 | **無し** |
| 0x25 `STW rS,[rD]` | レジスタ間接 | **無し** |
| 1F/11,13 `LDB A/B,[X]` | レジスタ間接 | **無し** |
| 1F/15,17 `STB A/B,[X]` | レジスタ間接 | **無し** |

**即値を伴わないレジスタ間接アドレッシングの命令が、例外なく 1 過大である。**
同じ動作でも `[imm16]` 形式（0x22 / 0x23 / `LDB,STB [imm16]`）は全て正しい。

レジスタ間接命令は前置に `LDW X,#imm` 等のセットアップを要し、即値形式とは
**前置命令列が異なる**。v1.0 作成時にこの前置分の差し引きを誤ったと考えられる。
§3.2 が自ら「前置が異なると非整数値が出る」と警告していた事象そのものである。

`RET`（−2）のみ系統が異なり、これは §10.2 のとおり**対測定の等分**という別要因である。

---

## 11. ★状態連鎖突合（M-3・全 46 エントリ）★

### 11.1 方式：実測を要しない第 3 の検証

表 `ysd8800_cycle_count_table_v1_0.md` §2 は、**全エントリに CPI 値と状態連鎖を併記**している。
状態連鎖は FSM の設計そのものであるから、**その状態数を数えれば CPI の理論値が机上で得られる**。

- RTL の実行も TB の作成も不要
- 全 46 エントリの突合が数十分で完了する
- 改版のたびに低コストで反復できる

**数え方**：`MEMR_LO/HI` のような複数バイト状態は 2、`POP_LO/HI×2` は 4 と数える。

> **v1.0 の誤りが残った理由は、この突合が一度も行われなかったことに尽きる。**
> KY34「実ファイルが真実」の「実ファイル」には、**対象文書自身**も含まれる。

### 11.2 突合結果：一致（40 エントリ）

| 命令 | opcode | CPI | 状態数 | 判定 |
|---|---|---:|---:|---|
| NOP / EI / DI | 0x00/02/03 | 2 | 2 | ✅ |
| SYSCALL | 0x05 | 3 | 3 | ✅ |
| IRET | 0x04 | 7 | 1+1+1+4 = 7 | ✅ |
| MOV / ADD / SUB / CMP / AND / OR / XOR / NOT / SHL / SHR / SAR | 0x20/40/42/44/50/52/54/56/57/58/59 | 4 | 4 | ✅ |
| LDW rD,#imm / ADDI / SUBI / CMPI / ANDI / ORI / XORI | 0x21/41/43/45/51/53/55 | 6 | 6 | ✅ |
| LDW rD,[imm16] | 0x22 | 8 | 1+1+1+1+1+2+1 = 8 | ✅ |
| STW rS,[imm16] | 0x23 | 7 | 1+1+1+1+1+2 = 7 | ✅ |
| LDW rD,[X+off] | 0x26 | 8 | 同 0x22 = 8 | ✅ |
| STW rS,[X+off] | 0x27 | 7 | 同 0x23 = 7 | ✅ |
| JMP / BEQ / BNE / BLT / BGE | 0x60-64 | 5 | 5 | ✅ |
| **JSR imm16** | **0x68** | **7** | **1+1+1+1+1+2 = 7** | ✅ **是正不要** |
| LDB A/B,[imm16] | 1F/10,12 | 7 | 1+1+1+1+1+1+1 = 7 | ✅ |
| STB A/B,[imm16] | 1F/14,16 | 7 | 同上 = 7 | ✅ |
| PUSH A/B/X | 1F/00-02 | 6 | 1+1+1+1+2 = 6 | ✅ |
| POP A/B/X | 1F/03-05 | 6 | 1+1+1+1+2 = 6 | ✅ |
| リセット | — | 2 | 2 | ✅ |

### 11.3 突合結果：不一致（6 エントリ）

| 命令 | opcode | 表 CPI | 状態数 | 差 | 是正値 |
|---|---|---:|---:|---:|---:|
| `LDW rD,[rS]` | 0x24 | 7 | 1+1+1+2+1 = **6** | +1 | **6** |
| `STW rS,[rD]` | 0x25 | 6 | 1+1+1+2 = **5** | +1 | **5** |
| `RET` | 0x69 | 7 | 1+1+1+2 = **5** | **+2** | **5** |
| `LDB A/B,[X]` | 1F/11,13 | 6 | **5** | +1 | **5** |
| `STB A/B,[X]` | 1F/15,17 | 6 | **5** | +1 | **5** |
| （`JSR` 0x68） | 0x68 | 7 | **7** | 0 | 是正不要 |

### 11.4 ★意図的な不一致（是正対象外・C-1 対応）★

以下 2 件は状態数と一致しないが、**既知かつ意図的**である。突合時に毎回議論を
繰り返さないよう、v1.1 の §2 注記に明示的な除外として記載する。

| エントリ | CPI | 状態数 | 差の理由 |
|---|---:|---:|---|
| HALT | (4) | 3 | 表 §2 注記「`dbg_halt` 到達検出コストを含む較正基準値」。命令実行としての意味を持たない |
| 割込受理 | 9 | 8 | 較正由来。`emu23_mc_design_v0_6.md` §3.1.1 で議論済（IRQCHK の符号非対称性） |

> **v1.1 §2 に追記する注記文（案）**
> 「HALT および割込受理の CPI は状態数より 1 大きい。これは検出コスト／較正分を
> 含む意図的な差であり、**是正対象ではない**。状態連鎖突合を行う際はこの 2 件を
> 除外すること。」

### 11.5 Q-3 への含意

**悉皆実測は不要である。** 突合により全 46 エントリの整合が確認でき、不一致は 6 件に
限定された。v0.1 §6 が挙げた `ANDI`(0x51) / `SHL`(0x57) / `SHR`(0x58) / `XOR`(0x54) は
いずれも突合済みで**一致**している（6 / 4 / 4 / 4）。

---

## 12. ★区間計数による実測裏取り（M-2）★

### 12.1 診断ビルド

`emu23_v2_10.c` から `emu23_diag_poc.c` を派生させ（KY38・本番不変更）、
命令実行回数を計数した。

**計数点は `mc_step()` 内**とした。ここは CPI 表を参照する**唯一の箇所**であり、
「計数した回数」と「課金された回数」が**定義上一致**する。

```c
if (diag_region) {                 /* 区間内のみ計数 */
    if (cpu.ir == 0x1F) diag_ext[fetch8(cpu.pc)]++;   /* ★下の CPI 参照と同一式 */
    else                diag_main[cpu.ir]++;
}
```

> **設計上の注意**：`mc_step()` は `ysd8002` 構造体の宣言より**前**にあるため、
> 区間判定に `ysd8002.sw_busy` を直接参照できない。独立フラグ `diag_region` を
> 用いて宣言順序制約を構造的に回避した。
> （HANDOVER_CHAT139 §7-1 に記録された `fetch8()`／`cpu` の順序制約と同型の問題）

### 12.2 ★母数の統一（レビュー §6 要因1 への対応）★

v0.1 §5 が用いた実行回数は**プログラム全体**の計数であった。一方、比較対象の
`cycles=` は **`SW_START`〜`SW_STOP` 区間**である。**引き算の両辺が揃っていなかった。**

v0.2 では計数を `sw_busy` 区間に限定し、`cycles=` と同一の母数に揃えた。

| エントリ | v0.1（全体） | **v0.2（区間内）** |
|---|---:|---:|
| 0x24 | 5,318 | **4,604** |
| 0x25 | 5,950 | **5,205** |
| 0x68 | 300 | **270** |
| 0x69 | 300 | **271** |

> ★**0x68 と 0x69 の回数は同数ではない**（270 対 271）。★
> v0.1 が「300 / 300 で同数」としたのは全体計数によるものであり、
> 区間内では **1 回の差**がある。この差が §12.4 で M-1 の正誤を判別する。

### 12.3 ★KY54 陽性対照：課金恒等式★

計数の正しさを、測定に先立って検証した。**全 opcode について
Σ(回数 × (CPI+1)) を再構成し、`score` と一致するか**を見る。

```
[DIAG] instructions=48785  recon_cycles=351761  score=351761  delta=0
```

- **delta = 0**：計数漏れ・二重計上ともにゼロ
- 命令数 48,785 は `-mc` 無指定時の `cycles=48,785`（CPI=1 固定）と一致
- delta=0 は、**区間内に割込受理が発生していない**ことも同時に示す

**この恒等式が成立して初めて、以降の内訳議論に意味が生じる。**

### 12.4 実測結果と削減量

| opcode | 命令 | 区間内回数 | 差分 | 削減量 |
|---|---|---:|---:|---:|
| 0x24 | `LDW rD,[rS]` | 4,604 | −1 | 4,604 |
| 0x25 | `STW rS,[rD]` | 5,205 | −1 | 5,205 |
| 0x68 | `JSR imm16` | 270 | **0** | **0** |
| 0x69 | `RET` | 271 | **−2** | **542** |
| 1F/11,13 | `LDB A/B,[X]` | 1,340 | −1 | 1,340 |
| 1F/15,17 | `STB A/B,[X]` | 830 | −1 | 830 |
| | **合計** | | | **12,521** |

```
是正前 emu23 -mc     = 351,761
削減量               =  12,521
─────────────────────────────
是正後 emu23 -mc     = 339,240
RTL 理想メモリ       = 339,240
残差                 =       0  (0.00%)
```

**v0.1 §6 の残差 0.64%（2,171）は完全に解消した。**
レビュー §8 の見立て（`LDB [X]` / `STB [X]` の是正漏れが有力）が的中している。
`scc23` は v2.04 以降 char のロードに `LDB` を用いるため、Dhrystone の
char 配列・文字列比較で `LDB [X]` が 1,340 回、`STB [X]` が 830 回実行されていた。

### 12.5 ★M-1 が実測で独立に裏付けられた★

区間内で `JSR`=270、`RET`=271 と**回数が 1 だけ異なる**ため、
2 つの案が **1 サイクルの分解能で判別できる**。

| 案 | 削減量 | 補正後 | RTL との差 |
|---|---:|---:|---:|
| v0.1 案（各 6・等分） | 270×1 + 271×1 = **541** | 339,241 | **+1** ❌ |
| **v0.2 案（7 据置 / 5）** | 0 + 271×2 = **542** | **339,240** | **0** ✅ |

**状態連鎖・RTL FSM 直読に加え、実測でも「等分は誤り」が確定した。**
M-1 是正要求3（`JSR` 単独・`RET` 単独の分離測定）は、非対称ループを組むまでもなく
**Dhrystone 自身の実行回数の非対称性**が判別してくれた。

### 12.6 偶然一致でないことの確認（KY57）

本日の KY 活動は「**合計値の一致をもって内訳の正しさとみなす危険**」であった。
M-1 でまさにその誤りを犯している以上、同じ罠を二度踏まないよう自己点検する。

| # | 点検項目 | 結果 |
|---|---|---|
| 1 | 恒等式 delta=0 により計数と課金が定義上一致 | ✅ 「だいたい合った」ではない |
| 2 | 各是正は **回数 × 状態連鎖由来の固定差分**。残差に合わせて値を調整した箇所は皆無 | ✅ |
| 3 | JSR/RET の非対称性が **1 サイクル分解能**で正誤を判別 | ✅ 辻褄合わせでは到達不能 |
| 4 | 母数を `sw_busy` 区間で `cycles=` と統一 | ✅ |

---

## 13. RTL 理想メモリ環境の再構築と再測定

### 13.1 経緯

v0.1 の比較対象 339,240 は前チャットの測定値であり、検証環境 4 ファイルのうち
`measure_cpi_poc.sh` 以外がプロジェクトナレッジに未登録であった（レビュー N-2）。
**過去測定への依存を断つため、理想メモリ環境をゼロから再作成し本日再測定した。**

### 13.2 作成した資産

| ファイル | 内容 |
|---|---|
| `ysd8800_ideal_mem_poc_v0_1.sv` | 理想メモリ本体（`ready` 常時 1・待ちゼロ） |
| `ysd8800_v9_membus_idealmem_poc_v0_1.sv` | `ysd8800_v5_membus_v0_2.sv` 派生。CDC ブリッジ＋PSRAM のみ置換 |
| `tb_cpu_v9_idealmem_poc_v0_1.sv` | `tb_cpu_v8b_prod_v0_2.sv` 派生。`+IMG=` 対応・`score_r` 読出 |
| `build_v9_idealmem_poc.sh` | ビルド手順（`ysd8800_decoder_v0_1.sv` 先頭指定を遵守） |

**設計の要点**

- **理想メモリの本質は `assign ready = 1'b1;`**。PSRAM の `LATENCY_NORMAL(12)`／
  `LATENCY_REFRESH(15)` と CDC ハンドシェイクを排除し、CPU の各メモリ状態が
  1 クロックで完了する状態を作る。これにより **実クロック数 = FSM 状態数の総和** となる。
- 配列名を `mem`、インスタンス名を `u_psram_ctrl` に揃えた。既存 TB の階層パス
  `u_membus.u_psram_ctrl.mem` がそのまま使え、`$readmemh` 先の互換性を保つ。
- 単一クロック（`cpu_clk` 4MHz）。`psram_clk` ポートは互換のため残すが未使用。

**KY38 遵守**：全ファイルに `_poc` を付与。差分は**メモリ応答部のみ**に局所化。
CPU コア（`ysd8800_cpu_v0_1_FIXED.sv`・v0.5.8）・デコーダ・MMU・アドレスデコーダ・
MMIO／YSD800x は**無改修**。**V8-b 確定構成には一切手を触れていない。**

### 13.3 測定結果

```
[V9] image  = dhry_final.hex
[V9] reset released
[V9] ★score_r = 339240 clk★
[V9]  (ref) cycle_count = 367473
```

**前チャットの測定値 339,240 と完全一致した。過去測定への依存は断てた。**

`cycle_count`（リセット解除〜）が `score_r` より大きいのは、スタートアップと
後処理を含むためであり整合する。

### 13.4 三者一致

| 出所 | 値 |
|---|---:|
| RTL 理想メモリ `score_r`（CPU v0.5.8 無改修・本日再測） | **339,240** |
| emu23 `-mc` 是正後（351,761 − 12,521・本日測定） | **339,240** |
| **差** | **0** |

### 13.5 途中で踏んだ誤り（記録）

TB 初版で CPU のポートを `irq0` / `irq1` と**推測で記述しビルドエラー**を出した。
実際は `irq_in[2:0]` の**割込番号入力方式**である。派生元 `tb_cpu_v8b_prod_v0_2.sv`
の結線をそのまま踏襲すべきであった。**KY34（実ファイルが真実）の一時的な失念**が原因。

---

## 14. レビュー Q-1〜Q-4 への回答（確定）

### Q-1：是正値の承認可否

**v0.1 の 4 エントリ案は部分的に誤りであった。** 確定版は §10.1 の 6 エントリ。

- 0x24→6 / 0x25→5：**維持**（増分法実測・状態連鎖・区間計数の三重の裏付け）
- 0x68：**是正不要**（7 据置）／0x69：**7→5**（RTL FSM ＋ 区間計数）
- 1F/11,13・1F/15,17：**6→5**（是正漏れを追加）

**増分法そのものに問題はなかった。** 問題は**複合測定の内訳を等分で割ったこと**にある。
v1.1 §1.4 には「対で測定した場合、**合計値のみが実測で保証され、内訳は別の根拠を要する**。
等分は根拠ではない」を明記する。

### Q-2：残差を未解決のまま段10 へ進んでよいか

**残差は解消した（0.64% → 0.00%）。** 論点自体が消滅した。
`emu23_mc_design` の「未確定値が下流を駆動する」構図には陥っていない。

### Q-3：他の「実測」エントリも信頼できるか

**問いの立て方が誤っていた。** 誤っていた 6 エントリの根拠欄は次のとおり**ばらばら**である。

| エントリ | 根拠欄 |
|---|---|
| 0x24 | 実測 |
| 0x25 | 同型展開 |
| 0x69 | 較正 |
| 1F/11,13・1F/15,17 | 実測 |

**「実測」だけが信頼できないのではない。根拠の種別を問わず誤りが混入していた。**
共通するのは**状態連鎖との突合が一度も行われなかったこと**であり、
問うべきは「どの根拠欄を疑うか」ではなく「**どの検証が欠けていたか**」である。

**悉皆実測は不要**（§11.5）。突合で不一致のエントリのみ実測すればよい。

### Q-4：版数差由来か、当初からの較正誤差か

**切り分け完了。当初からの較正誤差である。CHAT138 の結論（v0.5.6→v0.5.8 は
CPI に影響なし）は維持される。**

| # | 根拠 |
|---|---|
| 1 | **版数差の内容が当該命令の状態遷移に触れていない。** v0.5.7 は `S_SUBOP` への `mem_rd=1'b1` 追加（バス出力 `always_comb`）で `next_state` は無変更。v0.5.8 は `S_IRQCHK` ラッチ条件への `irq_pending==0` 追加で、割込受理可否のみを変える。いずれも状態数を増やさない |
| 2 | **誤っているのは CPI 値であって状態連鎖ではない。** 6 エントリの状態連鎖の記述自体は全て正しい。版数差が原因なら状態連鎖の方が変わるはずだが、変わっていない |
| 3 | **誤りのパターンが測定手順の弱点と一致する。**「即値を伴わないレジスタ間接」で例外なく揃っており（§10.4）、RTL 版数差では説明できない。前置命令列の差し引き誤りでのみ説明がつく |

**したがって「他にも版数差由来の乖離が潜在する可能性」は否定される。**
一方「v1.0 の測定手順に構造的弱点があった」は肯定され、**弱点は 3 つあった**ことになる。

| # | v1.0 の測定手順の弱点 | 発見者 |
|---|---|---|
| 1 | 差分法は共通項（S_IRQCHK）を相殺するため、全体が 1 ずれても検出できない | v0.1 §3.1 |
| 2 | 前置命令列が条件間で不統一だった（レジスタ間接系のセットアップ分が混入） | レビュー §1.2 |
| 3 | 状態連鎖との突合が一度も行われなかった | レビュー M-3 |

対象 RTL 版数の **v0.5.6 → v0.5.8 更新は、実測を v0.5.8 で行った事実の記録として妥当**。

---

## 15. 原則への昇格提案（kaizen.txt）

レビュー M-3 の提案を受け、以下を新原則として起票したい。

> **原則（案）：CPI 値と状態連鎖の両方を持つ表は、両者の突合を改版時の必須チェックとする。**
>
> 状態連鎖は FSM の設計そのものであり、状態数から CPI の理論値が机上で得られる。
> 実測を 1 回も要さず全エントリを検証でき、改版のたびに低コストで反復できる。
> v1.0 の誤りが長期間残った理由は、この突合が一度も行われなかったことに尽きる。
> **意図的な不一致（検出コスト・較正分）は表側に明示的な除外注記を置くこと。**

関連して、既存の **KY34「実ファイルが真実」の解釈を拡張**したい。
「実ファイル」には**対象文書自身に併記された一次情報**（状態連鎖・計算式・
図表）も含まれる。本文の結論だけを読み、併記データとの整合を確かめないのは
KY34 違反である。

---

## 16. 是正案（★確定版・§7 を差し替える★）

### 16.1 文書側（`ysd8800_cycle_count_table_v1_0.md` → v1.1）

1. §2 の **6 エントリ**の CPI を §10.1 のとおり是正する（0x68 は据置＝変更なし）
2. 各エントリの根拠欄を更新する
   - 0x24 / 0x25：**実測（増分法・2026-08-20）＋状態連鎖突合**
   - 0x69：**状態連鎖突合（JSR+RET 対測定 12 の内訳を分離）**
   - 1F/11,13・1F/15,17：**状態連鎖突合＋区間計数（2026-08-20）**
3. §1.4 に**増分法の記述を追加**する
   - 差分法が IRQCHK を相殺する旨の注意を明記
   - **「対測定は合計値のみが保証され、内訳は別の根拠を要する。等分は根拠ではない」を明記**
4. §1.4 に**状態連鎖突合を第 3 の検証方式として追加**する（§11.1）
5. §2 注記に **HALT / 割込受理を突合の除外対象**として明記する（§11.4）
6. §6 の「残差」記述を**解消済み（0.00%）**に更新する
7. 対象 RTL 版数を **v0.5.6 → v0.5.8** に更新する
8. ★**KY41：v1.0 の記述は削除せず、改版履歴に追記する**★

### 16.2 ソース側（`emu23_v2_10.c`）

`mc_init_default_table()` を是正する。**根拠の種別をコメントに残すこと**（C-2 対応）。

```c
mc_table[0x24]     = 6;  /* LDW rD,[rS] (v2.10: 7→6 増分法実測＋状態連鎖突合) */
mc_table[0x25]     = 5;  /* STW rS,[rD] (v2.10: 6→5 増分法実測＋状態連鎖突合) */
/* mc_table[0x68] = 7;      JSR imm16   ★是正不要★ 状態連鎖 7 と一致(v0.1 の 6 は誤り) */
mc_table[0x69]     = 5;  /* RET (v2.10: 7→5 状態連鎖突合により是正。
                            JSR+RET 対測定 12 の内訳を分離。JSR は IMML/IMMH を
                            持つが RET は持たないため等分は誤り) */
mc_table_ext[0x11] = 5;  /* LDB A,[X] (v2.10: 6→5 状態連鎖突合＋区間計数) */
mc_table_ext[0x13] = 5;  /* LDB B,[X] (同上) */
mc_table_ext[0x15] = 5;  /* STB A,[X] (同上) */
mc_table_ext[0x17] = 5;  /* STB B,[X] (同上) */
```

> **`JSR`（0x68）は現行値 7 のままであるから、コード変更は生じない。**
> ただし「一度 6 に是正しようとして誤りと判明した」経緯をコメントに残す。
> 残さないと、後日また同じ是正が提案されうる。

### 16.3 絶対ゲートへの影響

`mc_step()` は `mc_enable==0` のとき表を参照せず 1 を返すため、
**Dhrystone 絶対ゲート（819 / 48,785 / P:20）は本修正の影響を受けない。**

是正後の期待値：

| 項目 | 是正前 | **是正後** |
|---|---:|---:|
| `-mc` 単独（`cycles=`） | 351,761 | **339,240** |
| RTL 理想メモリ | 339,240 | 339,240 |
| 残差 | −12,521（−3.56%） | **0（0.00%）** |

---

## 17. 波及範囲（★確定版・§8 を差し替える★）

| 対象 | 影響 |
|---|---|
| emu23 改良4 段10（LAT 校正） | **分子が 3.56% 変わる。是正必須。** 是正後は残差 0 のため、メモリ待ち実費を正確に切り出せる |
| **`emu23_mc_design_v0_6.md` §4.4** | ★**デフォルト opcode 表**が 0x24/0x25/0x69 および 1F/11,13,15,17 の CPI をサイクル表から転記している。**改版必須**★（C-3 対応） |
| `emu23_mc_design_v0_6.md` §3.1.1 | 値の非対称性の記述に本件の追記が望ましい |
| `emu23_cache_base_design_v0_4.md` | 段10 の校正手順・§3.1.1 の RTL 版数記述 |
| 工程②（キャッシュ RTL 開発） | 損益分岐ヒット率の算定根拠に影響 |
| 工程①.5（PSRAM 性能改善） | メモリ待ち実費の見積りに影響 |
| `emu23_debug_manual_v1_6.md` | §2.6 の拡張命令 CPI 合計値（`LDB/STB [X]` を含む） |
| `tool_version_ledger` | emu23 のバージョン欄記述 |
| `kaizen.txt` | §15 の新原則・KY34 の解釈拡張 |
| 軸C（ISA3.0 / YSD8810） | 将来の性能見積りの基礎データ |

---

## 18. その他のレビュー指摘への対応

### N-1：増分法で 1 個版と 3 個版を用いた理由

v0.1 §3.2 の式は 2 点間の傾きであり、**厳密には線形性の確認になっていない**という
指摘は正しい。1 個 / 2 個 / 3 個の **3 点**を取れば真の線形性検証になる。

**v1.1 の §1.4 には「3 点以上で線形性を確認すること」を推奨として記載する。**
なお本件では、状態連鎖突合と区間計数という**独立した 2 つの検証**が同一の結論を
与えているため、増分法の線形性に依存した判断は行っていない。

### N-2：理想メモリ検証環境のナレッジ登録

**対応済み。** §13.2 の 4 ファイルを作成し、再現可能な資産として整備した。
プロジェクトナレッジ容量が逼迫している状況を踏まえ、**登録推奨は以下 3 点**とする
（`measure_cpi_poc.sh` は登録済）。

| ファイル | 登録推奨度 | 理由 |
|---|---|---|
| `ysd8800_ideal_mem_poc_v0_1.sv` | **高** | 理想メモリの中核。小規模（約 70 行） |
| `ysd8800_v9_membus_idealmem_poc_v0_1.sv` | **高** | 派生元があっても差分の再現は容易でない |
| `tb_cpu_v9_idealmem_poc_v0_1.sv` | **高** | `score_r` 区間読出の作り込みが資産 |
| `build_v9_idealmem_poc.sh` | 中 | 短いが `decoder` 先頭指定の記録として有用 |

> **これらは段10 の校正でも、工程②のキャッシュ RTL 評価でも繰り返し使う。**
> 「理想メモリでの基準値」はキャッシュ効果を測る際の**分母**そのものである。

---

## 19. 段9 完了判定と次段への進行可否

| レビュー要求 | 状態 |
|---|---|
| M-1（0x68 据置 / 0x69→5） | ✅ RTL FSM 直読＋区間計数の二重で確定 |
| M-2（`LDB/STB [X]` 是正・残差説明） | ✅ 残差 **0.00%** で完全説明 |
| M-3（全 46 エントリ状態連鎖突合） | ✅ 不一致 6 件・他 40 件整合 |
| Q-4 の結論反映 | ✅ §14 |
| C-1（HALT/割込受理の除外明記） | ✅ §11.4 |
| C-2（根拠種別をコメントに残す） | ✅ §16.2 |
| C-3（`emu23_mc_design` §4.4 を波及に追加） | ✅ §17 |
| N-1（線形性）／N-2（資産登録） | ✅ §18 |
| M-5 手順0（RTL と emu23 の一致） | ✅ **339,240 = 339,240** |

**段10（LAT 校正）進行の条件（残差の確定）は満たされた。**

ただし**本書 v0.2 の差分再レビュー承認までは、表 v1.1 の改版も
`emu23_v2_10.c` の是正実装も行わない**（原則43：設計→レビュー→承認→実装）。

---

## 改版履歴

| 版 | 日付 | 変更 |
|---|---|---|
| v0.1 | 2026-08-20 | 新規作成。emu23 改良4 段9（M-5 手順0）で発見したサイクル表 v1.0 の 4 エントリ過大を報告し、増分法による実測値と是正案を提示。 |

---

# ★★ 以下 §20 以降が v0.3 の追記部（v0.2 レビュー反映）★★

---

## 20. v0.2 差分再レビューの結果

`cycle_table_v1_1_revision_design_v0_2_review_v1_0.md`（v1.0・2026-08-20）にて
**判定＝承認／新規 M 指摘 0 件**。**再レビュー不要**。
表 v1.1 の改版および `emu23_v2_10.c` の是正実装に着手可。

レビューによる独立再計算（§0）で、残差が端数なく説明されることが確認された。

```
v0.1 の補正削減量 = 4,604 + 5,205 + 270 + 271        = 10,350
351,761 − 10,350                                     = 341,411  ← v0.1 §5 と一致
341,411 − 339,240                                    =  2,171   ← v0.1 §6 の残差
LDB[X] 1,340 + STB[X] 830 + RET の等分誤り 1         =  2,171   ← ★完全一致★
```

> **M-1（内訳の誤り）と M-2（是正漏れ）は独立に発見された別件でありながら、
> 合計するとちょうど残差になる。片方でも誤っていれば成立しない。
> 両者が相互に正しさを保証している。**

---

## 21. ★C-1：残差 0 が保証する範囲と、状態連鎖突合の限界★

### 21.1 指摘の要旨

- **残差 0 が保証するのは、区間内で実行された opcode のみ**である
- 状態連鎖突合（§11）は「CPI 値と状態連鎖の**記述**が整合するか」を見るに過ぎず、
  **状態連鎖の記述自体が誤っている可能性は検出できない**
- 誤った記述どうしが整合していれば、**突合は一致と誤判定する**

**この限界は §15 の原則昇格提案に必ず併記しなければならない。**
限界を書かずに「突合を必須チェックとする」とだけ登録すれば、
次の担当者が**突合を万能と誤解する**。

### 21.2 検証の強度は 3 段階ある

| 段階 | 保証内容 | 対象 |
|---|---|---|
| **強** | **実機一致**。CPI 値が RTL の実クロック数と一致 | 区間内で実行された 28 エントリ |
| 中 | **内部整合**。CPI 値と状態連鎖の記述が整合 | 全 46 エントリ |
| 弱 | 状態連鎖の記述の正しさに依存 | 未実行 23 エントリ |

### 21.3 区間内実行 opcode（28 エントリ・★実機一致が保証される★）

診断ビルドの計数結果より。**これらは残差 0 により CPI 値が実測で確定している。**

| opcode | CPI | 回数 | | opcode | CPI | 回数 |
|---|---:|---:|---|---|---:|---:|
| 0x20 MOV | 4 | 2,283 | | 0x45 CMPI | 6 | 1,691 |
| 0x21 LDW #imm | 6 | 1,594 | | 0x51 ANDI | 6 | 380 |
| 0x22 LDW [imm16] | 8 | 6,050 | | 0x52 OR | 4 | 10 |
| 0x23 STW [imm16] | 7 | 3,630 | | 0x57 SHL | 4 | 480 |
| **0x24 LDW [rS]** | 7→**6** | 4,604 | | 0x58 SHR | 4 | 380 |
| **0x25 STW [rD]** | 6→**5** | 5,205 | | 0x60 JMP | 5 | 1,651 |
| **0x26 LDW [X+off]** | **8** | **1,152** | | 0x61 BEQ | 5 | 2,203 |
| 0x40 ADD | 4 | 570 | | 0x62 BNE | 5 | 290 |
| 0x41 ADDI | 6 | 7,045 | | 0x63 BLT | 5 | 141 |
| 0x42 SUB | 4 | 90 | | 0x64 BGE | 5 | 10 |
| 0x43 SUBI | 6 | 6,044 | | **0x68 JSR** | **7** | 270 |
| 0x44 CMP | 4 | 521 | | **0x69 RET** | 7→**5** | 271 |
| X:0x10 LDB [imm16] | 7 | 50 | | **X:0x13 LDB B,[X]** | 6→**5** | 200 |
| **X:0x11 LDB A,[X]** | 6→**5** | 1,140 | | **X:0x15 STB A,[X]** | 6→**5** | 730 |

> ★**C-1 の懸念は解消した**★
> **`LDW rD,[X+off]`（0x26）は n=1,152 で実行されており、CPI=8 のまま残差 0 が成立している。**
> すなわち **`X + off` の実効アドレス計算に追加状態は不要**であることが、
> 状態連鎖の記述に依存せず**実測で確定**した。
> レビューが懸念した「真値 9 / 8」の可能性は**否定される**。

### 21.4 区間内未実行 opcode（23 エントリ・状態連鎖の記述に依存）

| 主 opcode | CPI | | 拡張（0x1F） | CPI |
|---|---:|---|---|---:|
| 0x00 NOP | 2 | | X:0x00 PUSH A | 6 |
| 0x01 HALT | (3) | | X:0x01 PUSH B | 6 |
| 0x02 EI | 2 | | X:0x02 PUSH X | 6 |
| 0x03 DI | 2 | | X:0x03 POP A | 6 |
| 0x04 IRET | 7 | | X:0x04 POP B | 6 |
| 0x05 SYSCALL | 3 | | X:0x05 POP X | 6 |
| 0x06 | 3 | | X:0x12 LDB B,[imm16] | 7 |
| **0x27 STW [X+off]** | **7** | | X:0x14 STB A,[imm16] | 7 |
| 0x50 AND | 4 | | X:0x16 STB B,[imm16] | 7 |
| 0x53 ORI | 6 | | | |
| 0x54 XOR | 4 | | | |
| 0x55 XORI | 6 | | | |
| 0x56 NOT | 4 | | | |
| 0x59 SAR | 4 | | | |

> **注意すべき点**：`PUSH`/`POP`（X:0x00-05）が Dhrystone 区間で 1 回も実行されていない。
> `scc23` は引数・局所変数をフレームポインタ相対（`[X+off]`）で扱うため、
> `PUSH`/`POP` を用いない。**CPI=6 は状態連鎖の記述に依存している。**
> YUI OS のコンテキストスイッチでは多用されるため、
> 段10 以降で YUI OS を対象に測定する際に実機一致が確認できる見込み。

### 21.5 ★0x27 `STW rS,[X+off]` の RTL 直読（C-1 要求3）★

未実行かつ状態連鎖が「同 0x23」としか記されていないため、RTL を直読した。

| 一次情報 | 内容 |
|---|---|
| `ysd8800_decoder_v0_1.sv` L121-122 | `8'h26: idec = ID_LDW_XI;` / `8'h27: idec = ID_STW_XI;` |
| `ysd8800_cpu_v0_1_FIXED.sv` L404 | `ID_LDW_XI, ID_STW_XI: eff_addr = rf_x + imm_r;` ← ★`always_comb` 内の**純組合せ加算**★ |
| 同 L641-642 | `ID_LDW_ABS, ID_STW_ABS, ID_LDW_XI, ID_STW_XI:` が**同一の遷移先にグループ化**されている |

**実効アドレス計算は組合せ回路で行われ、専用状態を持たない。**
また FSM 上 `[imm16]` 形式と `[X+off]` 形式は**同一の状態遷移**をたどる。

**したがって表の「同 0x22」「同 0x23」という記述は正しく、0x26=8 / 0x27=7 で確定。**
レビューが懸念した「表も状態連鎖も両方誤っていて突合が一致と誤判定する」ケースには
**該当しなかった**。0x26 は実測でも裏付けられている（§21.3）。

### 21.6 v1.1 §2 の根拠欄に用いる区分

| 区分 | 意味 |
|---|---|
| **実測検証済（Dhrystone 区間・2026-08-20）** | 区間内で実行され、残差 0 により**実機一致が保証**される（28 エントリ） |
| **状態連鎖突合のみ（未実行）** | 本測定では実行されず、状態連鎖の記述の正しさに依存する（23 エントリ） |
| **RTL 直読** | FSM を直接読んで確認した（0x27・0x68・0x69） |

---

## 22. C-2：RTL 側で割込受理が発生していないことの論証

§12.3 の delta=0 は **emu23 側**で区間内に割込受理が無いことを示すに過ぎない。
RTL 側についても以下により裏付けられる。

> **emu23（是正後）と RTL がともに 339,240 に到達している。**
> 割込受理は 1 回あたり 8 サイクル（表の 9 は S_IRQCHK 込み）を要するため、
> **片方にのみ受理があれば 8 の倍数だけ乖離する。**
> 完全一致している以上、**双方とも区間内の受理回数は 0 回**である。

Dhrystone は `dhry_timer.c` により YSD8002 のストップウォッチを用いるが、
計測区間中はタイマー割込が発生しない設定であることと整合する。

**本論証を v1.1 の測定条件欄にも記載する。**

---

## 23. C-3：拡張命令の定義ファイル書式（`X:0xNN`）

§16.2 のコードは `mc_table_ext[0x11]` 等に直接代入するが、
**外部定義ファイル（`.cyc`）における拡張命令のキーは `X:0xNN` 形式**である
（`emu23_mc_design_v0_6.md` §6.2）。

`LDB/STB [X]` の既定値を 6 → 5 に変更するため、利用者が定義ファイルで
上書きする際のキー表記を明示する必要がある。

```
# .cyc ファイルでの記述例（既定値を元に戻したい場合）
X:0x11 6
X:0x13 6
X:0x15 6
X:0x17 6
```

**§17 の波及範囲を以下のとおり具体化する。**

| 文書 | 更新内容 |
|---|---|
| `emu23_debug_manual_v1_6.md` §2.6 | 拡張命令 CPI **合計値**の更新（既定） |
| `emu23_debug_manual_v1_6.md` | ★**既定値表そのもの**の更新（`X:0x11/13/15/17` を 6→5）★ |
| `emu23_mc_design_v0_6.md` §4.4 | デフォルト opcode 表（主・拡張とも） |
| `emu23_mc_design_v0_6.md` §6.2 | 書式規約は変更なし（`X:0xNN` のまま） |

---

## 24. N 指摘・E 指摘への対応

### N-1：計数実行と比較対象が別実行であること

§12.3 は「命令数 48,785 が `-mc` 無指定時の `cycles=48,785` と一致」と述べたが、
`mc_step()` は `mc_enable==0` のとき表を参照せず即 1 を返すため、
**計数には `-mc` 指定が必須**であり、比較対象は**別実行**の値である。

両者が同一であることの根拠を明記する。

> **同一バイナリ（`dhry_final.bin`・21,846B）を同一オプション（`-q`）で実行し、
> 区間も同一（`SW_START`〜`SW_STOP`）である。**
> emu23 は決定論的に動作し、`-mc` は CPI の課金のみを変える（実行される命令列は不変）。
> したがって**命令数は両実行で同一**であり、恒等式の前提は満たされる。

### N-2：KY 番号の採番衝突確認

**確認済。衝突なし。**

| KY | 初出 | 内容 |
|---|---|---|
| KY54 | `kaizen.txt` 登録済 | 否定形テストを先行する |
| KY55 | 2026-07-13（V5 タイマー） | パルス/レベル無検討による二重カウント |
| KY55 | 2026-08-20（CHAT139） | ★**二重発番**★ メモリ課金の計上点 |
| KY56 | 2026-08-20（CHAT139） | 課金アドレスに論理アドレスを渡す危険 |
| KY57 | 2026-08-20（本件） | 合計一致をもって内訳の正しさとみなす危険 |

> ⚠️ **KY55 が過去に二重発番されている。**
> 原因は `kaizen.txt` への登録が **KY54 で止まっており**、
> KY55 以降がチャット内にしか存在しないことにある。
> **KY55〜KY57 を `kaizen.txt` に登録し、採番の単一起点を回復すべきである。**

### N-3：v0.1 §6 の残差 3 要因すべての決着

| 要因 | v0.1 §6 の記述 | 決着 |
|---|---|---|
| 要因1 | 母数差（診断ビルドは全体計数・`score_r` は区間のみ） | ✅ **該当**。§12.2 で区間内計数に統一して解消 |
| 要因2 | 低頻度命令の未実測 | ✅ **非該当**。§11.5 で否定（`ANDI`/`SHL`/`SHR`/`XOR` は突合一致） |
| 要因3 | リセットシーケンス（emu23 非計上・RTL 2 サイクル） | ✅ **非該当**。`score_r` 区間は**リセット後**であり、元より影響しない |

**3 要因すべてに決着がついた。** 実際の原因は要因1（母数差）が判別可能性を隠していたことと、
是正漏れ 2 エントリ＋内訳誤り 1 の合算であった。

### E-1：参照行番号の訂正

§10.2 の表における RTL 一次情報欄の行番号を訂正する。

| 誤 | 正 |
|---|---|
| PC←imm_r は `S_PUSH_HI` 内（**L1195**） | **L818-820 のコメント**「push完了(S_PUSH_HI, ctx=JSR)で PC←imm_r」。PC 更新は regfile 側で実施 |
| PC←{hi,lo} は `S_POP_HI` 内（**L1200**） | **L824-825 のコメント**「pop完了(S_POP_HI, ctx=RET)で PC←{hi,lo}」 |

L1195 / L1200 は `stack_ctx <= CTX_JSR;` `push_src <= rf_pc;` 等の**設定箇所**であり、
PC 更新そのものではない。**結論（PC 反映のための追加状態を持たない）は変わらない。**

> **本件は KY34 の再発である。** 行番号を目視で拾い、`grep -n` による機械照合を
> 行わなかった。過去にも `KY-CHAT48`（設計書の出典行番号ズレ）で同種の指摘を受けている。
> **出典行番号は必ず機械照合で生成すること。**

---

## 25. §15 原則昇格提案の改訂（★C-1 の限界を併記★）

§15 の提案を以下に差し替える。**限界の記述が本体と不可分である。**

> **原則125（案）：CPI 値と状態連鎖を併記する表は、両者の突合を改版時の必須チェックとする。
> ただし突合の限界を理解して用いること。**
>
> **【手法】** 状態連鎖は FSM の設計そのものであり、状態数から CPI の理論値が机上で得られる。
> 実測を 1 回も要さず全エントリを検証でき、改版のたびに低コストで反復できる。
>
> **【★限界★】** 突合が検出できるのは「CPI 値と状態連鎖の**記述**の不整合」だけである。
> **状態連鎖の記述自体が誤っている場合、誤った記述どうしが整合するため突合は一致と誤判定する。**
> 特に状態連鎖が展開されず「同 0xNN」「（〜と同）」とだけ記されたエントリは、
> **記述の正しさが検証されていない**。突合を万能と見なしてはならない。
>
> **【併用すべき検証】** 実測（実行された命令のみ実機一致を保証）と RTL 直読
> （未実行命令に対する唯一の一次情報）を組み合わせること。
> 表には**どのエントリがどの強度で検証されたか**を区別して記載する。
>
> **【意図的な不一致】** 検出コスト・較正分による意図的な差は、表側に明示的な除外注記を置くこと。

あわせて **KY34 の解釈拡張**を提案する（§15 から継続）。

> **KY34 の「実ファイル」には、対象文書自身に併記された一次情報**（状態連鎖・計算式・図表）
> **も含まれる。** 本文の結論だけを読み、併記データとの整合を確かめないのは KY34 違反である。
> また**出典行番号は目視転記せず `grep -n` で機械照合すること**（E-1・`KY-CHAT48` の再発防止）。

---

## 26. 次アクション（承認後）

| # | 作業 | 状態 |
|---|---|---|
| 1 | 本書 v0.3 の作成（再レビュー不要） | ✅ 本書 |
| 2 | 表 `ysd8800_cycle_count_table_v1_1.md` へ改版（§16.1＋§21.6 の根拠欄区分） | ⬜ |
| 3 | `emu23_v2_10.c` → **v2.11** 是正実装（§16.2） | ⬜ |
| 4 | 絶対ゲート確認（G-1〜G-6 ＋ `-mc` = **339,240**） | ⬜ |
| 5 | 波及文書改版（§17＋§23） | ⬜ |
| 6 | `kaizen.txt` 改版（原則125・KY55〜57 登録） | ⬜ |
| 7 | 段10（LAT 校正）へ進行 | ⬜ |

> ★**版数方針（KY58）**★
> 是正は **v2.11**、新機能（任意アドレス書き込み）は **v2.12** とし、
> **同一版数に混在させない**。是正で絶対ゲートを通してから新機能に着手する。
> 混在させると、回帰が崩れた際に是正と新機能のどちらが原因か切り分けられない。

---

| **v0.2** | **2026-08-20** | **レビュー指摘書 v1.0（条件付き差戻し）を反映。M-1（0x68 は是正不要・0x69 は 7→5）、M-2（`LDB/STB [X]` の是正漏れを追加・残差 0.64%→0.00%）、M-3（全 46 エントリ状態連鎖突合）を実施。Q-1〜Q-4 の回答を確定（Q-4：当初からの較正誤差・CHAT138 の結論維持）。C-1〜C-3・N-1/N-2 を反映。RTL 理想メモリ環境を再構築し 339,240 を本日再測定、過去測定への依存を解消（§13）。§15 に原則昇格提案を追加。★KY41：v0.1 の §1〜§9 は削除せず、誤記述には訂正注記を付して保存した★** |

| **v0.3** | **2026-08-21** | **v0.2 差分再レビュー（判定＝承認・新規 M 0 件）を反映。C-1（残差 0 の保証範囲と突合の限界・実行28/未実行23 エントリの区別・0x26 は実測済で CPI=8 確定・0x27 は RTL 直読で 7 確定）、C-2（RTL 側の割込受理 0 回の論証）、C-3（`.cyc` の `X:0xNN` 書式と既定値表の波及）、N-1〜N-3、E-1（参照行番号の訂正）を反映。§25 で原則125 案に★突合の限界★を併記する形へ改訂。KY41：v0.2 の §1〜§19 は削除せず保存。** |

*— 以上 `cycle_table_v1_1_revision_design_v0_3.md` v0.3 —*
