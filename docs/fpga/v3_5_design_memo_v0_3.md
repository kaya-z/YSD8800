# v3_5_design_memo_v0_3.md — FPGA V3.5（MMU統合）設計メモ

- 文書名: v3_5_design_memo_v0_3.md
- 版数: **v0.3（実装・検証完了）**
- 作成日: 2026-07-11（v0.1）／改版日: 2026-07-11（v0.2 レビュー承認）／改版日: 2026-07-11（v0.3 実装・検証結果反映）
- レビュー: `v3_5_design_review_reply_v1_0.docx` — **総合判定 承認（Mレベル指摘なし）**
- 対象工程: Step 8 FPGA実装 / **V3.5（MMU統合）**
- 実装・検証状況: **S1〜S7 完了。全TB ALL PASS（MMU無効時 V3全26ベクタ／MMU有効時 6ベクタ）→ §12**
- 前提工程: V3（メモリ・バス・MMIOデコード／PSRAM統合）完了
- 主設計書: `YSD8800_MMU_Design_v1_1_0.docx`（FM-11方式16ページMMU）→ **v1.2.0 へ改版要（§2.4／§9）**
- 黄金リファレンス: **emu23 v1.09（`emu23_v109.c`、`--mmu`）**
- 上位工程表: `fpga_impl_roadmap_v1_1.docx`

---

## 1. スコープ

### 1.1 やること

| # | 項目 |
|---|---|
| 1 | MMUモジュール新規実装（`ysd8800_mmu_v0_1.sv`・16論理ページ／4KB／20bit物理） |
| 2 | MMIOスタブへMMUレジスタ実装（PTR[0..15]@$FF00-$FF0F、MCR@$FF10） |
| 3 | **CDCブリッジの物理アドレス幅を20bit化**（現状16bit固定。§4.2） |
| 4 | 統合ラッパー`ysd8800_v3_membus`の`PHYS_AW`を16→20へ |
| 5 | MMU単体TB＋実CPU統合TB（emu23 `--mmu` 協調等価） |
| 6 | **MMU無効時のV3等価性（回帰デグレ無）確認** |

### 1.2 やらないこと（スコープ外）

- KRN_PROT（MCR bit1・ユーザモード保護）→ 設計書§10「将来拡張」
- TLBキャッシュ、ページ属性（R/W/X）→ 同上
- reset vector / SPI Flash→PSRAM展開の競合対策 → **V7/V8**
- MMIO実デバイス化（UART/TIMER/SD/IRQC）→ **V4以降**
- cycle一致検証 → **既定方針によりゲート対象外**（emu23はCPI=1固定）

---

## 2. ★最重要★ MMU挿入位置の確定（V3.5 最大の設計論点）

### 2.1 結論

> **アドレスデコーダ（論理$FC80判定）が前段。MMUはその後段・RAM側パスにのみ挿入する。**
> **MMIO（MMUレジスタ自身を含む）はアドレス変換を受けない。**

### 2.2 根拠（emu23 v1.09 実装照合・KY34）

`emu23_v109.c` の `rd16()`/`wr16()`/`rd8()`/`wr8()` の**ディスパッチ順序**を実ソースで確認した（設計書の記述ではなく実装が黄金）。

```c
uint16_t rd16(uint16_t a) {          // a = 【論理アドレス】
    if (a & 1) { /* アライメント例外 */ }

    /* ---- MMIO判定：すべて論理アドレス a と直接比較 ---- */
    if (a == UART_...)      ...      //  YSD8001 UART
    if (a == TIMER_/SCORE_) ...      //  YSD8002 タイマー
    if (a == SD_...)        ...      //  YSD8003 ストレージ
    if (a == IRQ_STAT_ADDR) return irq_stat;   //  YSD8004 IRQC
    if (a == IRQ_MASK_ADDR) return irq_mask;

    /* MMUレジスタ自身も論理アドレスで判定（変換を受けない） */
    if (mmu_mode && a >= MMU_PTR_BASE && a <= MMU_MCR_ADDR)
        return rd8(a) | (rd8(a+1) << 8);

    /* ---- ここで初めてMMU変換（通常メモリのみ） ---- */
    uint8_t lo = phys_rd8(mmu_translate(a));
    uint8_t hi = phys_rd8(mmu_translate(a + 1));
    return lo | (hi << 8);
}
```

`mmu_translate()` の呼び出しは `phys_rd8()`/`phys_wr8()` の引数としてのみ現れる（L152/638-639/771-772/793/820）。**MMIO分岐はすべてその手前で `return` するため、変換関数に到達しない。**

### 2.3 設計的必然性（なぜMMIOを変換外にするのか）

MMIOを変換対象にすると以下が破綻する。

| 破綻 | 内容 |
|---|---|
| **MMUの自己ロックアウト** | MCR（$FF10）自身が変換対象だと、ページテーブル設定次第でMCRに到達できなくなり、**MMUを切り戻す手段が失われる** |
| **割り込みコントローラの喪失** | IRQCが消えると、コンテキストスイッチ中に割り込みをマスク解除できない |
| **カーネルの足元崩壊** | YUI OSはコンテキストスイッチのたびにPTR[0..15]を書き換える。その書き換え動作自体がMMIO経由であり、ここが変換対象だと**プロセス切替コードが自分の実行基盤を壊す** |

FM-11・Dragon・CoCo3（GIME）等のMC6809+SAM/DAT系でもI/Oは変換の外側に固定されている。**「I/Oは常に見えていなければならない」がページングMMUの鉄則**であり、本設計もそれに従う。

**ユーザー判断（2026-07-11）**: 「MMIOを変換してしまうと外部デバイスが見えなくなってしまうということなら変換外とする」→ **変換外で確定**。

### 2.4 設計書との食い違い（★作業完了後に設計書改版が必要★）

| 箇所 | 設計書 v1.1.0 の記述 | 実装（黄金）との整合 |
|---|---|---|
| §7 FPGA実装概要 | 「MMUは**CPUコアとメモリバスの間**に挿入する」 | ❌ デコーダ前段に読めるが、正しくは**デコーダ後段・RAM側のみ** |
| §5 物理メモリマップ | `0xFC000〜0xFFFFF … I/O・MMUレジスタ` | ❌ I/Oは物理空間に存在しない（論理固定・変換外） |

→ **V3.5完了後に `YSD8800_MMU_Design_v1_2_0.docx` へ改版**（KY41：追記のみ・取り消し線で旧情報保持・4点整合）。本メモ §9 に改版項目を掲げる。

---

## 3. アーキテクチャ

### 3.1 V3.5 ブロック図

```
              CPUコア ysd8800_cpu v0.5.7（★無改修★）
                    │ mem_addr[15:0] / mem_wdata / mem_rdata
                    │ mem_rd / mem_wr / mem_ready
        ┌───────────┴────────────┐
        │  ysd8800_addr_decoder   │  ← 【論理アドレス】$FC80判定（V3のまま・位置不変）
        └──┬──────────────────┬───┘
     MMIO側│                  │RAM側（論理アドレス16bit）
           │                  │
  ┌────────┴─────────┐  ┌─────┴───────────────┐
  │ ysd8800_mmio_stub │  │  ysd8800_mmu  ★新規★ │
  │  ＋ MMU_REGS ★新規│  │  純組合せ            │
  │  PTR[16] @$FF00-0F│─▶│  phys = mcr_en       │
  │  MCR     @$FF10   │ptr│    ? {ptr[a[15:12]], │
  │  （変換外・常時可視）│mcr│       a[11:0]}      │
  └───────────────────┘  │    : {4'b0, a[15:0]} │
                          └─────┬───────────────┘
                                │ phys_addr[19:0] ★20bit★
                    ┌───────────┴────────────┐
                    │  ysd8800_cdc_bridge     │ ← ★PHYS_AWパラメタライズ改修★
                    │  req/ack 4相・2FF同期器  │
                    └───────────┬────────────┘
                                │ psram_addr[19:0]
                    ┌───────────┴────────────┐
                    │  ysd8800_psram_ctrl     │ ← PHYS_AW=20（既に対応済）
                    └────────────────────────┘
```

### 3.2 各モジュールの改修区分

| モジュール | 区分 | 内容 |
|---|---|---|
| `ysd8800_cpu_v0_1.sv`（v0.5.7） | **無改修** | 論理アドレスのみ扱う。MMUはCPUから透過 |
| `ysd8800_addr_decoder_v0_1.sv` | **無改修** | 論理$FC80判定のまま。位置も変えない |
| `ysd8800_mmio_stub_v0_1.sv` | **改修**（→ v0.2） | MMUレジスタ（PTR[16]/MCR）を実装。他は従来スタブ挙動 |
| `ysd8800_mmu_v0_1.sv` | **新規** | 純組合せアドレス変換 |
| `ysd8800_cdc_bridge_v0_1.sv` | **改修**（→ v0.2） | `PHYS_AW`パラメタライズ（現状16bit固定） |
| `ysd8800_psram_ctrl_v0_1.sv` | **無改修** | `PHYS_AW=20`デフォルトで既に対応済 |
| `ysd8800_v3_membus_v0_1.sv` | **改修**（→ `ysd8800_v35_membus_v0_1.sv` 新規） | MMU挿入・`PHYS_AW(16)`→`(20)` |

---

## 4. 詳細設計

### 4.1 `ysd8800_mmu_v0_1.sv`（新規・純組合せ）

```systemverilog
module ysd8800_mmu_v0_1 #(
    parameter int PHYS_AW = 20
) (
    input  logic [15:0]        logical_addr,
    input  logic               mmu_en,          // MCR bit0
    input  logic [7:0]         ptr [0:15],      // PTRレジスタ（MMIO側から供給）
    output logic [PHYS_AW-1:0] physical_addr
);
    logic [3:0] page;
    assign page = logical_addr[15:12];          // ★原則59: always_comb内のビット選択はassign外出し★

    always_comb begin
        if (mmu_en)
            physical_addr = {ptr[page], logical_addr[11:0]};      // 8+12 = 20bit
        else
            physical_addr = {{(PHYS_AW-16){1'b0}}, logical_addr}; // 恒等写像
    end
endmodule
```

**設計上の要点:**

1. **純組合せである必然性**
   CPUコアは16bitアクセスを `S_MEMR_LO` → `S_MEMR_HI`（アドレス+1）の**2バイトアクセスに分解するFSM**である。MMUを純組合せに置けば、**各バイトアクセスのアドレスがそれぞれ独立に変換される**。
   → 設計書§9-4「16bitアクセスのページ境界またぎは低位/高位バイトが個別変換される」および emu23 実装（`mmu_translate(a)` と `mmu_translate(a+1)` を別々に呼ぶ）と**自動的に一致**する。**RTL側で境界またぎの特別な細工は不要**。

2. **原則59適用**：`always_comb` 内の定数ビット選択は Icarus 12.0 で制約があるため、`page` の抽出を `assign` で外出しする。

3. **原則63チェック**：**MMUは新規stateを一切追加しない**（純組合せ）。したがってCPUコアのバス出力`always_comb`への影響なし。→ **原則63の突き合わせ対象外**であることを明記しておく（追加した場合は突き合わせ必須）。

### 4.2 `ysd8800_cdc_bridge` の20bit化（★V3の実態と要注意★）

**KY34で判明した実態:**

| モジュール | 現状（実ソース） |
|---|---|
| `ysd8800_psram_ctrl_v0_1` | ~~`parameter int PHYS_AW = 20`（デフォルト20bit）・ポート `addr[PHYS_AW-1:0]` → **既に20bit対応済**~~ **【v0.3訂正】ポートのみ20bit。内部実装は16bit固定だった（下記★参照）** |
| `ysd8800_cdc_bridge_v0_1` | `output logic [15:0] psram_addr;` / `assign psram_addr = cpu_mem_addr;` → **16bit固定** |
| `ysd8800_v3_membus_v0_1` | PSRAMを `.PHYS_AW(16)` でオーバーライド → **意図的に16bitへ絞っている** |

~~HANDOVER §4「物理アドレス線は既に20bit幅で引いてある」は**PSRAMコントローラのみに当てはまり、CDCブリッジには当てはまらない**。~~

#### ★【v0.3 重要訂正】上位ビット破棄は「2箇所」あった★

v0.2 の本節は「上位ビットを捨てているのは **CDCブリッジのみ**」と読める記述だったが、**S3の実装作業でPSRAMコントローラ内部にも同じ問題があることが判明した**。

| # | 箇所 | V3実装の実態 |
|---|---|---|
| 1 | `ysd8800_cdc_bridge_v0_1` | `output logic [15:0] psram_addr;` … **ポートが16bit固定** |
| 2 | **`ysd8800_psram_ctrl_v0_1`（★v0.2で見落とし★）** | **`assign addr_lo = addr[15:0];`**（上位ビットを黙って破棄）<br>**`logic [7:0] mem [0:65535];`**（64KB固定配列）<br>→ **`PHYS_AW=20` を与えても内部は下位16bitしか使わない** |
| 3 | `ysd8800_v3_membus_v0_1` | PSRAMを `.PHYS_AW(16)` でオーバーライド |

**「ポートが20bit」と「内部実装が20bit」は別物である。** #2 を放置したままMMUを接続しても**エラーは一切出ず**、物理 $14000 へのアクセスが黙って $4000 に化けるだけとなる。結果として MMU_IDENT_ON（恒等写像）だけがPASSし、他のリマップ系ベクタが原因不明のFAILを起こす――**「MMUは繋がっているのに効かない」という最も発見しにくい状態**に陥る。

S3 で両者を是正し、`tb_bridge_psram_20bit_v0_1.sv`（物理 $04000 と $14000 のエイリアス非発生を実証・23/23 PASS）で恒久的に守った。さらに **S7 のベクタ #3 MMU_ISOLATION が ALL PASS した**ことにより、実CPU経路でも上位ビットが生きていることが実証されている。

→ **kaizen 原則64 として登録**（引継文書の記述も実ソース照合の対象とする）。

V3.5では以下の改修が必須。

```systemverilog
module ysd8800_cdc_bridge_v0_2 #(
    parameter int PHYS_AW = 20                       // ★追加★
) (
    ...
    input  logic [PHYS_AW-1:0] cpu_phys_addr,        // ★MMU出力を受ける（旧: cpu_mem_addr[15:0]）★
    ...
    output logic [PHYS_AW-1:0] psram_addr,           // ★16 → PHYS_AW★
    ...
);
    assign psram_addr = cpu_phys_addr;               // 幅一致
```

そして統合ラッパーで `.PHYS_AW(20)` を指定する。

### 4.3 MMIOスタブへのMMUレジスタ実装（→ `ysd8800_mmio_stub_v0_2.sv`）

| アドレス | 名称 | R/W | 幅 | リセット値 |
|---|---|---|---|---|
| $FF00 + n（n=0..15） | PTR[n] | R/W | 8bit | **n（恒等写像）** |
| $FF10 | MCR | R/W | 8bit | **0x00（MMU無効）** |
| $FF11〜$FF1F | 予約 | — | — | 0 |
| 上記以外の$FC80〜$FFFF | 従来スタブ挙動 | — | — | 固定0x00リード・ライト無視 |

**インタフェース追加（MMUへ供給）:**
```systemverilog
output logic [7:0] ptr_o [0:15],   // PTRレジスタ配列
output logic       mmu_en_o        // MCR bit0
```

**設計上の要点:**
- **アクセス幅**: 設計書§3-1「すべて8bitアクセス」。CPUコアはバイト単位でMMIOへアクセスするため、V3のバイト粒度I/Fのままで整合する。
- **リセット値**: 設計書§4・emu23 `mmu_reset()`（`for(i<16) ptr[i]=i; mcr=0;`）と一致させる。**この恒等写像リセットにより、reset直後は MMU 有効/無効で挙動が同一**となる（V7/V8のreset vector問題への布石）。
- **`mmu_en_o` の抽出**: `assign mmu_en_o = mcr[0];`（原則59・EI/DI の FLAGS bit7 抽出と同じ轍を踏まない）

### 4.4 MMU有効/無効の動作分離（工程表 指摘3対応）

| MCR.EN | MMU挙動 | V3との関係 |
|---|---|---|
| **0（リセット値）** | `physical_addr = {4'b0, logical_addr}`（恒等写像） | **V3のRTLとbit-exact等価**。上位4bitは常に0 |
| **1** | `physical_addr = {PTR[page], offset}` | V3.5新規挙動 |

**等価性の担保方法**: V3で ALL PASS した全TB（`tb_cpu_v3_v0_1`(20)／`tb_cpu_v3mem_v0_1`(5)／`tb_cpu_v3boundary_v0_1`(1)）を、**V3.5構成（MMU挿入済み・MCR=0）で再実行し、ALL PASS を再確認**する。これがデグレ無の証明になる。

---

## 5. 検証計画（設計書単体TB＋emu23協調等価の「両肺」・工程表 指摘4対応）

### 5.1 単体TB

| TB | 対象 | 検証内容 |
|---|---|---|
| `tb_mmu_v0_1.sv` | MMU単体 | ①MCR=0で恒等写像（全16ページ）②MCR=1で `{PTR[page],offset}` ③PTR書換の即時反映 ④ページ境界（$0FFF/$1000）で変換ページが切り替わる |
| `tb_mmio_mmureg_v0_1.sv` | MMIOスタブ v0.2 | ①リセット後 PTR[n]=n / MCR=0 ②PTR/MCR の R/W ③非MMU領域は従来スタブ挙動 |

### 5.2 emu23協調等価TB（黄金＝emu23 v1.09 `--mmu`）

| TB | ベクタ | 内容 |
|---|---|---|
| `tb_cpu_v35_regress_v0_1.sv` | V3の26ベクタ再実行 | **MCR=0でV3等価**（デグレ無確認・§4.4） |
| `tb_cpu_v35mmu_v0_1.sv` | 新規MMUベクタ | MMU有効時の協調等価 |

**新規MMUベクタ（案・生成器 `gen_v35_mmu_vectors.py`）:**

| # | ベクタ名 | 内容 | 設計書根拠 |
|---|---|---|---|
| 1 | MMU_IDENT_ON | PTR恒等のままMCR=1 → 挙動不変 | §4 |
| 2 | MMU_REMAP_P4 | PTR[4]=$14 → 論理$4000 → 物理$14000 へSTW/LDW | §9-1 OK例 |
| 3 | MMU_ISOLATION | MMU ONで論理$4000へ書込 → MMU OFFで論理$4000読出 → 元値のまま | §9-2 |
| 4 | MMU_BOUNDARY | PTR[4]=$14,PTR[5]=$15 で $4FFF/$5000 の個別変換 | §9-4 |
| 5 | MMU_PTR_RW | PTR/MCR のリード検証 | §3 |
| **6** | **MMU_MMIO_BYPASS** | **★レビュー条件（v0.2追加）★ MMU ONかつPTRを書き換えた状態（例: PTR[15]=$1F で$F000台をリマップ）で $FF00台のMMUレジスタへアクセスし、変換を受けずに正しく読み書きできることを確認** | **§2（本メモ核心）** |

#### ★ベクタ#6（MMU_MMIO_BYPASS）追加の根拠（レビュー回答書 v1.0 §4）

**§2の核心「MMIOは変換外」をTBで恒久的に守るためのベクタ。** ベクタ1〜5はいずれもRAM側の変換検証が中心であり、**MMIO非変換の観点が薄い**。ここが崩れると**MMUの自己ロックアウト**（MCRに到達できずMMUを切り戻せない）が発生し、**しかもMMU ON時にしか顕在化しない**——**原則63の典型**（有効時のみ顕在化するバグ）である。

**設計上の注意**: MMIO協調ベクタ除外原則（emu23はMMIOにデバイス固有値を返す）との整合上、**アクセス対象はMMUレジスタ（$FF00-$FF10）に限定**する。UART/TIMER/SD/IRQC には触れない。

#### ★ページ境界跨ぎ16bitアクセスは「原理上発生しない」（レビュー整理・v0.2追記）

当初 §4.1 では「純組合せMMUなら各バイトが独立変換され、emu23（`mmu_translate(a)` と `mmu_translate(a+1)` を別々に呼ぶ）と自動一致する」と整理していたが、レビューによりさらに強い結論が得られた。

> **4KBページ境界（$1000, $2000, …, $5000 等）はすべて偶数アドレスである。**
> したがって境界を跨ぐ16bitアクセス（lo=$4FFF / hi=$5000）は**必ず奇数アドレス始まり**となり、**アライメント例外**（`if (a & 1)`）で弾かれる。
> ⇒ **境界を跨ぐ偶数16bitアクセスは原理上発生し得ない。**

**帰結**: MMU_BOUNDARY（#4）は **$4FFF/$5000 への個別（バイト）アクセスによる変換確認で十分**であり、「跨ぎ16bitアクセス」の観点でベクタを追加する必要はない。設計書§9-4 の「FPGA実装時はアドレス変換ユニットで境界またぎを考慮した実装が必要」という懸念は、**ISA2.3のアライメント制約により構造的に解消されている**。

**★遵守事項（V3から継承）★**
- **KY41/原則43**: ベクタ期待値は**手計算せず、emu23 `--mmu` を実行して黄金取得**する
- **MMIO協調ベクタ除外原則**: emu23はMMIOにデバイス固有値を返す。**MMUレジスタ（$FF00-$FF10）以外のMMIOアクセスをベクタに含めない**
- **SPリセット差異の中和**: emu23は SP=0xFC7E 既定、RTLは S_RESET_HI で SP<=0x0000。**全ベクタ先頭に `LDW SP,#imm16` を明示挿入**（V2-d方式）
- **§9-1 遵守**: **PTR[0]（コードページ）を変更してからMMU ONにしない**。ベクタは page4/page5 のみリマップする
- **§9-2 遵守**: MMU ON前に対象物理ページを**$00クリア**する（未初期化=$FF による偽FAIL防止）

### 5.3 ゲート基準

**既定方針どおり「完走＋論理結果一致」のみ**（cycle一致は対象外。emu23がCPI=1固定のため定義上不一致）。

---

## 6. 作業手順（1変更1検証・原則遵守）

| Step | 内容 | 検証 |
|---|---|---|
| S1 | `ysd8800_mmu_v0_1.sv` 実装 | `tb_mmu_v0_1` PASS |
| S2 | `ysd8800_mmio_stub_v0_2.sv`（MMUレジスタ追加） | `tb_mmio_mmureg_v0_1` PASS |
| S3 | `ysd8800_cdc_bridge_v0_2.sv`（PHYS_AW化） | 既存 `tb_cdc_bridge` / `tb_bridge_psram_integ` 再実行 PASS（デグレ無） |
| S4 | `ysd8800_v35_membus_v0_1.sv`（統合・PHYS_AW=20） | — |
| S5 | **V3全26ベクタをV3.5構成（MCR=0）で再実行** | **ALL PASS（デグレ無の証明）** |
| S6 | `gen_v35_mmu_vectors.py`（emu23 `--mmu` 黄金取得） | 黄金生成 |
| S7 | `tb_cpu_v35mmu_v0_1.sv` | **ALL PASS** |
| S8 | 文書改版（§9） | — |

**Icarusワークフロー厳守（原則60/61/62）**: build → `ls -la` タイムスタンプ確認 → **別コマンド**で `timeout 30 vvp`（`&&`チェーン禁止）。rc=124 は組合せループを疑う。

---

## 7. リスクと対策（KY）

| # | リスク | 対策 |
|---|---|---|
| R1 | **MMU挿入位置の取り違え**（MMIOを変換対象にしてしまう） | 本メモ§2で確定・実装照合済。デコーダは**位置不変** |
| R2 | **CDCブリッジ16bit固定の見落とし** | §4.2で明示。KY34で実ソース確認済。`PHYS_AW` パラメタライズ必須 |
| R3 | **MMU有効時のみ顕在化するバグ**（原則63の類型） | MCR=0（V3等価）とMCR=1の**両方**でTBを回す（S5+S7） |
| R4 | ベクタが §9-1（コードページPTR変更禁止）に違反しハングする | 生成器で page4/page5 のみリマップ。page0/1 は触らない |
| R5 | phys_mem 未初期化（$FF）による偽FAIL | §9-2 に従い、MMU ON 前に対象物理ページを $00 クリアするコードをベクタに含める |
| R6 | Icarus の `always_comb` 内ビット選択制約 | 原則59：`page` / `mmu_en` 抽出を `assign` で外出し（§4.1・§4.3に反映済） |

---

## 8. 未確定事項（レビューで確認したい点）

| # | 論点 | 現案 |
|---|---|---|
| Q1 | MMU無効時（MCR.EN=0）でも **PTR/MCR レジスタは読み書き可能**とするか | **可能とする**。emu23は `mmu_mode`（`--mmu`起動フラグ）が真ならMCR値に関わらずレジスタアクセス可。RTLには起動フラグの概念がないため、**MMUレジスタは常時アクセス可**とする（MCR=0でも書ける＝でないとMMUを有効化できない） |
| Q2 | PSRAM物理容量（現行TBは16bit=64KBモデル）を20bit=1MBに拡張するか | **拡張する**（MMUの意味がなくなるため）。ただしIcarusシミュレーション上のメモリ確保量に注意 |
| Q3 | 統合ラッパーのファイル名 | `ysd8800_v35_membus_v0_1.sv` 新規作成（V3の`v3_membus`は**そのまま残す**＝V3構成での回帰実行を可能にするため） |

---

## 9. 作業完了後の文書改版項目（KY41・追記のみ・4点整合）

| # | 文書 | 改版内容 |
|---|---|---|
| 1 | `YSD8800_MMU_Design_v1_1_0.docx` → **v1.2.0** | §7 MMU挿入位置を実装に整合（デコーダ後段・RAM側のみ）／§5 物理メモリマップからI/O行を是正（取り消し線で旧記述保持）／FPGA実装章にCDCブリッジPHYS_AW化を追記 |
| 2 | `v3_5_design_memo_v0_1.md` → **v0.2以降** | 実装結果・検証結果を追記 |
| 3 | `fpga_impl_roadmap_v1_1.docx` → **v1.2** | V3.5完了記録 |
| 4 | `fpga_source_version_ledger_v1_1.md` → **v1.2** | V3.5ソース一式（MMU/mmio_stub v0.2/cdc_bridge v0.2/v35_membus/TB/生成器）を登録 |
| 5 | `kaizen.txt` | **★レビューで登録推奨★ 新原則候補（原則64）: 「HANDOVER・引継文書の記述も実ソース照合の対象である。『配線は引いてある』『対応済』といった記述は、幅・パラメータ・オーバーライドまで実ソースで確認する」**＝KY34のRTL/引継文書適用。<br>**根拠**: HANDOVER §4「物理アドレス線は既に20bit幅で引いてある」を鵜呑みにしていれば、CDCブリッジ16bit固定・ラッパー`.PHYS_AW(16)`オーバーライドを見落とし、**MMU出力の上位4bitが物理的に捨てられてリマップが効かない**というV3.5の致命的バグに直結していた（レビュー回答書 §3-2「放置すればV3.5で確実に破綻していた」） |

---

## 10. レビュー結果（v0.2で追記）

**レビュー回答書**: `v3_5_design_review_reply_v1_0.docx`（v1.0・2026-07-11）
**総合判定**: **承認（Mレベル指摘なし）** — **原則43により実装着手可**

| 依頼# | 依頼事項（v0.1で提出） | 判定 | レビュア回答の要点 |
|---|---|---|---|
| 1 | §2 MMU挿入位置の確定（デコーダ前段・MMIO変換外） | **承認** | `rd8()` のディスパッチ順序を独立実照合し完全裏付け。MMIOは `mmu_translate` に到達しない。MC6809+SAM/DAT系（FM-11/CoCo3 GIME/Dragon）でI/Oが変換外に固定される鉄則と同じ |
| 2 | §4.2 CDCブリッジの20bit化 | **承認（重要指摘）** | KY34の模範的適用。V3レビュー指示「物理アドレス線は将来幅で引く」は粒度が粗くCDCブリッジまで貫くか明示していなかった（レビュア側の非として言及）。改修範囲は妥当 |
| 3 | §5 検証計画 | **条件付き承認** | **条件: MMU_MMIO_BYPASS ベクタ1本追加** → **v0.2で §5.2 に #6 として反映済** |
| 4 | §8 未確定事項 Q1〜Q3 | **全承認** | Q1: `mmu_mode`(L110)は`--mmu`起動フラグでMCR値と独立。レジスタ判定(L633/765/787)は`mmu_mode`のみ見て`MCR_EN`を見ない → RTLでは「常時アクセス可」が正しい（でなければMCRに書けずMMUを有効化できない＝鶏と卵）<br>Q2: `phys_rd8`は`phys_mem[pa & 0xFFFFF]`＝20bit/1MB。RTLも合わせるべき。**ただしIcarusのシミュレーション速度低下に注意（必要なら疎配列/連想配列も検討）**<br>Q3: `v3_membus`を残すのはデグレ検出の退路確保として good practice |
| 5 | §9 文書改版項目 | **承認** | 漏れなし。**kaizen候補「HANDOVER記述も実ソース照合の対象。『引いてある』は幅まで確認する」は登録推奨** |

### 10.1 レビュアの実照合結果（KY34・独立検証）

| 照合項目 | 結果 |
|---|---|
| MMIOは変換外（§2核心） | **一致** |
| `mmu_translate` 呼出箇所（`phys_rd8`/`phys_wr8` の引数のみ・L152/638-639/771-772/793/820） | **一致** |
| 変換式 `(ptr[logical>>12]<<12)｜(logical&0x0FFF)` | **一致（20bit）** |
| MMU定数（`MMU_PTR_BASE=0xFF00` / `MMU_MCR_ADDR=0xFF10` / `MCR_EN=bit0`） | **一致** |
| リセット値（`mmu_reset`: `ptr[i]=i` 恒等写像 / `mcr=0`） | **一致** |
| `mmu_mode`（Q1根拠） | **一致** |
| 物理空間幅（Q2根拠・`phys_mem[pa & 0xFFFFF]`＝1MB） | **一致** |
| CDCブリッジ16bit固定 | **未検証**（V3系.svがナレッジ未登録のためレビュア側で裏取り不可。本メモの実ソース確認を信頼） |

### 10.2 レビュア所見：恒等写像リセットの意義（V7/V8への布石）

> §4.3 の「恒等写像リセット（`PTR[i]=i`, `MCR=0`）により reset直後は MMU 有効/無効で挙動が同一」という観察は秀逸。
> `PTR[i]=i` のとき `(i<<12)|offset == logical` となり、**MCR=1にしても変換が恒等**になる。
> これは **V7/V8 の reset vector 問題**（reset解除時にベクタ$0000が読める必要がある）への布石として効く。**仮に起動時にMCRが誤って1になっても、PTRが恒等なら物理アドレスは論理と一致し、ベクタフェッチが破綻しない。安全側に倒れる設計**であり、**MC6809系のSAM/DATが電源投入時に素通し構成から始まるのと同じ思想**といえる。

---

## 11. （参考・v0.1原文）レビュー依頼事項

本メモ v0.1 提出時の依頼事項（KY41：旧情報保持のため残置）。**すべて §10 で回答済**。

1. **§2 MMU挿入位置の確定**（デコーダ前段・MMIO変換外）— ユーザー判断済だが設計として妥当か
2. **§4.2 CDCブリッジの20bit化** — HANDOVER記述との差異を含め、この改修範囲でよいか
3. **§5 検証計画**（MMUベクタ5本＋V3全26ベクタ再実行）— 過不足はないか
4. **§8 未確定事項 Q1〜Q3** — 現案でよいか
5. **§9 文書改版項目** — 漏れはないか

---

## 12. 実装・検証結果（v0.3 追記・2026-07-11）

### 12.1 実装成果物

| ファイル | 版 | 役割 | 備考 |
|---|---|---|---|
| `ysd8800_mmu_v0_1.sv` | v0.1 | MMU本体（純組合せ・16page/4KB/20bit物理） | 新規 |
| `ysd8800_mmio_stub_v0_2.sv` | v0.2 | MMIOスタブ＋MMUレジスタ（PTR[16]/MCR） | `ptr_flat_o` は **packed 128bit**（★原則65★） |
| `ysd8800_cdc_bridge_v0_2.sv` | v0.2 | CDCブリッジ＋`PHYS_AW` パラメタライズ | 20bit化 |
| `ysd8800_psram_ctrl_v0_2.sv` | v0.2 | PSRAM＋`MEM_AW` パラメタライズ | 1MB化・**上位ビット破棄を是正**（§4.2） |
| `ysd8800_v35_membus_v0_1.sv` | v0.1 | V3.5統合ラッパー | V3の `v3_membus` は**残す**（回帰の退路確保） |

**CPUコアは無改修**（v0.5.7 = `ysd8800_cpu_v0_1_FIXED.sv`）のままV3.5が成立した。§3.2 の改修区分どおり。

### 12.2 検証結果（★全項目 ALL PASS★）

| TB | 対象 | 結果 |
|---|---|---|
| `tb_mmu_v0_1.sv` | MMU単体 | **45/45 PASS** |
| `tb_mmio_mmureg_v0_1.sv` | MMIOスタブv0.2（MMUレジスタ） | **86/86 PASS** |
| `tb_bridge_psram_20bit_v0_1.sv` | 20bit貫通・**エイリアス非発生** | **23/23 PASS** |
| `tb_cdc_bridge_v02_poc.sv` | CDCブリッジv0.2回帰（`PHYS_AW=16` でv0.1等価） | **4/4 PASS** |
| `tb_cpu_v35regress_poc.sv` | 実CPU+V3.5（V2-a 20ベクタ） | **20/20 PASS** |
| `tb_cpu_v35mem_poc.sv` | 実CPU+V3.5（V3メモリ系5ベクタ） | **5/5 PASS** |
| `tb_cpu_v35boundary_poc.sv` | 実CPU+V3.5（境界+JSR/RET/BEQ） | **1/1 PASS** |
| **`tb_cpu_v35mmu_v0_1.sv`** | **★実CPU+V3.5・MMU有効時 協調等価（6ベクタ）★** | **6/6 PASS** |

- **S5（デグレ無の証明）**: MCR=0（リセット値）で **V3全26ベクタ ALL PASS**。§4.4 の「MMU無効時は `phys = {4'b0, logical}` でV3とbit-exact等価」が実証された。
- **S7（MMU有効時の等価）**: emu23 v1.09（`--mmu`）を黄金として **6/6 ALL PASS**。

### 12.3 S7 MMUベクタ 最終結果

| # | ベクタ | A | B | X | SP | F | MMIOアクセス数 |
|---|---|---|---|---|---|---|---|
| 1 | MMU_IDENT_ON | 1234 | 1234 | 0000 | 0400 | 00 | 1 |
| 2 | MMU_REMAP_P4 | 5678 | 5678 | 0000 | 0400 | 00 | 2 |
| 3 | MMU_ISOLATION | 0000 | 0000 | 0000 | 0400 | 01 | 3 |
| 4 | MMU_BOUNDARY | 0011 | 0022 | 0000 | 0400 | 01 | 3 |
| 5 | MMU_PTR_RW | 0001 | 0001 | **0055** | 0400 | 01 | 4 |
| 6 | **MMU_MMIO_BYPASS** | 0000 | **3C3C** | **0055** | 0400 | 00 | 5 |

**実証された設計上の要点:**

1. **MMU有効時のアドレス変換が RTL / emu23 で外部観測等価**（#1・#2）
2. **物理アドレス空間の分離が実効**（#3 MMU_ISOLATION）— §4.2 の20bit化が効いている証拠。上位ビット破棄が残っていれば物理 $14000 が $4000 にエイリアスし、本ベクタは必ずFAILする。
3. **4KB境界のページ個別変換**（#4）— §5.2 の整理どおり、境界跨ぎ16bitは原理上発生しないためバイトアクセスでの個別確認で十分。
4. **★MMIOはアドレス変換を受けない★**（#6）— PTR[15]=$1F で $F000台をリマップした状態でも $FF07（PTR[7]）に到達し（X=$0055）、さらに MCR($FF10) で MMU を切り戻せた（B=$3C3C）。**§2.3 の自己ロックアウト回避＝自己救済性が実証された。**

### 12.4 実装中に発生した不具合と対処

#### (a) MMUベクタ生成器のAレジスタ破壊（S6）

**現象**: ベクタ #5 / #6 で PTR[7] の読み返し値（$55）が最終状態に残らない。

**原因**: MMU操作ヘルパ `set_ptr()` / `mmu_on()` / `mmu_off()` はいずれも `LDW A,#imm16` → `STB A,[MMIO]` の2命令列であり、**内部でAレジスタを破壊する**。`get_ptr(A,7)` で読んだ直後に `mmu_on()` / `mmu_off()` を呼ぶと上書きされる。

**対処**: `mov(X, A)`（`MOV rD,rS = 0x20 [rD<<4|rS]`・emu23_v109.c L1305-1309 実照合）で X へ退避。**X は MMU操作ヘルパで一切使っていないため退避先として安全**。生成器を **v0.2** へ改版し、ヘルパに「★A破壊★」警告コメントを追加した。

#### (b) TB側MMIOカウンタ判定の偽FAIL（S7）

**現象**: S7 初回実行で #3 MMU_BOUNDARY が「MMIOアクセス無し」でFAIL。#0 のカウント値が `x`。

**原因**: `dbg_mmio_access_count` は `cpu_rst_n` でクリアされる**ベクタ毎の絶対カウンタ**（`ysd8800_mmio_stub_v0_2.sv` L134-141 実照合）。TB側で「累積カウンタ」と誤解し前ベクタとの差分で判定したため、#2 と #3 が同カウント値（共に3）となり差分0で誤FAILした。

**対処**: 判定を絶対値（`== 0` ならFAIL）へ変更。**RTL実装に誤りはなく、TBの判定ロジックのみが誤りだった。**

**教訓（★kaizen 新原則候補66★）**: 偽合格防止のために追加した診断判定そのものが、**対象RTLのリセットドメインを実照合せずに書かれたため偽FAILを生んだ**。診断信号（カウンタ・ラッチ）を判定に使うときは、**その信号のリセット条件・更新条件を実ソースで確認してから使う**（KY34の診断信号版）。

### 12.5 §2 の確定事項に変更なし

MMU挿入位置（**デコーダ後段・RAM側パスのみ・MMIOは変換外**）は、実装・検証を通じて**変更の必要が生じなかった**。§2.1 の結論はそのまま確定である。

---

## 改版履歴

| 版 | 日付 | 内容 |
|---|---|---|
| v0.1 | 2026-07-11 | 新規作成（レビュー用ドラフト）。emu23 v1.09実装照合によりMMU挿入位置を確定。CDCブリッジ16bit固定をKY34で検出し改修項目に追加 |
| **v0.2** | **2026-07-11** | **レビュー結果（`v3_5_design_review_reply_v1_0.docx`）反映。総合判定=承認（Mレベル指摘なし）・原則43により実装着手可。①依頼3の条件として §5.2 にベクタ#6 MMU_MMIO_BYPASS（MMIO非変換の実証・原則63類型の予防）を追加。②レビュー整理「4KB境界は偶数のため境界跨ぎ16bitアクセスはアライメント例外となり原理上発生し得ない」を §5.2 に追記（MMU_BOUNDARY は個別変換確認で十分と確定）。③§9 に kaizen 新原則候補の登録推奨を明記。④§10 をレビュー結果記録に差し替え** |
| **v0.3** | **2026-07-11** | **実装・検証完了（S1〜S7）に伴う結果反映。①§12「実装・検証結果」を新設（成果物一覧・全TB結果・S7 MMUベクタ最終値・不具合と対処）。②★§4.2 を重要訂正★ — v0.2 は上位ビット破棄を「CDCブリッジのみ」と記していたが、実際は `ysd8800_psram_ctrl_v0_1` 内部（`addr[15:0]` 切り捨て・64KB固定配列）にも存在し、**破棄箇所は2箇所**であった（KY41により旧記述は取り消し線で保持）。③S7 で MMU有効時の emu23協調等価 6/6 ALL PASS を達成し、§2 のMMU挿入位置・MMIO非変換が実証された（§2 の確定事項に変更なし）。④kaizen 新原則候補66（診断信号を判定に使う際はリセット条件を実照合する）を §12.4 に記録** |
