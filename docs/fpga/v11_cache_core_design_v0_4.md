# 工程② キャッシュRTL開発 ②-B：キャッシュ本体 設計書

- **文書版数**: v0.4
- **作成日**: 2026-08-30（v0.1 → v0.2 → v0.3 → v0.4 同日改版）
- **作成元チャット**: 「キャッシュ RTL 開発 (その4)」
- **対象工程**: ②-B（キャッシュ本体 RTL 実装）
- **状態**: ★**条件付き承認（段1・段2 着手可）**★／v0.3 指摘の M-11・M-12 を本版で反映済みのため ★**段3 も着手可**★（v0.4 は再レビュー不要・差分を工程報告に添付する）
- **レビュー指摘書**:
  - `review_v11_cache_core_design_v0_1_v1_0.md`（v1.0・判定＝差戻し）→ 全件決着
  - `review_v11_cache_core_design_v0_2_v1_0.md`（v1.0・判定＝条件付き承認）→ 全件決着
  - `review_v11_cache_core_design_v0_3_v1_0.md`（v1.0・判定＝条件付き承認・★v0.4 は再レビュー不要★）→ ★本版で対応★

### ★v0.3 指摘の反映状況（M×2 / C×1 / N×1）★

| ID | 指摘 | 反映 | 反映先 |
|---|---|---|---|
| **M-11** | §6.3 の非対称性が §7 の式に伝播していない | ★**是正**★ | §7.1（`data_q_valid` 導入）／§7.2／§8.4 TB |
| **M-12** | DPB ポート用途表に書込ヒットが無い（SDPB では実装不可） | ★**是正**★ | §1.3（ポート用途表を新設・★DPB 必須を明記★） |
| C-8 | 中止時も `fill_hit_byte_r` を返してよいか | ★**確認・明記**★ | §7.6（★登録の中止と応答の中止は別★）／§8.4 TB |
| N-5 | §6.3 を機能の前提にしない | ★採用★ | §6.3 冒頭の位置づけ注記／§7.1 の N-5 注記 |

### ★v0.2 指摘の反映状況（M×3 / C×3 / N×1）★

| ID | 指摘 | 反映 | 反映先 |
|---|---|---|---|
| **M-8** | `mem_addr` 先行が実源に反する | ★**実源解析で確定**★ | **§6.3 新設**（フェッチ 1 cyc／データ 2 cyc の非対称を確定）／§6.4（損益分岐 68.9%） |
| **M-9** | ミス完了時に誤バイトを返す | ★**是正（案 a 採用）**★ | §7.1（`fill_hit_byte_r` キャプチャ）／§7.2 手順 0・4 |
| **M-10** | 配線図に帰り経路・`burst_len` が無い | ★採用★ | §1.2（3 本追記＋membus v0.7 への版数変更を明記）／§9 段2 |
| C-5 | Icarus ハングの確認項目化 | ★採用★ | §6.6 リスク 3／§9 段2 のゲート |
| C-6 | フィル所要は `LAT+31=43` psram cyc | ★採用★ | §7.5／§6.4（*P* の内訳と整合） |
| C-7 | DPB は「発生するが無害」 | ★採用★ | §1.5（全面書き換え） |
| N-4 | D-B2 を暫定確定に | ★**確定を維持**★ | §11.1（M-8 が 1 cyc 側に出たため。ただし *f* は emu23 で再確認） |

### ★v0.1 指摘の反映状況（M×7 / C×4 / N×3 / E×1）★

| ID | 指摘 | 反映 | 反映先 |
|---|---|---|---|
| **M-1** | クロックドメイン・配置未定義 | ★全面採用★ | **§1 新設**（DPB 構成・ドメイン分割） |
| **M-2** | ヒット経路のサイクル設計が無い | ★全面採用★ | **§8 新設**（サイクル図・`tag_ram`＝FF アレイに一意決定） |
| **M-3** | バスは 8 ビット・跨ぎは存在しない | ★全面採用★ | §5.2 是正／旧§7.3 **削除**／**D-B7 撤回**／KY-B6 非該当化 |
| **M-4** | 基準値に出典欄が無い | ★採用★ | §10.1（出典・一次/二次の別を明記） |
| **M-5** | CCR ビット定義の欠落 | ★採用★ | §7.1（7:4 予約・種別是正・バイト割付） |
| **M-6** | 「ビンを跨ぐか」は成立しない | ★全面採用★ | §0.3 後半を撤回／§10.5 判断基準を差替 |
| **M-7** | カウンタ 16 bit は飽和する | ★採用（案 c）★ | §7.2 D-B5（初版は非実装） |
| C-1 | `addr_decoder` との前後 | ★採用★ | §1.2（PSRAM 側分岐の内側に置く＝`is_mmio` 条件が構造的に不要） |
| C-2 | フィル中の書込ヒット | ★採用★ | §9.4（フィル中は `mem_ready` で待たせる） |
| C-3 | DMA マスタの有無 | ★実源確認済★ | §1.4（PSRAM 書込経路は CPU のみ） |
| C-4 | `FLUSH` とフィルの相互作用 | ★採用★ | §9.5（フィル中止・valid を立てない） |
| N-1 | D-B1〜D-B7 への回答 | ★全て一致★ | §11 |
| N-2 | `CEN=0` は純組合せバイパス | ★採用★ | §9.1 |
| N-3 | 単体 TB に CDC 項目追加 | ★採用★ | §10.4 |
| E-1 | BSRAM 算出根拠 | ★採用★ | §2.1 脚注 |
- **前提文書**:
  - `v10_psram_burst_design_v0_4.md`（②-A・承認済み）— バースト IF の契約
  - `emu23_cache_base_design_v1_0.md`（承認済み）— キャッシュモデルの意味論
  - `v9_psram_perf_baseline_v1_2.md` — 性能基準値
  - `fpga_source_version_ledger_v1_14.md` — RTL 版数台帳

---

## 0. 本書のスコープと、②-A' の帰結の反映

### 0.1 スコープ

`ysd8800_v5_membus` にダイレクトマップ・ユニファイドキャッシュ段を新設し、
CCR（キャッシュ制御レジスタ）で有効／無効を切り替えられるようにする。

### 0.2 非目標（本工程で扱わない）

| 項目 | 理由 |
|---|---|
| セットアソシアティブ化 | ダイレクトマップで G-0 を通してから検討する（1変更1検証） |
| ライトバック方式 | `emu23_cache_base_design_v1_0.md` §5.1 がライトスルーを前提に課金式を組んでいる。方式変更は課金式の再設計を伴う |
| ライトバッファ | 同 §5.1 M-2 で「無しを仮定」と明記。導入は別途モデル化が必要 |
| クリティカルワードファースト | 同 §4.2.2 で対象外 |
| I/D 分離 | 同 §5.2（H-5）で統合と決定済み。分離は ISA2.3 に無いキャッシュ無効化命令を要求する |
| 命令キャッシュ専用の最適化 | 同上 |

### 0.3 ★②-A' の帰結を本工程に反映する（本書で最も重要な前提）★

②-A' の結論は「速度依存デッドロックは実在せず、TB v0.2 の M-5 判定が
速度依存であったこと」であった。②-B はキャッシュヒット時に実効レイテンシが
LAT=4 相当より短くなるため、**同じ判定器の脆弱性を必ず踏む**。

したがって本工程では以下を**遵守事項**とする。

| # | 遵守事項 |
|---|---|
| B-1 | 検証には **`tb_cpu_v8b_prod_v0_3.sv` 以降**を使用する。v0.2 の使用を禁止する |
| B-2 | 新たに判定を追加する場合、**速度非依存であること**を設計時に明記する（原則137） |
| B-3 | FAIL が出た場合、**RTL を疑う前に判定器を疑う**。判定器の速度非依存性を先に確認する |

**②-A' で得た量子化ビン構造（実測・2026-08-30）**

| ビン | LATENCY_NORMAL | M-5 到達 cycle |
|---|---|---|
| 群1 | 4 / 5 / 6 | 2,810,474 |
| 群2 | 7 / 12 | 3,754,937 |

CPU:PSRAM = 8:1 のため PSRAM レイテンシは CPU 視点で 8 単位に量子化される。
**LAT を 1 減らしても、同一ビン内であれば性能は 1 サイクルも改善しない。**

~~②-B の性能評価は「LAT を下げる」ではなく「アクセスそのものを消す」ことで
ビンを跨ぐ、という観点で行う。~~

★**v0.2 是正（M-6）**★：後半は誤りである。**量子化ビンは `LATENCY_NORMAL` を
掃引したときに M-5 が階段状にしか動かないという PSRAM レイテンシ側の性質**であり、
キャッシュは `LAT` を変えずに**アクセス回数そのものを減らす**。
削減されたアクセス数に比例して総サイクル数は**連続的に**減るのであって、
ビン構造には従わない。

> ⚠️ この基準を採ると、**群1（2,810,474）に届かない改善を「効果なし」と誤判定する**
> 危険があった。ヒット率 40% でも十分な改善であり得る。

**正しい観点**：ビン構造が示すのは「**LAT の微調整では性能が動かない＝レイテンシを
削る方向は打ち止めである**」ことのみ。だからこそ ②-B（アクセス回数を減らす方向）に
意味がある、という**動機づけ**として用いる。合否基準には用いない（→ §10.5）。

---

## 1. ★クロックドメインと配置（M-1 対応・本版の中核）★

### 1.1 実源が示す構造

`ysd8800_v5_membus_v0_6.sv` L196-212 および L150-171 のブロック図より：

```
  CPUコア（cpu_clk 4MHz・論理アドレス16bit）
        │ mem_addr[15:0] / mem_wdata[7:0] / mem_rdata[7:0] / mem_rd / mem_wr / mem_ready
  ┌─────┴──────────────┐
  │ ysd8800_addr_decoder │  ← $FC80 判定
  └──┬───────────────┬──┘
 MMIO側│               │RAM側
  ┌───┴────────┐  ┌───┴──────────┐
  │ mmio_stub  │  │ ysd8800_mmu  │ ← 純組合せ
  └────────────┘  └───┬──────────┘
                       │ phys_addr[19:0]
              ┌────────┴────────┐
              │  cdc_bridge     │ ★ここがドメイン境界★
              └────────┬────────┘
              ┌────────┴────────┐
              │  psram_ctrl     │（psram_clk 32MHz）
              └─────────────────┘
```

**クロック比は 8:1**（`cpu_clk` 4MHz / `psram_clk` 32MHz）。

### 1.2 ★配置の決定（C-1 対応）★

**キャッシュ段は `addr_decoder` の RAM 側分岐の内側、`mmu` の後段、
`cdc_bridge` の CPU 側に置く。**

```
  ysd8800_mmu ──┬─► phys_addr[19:0]
                │
        ┌───────┴────────────────┐
        │ ★ysd8800_cache（新規）★ │ ← ヒット判定は cpu_clk
        └──┬──────────────────▲──┘
           │ ①要求（ミス時のみ）  │ ②③フィルデータ（psram_clk・直結）
        ┌──┴────────────┐        │
        │  cdc_bridge     │        │
        └──┬────────────┘        │
           │                       │
        ┌──┴────────────────────┴──┐
        │       psram_ctrl           │（psram_clk）
        └────────────────────────────┘
```

★**v0.3 追記（M-10）：図に現れていなかった 3 本の信号**★

| # | 信号 | 方向 | ★ドメイン★ | 備考 |
|---|---|---|---|---|
| ① | キャッシュ → `psram_ctrl.burst_len` | 下り | ★`cpu_clk`★ | req 受理時にラッチされる契約（②-A 設計書 §2.3）。ミス時 32／`CEN=0` 時 1 |
| ② | `psram_ctrl.beat_valid` → キャッシュ DPB 書込イネーブル | ★上り★ | ★`psram_clk`★ | ★`cdc_bridge` を経由せず直結★ |
| ③ | `psram_ctrl.rdata` → キャッシュ DPB 書込データ | ★上り★ | ★`psram_clk`★ | 同上。②③が §1.3 の DPB 構成を成立させる |

> ★**②③が `cdc_bridge` を経由してはならない。**★
> 経由すると 1 バイトごとに CDC が発生し、クロック比 8:1 では追従できない（KY-B7）。
> `cdc_bridge` を通るのは①の要求と `ack` の完了通知だけである。

★**membus トップレベルの配線変更が必須である（M-10）**★

②-A の `membus v0.6` は `.burst_len(PS_BLEN_W'(1))` と**定数で固定**している
（②-A 設計書 §4）。②-B ではキャッシュ段がこれを 32／1 で駆動するため、
★**ポート接続の実質的な変更**★になる。

| 工程 | 変更の性質 | 版数の扱い |
|---|---|---|
| ②-A | ポート接続 2 本追加のみ（Case X-1） | v0.5 → v0.6 |
| ★②-B★ | ★**接続の実質変更＋新規モジュール挿入**★ | ★v0.6 → **v0.7**★（→ §9 段2） |

**この配線変更は §9 の段2（キャッシュ骨格追加）に属する。**
段2 では `CEN=0` 固定であるため `burst_len` は常に 1 を出力し、
★G-0（バイト完全等価）で配線変更の非退行を検証できる★。

**この配置の帰結**

| 項目 | 内容 |
|---|---|
| `is_mmio` 条件 | ★**不要**★。MMIO アクセスは `addr_decoder` で分岐済みでキャッシュ段に到達しない（C-1 の推奨どおり） |
| インデクス | **物理アドレス**（MMU 後段）でインデクスする。コンテキストスイッチ時のフラッシュ不要 |
| ヒット時 | ★**CDC を越えない**★。これが利得の源泉である |

> ★レビュー指摘 M-1 の (B) 案（PSRAM 側配置）を採らない理由★
> (B) ではヒットでも CDC 往復（A-2q 後で約 3 cpu cyc）を必ず払う。
> 現行のミスが約 4 cpu cyc なので **4 → 3 にしかならず、キャッシュを入れる意味が消える**。
> よって **(A) 側配置は必須**であり、選択の余地はない。

### 1.3 ★ラインフィルの成立条件（M-1 の矛盾の解消）★

指摘のとおり、`beat_valid` の 32 サイクルは **`psram_clk` の 32 サイクル
＝ `cpu_clk` の 4 サイクル**である。キャッシュ段全体を `cpu_clk` に置くと
32 バイトを取り込めない。**そこで記憶構造をドメインで分割する。**

| 構造 | ドメイン | 実体 | 理由 |
|---|---|---|---|
| `data_ram` | ★**デュアルクロック・トゥルーデュアルポート BSRAM（DPB）**★ | BSRAM | ★**v0.4 是正（M-12）**★ ~~書込＝`psram_clk`（フィル）／読出＝`cpu_clk`（ヒット）~~ → 下表参照 |
| `tag_ram` | `cpu_clk` | ★**FF アレイ**★（→ §8.2） | ヒット判定は CPU 側で完結する必要がある |
| `valid_r` | `cpu_clk` | FF アレイ | 一括クリアのため（§4.1） |

**CDC が発生する箇所は 1 つだけである。**

#### ★`data_ram` のポート用途（M-12 対応・v0.4 是正）★

| ポート | クロック | 用途 | 方向 |
|---|---|---|---|
| ポート A | `psram_clk` | ラインフィル（beat 書込） | ★書込★ |
| ポート B | `cpu_clk` | ①投機読出（毎サイクル・§6.5）<br>②★**書込ヒット時のバイト更新（ライトスルー・§7.3）**★ | ★**読出＋書込の両用**★ |

★**プリミティブは DPB（トゥルーデュアルポート）が必須である。**★

Gowin の BSRAM には ★SDPB（セミデュアルポート：書込専用＋読出専用）★ と
★DPB（トゥルーデュアルポート：両ポートが読書可）★ がある。
v0.3 は `cpu_clk` 側を「読出」とのみ記述しており、そのまま SDPB を選ぶと
★**§7.3 のライトスルー（書込ヒット時のキャッシュ更新）が実装できない**★。

> ★投機読出と書込ヒットは同一ポート（B）を共有するが、**排他である**。★
> `mem_wr` のサイクルでは読出結果が不要であるため、調停は不要。
> ★実装時に「書込サイクルで投機読出が止まる」ことを不具合と誤認しないこと。★

> ★BSRAM 概算（§2.1）への影響はない。★ DPB でもブロック数は同じである
> （ポート構成の違いであって容量の違いではない）。

```
  フィル要求発行  : cpu_clk → psram_clk （既存 cdc_bridge の req 経路に相乗り）
  各 beat の書込  : psram_clk 内で完結（★CDC 不要★・DPB の書込ポート）
  フィル完了通知  : psram_clk → cpu_clk （既存 ack 経路 L139/L148 に相乗り）
  タグ／valid 更新: cpu_clk 内（★フィル完了時の 1 イベントのみ★）
```

> ★**ビート毎の CDC は発生しない**★（レビュー M-1 の指摘どおり）。
> これが本構成の要である。ビート毎に CDC を行う設計にすると
> 8:1 のクロック比では追従できない。

> ⚠️ ★`ack` はレベルではなく**立上りエッジ**が `cdc_bridge_v0_4` L139
> （`suppress_r <= ack_sync[0] & ~ack_sync[1];`）で 1 cpu cyc パルス化される。★
> バースト設計書 §1.3 M-2 の警告に従い、**各 beat で `ack` を立てる改造をしてはならない。**

### 1.4 ★PSRAM 書込経路は CPU のみ（C-3 対応・実源確認済）★

`ysd8800_v5_membus_v0_6.sv` L275/L378/L403 のインスタンス接続より、
`psram_ctrl` への要求経路は `addr_decoder` → `mmu` → `cdc_bridge` の**単一経路**であり、
**DMA マスタは存在しない**。YSD8003（ストレージ）は `mmio_stub` 内部に
インスタンス化され、CPU の `LDB`/`STB` 経由でのみデータが移動する。

★**したがってコヒーレンシ問題は発生しない。**★

> ⚠️ ②.5 以降で DMA を導入する場合、**本前提が崩れる**。
> その際は DMA 対象領域の非キャッシュ化、またはフラッシュ機構が必須になる。
> 本書 §7.1 の `FLUSH` ビットはその布石でもある。

### 1.5 ★DPB の read-during-write（C-7 対応・v0.3 是正）★

~~フィル進行中（`psram_clk` 側で書込中）に、`cpu_clk` 側から同一ラインを読む
可能性がある。**本設計では発生させない**。~~

★**v0.3 是正（C-7）**★：上記は不正確であった。§6.5 の投機読出は
★**フィル進行中も毎サイクル `data_ram` を読み続ける**★。したがって
同一アドレスへの read-during-write は ★**回避されるのではなく常時発生する**★。

**結論（無害）は変わらない。理由を正確に述べる：**

| 事象 | 内容 |
|---|---|
| 発生 | ★**常時発生する**★（投機読出が止まらないため） |
| Gowin DPB の挙動 | read-during-write 時の読出データは★**不定**★になる |
| ★なぜ無害か★ | フィル中は `valid_r[index] = 0` である（valid はフィル完了時に立てる）。★**ヒット判定が必ず不成立となり、不定値は捨てられる**★ |
| 二重の保護 | さらに §7.5 により、フィル中の CPU 要求は `mem_ready=0` で待たされる |

★**「valid をフィル完了時にのみ立てる」ことが、DPB 競合の回避策そのものである。**★
競合を調停ロジックで解くのではなく、**競合が起きても結果が使われない構造**にして
消している。

> ★`tb_cache_cdc_poc` の検証意図はここにある（§8.4）。★
> 「不定値が出ること」を確認するのではなく、★**不定値が出ても
> CPU に伝播しないこと**★を確認する。

---

---

## 2. 構成の決定（★要レビュー論点 その1★）

### 2.1 資源見積もり（GW1NR-9・BSRAM 26 ブロック × 18Kbit）

物理アドレス 20 ビット（`MEM_AW=20`）を前提とする。

| 容量 | ライン | ライン数 | index | offset | tag幅 | データ | タグ | 概算BSRAM |
|---|---|---|---|---|---|---|---|---|
| 4 KB | 32 B | 128 | 7 | 5 | 8 b | 32 Kb | 1.1 Kb | 約 2.1 |
| 8 KB | 32 B | 256 | 8 | 5 | 7 b | 64 Kb | 2.0 Kb | 約 4.1 |
| 16 KB | 32 B | 512 | 9 | 5 | 6 b | 128 Kb | 3.5 Kb | 約 8.2 |
| 16 KB | 16 B | 1024 | 10 | 4 | 6 b | 128 Kb | 7.0 Kb | 約 8.4 |

（タグ幅 = 20 − index − offset、有効ビット 1 を各ラインに付加）

> ★E-1 対応・「概算 BSRAM」列の算出根拠★
> GW1NR-9 の BSRAM は 18Kbit ブロックだが、★**x9 構成（パリティ付き）ではなく
> x8 構成で使うため、1 ブロックあたりの実効容量は 16Kbit**★である。
> 例（8 KB / 32 B）：データ 64Kb ÷ 16Kb = **4.0 ブロック** ＋ タグ 1 ブロック ≒ **4.1**。
> `valid_r` は BSRAM ではなく FF アレイに置くため（§4.1）、本列には含まない。

### 2.2 ★推奨案：8 KB / 32 バイトライン★

**推奨理由**

1. **BSRAM 消費が約 4 ブロック**で、26 ブロックのうち 22 ブロックを他用途
   （②.5 の GPIO 拡張バス・デバッグ DIO、将来の MMU 拡張）に残せる。
   16 KB は 8 ブロックを消費し、余裕が減る。
2. ライン 32 バイトは ②-A の `BURST_MAX=32` と**一致**する。
   バースト設計書 §1.2 は「②-B のライン長上限。②-B で最終確定」としており、
   32 を採るとバースト側の変更が不要になる（1変更1検証に資する）。
3. YUI OS 本体は 56,416 バイト。8 KB は約 14 %。ダイレクトマップの
   コンフリクトミスが問題になるかは実測で判断すべきであり、
   まず小さい構成で G-0 を通し、必要なら容量を上げるのが安全である。
4. emu23 側の既定は 16 KB だが、`--cache-size` で可変であるため、
   **RTL 実装値に合わせて emu23 側を 8 KB で走らせれば比較可能**である。

### 2.3 ★決定を要する事項★

| ID | 論点 | 推奨 | 代替 |
|---|---|---|---|
| **D-B1** | キャッシュ容量 | **8 KB** | 4 KB（資源優先）／16 KB（性能優先・emu23 既定と一致） |
| **D-B2** | ラインサイズ | **32 B** | 16 B（ミスペナルティ半減・バースト側変更不要だが空間局所性を活かせない） |
| **D-B3** | 方式 | **ダイレクトマップ**（emu23 §5.1 と一致） | 2-way（BSRAM は足りるが比較器 2 系統・置換ロジックが増える） |

> ★レビュー時にこの 3 点をご判断いただきたい。★
> 以降の記述は D-B1=8KB / D-B2=32B / D-B3=ダイレクトマップを仮に採った場合のものである。

---

## 3. アドレス分解

```
物理アドレス 20 ビット（MMU 後段）

  19                 13 12          5 4        0
 +---------------------+-------------+----------+
 |     tag  (7 bit)    | index (8bit)| off(5bit)|
 +---------------------+-------------+----------+
```

- 配置は **MMU 物理アドレスの後段**（`emu23_cache_base_design_v1_0.md` §5.1）。
  物理アドレスでインデクスするため、**コンテキストスイッチ時のフラッシュが不要**。
  （OS-9 の MMU 運用と同じ思想。論理アドレスでインデクスすると
  プロセス切替のたびに全無効化が要り、YUI OS のように切替頻度が高い系では損になる。）

---

## 4. 記憶構造

| 配列 | 幅 | 深さ | 実体 |
|---|---|---|---|
| `tag_ram` | 7 | 256 | BSRAM または分散 RAM |
| `valid_r` | 1 | 256 | ★FF アレイ（BSRAM ではない）★ |
| `data_ram` | 8 | 8192 | BSRAM（バイトアドレッサブル） |

### 4.1 ★`valid` を FF に置く理由（重要）★

`valid` を BSRAM に置くと、**一括クリアが 1 サイクルでできない**。
CCR による無効化・全フラッシュは全ライン同時クリアを要求するため、
256 ビットの FF アレイとして持ち、`valid_r <= '0` で一括クリアする。

256 FF は GW1NR-9 の資源（8,640 LUT / 6,480 FF 相当）に対して十分小さい。

### 4.2 ★データ RAM をバイト幅にする理由（M-3 是正）★

~~ISA2.3 は `LDB`/`STB` を持ち、奇数アドレスアクセスが発生する。ワード幅 RAM に
するとバイト書き込みでリードモディファイライトが必要になり、ライトスルーの
タイミングが複雑化する。バイト幅とし、16 ビットアクセスは 2 バイト分を同時に扱う。~~

~~⚠️ ライン境界を跨ぐ 16 ビットアクセス（offset = 31 のワードアクセス）は
2 ラインに跨る。この扱いを §7.3 で規定する。~~

★**v0.2 是正（M-3・KY34 実源確認）**★

`ysd8800_v5_membus_v0_6.sv` **L205-206**：

```systemverilog
input  logic [7:0]  mem_wdata,
output logic [7:0]  mem_rdata,
```

★**CPU 抽象バスのデータ幅は 8 ビットである。**★
`cdc_bridge_v0_4` L121 以降も 8 ビットで一貫している。

**したがって：**

| 事項 | v0.1 の認識 | ★実際★ |
|---|---|---|
| メモリ段から見た 16 ビットアクセス | 存在する | ★**存在しない**★ |
| `LDW`/`STW` の扱い | メモリ段で 2 バイトを同時処理 | ★**CPU コアが既に 2 回のバイトトランザクションに分解して発行している**★ |
| ライン境界跨ぎ | offset=31 で 2 ラインに跨る | ★**発生しない**★。offset=31 のバイトアクセスと offset=0 のバイトアクセスが**独立に 2 回**来るだけで、それぞれが独立にヒット／ミス判定されて正しく動く |

**結論（バイト幅とすること自体）は正しいので維持する。ただし理由を差し替える：**

> `data_ram` をバイト幅（8 × 8192）とするのは、★**CPU 抽象バスがそもそも
> 8 ビットだから**★である。ワード幅にすると、8 ビットのバス幅との整合のために
> かえってバイトイネーブル制御が必要になる。

> ★KY34（実ファイルが真実）の適用事例である。★
> 「ISA2.3 に `LDW`/`STW` がある」ことと「メモリバスが 16 ビットである」ことは
> **別の話**であり、後者は実源を見なければ分からなかった。
> v0.1 は前者から後者を推論してしまい、★**存在しない問題（ライン跨ぎ）に
> 対策（D-B7・バイパス機構）を設計しかけていた。**★
> 実装・検証（`tb_cache_wt_poc` の跨ぎ検証）まで無駄が波及するところであった。

---

## 5. CCR（キャッシュ制御レジスタ）（★要レビュー論点 その2★）

### 5.1 ビット定義（★M-5 対応・是正版★）

CCR は 16 ビットレジスタだが、★**バスは 8 ビット**★（§4.2）である。
実源の他デバイス（`mmio_stub` L228-230：`$FC80 TX（下位）/ $FC81（上位）`）に倣い、
★**偶数番地＝下位バイト、奇数番地＝上位バイト**★で割り付ける。

#### 下位バイト（`$FF12`）

| ビット | 名称 | 種別 | リセット値 | 意味 |
|---|---|---|---|---|
| 0 | `CEN` | R/W | **0** | 1 = キャッシュ有効。**リセット値 = disable**（決定済み） |
| 1 | `FLUSH` | ★**W1T（write-1-to-trigger, self-clearing）**★ | 0 | 1 書込で全ライン無効化を起動。★**読み出しは常に 0 を返す**★（起動中も 0） |
| **7:2** | — | RO | 0 | ★**予約（読み出しは常に 0）**★ |

#### 上位バイト（`$FF13`）

| ビット | 名称 | 種別 | リセット値 | 意味 |
|---|---|---|---|---|
| 8 | `HIT_EN` | R/W | 0 | ヒット／ミスカウンタ計数有効 ★**初版では非実装**（M-7・D-B5）★ |
| **15:9** | — | RO | 0 | ★**予約（読み出しは常に 0）**★ |

★**v0.1 からの是正点（M-5）**★

| # | v0.1 | ★v0.2★ |
|---|---|---|
| ① | ビット 4・5・6・7 が定義も予約もされていなかった | ★**7:2 を予約（読み出し 0）と明記**★。抜けたまま実装すると読み出し値が実装依存になる |
| ② | `FLUSH` を「W1S」と表記 | ★**W1T（write-1-to-trigger, self-clearing）**★ に是正。W1S ではない。読み出しは常に 0 と規定 |
| ③ | 16 ビットレジスタのバイト割付が未記述 | ★`$FF12`＝下位／`$FF13`＝上位 と明記★。`HIT_EN`（ビット 8）は `$FF13` に載る |

> ⚠️ ③は `kernel_v12_11.asm` 側の定義と直結する。★KY41 4 点整合の対象★であり、
> RTL・カーネル・`yuios_memmap_design` の 3 箇所に**同時反映**すること。

### 5.2 ★決定を要する事項★

| ID | 論点 | 内容 |
|---|---|---|
| **D-B4** | CCR の MMIO アドレス | ★実源確認済み（下表）。**推奨：`$FF12`**★ |
| **D-B5** | ヒット／ミスカウンタの要否 | ★**v0.2 決定：初版は非実装（案 c）**★（下記 M-7 参照） |
| **D-B6** | `CEN` を 0→1 に変えた瞬間の扱い | **推奨：`CEN` の立上りで暗黙に全無効化する**。古いタグが残ったまま有効化されると、キャッシュ無効期間中に書かれたメモリとの不整合が生じる |

### 5.3 ★MMIO マップ実源確認（`ysd8800_mmio_stub_v0_8.sv` L219-243）★

| 範囲 | 用途 | 出典 |
|---|---|---|
| `$FC80`–`$FC87` | YSD8001 UART | L229-230 |
| `$FC88`–`$FC8F` | **空き** | — |
| `$FC90`–`$FC9F` | YSD8002 Timer | L236-237 |
| `$FCA0`–`$FCB1` | YSD8003 Storage | L242-243 |
| `$FCB2`–`$FCB5` | YSD8004 IRQ | L224-225 |
| `$FCB6`–`$FEFF` | **空き** | — |
| `$FF00`–`$FF0F` | MMU PTR[0..15] | L219-220 |
| `$FF10` | MMU MCR | L221 |
| `$FF11`–`$FFFF` | **空き** | — |

**推奨 `$FF12`（および将来 `$FF14`/`$FF16` をカウンタ用に予約）の理由**

1. キャッシュは MMU 物理アドレスの後段に置かれる**メモリ系制御**であり、
   MMU MCR（`$FF10`）の直後に並べるとアドレスマップの意味的まとまりが保てる。
2. `$FC80` 台のデバイス群（UART/Timer/Storage/IRQ）は**周辺デバイス**であり、
   そこに混ぜるとデバイスとメモリ制御の区別が失われる。
3. `$FF11` ではなく `$FF12` とするのは、**16 ビットアクセスの偶数アラインを保つ**ため。
   ISA2.3 はバイトアドレッサブルだが、`LDW`/`STW` の奇数アドレスアクセスは
   アラインメント例外の対象であり、制御レジスタは偶数番地に置くのが安全である。

> ⚠️ 確定にあたっては `kernel_v12_11.asm` および `yuios_memmap_design` 側の
> MMIO 定義とも突合し、**両者に同時に反映**すること（KY41 4点整合）。

### 5.4 ★ヒット／ミスカウンタの決定（M-7 対応）★

**v0.1 は幅を定義していなかった。試算すると 16 ビットでは成立しない。**

| 項目 | 値 |
|---|---|
| M-5 到達までの総実行時間 | 約 **3,754,937** cpu cycle |
| メモリアクセス 1 バイトあたり | 約 4 cyc |
| ★アクセス総数★ | ★**数十万〜百万オーダー**★ |
| 16 ビットカウンタの上限 | 65,535 |
| 結論 | ★**測定開始から 1 秒未満で飽和する。ヒット率が算出できず、目的を果たさない**★ |

**★v0.2 決定：案 (c) — 初版では実装しない。**★

| 案 | 内容 | 採否 |
|---|---|---|
| (a) 32 ビット化 | `$FF14`-`$FF17`（ヒット）／`$FF18`-`$FF1B`（ミス） | 見送り |
| (b) 16 bit ＋ラップ | ソフトで周期読出して積算。★割込が絡むと取りこぼす★ | 不採用 |
| ★**(c) 非実装**★ | emu23 の `--cache-stats` で代替 | ★**採用**★ |

**採用理由**

1. §8.5 の性能判断基準②が「emu23 のヒット率と RTL 実測の整合」であり、
   ★**ヒット率は emu23 側から得られる。RTL 側にカウンタが無くても評価が成立する。**★
2. ②-B のスコープを絞れる（1 変更 1 検証）。
3. 32 ビット×2 本のカウンタと MMIO 読出経路は、`tag_ram` に 1,792 FF を
   使う本設計（§6.2）にとって無視できない追加資源である。

> ★アドレス `$FF14`-`$FF1B` は将来のカウンタ用に**予約**しておく。★
> 必要になった時点で (a) として追加する。その際は**飽和保持（ラップしない）**とし、
> 飽和したことが判別できるようにする。

> ★D-B6 は安全性に直結する。★
> 「無効化中に書いた内容がキャッシュに反映されない」問題は、
> ライトスルーであっても **無効化中はキャッシュを更新しない**限り発生する。
> §7.2 で「無効化中は書き込みでもキャッシュを触らない」と規定するため、
> `CEN` 立上り時の暗黙フラッシュは**必須**である。

---

## 6. ★ヒット経路のサイクル設計（M-2 対応）★

### 6.1 なぜ本節が要るのか

v0.1 は「ヒット時 `mem_ready` は 1 cpu cycle で返す」と書いたが、根拠が無かった。
★**Gowin の BSRAM は同期読み出し**★であり、アドレスをクロック端で取り込んで
出力は次サイクルに現れる。したがって `tag_ram` を BSRAM に置くと
**タグ比較が 1 サイクル遅れ、ヒットは最短でも 2 cpu cyc になる**。

★これは損益分岐ヒット率を大きく動かす。★

| ヒット遅延 | 損益分岐（現行ミス 4 cyc・キャッシュミス 10 cyc と仮定） | 条件 |
|---|---|---|
| **1 cyc** | `1h + 10(1−h) < 4` → ★`h > 66.7%`★ | `tag_ram` が組合せ読出 ★かつ★ `mem_addr` が先行 |
| 2 cyc | `2h + 10(1−h) < 4` → ★`h > 75.0%`★ | `tag_ram` が BSRAM ★または★ `mem_addr` が先行しない |

★**8 KB ダイレクトマップで 75% は容易でない。1 cyc ヒットを設計目標とする。**★

**本節で決めるべきことは 2 つある。**

| # | 論点 | 決定 |
|---|---|---|
| ① | `tag_ram` の実体（BSRAM か組合せか） | → §6.2（★FF アレイ＝組合せ読出★） |
| ② | ★`mem_addr` が `mem_rd` より先行するか★ | → §6.3（★**実源解析：フェッチのみ先行**★） |

> ⚠️ ★v0.2 は①のみを決め、②を「先行しているはず」と推論して 1 cyc を主張した。
> これが M-8 の指摘を受けた原因である。★ ①だけでは 1 cyc は成立しない。
> **両方が揃って初めて 1 cyc になる。**

### 6.2 ★`tag_ram` の実体を一意に決定する★

| 構造 | v0.1 | ★v0.2 決定★ | 理由 |
|---|---|---|---|
| `tag_ram` | 「BSRAM **または** 分散 RAM」（両論併記） | ★**FF アレイ（256 × 7 bit = 1,792 FF）**★ | 組合せ読出により `valid_r` と同位相で比較でき、1 cyc ヒットが成立する |
| `valid_r` | FF アレイ | FF アレイ（変更なし） | 一括クリア（§4.1） |
| `data_ram` | BSRAM | BSRAM（DPB・変更なし） | 8 KB は FF では不可能 |

**資源影響**：`tag_ram` 1,792 FF ＋ `valid_r` 256 FF ＝ 約 2,048 FF。
GW1NR-9 は 6,480 FF 相当であり、約 32% を消費する。

> ⚠️ ★これは軽い消費ではない。★ 容量を 16 KB に上げると `tag_ram` は
> 512 × 6 = 3,072 FF となり、`valid_r` と合わせて約 3,584 FF（55%）に達する。
> ★**D-B1 で 8 KB を選ぶ根拠が、BSRAM だけでなく FF 側にも生じた。**★

### 6.3 ★`mem_addr` 先行の有無 — CPU ステートマシン実源解析（M-8 対応）★

> ★**本節の位置づけ（N-5・v0.4 追記）**★
> 本節の解析結果は ★**性能予測（§6.4 の損益分岐）の根拠**★ であって、
> ★**機能の前提ではない**★。
> RTL は §7.1 の `data_q_valid`（投機読出アドレスと現要求の一致比較）により
> ★**解析結果を定数として持たずに、自力で正しく振る舞う**★。
> したがって本節の解析が万一誤っていても、★機能は壊れず、遅くなるだけ★である。
> CPU コアが将来変更されても（ISA3.0／YSD8810 等）キャッシュ側は自動追従する。

v0.2 は「`mem_addr` は cycle N-1 に確定している」と**推論**したが、これは
実源を見ない推論であった（M-3 と同型の誤り）。**実源で確定させる。**

#### 6.3.1 バス出力の駆動（`ysd8800_cpu_v0_1_FIXED.sv` L853-895）

```systemverilog
always_comb begin
    mem_addr  = rf_pc;      // ★default★
    mem_rd    = 1'b0;
    case (state)
        S_FETCH:    begin mem_addr = rf_pc;    mem_rd = 1'b1; end
        S_MEMR_LO:  begin mem_addr = addr_r;         mem_rd = 1'b1; end
        S_MEMR_HI:  begin mem_addr = addr_r + 16'd1; mem_rd = 1'b1; end
        S_MEMW_LO:  begin mem_addr = addr_r;  ... mem_wr = 1'b1; end
        S_POP_LO:   begin mem_addr = rf_sp;    mem_rd = 1'b1; end
        S_PUSH_LO:  begin mem_addr = sp_m2;   ... mem_wr = 1'b1; end
        default: ;   // ★default の mem_addr = rf_pc が生きる★
    endcase
end
```

★**`mem_addr` の default は `rf_pc` であり、メモリアクセスを行わない状態でも
`rf_pc` が出続けている。**★ これが先行の有無を分ける。

#### 6.3.2 ★命令フェッチ：1 サイクル先行する（成立）★

`next_state = S_FETCH` となる箇所は**実源で 2 箇所のみ**（L614 / L757）：

| # | 直前状態 | 経路 | バス出力 case | `mem_addr` |
|---|---|---|---|---|
| 1 | ★`S_IRQCHK`★（L614） | 通常の命令実行完了後 | ★**case に無い＝default**★ | ★**`rf_pc`**★ |
| 2 | `S_MEMR_HI`（L757） | 割込ベクタ読み完了 | `addr_r + 1` | 先行しない |

`next_state = S_IRQCHK` は**実源に 12 箇所**あり、★**全ての命令が実行完了後に
`S_IRQCHK` を経由して `S_FETCH` に入る**★（経路 2 は割込受理時のみ）。

`S_IRQCHK` は `mem_ready` を待たず**必ず 1 サイクルで抜ける**（L609-615）。
また `S_IRQCHK` では `rf_we_pc` が立たない（L940 以降の case に `S_IRQCHK` は無い）。
PC の更新は `S_FETCH`（L951）／`S_OPFETCH`（L958）等で完了済みである。

★**結論：`S_IRQCHK` のサイクル中、`mem_addr` には既に次のフェッチアドレス
（`rf_pc`）が出ている。したがって命令フェッチについては `mem_addr` が
`mem_rd` より 1 サイクル先行する。**★

#### 6.3.3 ★データアクセス：先行しない（2 cyc 確定）★

`S_MEMR_LO` / `S_MEMR_HI` / `S_MEMW_*` / `S_MEMR8` / `S_MEMW8` /
`S_PUSH_*` / `S_POP_*` は `addr_r`・`rf_sp`・`sp_m2` 等を出す。
これらの**直前状態では `mem_addr` は default の `rf_pc`** を出しており、
★**当該データアドレスは先行していない**★。

★**結論：データアクセスのヒットは 2 cpu cyc となる。**★

#### 6.3.4 ★確定した非対称性★

| アクセス種別 | `mem_addr` 先行 | ヒット遅延 | 根拠 |
|---|---|---|---|
| ★命令フェッチ★ | ★**あり**（`S_IRQCHK` 経由）★ | ★**1 cpu cyc**★ | §6.3.2 |
| データアクセス | なし | ★**2 cpu cyc**★ | §6.3.3 |

**これは偶然ではなく、`mem_addr` の default を `rf_pc` にした CPU 設計の帰結である。**
YUI OS の実行はフェッチ主体であるため、★実効性能は 1 cyc 側に寄る★。

> **MC6809 が命令フェッチ中に次アドレスを出し続ける（いわゆる VMA 的な振る舞い）のと
> 同じ性質が、`mem_addr` の default 代入という形で現れている。**
> 設計時に意図されたものか副産物かは判らないが、★キャッシュにとっては好都合である★。

### 6.4 ★損益分岐の再計算（M-8 波及・N-4 対応）★

フェッチ比率を *f*、フェッチヒット率を *h_f*、データヒット率を *h_d* とする。
現行（キャッシュ無し）のアクセスコストを 4 cyc、ミス時のコストを *P* とする。

```
  平均コスト = f {1·h_f + P(1−h_f)} + (1−f) {2·h_d + P(1−h_d)}  <  4
```

**代表値での試算**（*f* = 0.7・*h_f* = *h_d* = *h*・*P* = 10 と仮定）

★**ミスペナルティ *P* の内訳（C-6 と整合）**★

| 要素 | psram cyc | cpu cyc 換算 |
|---|---|---|
| バースト所要（`LAT + 31 = 12 + 31`） | 43 | 約 5.4 |
| CDC 往復（A-2q 適用後） | — | 約 3 |
| 合計 | — | ★**約 8〜9**★ |

試算では保守側に丸めて *P* = 10 とする。★C-6 の指摘どおり、この値は
§7.5 のフィル所要と同一の物理現象であり、両者は整合していなければならない。★

| 構成 | ヒット遅延 | 損益分岐 |
|---|---|---|
| v0.2 の想定（全て 1 cyc） | 1 | `h > 66.7%` |
| ★**本版の確定値（非対称）**★ | フェッチ 1 / データ 2 | ★**`h > 68.9%`**★ |
| レビュー M-8 の保守側（全て 2 cyc） | 2 | `h > 75.0%` |

★**フェッチが 7 割を占めるため、実効は 1 cyc 側に近い 68.9% となる。**★
75% との差は大きく、**D-B2（32 B）の維持根拠は保たれる**（→ §11.1）。

> ⚠️ *f*・*P* は仮定値である。★**確定は §8.5 の emu23 突合で行う**★。
> `emu23 --trace-addr` はフェッチとデータを区別できるため、実測の *f* を得られる。

### 6.5 ヒット経路のサイクル図

`mem_rd` 立上りサイクルを N とする。

```
        │  cycle N-1  │   cycle N    │  cycle N+1  │
────────┼─────────────┼──────────────┼─────────────┤
mem_addr│    ────     │   有効       │             │
phys_addr│   ────     │ 有効(MMU純組合せ・同一サイクル)
index    │   ────     │ 有効(組合せ) │             │
tag_ram  │   ────     │ 出力有効(FF・組合せ読出)   │
valid_r  │   ────     │ 出力有効(FF) │             │
hit      │   ────     │ ★確定(組合せ比較)★        │
data_ram │ アドレス供給│ ★出力有効★  │             │
mem_ready│   ────     │ ★1★         │             │
mem_rdata│   ────     │ ★有効★      │             │
```

★**成立条件**★：`data_ram`（BSRAM・同期読出）の出力を cycle N に間に合わせるには、
**アドレスを cycle N-1 に与えておく必要がある**。

**解決策：`data_ram` は投機読出とする。**

```
  data_ram の読出アドレス = { index, offset }   ← mem_addr から純組合せで生成
  ★hit 判定を待たずに常時読み出す★
  hit が成立しなければ結果を捨てるだけ（副作用なし）
```

~~`mem_addr` は CPU が cycle N-1 の終わりには確定させているため、`cpu_clk` の
立上りで `data_ram` にアドレスが取り込まれ、cycle N に出力が揃う。
★タグ比較（組合せ）と同一サイクルで揃うため、1 cyc ヒットが成立する。★~~

★**v0.3 是正（M-8）**★：上記は実源を見ない推論であった。§6.3 の解析により、
**先行の有無はアクセス種別によって異なる**ことが確定した。

| アクセス種別 | 直前サイクルの `mem_addr` | `data_ram` 出力 | ヒット遅延 |
|---|---|---|---|
| ★命令フェッチ★ | ★`S_IRQCHK` で既に `rf_pc`（＝当該フェッチアドレス）を出力★ | cycle N に揃う | ★**1 cyc**★ |
| データアクセス | default の `rf_pc`（＝当該データアドレスではない） | cycle N+1 | ★**2 cyc**★ |

★**投機読出は「フェッチについてのみ」1 cyc を成立させる。**★
データアクセスでは投機読出が空振りし、cycle N でアドレスを取り込んで
cycle N+1 に出力が揃うため 2 cyc となる。

> **投機読出そのものは無駄ではない。** フェッチが全アクセスの約 7 割を占めるため、
> 空振りするのは残り 3 割である。空振りしても副作用は無い（§6.5 末尾）。

> **MC6809 で言えば、命令フェッチ中に次アドレスをバスに出し続ける振る舞いに近い。**
> 読出に副作用が無い（MMIO はここに来ない・§1.2）からこそ許される最適化であり、
> ★MMIO をキャッシュ段の外に出した §1.2 の判断が、ここで効いている。★

### 6.6 ★タイミング上のリスク（実装時に要確認）★

| # | リスク | 確認方法 |
|---|---|---|
| 1 | `mem_addr` → `mmu`（純組合せ）→ `index` → `tag_ram`（組合せ読出）→ 比較 → `mem_ready` が **1 サイクル（250ns @4MHz）に収まるか** | 工程③（FPGA 実装）の合成時にタイミングレポートで確認 |
| 2 | 250ns は 4MHz としては極めて緩い制約であり、通常は問題にならない | — |
| ★3★ | ★**C-5：`state → mem_addr → mmu → tag_ram → hit → mem_ready` という新しい組合せ経路が生まれる**★。CPU L197-198 には `mem_addr → mem_ready → state → stack_ctx` の閉路で Icarus がハングした履歴がある | ★**段2 の最初のシミュレーションで「Icarus がハングしないこと」を明示的な確認項目とする**★。ハングは他の不具合と症状が全く異なるため、想定していれば切り分けが一瞬で済む |

**C-5 の解析（レビュー見解に同意）**：`mem_ready → next_state → (posedge) → state
→ mem_addr` の経路には ★`state` レジスタが挟まる★ため組合せループは閉じない。
また L853-895 の `always_comb` は `state` とレジスタファイルのみに依存し、
`mem_ready` には依存しない。**理論上は安全である。**
ただし①.5 の A-2q でも同種の議論に工数を要したため、確認項目として残す。

★4MHz という低速動作が、この組合せ経路を許容する。★
高クロック設計であれば `tag_ram` を BSRAM に置いて 2 cyc ヒットを受け入れる
ことになるが、本設計ではその必要がない。

---

## 7. 動作仕様

### 7.1 ★`CEN=0` 時のバイパス経路（N-2 対応・G-0 の成立条件）★

★**`CEN=0` のとき、信号は純組合せでバイパスされ、レジスタを一切経由しない。**★

```systemverilog
// 概念（実装時に厳密化）
assign fill_req   = CEN ? (miss & mem_rd) : 1'b0;   // CEN=0 ならフィル要求を出さない
assign to_bridge  = CEN ? cache_req : cpu_req;      // ★純組合せ selector★

// ★v0.4 是正（M-11）★ 投機読出アドレスを 1 サイクル保持し、
//   data_ram_q が「今の要求」に対応しているときだけ ready を返す
always_ff @(posedge cpu_clk) spec_addr_r <= cur_cache_addr;
assign data_q_valid = (spec_addr_r == cur_cache_addr);

assign mem_ready  = (~CEN)                ? bridge_ready
                  : (hit & data_q_valid)  ? 1'b1        // ★ヒットかつデータ有効★
                  : fill_done             ? 1'b1
                  :                         1'b0;

// ★v0.3 是正（M-9）★ ミス時の供給元は bridge_rdata ではない
assign mem_rdata  = (~CEN)          ? bridge_rdata      // 素通し
                  : hit             ? data_ram_q        // ヒット
                  :                   fill_hit_byte_r;  // ★ミス完了：キャプチャ値★
```

### ★M-11：`hit` だけで `mem_ready` を返してはならない★

~~`assign mem_ready = (CEN & hit) ? 1'b1 : bridge_ready;`~~

**v0.3 の上式は §6.3 で確定した非対称性を反映しておらず、誤動作を生む。**

| 項目 | 内容 |
|---|---|
| 文書内矛盾 | §6.3.4・§6.5 は★**データアクセスのヒットは 2 cpu cyc**★と確定させたが、v0.3 の式は `hit` が真になった**そのサイクル**で無条件に `mem_ready=1` を返していた |
| 不具合 | `hit` は `tag_ram`（FF アレイ・組合せ読出）と `valid_r` から求まるため、★**データアクセスでも cycle N に真になる**★。しかし `data_ram_q`（BSRAM 同期読出）は cycle N+1 まで有効にならない |
| 帰結 | cycle N に ready を返すと、CPU は★**前サイクルの投機読出結果（＝`S_IRQCHK` 由来の `rf_pc` ベースで読んだ無関係なバイト）**★をラッチする |
| 危険度 | ★M-9 と同型の「静かな誤データ」★。返る値は直前の PC 近傍のバイトであるため、★命令列とデータが偶然一致すると正しく見え、G-1（UART 出力一致）でも見逃し得る★ |

**★`data_q_valid` による解決（1 つの機構で非対称性が自動的に出る）★**

| アクセス種別 | 直前の投機読出アドレス | `data_q_valid` | ready |
|---|---|---|---|
| ★命令フェッチ★ | `S_IRQCHK` で既に同一アドレスを読済み | ★一致★ | ★cycle N ＝ **1 cyc**★ |
| データアクセス | 直前は `rf_pc` を読んでいた | 不一致 → 次サイクルで一致 | ★cycle N+1 ＝ **2 cyc**★ |

★**この式だけで、§6.3 が導いた非対称性が自動的に再現される。**★
★**CPU のステートマシン解析結果を RTL に定数として埋め込む必要がない。**★

> ★**N-5：§6.3 は「性能予測の根拠」であって「機能の前提」ではない。**★
> `data_q_valid` 方式の利点は次の 3 点である。
> 1. ★CPU コアが将来変更されても（ISA3.0／YSD8810 で `mem_addr` の駆動が変わっても）、キャッシュ側は自動追従する★
> 2. ★**§6.3 の解析が万一間違っていても機能は壊れない（遅くなるだけ）**★
> 3. `tb_cache_core_poc` の検証主眼を「1 cyc であること」ではなく★**「返るバイトが正しいこと」**★にできる
>
> ★設計解析の結論を RTL の前提条件にするのではなく、RTL が自力で正しく振る舞う
> 構造にしておく。★ これは ①.5 の A-2q（グリッチ源を構造的に消した）や
> §1.5（競合しても結果が使われない構造）と同じ思想である。

### ★M-9：ミス完了時に `bridge_rdata` を返してはならない★

~~`assign mem_rdata = (CEN & hit) ? data_ram_q : bridge_rdata;`~~

**v0.2 の上式は機能的な誤動作を生む。**

| 項目 | 内容 |
|---|---|
| 実源 | `ysd8800_cdc_bridge_v0_4.sv` L149：`assign cpu_mem_rdata = psram_rdata;`（★組合せ★） |
| 不具合 | ミス時、キャッシュは `burst_len = 32` のバーストを発行する。②-A 設計書 §3.3 より `PS_BURST` は毎 beat `rdata <= mem[mem_idx]` を更新し `PS_WAIT_REQ_LOW` で保持する。★**バースト完了時点で `psram_rdata` が保持しているのは最終ビート＝ライン内 offset 31 のバイト**★ |
| 帰結 | ★**CPU は要求したアドレスのバイトではなく、常にライン末尾のバイトを受け取る**★ |
| 危険度 | ★**offset=31 を要求したときだけ偶然正しくなる＝1/32 の確率で正解する「静かな誤データ」**★。②-A の R-2／R-5 で最も恐れた失敗形態そのものである |

**★v0.3 決定：案 (a) 要求ビートのキャプチャ★**

```
  psram_clk ドメイン:
    beat_cnt が 要求 offset（req_offset_r）と一致した beat で
        fill_hit_byte_r <= psram_rdata;
    ※ req_offset_r は req 受理時に cpu_clk 側からラッチ済み（変化しない）

  cpu_clk ドメイン:
    ack 立上り（cdc_bridge L139 のパルス）で mem_ready を返し、
    mem_rdata に fill_hit_byte_r を出す
```

| 案 | 内容 | 採否 |
|---|---|---|
| ★**(a) 要求ビートのキャプチャ**★ | ★**1 バイトの FF 追加のみ。追加サイクルが発生しない**★ | ★**採用**★ |
| (b) フィル完了後に `data_ram` を読み直す | 実装は単純だがミスペナルティが 1〜2 cyc 増える | 不採用 |

> ★`fill_hit_byte_r` は `psram_clk` で書かれ `cpu_clk` で読まれるが、
> **新規の CDC にはならない**★。`ack` 到達時点で値は既に安定しており、
> `ack` 自体が既存の CDC 経路（L139）を通っているため、
> ★データが安定してから制御が渡る＝正しいハンドシェイク★になっている。
> （①.5 の `suppress_r` と同じ構図。）

> ★**この不具合は §7.1 でバイパス式を具体化したからこそ検出できた。**★
> v0.1 のように式を書かずに文章だけで済ませていれば、実装時まで潜伏していた。
> **設計を具体化することが検証になる**という実例として記録する（KY-B11）。

**なぜ必須か（N-2 の指摘）**：キャッシュ段を挿入した時点でパスにレジスタが
1 段入ると、★`CEN=0` でも 1 サイクル増えて G-0 が落ちる★。
②-A の `burst_len=1` 完全等価性（バースト設計書 §3.4）と同型の保証を
成立させるには、バイパスが**組合せでなければならない**。

> ★実装時の確認方法★：段2 完了時点で G-0 を実施し、M-1〜M-5 が
> ②-A' 実測値と**1 サイクルも違わない**ことを確認する（§9 段2）。
> 1 でもずれたら、それはバイパス経路にレジスタが入っている証拠である。

### 7.2 読み出し

```
  hit = valid_r[index] && (tag_ram[index] == tag) && CEN

  hit :
      data_ram から返す（投機読出・§6.5）。PSRAM アクセスを発行しない。
      ★v0.4 是正（M-11）★ ~~mem_ready は 1 cpu cycle で返す~~
        mem_ready は「hit かつ data_q_valid」のときに返す（§7.1）。
        結果として ★フェッチ 1 cpu cyc ／ データアクセス 2 cpu cyc★
        となる（§6.3.4）。この非対称性は RTL に定数として書かれるのではなく、
        data_q_valid の一致判定から自動的に発現する（N-5）。

  miss:
      0) req 受理時に req_offset_r <= mem_addr[4:0] をラッチ（★M-9★）
      1) ライン先頭アドレス（下位 5 ビット = 0）で
         psram_ctrl に burst_len = 32 のリード要求を発行
      2) beat_valid が psram_clk の 32 サイクル連続で 1 になるので、
         各 beat を data_ram[index*32 + n] に書く
         ★この書込は psram_clk ドメイン・DPB の書込ポート（§1.3）★
         ★同時に beat_cnt == req_offset_r の beat を
           fill_hit_byte_r にキャプチャする（§7.1・M-9）★
      3) 全 beat 完了（ack の立上り）後に、cpu_clk ドメインで
         tag_ram[index] <= tag ; valid_r[index] <= 1
         （ただし fill_abort_r が立っていれば更新しない・§7.6）
      4) ★要求されたバイトは fill_hit_byte_r から返す★
         ★bridge_rdata を返してはならない（M-9）★
```

> ★`is_mmio` 条件は不要である（C-1）。★
> キャッシュ段は `addr_decoder` の RAM 側分岐の内側に置かれるため、
> MMIO アクセスはそもそもキャッシュ段に到達しない（§1.2）。

> ★`ack` はレベルではなく**立上りエッジ**が `cdc_bridge_v0_4` L139 で
> 1 cpu cyc パルス化される。★
> バースト設計書 §2.3 M-2 の警告に従い、**各 beat で `ack` を立てる改造を
> してはならない**。

### 7.3 書き込み（ライトスルー / ノーライトアロケート）

| 状況 | キャッシュ | PSRAM |
|---|---|---|
| `CEN=0` | ★触らない★ | 書く |
| ヒット | 該当バイトを更新 | 書く |
| ミス | ★触らない（ノーライトアロケート）★ | 書く |
| MMIO | 触らない | 書く（従来経路） |

`we=1` のとき `burst_len` は必ず 1（バースト設計書 §2.3 M-3）。

### 7.4 ~~ライン境界を跨ぐ 16 ビットアクセス~~ ★本節は撤回（M-3）★

~~offset = 31 の 16 ビットアクセスは 2 ラインに跨る。推奨案：跨ぎアクセスは
キャッシュをバイパスし、従来経路で処理する。理由：2 ライン同時ヒット判定は
比較器 2 系統とインデックス 2 系統を要し、ダイレクトマップの利点である
単純さを失う。~~

~~| **D-B7** | 跨ぎアクセスの扱い。推奨：バイパス（キャッシュ非更新） |~~

★**v0.2 撤回（M-3）**★：CPU 抽象バスは 8 ビット（§4.2）であり、
メモリ段から見た 16 ビットアクセスは**存在しない**。
ライン跨ぎという事象そのものが発生しないため、**本節および D-B7 は不成立**。
対策は不要である。

### 7.5 ★ラインフィル中の CPU 要求（C-2 対応）★

フィルは `psram_clk` 側で進行するが、その間に CPU から要求が来る可能性がある。
また `data_ram` の書込ポートは `psram_clk` 側（フィル用）であり、
CPU 側の書込ヒットと競合し得る。

★**決定：ラインフィル進行中は、CPU 側の要求を `mem_ready=0` で待たせる。**★

| 項目 | 内容 |
|---|---|
| 実害 | ~~フィルは psram_clk 32 サイクル ＝ cpu_clk 約 4 サイクル~~ ★**v0.3 是正（C-6）**★：正しくは ★**`LAT + 31 = 43` psram cyc**★（初回ビートまでに `LATENCY_NORMAL=12` を要する。②-A 設計書 §3.3 の検算どおり）。cpu_clk 換算で ★**約 5.4 サイクル**★、CDC 往復を加えると ★**約 8〜9 サイクル**★。★待たせても軽微という結論は変わらない★ |
| 利点 | DPB のポート競合・調停ロジックが**丸ごと不要**になる |
| 副次効果 | フィル中に同一ラインへ書込ヒットが来る競合も、構造的に発生しない |

> ★ダイレクトマップ＋ブロッキングキャッシュ（ノンブロッキングにしない）と
> することで、②-B の実装複雑度を大幅に下げている。★
> ノンブロッキング化は ②-B を閉じたあとの改良項目とする。

### 7.6 ★`FLUSH` とラインフィルの相互作用（C-4 対応）★

`FLUSH`（または `CEN` 立上りの暗黙フラッシュ・D-B6）が**フィル途中**に
発行されると、`valid_r <= '0` した直後にフィルが完了して
`valid_r[index] <= 1` してしまい、★**フラッシュしたはずのラインが復活する**★。

★**決定：フラッシュ発行時、進行中のフィルは「中止」扱いとし、
完了しても `valid_r` / `tag_ram` を更新しない。**★

```
  flush 発行 → fill_abort_r <= 1
  ack 到達時 : if (fill_abort_r) 何もしない（valid を立てない）
               else             tag_ram[index] <= tag ; valid_r[index] <= 1
  fill_abort_r は ack 受領でクリア
```

- PSRAM 側のバースト転送は**中断せず最後まで走らせる**
  （途中で `req` を落とすとバースト設計書 §2.3 の req/ack 4 相契約に違反する）
- 読み捨てるだけなので副作用は無い（読出に副作用が無い＝§6.5 と同じ根拠）

#### ★C-8：中止時も CPU には正しいバイトを返す（v0.4 追記）★

★**「キャッシュへの登録を中止する」ことと「CPU への応答を中止する」ことは
別である。混同してはならない。**★

| 事項 | 中止時の扱い |
|---|---|
| `valid_r` / `tag_ram` の更新 | ★**行わない**★（ラインを登録しない） |
| `mem_rdata` ← `fill_hit_byte_r` | ★**行う（返す）**★ |
| `mem_ready` | ★**返す**★ |

`fill_hit_byte_r` は ★PSRAM から実際に読み出した正しいバイト★であり、
フラッシュされたのは「キャッシュに載せる」という判断だけである。
両者は独立している。

> ⚠️ ★**ここで応答まで止めると `mem_ready` が返らず CPU がハングする。**★
> `S_SUBOP` で `mem_rd` が立たずハングした事例（CPU L858-865・2026-07-11 の
> バグ修正）と同型の失敗であり、実装時に最も取り違えやすい箇所である。
> `tb_cache_flush_poc` で★フィル途中フラッシュ後に `mem_ready` が返ること★を
> 明示的に検証する（§8.4）。

> ★§8.4 の `tb_cache_flush_poc` に本ケースを検証項目として追加する。★

---

## 8. 検証設計

### 8.1 ★G-0：バイト完全等価ゲート（最上位・全てに優先）★

| 項目 | 内容 |
|---|---|
| 条件 | **CCR リセット値のまま（`CEN=0`）** |
| 判定 | `tb_cpu_v8b_prod_v0_3` で M-1〜M-5 が下表と**完全一致**すること |
| 不一致時 | ★即座に実装を差し戻す。性能測定に進んではならない★ |

#### ★基準値表（M-4 対応・KY70 運用：出典欄と一次/二次の別を明記）★

| ビン | M-1 | M-2 | M-3 | M-4 | M-5 | ★一次/二次★ | ★出典★ |
|---|---|---|---|---|---|---|---|
| **LAT=12** | 7 | 74,512 | 222,994 | 320,156 | **3,754,937** | ★**一次**★ | 本チャット（キャッシュRTL開発 その4）2026-08-30 実測。`tb_cpu_v8b_prod_v0_3` / `rtl_v03_lat12.f` / MAX_CYCLES=10,000,000。`=== V8-b PROD PASS ===` |
| **LAT=7** | 7 | 74,512 | 222,994 | 320,156 | **3,754,937** | ★**一次**★ | 同上（`rtl_v03_lat7.f`）。★LAT=12 と完全同値（同一量子化ビン）★ |
| **LAT=5** | 6 | 59,691 | 160,744 | 272,818 | **2,810,474** | ★**一次**★ | 同上（`rtl_v03_lat5.f`）。②-A' 段4/(a) で確定 |
| **LAT=4 / 6** | 6 | 59,691 | 160,744 | 272,818 | **2,810,474** | ★**一次**★ | 同上（`rtl_v03_lat4.f` / `rtl_v03_lat6.f`） |

**★一次確定の宣言★**

②-A 設計書 §5.1 は基準値を「全て二次情報。段0 で一次確定させる」と留保していた。
★**本書の時点で、LAT=4/5/6/7/12 の全 5 ビンについて一次確定が完了している。**★
根拠は、②-A' において `tb_cpu_v8b_prod_v0_3`（判定を速度非依存に是正済み）で
全ビンを実走行し、`PROD PASS` を得たことである。

> ⚠️ ★重要な注記★
> LAT=12 の M-5 = 3,754,937 は、①.5（CDC 削減 A-2q）**適用後**の値である。
> A-2q 適用前の 4,772,836 と混同してはならない（−21.3% 改善後の値）。
> また ②-A 設計書 §5.1 に記載された値は **v0.2 の TB による測定**であり、
> ★判定器が速度依存であった時期のものであるが、LAT=12 群については
> 判定が成立していたため値そのものは有効★である。

**G-0 の意義**：`CEN=0` ならキャッシュ段は「素通し」でなければならない。
1 サイクルでもずれたら、それは無効化されていないこと（＝バイパス経路に
レジスタが入っていること・§7.1）を意味する。
②-A の `burst_len=1` 完全等価性（バースト設計書 §3.4）と同型の負制御である。

### 8.2 G-1：機能等価（`CEN=1`）

| 項目 | 内容 |
|---|---|
| 条件 | `CEN=1` |
| 判定 | **UART 出力バイト列が `CEN=0` 時と一致**すること（順序含む） |
| 注意 | ★M-1〜M-5 の cycle 値は一致しない（速くなるのが目的）★ |

> ⚠️ ★②-A' の教訓★：`CEN=1` では `'0'` とプロンプトの出力順序が
> さらに入れ替わる可能性がある（TKT-U1 のレースが顕在化する）。
> **順序が変わっても機能 FAIL と判定してはならない。**
> 判定はバイト集合と、`0→1→2→3→M→D` の部分列順序で行う（TB v0.3 準拠）。

### 8.3 G-2：既存の絶対ゲート（不変）

- Dhrystone: 819 / 48,785 / P:20
- `yuios_road2.bin`: 56,416 B / md5 `599a7f9d1ebf103f81f58450ea1b6491`

### 8.4 単体 TB（新規）

| TB 名（案） | 検証内容 |
|---|---|
| `tb_cache_core_poc.sv` | ヒット／ミス判定、タグ比較、index 分解、★**ヒット遅延の実測（フェッチ 1 cyc／データ 2 cyc・§6.3 の検証）**★、★**M-9：ミス完了時に返るバイトが要求 offset と一致するか（offset 0〜31 の全 32 パターン）**★、★**M-11：データアクセスのヒットで、返るバイトが要求アドレスのものであること**★ |
| `tb_cache_cdc_poc.sv` | ★**新規（N-3）**★ `psram_clk` 側フィル中に `cpu_clk` 側から同一ラインを読むケース。DPB の同一アドレス同時アクセス（§1.5）と §7.4 の待たせ制御 |
| `tb_cache_flush_poc.sv` | `FLUSH` ビット、`CEN` 立上り時の暗黙無効化（D-B6）、★**フィル途中のフラッシュ（C-4・§7.6）**★、★**C-8：中止後も `mem_ready` が返り、正しいバイトが返ること（ハング検出）**★ |
| `tb_cache_wt_poc.sv` | ライトスルー、ノーライトアロケート ~~、跨ぎバイパス（D-B7）~~ ★跨ぎ検証は M-3 により不要（削除）★ |

★KY38 遵守：全て `_poc` 接尾辞。本番 `.sv` への `sed -i` 禁止（KY74）。★

> ★N-3 の指摘のとおり、`tb_cache_cdc_poc` は①.5 で `ack_sync` に費やした
> 議論と同種の問題を扱う。★ CDC は結合後に発覚すると切り分けが極めて困難に
> なるため、単体 TB で先に潰す価値が高い。

### 8.5 性能測定（G-0 通過後のみ）

| 指標 | 方法 |
|---|---|
| M-5 到達 cycle | LAT=12（本番想定値）で `CEN=0` と `CEN=1` を比較 |
| ~~判断基準~~ | ~~★量子化ビンを跨ぐか★（群2 = 3,754,937 から改善するか）~~ ★**M-6 により撤回**★ |

#### ★判断基準（M-6 対応・差替版）★

| # | 基準 | 内容 |
|---|---|---|
| **①** | **削減量の絶対値** | M-5 到達 cycle の減少量を★**連続量として**★評価する。基準値は §8.1 の LAT=12 一次確定値 **3,754,937** |
| **②** ★本命★ | **emu23 予測との整合** | `emu23 --cache-size 8192 --cache-line 32 --cache-stats` でヒット率 *h* を取得し、*h* から予測される削減量と RTL 実測の削減量が整合するか |
| ③ | 参考値 | 群1（2,810,474）は「PSRAM レイテンシ改善で到達し得た水準」として**併記のみ**。★合否基準にはしない★ |

**なぜ②が本命か**：ヒット率という**独立変数**で予測が立つため、
「キャッシュが効いたか」だけでなく★**測定系そのものが妥当か**★を同時に検証できる。
予測と実測が乖離した場合、それはキャッシュの効果不足ではなく、
**モデルか実装のどちらかが間違っている**ことを示す。

> ⚠️ ★v0.1 の「ビンを跨ぐか」を採ってはならない理由（M-6）★
> 量子化ビンは `LATENCY_NORMAL` を掃引したときの PSRAM 側の性質であり、
> キャッシュは `LAT` を変えずアクセス回数を減らす。削減は**連続的**である。
> この基準を採ると★**ヒット率 40% でも十分な改善であるものを
> 「ビンを跨いでいないので効果なし」と誤判定する**★。

**検証順序**（KY-K-add7）：emu23 ゴールデン測定 → RTL 測定 → 差分確認。

---

## 9. 実装段階（1 変更 1 検証）

| 段 | 内容 | ゲート | ★着手可否★ |
|---|---|---|---|
| 段1 | CCR を `mmio_stub` に追加（キャッシュ本体なし・レジスタのみ）。`$FF12`/`$FF13` | G-0（値は読み書きできるが動作に影響しない） | ★**着手可**★ |
| 段2 | キャッシュ段の骨格を追加。`CEN=0` 固定で純組合せバイパス（§7.1）。★**membus の配線変更（M-10：`burst_len` 駆動・`beat_valid`/`rdata` 直結）を含む。membus v0.6 → v0.7**★ | **★G-0★**（ここが最重要）＋★C-5：Icarus がハングしないこと★ | ★**着手可**★ |
| 段3 | 読み出しヒット／ミス＋ラインフィル実装。★`fill_hit_byte_r` キャプチャ（M-9）・`data_q_valid`（M-11）を含む★ | G-1（`CEN=1` で機能等価）＋`tb_cache_core_poc`（offset 全 32 パターン・データアクセス返却バイト） | ★**着手可**★（M-11・M-12 を v0.4 で反映済み） |
| 段4 | ライトスルー実装 | G-1 | 段3 後 |
| 段5 | `FLUSH` / `CEN` 立上り無効化 / `fill_abort_r`（§7.6） | 単体 TB | 段4 後 |
| 段6 | 性能測定・emu23 突合（実測 *f* の取得を含む） | — | 段5 後 |

★**段1・段2 は M-8／M-9 と独立であり、今すぐ着手できる**★（レビュー §5 の判断）。
段2 の G-0 は §7.1 のバイパス経路（純組合せ）のみを検証対象とし、
ヒット経路もミス経路も動かないためである。
★むしろ G-0 を先に通してから段3 に入るほうが、切り分けの土台が固まる。★

★段2 の G-0 を通さずに段3 へ進んではならない。★

---

## 10. リスクと KY

| ID | リスク | 対策 |
|---|---|---|
| KY-B1 | ★判定器の速度依存性を再び踏む★ | TB v0.3 以降を使用（B-1）。FAIL 時は判定器を先に疑う（B-3・原則137） |
| KY-B2 | `ack` をレベルと誤解し各 beat で立てる改造 | バースト設計書 §1.3 M-2 の警告を実装前に再読する |
| KY-B3 | 本番 `.sv` への `sed -i` による破壊 | KY74。`sed 入力 > 出力_poc.sv` のみ。生成後に本番側を grep で無傷確認 |
| KY-B4 | `CEN` 有効化時に古いタグが残り不整合 | D-B6（`CEN` 立上りで暗黙フラッシュ）を必須とする |
| KY-B5 | BSRAM 不足で ②.5 の GPIO 拡張が実装できなくなる | 8 KB 案（約 4 ブロック）を推奨し、22 ブロックを残す |
| ~~KY-B6~~ | ~~ライン跨ぎで片側だけ更新し不整合~~ | ★**非該当（M-3）**★。バスが 8 ビットのため跨ぎは発生しない |
| ★KY-B7★ | ★**ビート毎に CDC を行う設計にしてしまう**★ | クロック比 8:1 では追従不可能。CDC は「フィル要求」と「フィル完了」の 2 イベントのみ（§1.3）。`ack` はレベルでなく立上りエッジで使われる（`cdc_bridge_v0_4` L139） |
| ★KY-B8★ | ★**`CEN=0` のバイパスにレジスタを入れて G-0 を落とす**★ | 純組合せバイパスを設計として明記（§7.1・N-2）。段2 の G-0 で必ず検出できる |
| ★KY-B9★ | ★**フィル途中のフラッシュでラインが復活する**★ | `fill_abort_r` による中止扱い（§7.6・C-4）。`tb_cache_flush_poc` で検証 |
| ★KY-B10★ | ★**実源を見ずに前提を推論する**★ | M-3（ISA→バス幅）と ★**M-8（CPU のバス駆動タイミング）**★ の 2 例が発生。★**適用範囲を「ISA → 構造」だけでなく「動作タイミングに関する前提」まで広げる**★。構造・タイミングいずれも必ず実源で確認する（KY34） |
| ★KY-B11★ | ★**設計を文章で済ませ、式を書かないことで不具合が潜伏する**★ | M-9 は §7.1 でバイパス式を具体化したからこそ検出できた。★**データパスは必ず式（assign 文）まで書き下す。**★ 文章だけの設計は検証されていないのと同じ |
| ★KY-B12★ | ★**ミス完了時に `bridge_rdata` を返し、ライン末尾のバイトを CPU に渡す**★ | M-9。`fill_hit_byte_r` によるキャプチャ（§7.1）。★1/32 の確率で偶然正解する「静かな誤データ」であり、G-1 でも見逃す恐れがある★。`tb_cache_core_poc` で **offset を変えた全 32 パターン**を検証する |
| ★KY-B13★ | ★**Icarus が組合せ閉路でハングする**★ | C-5。CPU L197-198 に同種の履歴あり。★段2 の最初のシミュレーションで「ハングしないこと」を明示的な確認項目とする★（§6.6） |
| ★KY-B14★ | ★**上流の解析結果が下流の式に伝播しない（文書内矛盾）**★ | M-11。§6.3 で「データは 2 cyc」と結論したのに §7.1 の式は 1 cyc のままだった。★**解析節と仕様節の結論が一致しているかを改版のたびに突合する。**★ KY-B11（式まで書き下す）の拡張 |
| ★KY-B15★ | ★**「登録の中止」と「応答の中止」を混同し `mem_ready` を返さずハングさせる**★ | C-8。`S_SUBOP` で `mem_rd` が立たずハングした事例（CPU L858-865）と同型。★フラッシュ・エラー等の異常系でも、CPU への応答は必ず返す★（§7.6） |
| ★KY-B16★ | ★**SDPB を選んで書込ヒットが実装できなくなる**★ | M-12。`data_ram` は ★DPB（トゥルーデュアルポート）必須★。`cpu_clk` 側ポートは投機読出と書込ヒットの両用（§1.3） |

---

## 11. ★決定事項（D-B1〜D-B7）と再レビュー依頼★

### 11.1 決定事項（レビュー N-1 で全項目一致・確定）

| ID | 論点 | ★決定★ | 状態 |
|---|---|---|---|
| D-B1 | キャッシュ容量 | ★**8 KB**★ | ✅ 確定（N-1 一致）。★§6.2 により FF 側の根拠も追加された★ |
| D-B2 | ラインサイズ | ★**32 B**★ | ✅ 確定（M-8 解析により根拠が実源で裏づけられた・下記参照） |
| D-B3 | 方式 | ★**ダイレクトマップ**★ | ✅ 確定（N-1 一致・emu23 をゴールデンモデルに使える） |
| D-B4 | CCR アドレス | ★**`$FF12`（下位）／`$FF13`（上位）**★ | ✅ 確定（M-5 ③を反映） |
| D-B5 | カウンタ | ★**初版は非実装（案 c）**★ | ✅ 確定（M-7・§5.4） |
| D-B6 | `CEN` 立上り暗黙フラッシュ | ★**必須**★ | ✅ 確定。C-4（フィル中）と併せ §7.5 で仕様化 |
| ~~D-B7~~ | ~~ライン跨ぎ~~ | ★**論点不成立・撤回**★ | ✅ M-3（バスは 8 ビット） |

**★D-B2 について（N-1 の条件付き支持・N-4 への回答）★**

v0.2 は「ヒットは 1 cyc だから 32 B 維持」としたが、★その根拠（`mem_addr` の
1 サイクル先行）は実源に反していた（M-8）★。v0.3 では実源解析により
**フェッチ 1 cyc／データ 2 cyc の非対称**であることを確定させた（§6.3）。

| 前提 | 損益分岐 | 判断 |
|---|---|---|
| v0.2 の想定（全て 1 cyc・★誤★） | `h > 66.7%` | — |
| ★**v0.3 確定値（フェッチ 1／データ 2・*f*=0.7）**★ | ★**`h > 68.9%`**★ | ★**32 B 維持が妥当**★ |
| M-8 保守側（全て 2 cyc） | `h > 75.0%` | 16 B の再評価が必要になる水準 |

★**フェッチが約 7 割を占めるため、実効は 68.9% となり 75% には達しない。
したがって 32 B を維持する。**★

> N-4 は「D-B2 を暫定確定に変更されたい」とのご指摘であったが、
> ★**M-8 の解析結果が「フェッチは 1 cyc で成立する」側に出たため、確定を維持する。**★
> ただし *f*（フェッチ比率）は仮定値であり、★§8.5 の emu23 突合で実測 *f* を得て
> 再確認する★（§6.4 の注記）。もし *f* が想定を大きく下回れば 16 B を再評価する。
>
> なお 16 B へ変更する場合も、★`BURST_MAX=32` のまま `burst_len=16` を発行すれば
> よく、②-A 側の RTL 変更は不要★である（M-10 の対応により自然に吸収される）。

### 11.2 再レビュー依頼

指摘書 §6 により、再レビューは ★**M-1・M-2 に限定した差分レビュー**★ とされている。

| # | 条件 | 本書での対応 |
|---|---|---|
| **1** | **M-1**：クロックドメイン・CDC 前後関係・DPB 構成 | ★**§1 を新設**★（1.1 実源構造／1.2 配置決定／1.3 ラインフィル成立条件／1.4 DMA 不在／1.5 DPB 競合回避） |
| **2** | **M-2**：ヒット経路のサイクル図・`tag_ram` の実体を一意決定 | ★**§6 を新設**★（6.1 損益分岐への影響／6.2 `tag_ram`＝FF アレイに決定／6.3 サイクル図＋投機読出／6.4 タイミングリスク） |
| 3〜8 | M-3〜M-7・C-1〜C-4・N-1〜N-3・E-1 | 本書冒頭の反映状況表を参照（全件反映済み） |

**D-B4 は実源（`ysd8800_mmio_stub_v0_8.sv` L219-243）を確認のうえ §5.3 に
マップを整理し、`$FF12` を推奨案として提示した。**確定時は
`kernel_v12_11.asm` および `yuios_memmap_design` への同時反映が必要（KY41）。

---

## 12. 変更履歴

| 版数 | 日付 | 変更内容 |
|---|---|---|
| v0.1 | 2026-08-30 | 初版。②-A' の帰結（判定器の速度非依存性・量子化ビン構造）を前提に反映。構成・CCR・G-0/G-1・実装段階・レビュー論点 D-B1〜D-B7 を提示 |
| **v0.2** | 2026-08-30 | `review_v11_cache_core_design_v0_1_v1_0.md`（判定＝差戻し）の全指摘を反映。★**§1「クロックドメインと配置」を新設（M-1）**★／★**§6「ヒット経路のサイクル設計」を新設（M-2）**★／M-3（バスは 8 ビット・D-B7 撤回・§7.3 撤回・KY-B6 非該当化）／M-4（G-0 基準値に出典欄・全 5 ビン一次確定を宣言）／M-5（CCR ビット 7:2 予約・W1T 是正・バイト割付）／M-6（性能判断基準を「削減量の絶対値＋emu23 整合」に差替・§0.3 後半を撤回）／M-7（カウンタは初版非実装）／C-1〜C-4・N-1〜N-3・E-1 反映。節番号を通し直し（旧§1→§2 等） |

| **v0.3** | 2026-08-30 | `review_v11_cache_core_design_v0_2_v1_0.md`（判定＝条件付き承認）を反映。★**M-8：CPU ステートマシンを実源で解析し、`mem_addr` 先行はフェッチのみ（`S_IRQCHK` 経由）と確定。ヒット遅延はフェッチ 1 cyc／データ 2 cyc の非対称、損益分岐 68.9%**★／★**M-9：ミス完了時の `mem_rdata` を `fill_hit_byte_r` キャプチャに是正（`bridge_rdata` はライン末尾バイトを返す誤動作）**★／M-10（配線 3 本追記・membus v0.7）／C-5（Icarus ハング確認項目化）／C-6（フィル所要 43 psram cyc）／C-7（DPB は発生するが無害）／N-4（D-B2 は確定維持）。KY-B10 の適用範囲を「動作タイミング」まで拡張、KY-B11〜B13 を追加 |

| **v0.4** | 2026-08-30 | `review_v11_cache_core_design_v0_3_v1_0.md`（判定＝条件付き承認・v0.4 は再レビュー不要）を反映。★**M-11：`mem_ready` を `data_q_valid`（投機読出アドレスと現要求の一致比較）で修飾し、フェッチ 1 cyc／データ 2 cyc の非対称性が RTL に定数を埋め込まずに自動発現する構造に是正**★／★**M-12：`data_ram` のポート用途表を新設し、`cpu_clk` 側を投機読出＋書込ヒットの両用と明記。プリミティブは DPB（トゥルーデュアルポート）必須**★／C-8（登録の中止と応答の中止は別・`mem_ready` は必ず返す）／N-5（§6.3 は性能予測の根拠であり機能の前提ではない）。KY-B14〜B16 を追加 |

以上
