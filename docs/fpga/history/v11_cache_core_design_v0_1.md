# 工程② キャッシュRTL開発 ②-B：キャッシュ本体 設計書

- **文書版数**: v0.1
- **作成日**: 2026-08-30
- **作成元チャット**: 「キャッシュ RTL 開発 (その4)」
- **対象工程**: ②-B（キャッシュ本体 RTL 実装）
- **状態**: ★レビュー未実施・未承認★（原則43により本書の承認前に実装着手禁止）
- **前提文書**:
  - `v10_psram_burst_design_v0_4.md`（②-A・承認済み）— バースト IF の契約
  - `emu23_cache_base_design_v1_0.md`（承認済み）— キャッシュモデルの意味論
  - `v9_psram_perf_baseline_v1_2.md` — 性能基準値
  - `fpga_source_version_ledger_v1_14.md` — RTL 版数台帳

---

## 0. 本書のスコープと、②-A' の帰結の反映

### 0.1 スコープ

`ysd8800_v5_membus` にダイレクトマップ・ユニファイドキャッシュ段を新設し、
CCR（キャッシュ制御レジスタ）で有効／無効を切り替えられるようにする。

### 0.2 非目標（本工程で扱わない）

| 項目 | 理由 |
|---|---|
| セットアソシアティブ化 | ダイレクトマップで G-0 を通してから検討する（1変更1検証） |
| ライトバック方式 | `emu23_cache_base_design_v1_0.md` §4.1 がライトスルーを前提に課金式を組んでいる。方式変更は課金式の再設計を伴う |
| ライトバッファ | 同 §4.1 M-2 で「無しを仮定」と明記。導入は別途モデル化が必要 |
| クリティカルワードファースト | 同 §3.2.2 で対象外 |
| I/D 分離 | 同 §4.2（H-5）で統合と決定済み。分離は ISA2.3 に無いキャッシュ無効化命令を要求する |
| 命令キャッシュ専用の最適化 | 同上 |

### 0.3 ★②-A' の帰結を本工程に反映する（本書で最も重要な前提）★

②-A' の結論は「速度依存デッドロックは実在せず、TB v0.2 の M-5 判定が
速度依存であったこと」であった。②-B はキャッシュヒット時に実効レイテンシが
LAT=4 相当より短くなるため、**同じ判定器の脆弱性を必ず踏む**。

したがって本工程では以下を**遵守事項**とする。

| # | 遵守事項 |
|---|---|
| B-1 | 検証には **`tb_cpu_v8b_prod_v0_3.sv` 以降**を使用する。v0.2 の使用を禁止する |
| B-2 | 新たに判定を追加する場合、**速度非依存であること**を設計時に明記する（原則137） |
| B-3 | FAIL が出た場合、**RTL を疑う前に判定器を疑う**。判定器の速度非依存性を先に確認する |

**②-A' で得た量子化ビン構造（実測・2026-08-30）**

| ビン | LATENCY_NORMAL | M-5 到達 cycle |
|---|---|---|
| 群1 | 4 / 5 / 6 | 2,810,474 |
| 群2 | 7 / 12 | 3,754,937 |

CPU:PSRAM = 8:1 のため PSRAM レイテンシは CPU 視点で 8 単位に量子化される。
**LAT を 1 減らしても、同一ビン内であれば性能は 1 サイクルも改善しない。**
②-B の性能評価は「LAT を下げる」ではなく「**アクセスそのものを消す**」ことで
ビンを跨ぐ、という観点で行う。

---

## 1. 構成の決定（★要レビュー論点 その1★）

### 1.1 資源見積もり（GW1NR-9・BSRAM 26 ブロック × 18Kbit）

物理アドレス 20 ビット（`MEM_AW=20`）を前提とする。

| 容量 | ライン | ライン数 | index | offset | tag幅 | データ | タグ | 概算BSRAM |
|---|---|---|---|---|---|---|---|---|
| 4 KB | 32 B | 128 | 7 | 5 | 8 b | 32 Kb | 1.1 Kb | 約 2.1 |
| 8 KB | 32 B | 256 | 8 | 5 | 7 b | 64 Kb | 2.0 Kb | 約 4.1 |
| 16 KB | 32 B | 512 | 9 | 5 | 6 b | 128 Kb | 3.5 Kb | 約 8.2 |
| 16 KB | 16 B | 1024 | 10 | 4 | 6 b | 128 Kb | 7.0 Kb | 約 8.4 |

（タグ幅 = 20 − index − offset、有効ビット 1 を各ラインに付加）

### 1.2 ★推奨案：8 KB / 32 バイトライン★

**推奨理由**

1. **BSRAM 消費が約 4 ブロック**で、26 ブロックのうち 22 ブロックを他用途
   （②.5 の GPIO 拡張バス・デバッグ DIO、将来の MMU 拡張）に残せる。
   16 KB は 8 ブロックを消費し、余裕が減る。
2. ライン 32 バイトは ②-A の `BURST_MAX=32` と**一致**する。
   バースト設計書 §2.2 は「②-B のライン長上限。②-B で最終確定」としており、
   32 を採るとバースト側の変更が不要になる（1変更1検証に資する）。
3. YUI OS 本体は 56,416 バイト。8 KB は約 14 %。ダイレクトマップの
   コンフリクトミスが問題になるかは実測で判断すべきであり、
   まず小さい構成で G-0 を通し、必要なら容量を上げるのが安全である。
4. emu23 側の既定は 16 KB だが、`--cache-size` で可変であるため、
   **RTL 実装値に合わせて emu23 側を 8 KB で走らせれば比較可能**である。

### 1.3 ★決定を要する事項★

| ID | 論点 | 推奨 | 代替 |
|---|---|---|---|
| **D-B1** | キャッシュ容量 | **8 KB** | 4 KB（資源優先）／16 KB（性能優先・emu23 既定と一致） |
| **D-B2** | ラインサイズ | **32 B** | 16 B（ミスペナルティ半減・バースト側変更不要だが空間局所性を活かせない） |
| **D-B3** | 方式 | **ダイレクトマップ**（emu23 §4.1 と一致） | 2-way（BSRAM は足りるが比較器 2 系統・置換ロジックが増える） |

> ★レビュー時にこの 3 点をご判断いただきたい。★
> 以降の記述は D-B1=8KB / D-B2=32B / D-B3=ダイレクトマップを仮に採った場合のものである。

---

## 2. アドレス分解

```
物理アドレス 20 ビット（MMU 後段）

  19                 13 12          5 4        0
 +---------------------+-------------+----------+
 |     tag  (7 bit)    | index (8bit)| off(5bit)|
 +---------------------+-------------+----------+
```

- 配置は **MMU 物理アドレスの後段**（`emu23_cache_base_design_v1_0.md` §4.1）。
  物理アドレスでインデクスするため、**コンテキストスイッチ時のフラッシュが不要**。
  （OS-9 の MMU 運用と同じ思想。論理アドレスでインデクスすると
  プロセス切替のたびに全無効化が要り、YUI OS のように切替頻度が高い系では損になる。）

---

## 3. 記憶構造

| 配列 | 幅 | 深さ | 実体 |
|---|---|---|---|
| `tag_ram` | 7 | 256 | BSRAM または分散 RAM |
| `valid_r` | 1 | 256 | ★FF アレイ（BSRAM ではない）★ |
| `data_ram` | 8 | 8192 | BSRAM（バイトアドレッサブル） |

### 3.1 ★`valid` を FF に置く理由（重要）★

`valid` を BSRAM に置くと、**一括クリアが 1 サイクルでできない**。
CCR による無効化・全フラッシュは全ライン同時クリアを要求するため、
256 ビットの FF アレイとして持ち、`valid_r <= '0` で一括クリアする。

256 FF は GW1NR-9 の資源（8,640 LUT / 6,480 FF 相当）に対して十分小さい。

### 3.2 ★データ RAM をバイト幅にする理由★

ISA2.3 は `LDB`/`STB` を持ち、**奇数アドレスアクセスが発生する**
（既知の技術的知見「バイトアドレッサブル設計必然」）。
ワード幅 RAM にするとバイト書き込みでリードモディファイライトが必要になり、
ライトスルーのタイミングが複雑化する。バイト幅とし、
16 ビットアクセスは 2 バイト分を同時に扱う。

> ⚠️ **ライン境界を跨ぐ 16 ビットアクセス**（offset = 31 のワードアクセス）は
> 2 ラインに跨る。この扱いを §5.3 で規定する。

---

## 4. CCR（キャッシュ制御レジスタ）（★要レビュー論点 その2★）

### 4.1 ビット定義（案）

| ビット | 名称 | R/W | リセット値 | 意味 |
|---|---|---|---|---|
| 0 | `CEN` | R/W | **0** | 1 = キャッシュ有効。**リセット値 = disable**（決定済み） |
| 1 | `FLUSH` | W1S | 0 | 1 書込みで全ライン無効化。自動クリア |
| 2 | — | — | 0 | 予約 |
| 3 | — | — | 0 | 予約 |
| 8 | `HIT_EN` | R/W | 0 | 1 = ヒット／ミスカウンタ計数有効（性能測定用） |
| 15:9 | — | — | 0 | 予約 |

### 4.2 ★決定を要する事項★

| ID | 論点 | 内容 |
|---|---|---|
| **D-B4** | CCR の MMIO アドレス | ★実源確認済み（下表）。**推奨：`$FF12`**★ |
| **D-B5** | ヒット／ミスカウンタの要否 | 性能測定に有用だが、無くても emu23 側で代替可能。RTL 資源とのトレードオフ |
| **D-B6** | `CEN` を 0→1 に変えた瞬間の扱い | **推奨：`CEN` の立上りで暗黙に全無効化する**。古いタグが残ったまま有効化されると、キャッシュ無効期間中に書かれたメモリとの不整合が生じる |

### 4.3 ★MMIO マップ実源確認（`ysd8800_mmio_stub_v0_8.sv` L219-243）★

| 範囲 | 用途 | 出典 |
|---|---|---|
| `$FC80`–`$FC87` | YSD8001 UART | L229-230 |
| `$FC88`–`$FC8F` | **空き** | — |
| `$FC90`–`$FC9F` | YSD8002 Timer | L236-237 |
| `$FCA0`–`$FCB1` | YSD8003 Storage | L242-243 |
| `$FCB2`–`$FCB5` | YSD8004 IRQ | L224-225 |
| `$FCB6`–`$FEFF` | **空き** | — |
| `$FF00`–`$FF0F` | MMU PTR[0..15] | L219-220 |
| `$FF10` | MMU MCR | L221 |
| `$FF11`–`$FFFF` | **空き** | — |

**推奨 `$FF12`（および将来 `$FF14`/`$FF16` をカウンタ用に予約）の理由**

1. キャッシュは MMU 物理アドレスの後段に置かれる**メモリ系制御**であり、
   MMU MCR（`$FF10`）の直後に並べるとアドレスマップの意味的まとまりが保てる。
2. `$FC80` 台のデバイス群（UART/Timer/Storage/IRQ）は**周辺デバイス**であり、
   そこに混ぜるとデバイスとメモリ制御の区別が失われる。
3. `$FF11` ではなく `$FF12` とするのは、**16 ビットアクセスの偶数アラインを保つ**ため。
   ISA2.3 はバイトアドレッサブルだが、`LDW`/`STW` の奇数アドレスアクセスは
   アラインメント例外の対象であり、制御レジスタは偶数番地に置くのが安全である。

> ⚠️ 確定にあたっては `kernel_v12_11.asm` および `yuios_memmap_design` 側の
> MMIO 定義とも突合し、**両者に同時に反映**すること（KY41 4点整合）。

> ★D-B6 は安全性に直結する。★
> 「無効化中に書いた内容がキャッシュに反映されない」問題は、
> ライトスルーであっても **無効化中はキャッシュを更新しない**限り発生する。
> §5.2 で「無効化中は書き込みでもキャッシュを触らない」と規定するため、
> `CEN` 立上り時の暗黙フラッシュは**必須**である。

---

## 5. 動作仕様

### 5.1 読み出し

```
  hit = valid_r[index] && (tag_ram[index] == tag) && CEN && !is_mmio

  hit :
      data_ram から返す。PSRAM アクセスを発行しない。
      mem_ready は 1 cpu cycle で返す。

  miss:
      1) ライン先頭アドレス（下位 5 ビット = 0）で
         psram_ctrl に burst_len = 32 のリード要求を発行
      2) beat_valid が 32 サイクル連続で 1 になるので、
         各 beat を data_ram[index*32 + n] に書く
      3) 全 beat 完了（ack）後に
         tag_ram[index] <= tag ; valid_r[index] <= 1
      4) 要求されたバイトを返す
```

> ★`ack` はレベルではなく**立上りエッジ**が `cdc_bridge` で 1 cpu cyc パルス化される。★
> バースト設計書 §2.3 M-2 の警告に従い、**各 beat で `ack` を立てる改造をしてはならない**。

### 5.2 書き込み（ライトスルー / ノーライトアロケート）

| 状況 | キャッシュ | PSRAM |
|---|---|---|
| `CEN=0` | ★触らない★ | 書く |
| ヒット | 該当バイトを更新 | 書く |
| ミス | ★触らない（ノーライトアロケート）★ | 書く |
| MMIO | 触らない | 書く（従来経路） |

`we=1` のとき `burst_len` は必ず 1（バースト設計書 §2.3 M-3）。

### 5.3 ★ライン境界を跨ぐ 16 ビットアクセス★

offset = 31 の 16 ビットアクセスは 2 ラインに跨る。**推奨案：跨ぎアクセスは
キャッシュをバイパスし、従来経路で処理する。**

**理由**：2 ライン同時ヒット判定は比較器 2 系統とインデックス 2 系統を要し、
ダイレクトマップの利点である単純さを失う。跨ぎは頻度が低く、
バイパスしても性能への影響は小さい。**ただしその場合もキャッシュを更新しない**
（更新すると片側だけ新しくなり不整合を生む）。

| ID | 論点 |
|---|---|
| **D-B7** | 跨ぎアクセスの扱い。**推奨：バイパス（キャッシュ非更新）**／代替：2 ライン処理 |

---

## 6. 検証設計

### 6.1 ★G-0：バイト完全等価ゲート（最上位・全てに優先）★

| 項目 | 内容 |
|---|---|
| 条件 | **CCR リセット値のまま（`CEN=0`）** |
| 判定 | `tb_cpu_v8b_prod_v0_3` で M-1〜M-5 が**②-A' 実測値と完全一致**すること |
| 期待値（LAT=12） | M-1=7 / M-2=74,512 / M-3=222,994 / M-4=320,156 / **M-5=3,754,937** |
| 期待値（LAT=5） | M-1=6 / M-2=59,691 / M-3=160,744 / M-4=272,818 / **M-5=2,810,474** |
| 不一致時 | ★即座に実装を差し戻す。性能測定に進んではならない★ |

**G-0 の意義**：`CEN=0` ならキャッシュ段は「素通し」でなければならない。
1 サイクルでもずれたら、それは無効化されていないことを意味する。
②-A の `burst_len=1` 完全等価性（バースト設計書 §3.4）と同型の負制御である。

### 6.2 G-1：機能等価（`CEN=1`）

| 項目 | 内容 |
|---|---|
| 条件 | `CEN=1` |
| 判定 | **UART 出力バイト列が `CEN=0` 時と一致**すること（順序含む） |
| 注意 | ★M-1〜M-5 の cycle 値は一致しない（速くなるのが目的）★ |

> ⚠️ ★②-A' の教訓★：`CEN=1` では `'0'` とプロンプトの出力順序が
> さらに入れ替わる可能性がある（TKT-U1 のレースが顕在化する）。
> **順序が変わっても機能 FAIL と判定してはならない。**
> 判定はバイト集合と、`0→1→2→3→M→D` の部分列順序で行う（TB v0.3 準拠）。

### 6.3 G-2：既存の絶対ゲート（不変）

- Dhrystone: 819 / 48,785 / P:20
- `yuios_road2.bin`: 56,416 B / md5 `599a7f9d1ebf103f81f58450ea1b6491`

### 6.4 単体 TB（新規）

| TB 名（案） | 検証内容 |
|---|---|
| `tb_cache_core_poc.sv` | ヒット／ミス判定、タグ比較、index 分解 |
| `tb_cache_flush_poc.sv` | `FLUSH` ビット、`CEN` 立上り時の暗黙無効化（D-B6） |
| `tb_cache_wt_poc.sv` | ライトスルー、ノーライトアロケート、跨ぎバイパス（D-B7） |

★KY38 遵守：全て `_poc` 接尾辞。本番 `.sv` への `sed -i` 禁止（KY74）。★

### 6.5 性能測定（G-0 通過後のみ）

| 指標 | 方法 |
|---|---|
| M-5 到達 cycle | LAT=12（本番想定値）で `CEN=0` と `CEN=1` を比較 |
| 判断基準 | ★量子化ビンを跨ぐか★（群2 = 3,754,937 から改善するか） |
| emu23 との突合 | `--cache-size 8192 --cache-line 32 --cache-stats` でヒット率を取得し、RTL 実測と照合 |

**検証順序**（KY-K-add7）：emu23 ゴールデン測定 → RTL 測定 → 差分確認。

---

## 7. 実装段階（1 変更 1 検証）

| 段 | 内容 | ゲート |
|---|---|---|
| 段1 | CCR を `mmio_stub` に追加（キャッシュ本体なし・レジスタのみ） | G-0（値は読み書きできるが動作に影響しない） |
| 段2 | キャッシュ段の骨格を追加。`CEN=0` 固定で素通し配線 | **★G-0★**（ここが最重要） |
| 段3 | 読み出しヒット／ミス＋ラインフィル実装 | G-1（`CEN=1` で機能等価） |
| 段4 | ライトスルー実装 | G-1 |
| 段5 | `FLUSH` / `CEN` 立上り無効化 | 単体 TB |
| 段6 | 性能測定・LAT 再キャリブレーション | — |

★段2 の G-0 を通さずに段3 へ進んではならない。★

---

## 8. リスクと KY

| ID | リスク | 対策 |
|---|---|---|
| KY-B1 | ★判定器の速度依存性を再び踏む★ | TB v0.3 以降を使用（B-1）。FAIL 時は判定器を先に疑う（B-3・原則137） |
| KY-B2 | `ack` をレベルと誤解し各 beat で立てる改造 | バースト設計書 §2.3 M-2 の警告を実装前に再読する |
| KY-B3 | 本番 `.sv` への `sed -i` による破壊 | KY74。`sed 入力 > 出力_poc.sv` のみ。生成後に本番側を grep で無傷確認 |
| KY-B4 | `CEN` 有効化時に古いタグが残り不整合 | D-B6（`CEN` 立上りで暗黙フラッシュ）を必須とする |
| KY-B5 | BSRAM 不足で ②.5 の GPIO 拡張が実装できなくなる | 8 KB 案（約 4 ブロック）を推奨し、22 ブロックを残す |
| KY-B6 | ライン跨ぎで片側だけ更新し不整合 | D-B7（バイパス・非更新） |

---

## 9. ★レビュー依頼事項（本書の承認に必要な判断）★

| ID | 論点 | 推奨案 |
|---|---|---|
| D-B1 | キャッシュ容量 | 8 KB |
| D-B2 | ラインサイズ | 32 B（`BURST_MAX=32` と一致） |
| D-B3 | 方式 | ダイレクトマップ |
| D-B4 | CCR の MMIO アドレス | **`$FF12`**（MMU MCR `$FF10` の直後・偶数アライン） |
| D-B5 | ヒット／ミスカウンタの要否 | 要（`$FF14`/`$FF16` を予約。資源次第で削減可） |
| D-B6 | `CEN` 立上り時の暗黙フラッシュ | 必須とする |
| D-B7 | ライン跨ぎ 16 ビットアクセス | バイパス（非更新） |

**D-B4 は実源（`ysd8800_mmio_stub_v0_8.sv` L219-243）を確認のうえ §4.3 に
マップを整理し、`$FF12` を推奨案として提示した。**確定時は
`kernel_v12_11.asm` および `yuios_memmap_design` への同時反映が必要（KY41）。

---

## 10. 変更履歴

| 版数 | 日付 | 変更内容 |
|---|---|---|
| v0.1 | 2026-08-30 | 初版。②-A' の帰結（判定器の速度非依存性・量子化ビン構造）を前提に反映。構成・CCR・G-0/G-1・実装段階・レビュー論点 D-B1〜D-B7 を提示 |

以上
