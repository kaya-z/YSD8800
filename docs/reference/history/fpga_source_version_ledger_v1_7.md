# YSD8800 FPGA実装 ソースコード版数管理台帳

Version 1.7 / 2026-07-21

| 項目 | 内容 |
| --- | --- |
| 文書番号 | FPGA-SOURCE-LEDGER-001 |
| 目的 | FPGA実装（SystemVerilog RTL・TB）、V2/V3検証生成器（Python）、黄金値ファイル、関連設計メモの現行版数を一元管理する。kaizen原則「ツール類はバージョンを常にリスト管理」を、ツールチェーンだけでなくFPGAソース側にも適用したもの。 |
| 対象 | Step 8 FPGA V1（CPUコア実装）・V2（CPUコア単体検証）・V3（メモリ・バス・MMIOデコード／PSRAM統合）・**V3.5（MMU統合）**・**V3.7（YSD8004割込コントローラ）** 関連の全ソース・検証資産 |
| 作成日 | 2026-07-11 |
| 最終改版日 | 2026-07-21 |
| ステータス | v1.0初版（要レビュー）→ v1.1（V3追加分）→ v1.2（V3.5追加分）→ v1.3（V3.7 YSD8004追加分）→ v1.4（V4 YSD8001 UART追加分）→ v1.5（V5 YSD8002 タイマー昇格分）→ v1.6（EN是正・案B完了分）→ **v1.7（V6-A ストレージ完了・V8 cat/ls統合ALL PASS分・要レビュー）** |
| 既存関連文書 | tool_version_ledger_v1_10.md（ツールチェーン側。本台帳とは対象を分離） |

## 改版履歴

| 版数 | 日付 | 内容 | 担当 |
| --- | --- | --- | --- |
| v1.0 | 2026-07-11 | 初版作成。KY34（実ファイルが真実）に基づき、記憶・要約に頼らず `ls`/`grep`/ヘッダ実査で全FPGA関連ファイルを確認。V1 RTL4本・V1 TB群・V2生成器/TB/黄金値一式の版数を記録。あわせて、実装・検証はALL PASSしている旨が過去チャット記録にあるにも関わらず、ソースファイル自体がプロジェクトナレッジに未登録という欠落を複数件検出・明記した。 | Claude |
| **v1.1** | **2026-07-11** | **V3（メモリ・バス・MMIOデコード／PSRAM統合）実装分を追加。** ①§2対象範囲にV3を追加。②§3のcpuファイル版数を**v0.5.6→v0.5.7**に更新（S_SUBOP mem_rdアサート漏れバグ修正。V3統合検証で発覚・詳細は§9参照）。③新設§9「V3 RTL・検証TB・生成器 現行版一覧」を追加：新規RTL5本（アドレスデコーダ／MMIOスタブ／CDCブリッジ／PSRAMビヘイビアモデル／V3メモリサブシステム統合ラッパー）、単体TB4本、実CPUコア統合TB3本、ベクタ生成器2本（V2-a生成器を流用した分含む）を記録。④§6関連設計メモ表にv3_design_memo_v0_3.md・fpga_v1_cpucore_design_v1_2.md・fpga_impl_roadmap_v1_1.docxを追加。⑤§7所見にV3関連の新規所見を追加。過去版系譜（v1.0の内容）は欠落させず全文保持。 | Claude |
| **v1.2** | **2026-07-12** | **V3.5（MMU統合）実装分を追加。** ①§2対象範囲にV3.5を追加。②新設§10「V3.5 RTL・検証TB・生成器 現行版一覧」を追加：新規RTL2本（MMU本体／V3.5統合ラッパー）・改版RTL3本（MMIOスタブv0.2／CDCブリッジv0.2／PSRAMコントローラv0.2）、検証TB8本、生成器・スクリプト3本、黄金値一式を記録。③§10.4にV3.5で検出・修正した実装バグ4件を記録（物理アドレス上位ビット破棄2箇所／unpacked array port X固着／生成器Aレジスタ破壊／TB側カウンタ判定の偽FAIL）。④CPUコアはV3.5で**無改修**（v0.5.7のまま）である旨を明記。過去版系譜（v1.0・v1.1の内容）は欠落させず全文保持。 | Claude |
| **v1.3** | **2026-07-12** | **V3.7（YSD8004 割込コントローラ）実装分を追加。** ①§2対象範囲にV3.7を追加。②新設§11「V3.7 RTL・検証TB・生成器 現行版一覧」を追加：新規RTL2本（YSD8004本体 `ysd8800_ysd8004_v0_1.sv` ／ V3.7統合ラッパー `ysd8800_v37_membus_v0_1.sv`）、改版RTL1本（MMIOスタブ v0.2→**v0.3**：YSD8004をインスタンス化し `$FCB2`-`$FCB5` をルーティング）、検証TB7本（**S3単体21項目 / S5 CPU結合（本丸）** ／ S4回帰4本 ／ 診断1本）、生成器・スクリプト3本、黄金値一式を記録。③**CPUコアは V3.5に続き V3.7 でも無改修**（v0.5.7のまま）である旨を明記。④§11.7に**★BUG-1★**（`addr_i`を2bitにしたため **IRQ_STATとIRQ_MASKが入れ替わっていた**。`$FCB2[1:0]=2'b10` / `$FCB4[1:0]=2'b00` で `mmio_addr[2]` でしか区別できず、症状は割込の無限ループ。**S3単体TBは21/21 PASSしていたのに見逃した＝偽合格**。→ 3bit化して修正。kaizen原則67として登録）を記録。⑤§11.8所見に、**IRQ1レベル出力の設計判断（KY-D）がemu23の再評価機構をRTL側で自動成立させた**ことを記録。過去版系譜（v1.0・v1.1・v1.2の内容）は欠落させず全文保持。 | Claude |
| **v1.4** | **2026-07-13** | **V4（YSD8001 UART）実装分を追加。** ①§2対象範囲にV4を追加。②新設§12「V4 RTL・検証TB・生成器 現行版一覧」を追加：新規RTL2本（YSD8001本体 `ysd8800_ysd8001_v0_1.sv` ／ V4統合ラッパー `ysd8800_v4_membus_v0_1.sv`）、改版RTL1本（MMIOスタブ v0.3→**v0.4**：YSD8001をインスタンス化し `$FC80`-`$FC87` をルーティング）、検証TB2本（`tb_ysd8001_v0_1.sv` 単体 ／ **`tb_cpu_v4uart_v0_2.sv`（★真のゲート★ 23/23 ALL PASS）**）、生成器3本を記録。③**CPUコアは V3.5・V3.7 に続き V4 でも無改修**（v0.5.7のまま・3フェーズ連続）。④§12.6に**★S5(c)-ISSUE-1★**（`irq_pending` の**ラッチ**は IE を見ない。**受理**だけが IE を見る。その帰結としてハンドラは「余分に1回」起動しうる＝**割込ハンドラは冪等に書かねばならない**。emu23/RTL 一致の正しい挙動でありバグではない。MC6809/OS-9 と同型。kaizen原則69）を記録。⑤§12.7に検出・修正した問題3件を記録（**`tb_cpu_v4uart_v0_1.sv` で `irq_in` 未結線＝CPUに割込が入っていなかった** ／ Icarus `ref` ポート未サポート ／ c6 期待値を実測に合わせて書き換えかけた＝偽合格の回避）。⑥§12.8に**★KY49の実害2例目★**（HANDOVER に「✅完成」と書かれた成果物がナレッジ未登録だった。kaizen原則71）を記録。過去版系譜（v1.0〜v1.3の内容）は欠落させず全文保持。 | Claude |
| **v1.5** | **2026-07-18** | **V5（YSD8002 タイマー）poc昇格分を追加。** ①§2対象範囲にV5を追加。②新設§13「V5 RTL・検証TB・生成器 現行版一覧」を追加：昇格RTL3本（タイマー本体 `ysd8800_ysd8002_v0_2.sv` md5=`f3aa7170…` ／ MMIOスタブ v0.4→**v0.5** `ysd8800_mmio_stub_v0_5.sv` md5=`9c8cd6e9…`：YSD8002をインスタンス化し `irq0_ack` 経路3点を実装 ／ V5統合ラッパー `ysd8800_v5_membus_v0_1.sv` md5=`aec1839c…`）、昇格検証TB2本（`tb_cpu_v5timer.sv` full ／ `tb_cpu_v5timer_short.sv` 短縮：**ack側 T0〜T5 ALL PASS**）、テストベクタ2本（`v5t_ack_short.hex` / `v5t_noack_short.hex`）を記録。③**CPUコアは V3.5・V3.7・V4 に続き V5 でも無改修**（pending保護入りの v0.5.8 を使用・4フェーズ連続で論理不変）。④§13.5に**★設計負債（TCR EN=OR）★**（`fire_en = |tcr_r[1:0]` の OR 実装。IRQ_EN(bit1) が名前どおり機能せず TIMER_EN(bit0) のみで発火する。デバイス契約違反だが**V6以降の独立工程として是正**・ユーザ承認 2026-07-14/17。「タイマー回すが割込マスク」実装の近接予定なしを確認済のため負債据置）を記録。⑤§13.6に**★irq_timer_o レベル化（案B-1a）★**（1クロックパルスは実CPI≒18で取りこぼす→レベル＋TCR-ACKハンドシェイク＝MC6840 PTM作法。クロック非依存。emu23「1回で自己沈黙」とRTL「ACK無しでハング」で意味論が異なるが、両ソース(kernel_v12_8/startup_harness23_v16)はACK先行で契約充足済）を記録。⑥§13.7に**★poc昇格の教訓★**（是正版mmio_stub md5=`d2bef39c…` がスナップショット未反映で旧版 `3058ffbb…` が見えた。同一セッション内の実体(`/home/claude/w/`)を正とすれば台帳作成は登録前でも可能。KY34の再確認）を記録。過去版系譜（v1.0〜v1.4の内容）は欠落させず全文保持。 | Claude |
| **v1.7** | **2026-07-21** | **V6-A ストレージコントローラ（YSD8003）実装完了分・V8 YUI OS統合（cat/lsフル統合・選択肢A-1）ALL PASS分を追加。** ①§2対象範囲にV6-A・V8を追加。②新設§15「V6-A ストレージ RTL・検証TB 現行版一覧」を追加：新規RTL2本（YSD8003本体 `ysd8800_ysd8003_v0_4.sv`（内部改版・モジュール名v0_1維持＝KY60運用）／ V5-membus改版 `ysd8800_v5_membus_v0_2.sv`（YSD8003結線対応））、改版RTL1本（MMIOスタブ v0_6→**v0_7**：YSD8003ルーティング追加 `$FCB6-$FCB9`）、単体TB1本（`tb_ysd8003_v0_3.sv`）、上位結合TB1本（`tb_cpu_v6sdread.sv`：sd_read契約検証）、SDモデル `sd_spi_model_v0_3_poc.sv`（V8で$readmemh化）、テストベクタ `v6t_sdread.asm/.hex`。③新設§16「V8 YUI OS統合（cat/lsフル統合）RTL・検証TB・生成物 現行版一覧」を追加：新規TB1本（`tb_cpu_v8catls_poc.sv`：UARTキャプチャ44B照合＋KY54負例枠）、新規デモC1本（`v8_catls_demo_poc.c`：read専用cat/ls・sd_read/putchar契約流用）、生成物3本（`v8t_catls.hex` 17408B ／ `sd_image.hex` mkfs正例8KB ／ `sd_image_neg.hex` 内容差替負例）、設計メモ `v8_catls_integ_design_memo_v0_2.md`。④**CPUコアは V5・EN是正に続き V6-A・V8 でも無改修**（v0.5.8 のまま・**7フェーズ連続で論理不変**）。⑤§16.5に**★V8統合結果（2026-07-21）★**を記録：**正例 PASS=4/4（S1_halt / S2_uart_len=44 / S3_uart_match mism=0 / T0_negative）／ 負例（KY54内容差替）で S3_uart_match が期待通りFAIL（mism=11）・「デモCがSD実内容を読む＝ハードコード無し」を直接証明。3,750,467サイクルで完走・PC=0x0025 でHALT。UART出力 `"ls:\nHELLO.TXT\ncat HELLO.TXT:\nHello, YUI OS!\n"` 完全一致**。⑥§16.6に**★kaizen候補★**（統合TBビルドは decoder/regfile/alu 先頭必須＝idec_pkg前方参照 ／ 期待文字列はPython実バイト列挙で確定 ／ 正例hexは負例ラン前に必ず退避＋md5二重確認）を記録。⑦**無改修死守**: sd_sample.c / mkfs_yuifs_v1_1.py / RTL（CPU・YSD8003 v0.4等）は一切触らず＝V8統合成功の核。過去版系譜（v1.0〜v1.6の内容）は欠落させず全文保持。 | Claude |
| **v1.6** | **2026-07-18** | **EN是正工程（案B・TCR発火EN OR→AND）分を追加。** ①§2対象範囲にEN是正を追加。②新設§14「EN是正 RTL・検証TB・生成器 現行版一覧」を追加：改版RTL2本（タイマー本体 `ysd8800_ysd8002_v0_2.sv`→**`v0_3`**：発火条件 `fire_en = |tcr_r[1:0]`〈OR〉 → `fire_en = timer_en_r & irq_en_r`〈AND〉。★ロジック変更ありゆえファイル名=モジュール名を v0_3 に統一〈案X〉★ ／ MMIOスタブ `mmio_stub_v0_5`→**ファイル名`v0_6`**：u_ysd8002 参照先を v0_2→v0_3 へ追従。★ロジック無変更・参照追従のみゆえモジュール名は v0_5 据え置き〈案X-1〉★）、新規検証TB1本（**`tb_cpu_v6mask.sv`**：EN是正 負例統合TB。TCR=$0001〈TIMER_EN=1/IRQ_EN=0〉で **N0〜N3 ALL PASS〈4/4〉**・本丸N1=CNT0 で AND是正を直接実証）、テストベクタ1本（`v6t_mask.asm`／`v6t_mask_short.hex`：内ループ#100 short版・RTLシムMAX_CYC=3M整合）、ビルドスクリプト1本（`build_v5en.sh`）を記録。③**CPUコアは V3.5・V3.7・V4・V5 に続き EN是正でも無改修**（v0.5.8 のまま・5フェーズ連続で論理不変。V1/V2 全ベクタ 20/64/75/82 ALL PASS でデグレゼロ実証）。④§14.5に**★案A（TIMER_EN=0でカウンタ歩進停止）は将来課題★**（V6以降・ユーザ承認2026-07-17/18。案BのみでIRQ_EN割込マスク契約は回復済。正式記録=ysd8002_timer_design v1.2 §11.1）を記録。⑤§14.6に**★案X/案X-1の非対称判断★**（ロジック変更のあるYSD8002はモジュール名昇格・参照追従のみのmmio_stubはモジュール名据え置き＝波及範囲最小化。membus/TB群は無改修）を記録。⑥§14.7に**★negTBループ長の教訓★**（負例asmの内ループ長はshort版hexのループ定数と揃える必要。フル版#1000だとRTLシムMAX_CYCでタイムアウト。#100へ短縮で解決＝AND化ロジックとは無関係）を記録。⑦検証: Dhrystone826/48405/P:20不変・MMU黄金v111決定性一致(md5=0b7c941e…)。過去版系譜（v1.0〜v1.5の内容）は欠落させず全文保持。 | Claude |

---

## 1. 目的・背景

ツールチェーン（Force/scc23/hasm23/lnk23/emu23等）は `tool_version_ledger` で版数を一元管理してきたが、FPGA実装のRTL・TB・V2検証生成器（Python）・黄金値ファイル側には同種の台帳が存在しておらず、版数管理が手薄になっていた（かやぬまさんご指摘、2026-07-11）。本台帳はその是正として新規に独立させたものである。

## 2. 対象範囲

1. V1 CPUコアRTL本体（decoder / regfile / alu / cpu 各1本）
2. V1 CPUコア検証TB群（Stage1「フェッチ」〜Stage5「スタック/制御/割込」）
3. V2 CPUコア単体検証 一式（生成器Python・TB SystemVerilog・黄金値ファイル、V2-a〜V2-e）
4. 上記に対応する設計メモ・レビュー文書
5. **【v1.1追加】V3 メモリ・バス・MMIOデコード（PSRAM統合）関連の全RTL・検証TB・ベクタ生成器・黄金値・設計メモ**
6. **【★v1.4追記（v1.2で追加すべきだった漏れの補完）★】V3.5 MMU統合 関連の全RTL・検証TB・ベクタ生成器・黄金値・設計メモ**
7. **【★v1.4追記（v1.3で追加すべきだった漏れの補完）★】V3.7 YSD8004 割込コントローラ 関連の全RTL・検証TB・設計メモ**
8. **【v1.4追加】V4 YSD8001 UART 関連の全RTL・検証TB・ベクタ生成器・設計メモ**
9. **【v1.5追加】V5 YSD8002 タイマー 関連の全RTL・検証TB・テストベクタ・設計メモ（poc→正式版昇格分）**
10. **【v1.7追加】V6-A ストレージコントローラ YSD8003 関連の全RTL・検証TB・テストベクタ・設計メモ**
11. **【v1.7追加】V8 YUI OS統合（cat/lsフル統合・選択肢A-1）関連のTB・デモC・SDモデル改修版・生成物（イメージhex）・設計メモ**

---

## 3. V1 CPUコアRTL 現行版一覧（実ファイルヘッダ確認済・2026-07-11時点）

| ファイル | 版 | 日付 | 役割 | 直近の変更 |
| --- | --- | --- | --- | --- |
| ysd8800_decoder_v0_1.sv | v0.1 | 2026-06-30 | 命令デコーダ（案A: 0x1Fサブop正規化） | 初版から変更履歴なし。2026-06-30時点のまま安定（実ファイル確認：ヘッダ以外にv0.x追記なし） |
| ysd8800_regfile_v0_1.sv | v0.1 | 2026-06-30 | レジスタファイル（A/B/X/SP/PC/FLAGS、rD/rS=4,5抑止） | 同上 |
| ysd8800_alu_v0_1.sv | v0.1 | 2026-06-30 | ALU（純組合せ、ADD/SUB/AND/OR/XOR/NOT/SHL/SHR/SAR） | 同上 |
| **ysd8800_cpu_v0_1.sv** | ~~v0.5.6~~ → **v0.5.7** | **2026-07-11** | トップFSM | **【v1.1更新】V3統合検証でS_SUBOP（EXTプリフィックス0x1Fサブopcodeフェッチ）のmem_rdアサート漏れバグを検出・修正。バス出力always_combに`S_SUBOP: begin mem_addr=rf_pc; mem_rd=1'b1; end`を追記（詳細§9・v3_design_memo_v0_3.md §9）。V1/V2由来の理想メモリTB（tb_cpu_v2a_v0_1、20ベクタ）も回帰ALL PASSでデグレ無を確認。** |

**⚠️既知の版数表記課題（ysd8800_cpu_v0_1.sv 冒頭コメントに明記済の運用）：**
ファイル名は `_v0_1` のまま固定し、実版数（現在v0.5.7）はヘッダ内のバージョン文字列でのみ管理する方針が明記されている（「V1完成時に一括リネーム予定」）。decoder/regfile/aluの3本は実際にv0.1のまま2026-06-30から変化がなくファイル名と内容が一致しているが、**cpuファイルのみファイル名(v0_1)と実体(v0.5.7)が乖離**している。これは設計変更ではなく意図的な運用方針だが、リネーム忘れ防止のため本台帳で追跡対象とする。

---

## 4. V1 CPUコア検証TB 現行版一覧

| # | ファイル | 版 | 日付 | 検証対象 | 状態 | 登録状況 |
| --- | --- | --- | --- | --- | --- | --- |
| 1 | tb_cpu_fetch_v0_1.sv | v0.1 | 2026-07-01 | フェッチ経路（リセットベクタ/PC前進/HALT） | ALL PASS | ✅登録済 |
| 2 | tb_cpu_mem_v0_1.sv | v0.1 | 2026-07-04 | 第4-A段 LDW/STW全アドレッシング・LE・役割逆転 | ALL PASS | ✅登録済 |
| 3 | tb_cpu_byte_v0_1.sv | v0.1 | 2026-07-04 | 第4-B段 LDB/STB（EXT 0x1F経由） | ALL PASS | ✅登録済 |
| 4 | tb_cpu_memalign_v0_1.sv | v0.1 | 2026-07-04 | アライメント例外検出・書換抑止 | ALL PASS | ✅登録済 |
| 5 | **(欠落)** CPU_STACK_TB 相当 | — | 2026-07-04頃 | 第5-1段 PUSH/POP/EI/DI/SYSCALL | ysd8800_cpu_v0_1.sv v0.5.2 変更履歴に「ALL PASS」記載あり | **❌未登録（ソース実体なし）** |
| 6 | **(欠落)** CPU_JSR_TB 相当 | — | 2026-07-04頃 | 第5-2段 JSR/RET | 同v0.5.3変更履歴に「ALL PASS」記載あり | **❌未登録（ソース実体なし）** |
| 7 | tb_cpu_iret_v0_1.sv | v0.1 | 2026-07-04 | 第5-3(a) IRET単体（FLAGS下位8bit復元=D-2実証） | ALL PASS | ✅登録済 |
| 8 | tb_cpu_irq_v0_1.sv | v0.1 | 2026-07-05 | 第5-3(b) 割込受理 S_IRQ_ACCEPT | ALL PASS（v0.5.6でIE bit7誤りを修正し7FAIL→0） | ✅登録済 |
| 9 | tb_cpu_irq_iret_v0_1.sv | v0.1 | 2026-07-06 | 第5-3(c) 受理→IRET往復統合 | ALL PASS | ✅登録済 |
| 10 | tb_cpu_align_irq_v0_1.sv | v0.1 | 2026-07-06 | 第5-3(c) align例外受理（E-1、LDW/STW両経路） | ALL PASS | ✅登録済 |
| 参考 | tb_cpu_irq_diag_poc.sv | (POC・KY38) | — | 割込受理7FAIL原因切り分け診断専用 | 本番非対象・保持のみ | ✅登録済（POCとして） |

**⚠️重大な欠落：5・6行目。** cpu RTL変更履歴（v0.5.2/v0.5.3）には「新規CPU_STACK_TB」「新規CPU_JSR_TB」がALL PASSした記載があるが、該当TBファイルの実体がプロジェクトナレッジ上に存在しない（`ls tb_cpu_*.sv` で非実在を確認）。実装自体は完了しているが、**検証結果を再現・監査するためのTBソースが版数管理から漏れている状態**であり、今回ご指摘の「バージョン管理の甘さ」の具体例と考えられる。

---

## 5. V2 CPUコア単体検証 現行版一覧（生成器・TB・黄金値）

| グループ | 生成器(Python) | TB(SystemVerilog) | 黄金値 | 検証本数 | 状態 | 登録状況 |
| --- | --- | --- | --- | --- | --- | --- |
| V2-a（C1レジスタALU） | gen_v2_vectors.py v0.1（2026-07-08） | tb_cpu_v2a_v0_1.sv v0.1（2026-07-08） | golden_v2a.txt | 20 | ALL PASS | ✅登録済 |
| V2-b（C2即値ALU/C3比較） | gen_v2_vectors_v2b_poc.py（推定ファイル名） | tb_cpu_v2b_v0_1.sv（推定） | golden_v2b.txt（推定） | 41 | ALL PASS | **❌未登録** |
| V2-c（C4分岐/C5メモリ） | gen_v2_vectors_v2c_poc.py（推定） | tb_cpu_v2c_v0_1.sv（推定） | golden_v2c.txt（推定） | 64 | ALL PASS | **❌未登録** |
| V2-d（C6スタック/C7サブルーチン） | gen_v2_vectors_v2d_poc.py（HANDOVER_CHAT78記載名） | tb_cpu_v2d_v0_1.sv（同上） | golden_v2d.txt（推定） | 75 | ALL PASS・承認済 | **❌未登録** |
| V2-e（C8制御割込） | gen_v2_vectors_v2e_poc.py（作成予定） | tb_cpu_v2e_v0_1.sv（作成予定） | — | 約6〜8本追加予定 | 設計メモv0.2承認済（Mなし）・実装未着手 | （これから作成） |

**注：** 「推定」と付したファイル名は、プロジェクトナレッジ内に実体が見当たらないため、HANDOVER_CHAT78.md本文の記載名から論理的に類推したものであり、実際にその名称・内容で存在するかは未確認（KY34違反を避けるためここに明記する）。

---

## 6. 関連設計メモ・レビュー文書 版数一覧

| 文書 | 版 | 登録状況 | 備考 |
| --- | --- | --- | --- |
| fpga_v1_cpucore_design_v1_1.md | ~~v1.1~~ → **v1.2** | ⬜**未登録（本セッション成果物・要登録依頼）** | V1 CPUコア設計根拠。**【v1.1追加】v1.2でS_SUBOP mem_rdバグ修正を§7.1に追記（KY41）** |
| fpga_v0_impl_spec_skeleton_v1_2.md | v1.2 | ✅登録済 | V0実装仕様 |
| stage5_3_design_memo_v0_1.md | v0.1 | ✅登録済 | 第5-3段(a) IRET設計メモ |
| stage5_3_design_review_v1_0.docx | v1.0 | ✅登録済 | 同レビュー回答 |
| stage5_3c_design_memo_v0_1.md | v0.1 | ✅登録済 | 第5-3段(c) 受理→IRET往復・align例外受理設計 |
| fsm_pcsp_paths_memo_v0_1.md | v0.1 | ✅登録済 | PC/SP経路整理メモ |
| **V2親設計メモ（V2 CPUコア単体検証、v0.1）** | v0.1 | **❌未登録** | v2d_design_memo_v0_1.md内で「親: V2 CPUコア単体検証（設計メモ v0.1 承認済）」と参照されるが、実ファイルは未確認 |
| v2d_design_memo_v0_1.md | v0.1 | ✅登録済 | V2-d設計メモ（承認済） |
| v2e_design_memo_v0_2.md | v0.2 | ✅登録済 | V2-e設計メモ（レビュー承認済・Mなし） |
| v2e_design_review_reply_v1_0.docx | v1.0 | ❌未登録 | v2e_design_memo_v0_2.md内で言及される回答書。実ファイル未確認 |
| **【v1.1追加】v3_design_memo_v0_3.md** | v0.3 | ⬜**未登録（本セッション成果物・要登録依頼）** | V3設計メモ。v0.1(ドラフト)→v0.2(レビュー反映クローズ)→v0.3(実装完了・全TB結果・§9 CPUコアバグ記録) |
| **【v1.1追加】v3_design_fixorder_v1_0.docx** | v1.0 | ✅登録済（ユーザーアップロード） | v3_design_memo_v0_2への有識者レビュー指示書（指示No.1〜9） |
| **【v1.1追加】fpga_impl_roadmap_v1_1.docx** | ~~v1.0~~ → v1.1 | ⬜**未登録（本セッション成果物・要登録依頼）** | 工程表本文。BRAM→PSRAM記述是正（V0/V3/V8行）＋V3完了状況を追記 |

---

## 7. 所見・課題（今回の実査で判明した事項）

1. **V2-b/c/dの生成器・TB・黄金値ソース一式がプロジェクトナレッジに未登録。** HANDOVER_CHAT78.mdにも「★V2-d/c/bの成果物はプロジェクト未登録の可能性大」と記載されており、今回`ls`/`grep`による実ファイル確認でこれを裏付けた。41/64/75ベクタのALL PASS実績は過去チャットの記録上は存在するが、KY34（実ファイルが真実）に照らすと、これらの検証を将来再現・監査する手段が現状存在しない。
2. **V1 Stage5-1（スタック）・5-2（JSR/RET）のTBソースも同様に未登録。** cpu RTL側の変更履歴には検証ALL PASSの記載があるが、対応するTBファイル実体が見つからない。
3. **V2親設計メモ（v0.1）の実体不明。** v2d設計メモから参照されているが、ファイルとして存在するか確認できなかった。
4. **ysd8800_cpu_v0_1.sv はファイル名(_v0_1)と実版数(v0.5.7)が意図的に乖離**（ヘッダに明記済の運用方針）。V1完成（V2完了後の一括リネーム）までは現状維持で問題ないが、リネーム忘れ防止のため本台帳の追跡対象とする。
5. decoder/regfile/aluの3ファイルはv0.1（2026-06-30作成）から一度も改版されていないことを実ファイルで確認した（第2〜5段の大幅なFSM拡張の間、これら3本には手を入れずに済んでいる＝良好な安定性）。
6. **【v1.1追加】V3実装一式（RTL5本・TB7本・生成器2本・golden一式）が本セッション終了時点でプロジェクトナレッジ未登録。** §1〜2の教訓（V2-b/c/dの二の舞）を踏まえ、本台帳§9に一覧化した上でユーザーへ登録を依頼する。
7. **【v1.1追加】理想メモリTBの限界が実例として顕在化。** V1/V2の理想メモリTB（`mem_ready`固定1）は、ysd8800_cpu_v0_1.svのS_SUBOP mem_rdアサート漏れという実バグを「82ベクタALL PASS」の裏で見過ごしていた。V3の実req/ack型メモリ統合で初めて発覚。kaizen.txt原則63として一般原則化済み（詳細§9・v3_design_memo_v0_3.md §9）。

## 8. 運用ルール（tool_version_ledgerに準拠）

- FPGA関連ファイル（RTL/TB/生成器/黄金値/設計メモ）を新規作成・改修したら、本台帳の該当表を都度更新する。
- 「ALL PASS」等の検証結果をチャット上で報告・記録する際は、対応するソースファイル本体もプロジェクトナレッジへの登録（またはユーザーへの登録依頼）を都度セットで実施する（§7-1/2の再発防止）。
- 現行版更新時も、過去版の系譜（v0.1→v0.5.x等の変更履歴）は欠落させない。
- 本台帳はツールバージョン台帳（tool_version_ledger）とは対象を分離した独立文書として維持する（ツールチェーン側 vs FPGAソース側）。
- ファイル名と内容実版数が乖離する運用（§3のcpuファイル等）を行う場合は、ヘッダにその旨を明記し、本台帳にも追跡事項として記載する。

---

## 9. V3（メモリ・バス・MMIOデコード／PSRAM統合）現行版一覧（★v1.1新設）

### 9.1 V3 RTL 現行版一覧

| ファイル | 版 | 日付 | 役割 | 登録状況 |
| --- | --- | --- | --- | --- |
| ysd8800_addr_decoder_v0_1.sv | v0.1 | 2026-07-11 | アドレスデコーダ（$FC80境界でRAM/MMIO振り分け） | ⬜未登録（要依頼） |
| ysd8800_mmio_stub_v0_1.sv | v0.1 | 2026-07-11 | MMIOスタブ（即時ready・固定0x00・アクセスカウンタ診断） | ⬜未登録（要依頼） |
| ysd8800_cdc_bridge_v0_1.sv | v0.1 | 2026-07-11 | CPU(4MHz)⇔PSRAM(高速)間CDCブリッジ（req/ack4相ハンドシェイク・2FF同期器） | ⬜未登録（要依頼） |
| ysd8800_psram_ctrl_v0_1.sv | v0.1 | 2026-07-11 | PSRAMビヘイビアモデル（通常12/リフレッシュ15サイクル可変レイテンシ） | ⬜未登録（要依頼） |
| ysd8800_v3_membus_v0_1.sv | v0.1 | 2026-07-11 | V3メモリサブシステム統合ラッパー（上記4本を結線） | ⬜未登録（要依頼） |
| ysd8800_cpu_v0_1.sv | v0.5.6→**v0.5.7** | 2026-07-11 | （§3参照）S_SUBOP mem_rdバグ修正 | ⬜未登録（要依頼・差替） |

### 9.2 V3 検証TB 現行版一覧

| ファイル | 版 | 対象 | 結果 | 登録状況 |
| --- | --- | --- | --- | --- |
| tb_addr_decoder_v0_1.sv | v0.1 | アドレスデコーダ+MMIOスタブ単体 | 6/6 PASS | ⬜未登録 |
| tb_cdc_bridge_v0_1.sv | v0.1 | CDCブリッジ単体（背中合わせ連続要求含む） | 4/4 PASS | ⬜未登録 |
| tb_psram_ctrl_v0_1.sv | v0.1 | PSRAMビヘイビアモデル単体 | 2/2 PASS | ⬜未登録 |
| tb_bridge_psram_integ_v0_1.sv | v0.1 | CDCブリッジ+PSRAMコントローラ実結合 | 2/2 PASS | ⬜未登録 |
| tb_cpu_v3_v0_1.sv | v0.1 | 実CPUコア+V3メモリサブシステム（ALU系・V2-aベクタ再利用） | 20/20 PASS | ⬜未登録 |
| tb_cpu_v3mem_v0_1.sv | v0.1 | 実CPUコア+V3メモリサブシステム（LDW/STW絶対・XI・間接・PUSH/POP・LDB/STB） | 5/5 PASS | ⬜未登録 |
| tb_cpu_v3boundary_v0_1.sv | v0.1 | 実CPUコア+V3メモリサブシステム（境界$FC7F/$FC80＋JSR/RET/BEQ複合） | 1/1 PASS | ⬜未登録 |

### 9.3 V3 検証生成器・黄金値一覧

| 生成器(Python) | 版 | 対象ベクタ | 黄金値 | 登録状況 |
| --- | --- | --- | --- | --- |
| gen_v2_vectors.py | v0.1（既存・§5と同一） | ALU系20（V3でも再利用） | golden_v2a.txt（既存） | ✅登録済（既存） |
| gen_v3_mem_vectors.py | v0.1 | LDW/STW絶対・[X+imm16]・[rS]間接・PUSH/POP・LDB/STB（5本） | v3mem/golden_v3mem.txt | ⬜未登録 |
| gen_v3_boundary_vectors.py | v0.1 | 境界$FC7F/$FC80＋JSR/RET/BEQ複合（1本・簡易2パスアセンブラ内蔵） | v3boundary/golden_v3boundary.txt | ⬜未登録 |

### 9.4 V3セッションで検出・修正した実装バグ

| バグ | 検出TB | 内容 | 修正 |
| --- | --- | --- | --- |
| CDCブリッジ req取りこぼし | tb_cdc_bridge_v0_1（自己レビュー） | mem_rdが下がらずアドレスだけ変わる背中合わせ連続アクセスで新規要求が握りつぶされる | ack後1サイクルのみreqを強制的に下げる方式に是正 |
| PSRAMコントローラ 確率閾値オーバーフロー | tb_psram_ctrl_v0_1（自己レビュー・PoC） | 32bit演算オーバーフローでリフレッシュ確率が意図値と乖離 | 64bit(longint)演算に是正 |
| **ysd8800_cpu_v0_1.sv S_SUBOP mem_rd欠落**（§3/§4参照） | tb_cpu_v3mem_v0_1（PUSH_POP/LDB_STBでハング） | バス出力always_combにS_SUBOP caseが欠落、V1/V2理想メモリTBでは検出不能 | `S_SUBOP: begin mem_addr=rf_pc; mem_rd=1'b1; end`を追記 |

### 9.5 V3関連設計メモ・工程表

（§6参照：v3_design_memo_v0_3.md／v3_design_fixorder_v1_0.docx／fpga_impl_roadmap_v1_1.docx）

---

## 10. V3.5（MMU統合）現行版一覧（★v1.2新設）

対象期間: 2026-07-10〜2026-07-11（CHAT81・CHAT82）
黄金リファレンス: emu23 v1.09（`emu23_v109.c` / `--mmu`）
ゲート基準: 「完走＋論理結果一致」のみ（cycle一致は対象外。emu23がCPI=1固定のため）

### 10.1 V3.5 RTL 現行版一覧

- **`ysd8800_mmu_v0_1.sv`** … v0.1 / **新規** / MMU本体。純組合せ・16ページ×4KB・20bit物理アドレス。MCR.EN=0 のとき恒等写像（`phys = {4'b0, logical}`）。新規stateを持たないため原則63の突き合わせ対象外 / 検証: tb_mmu **45/45 PASS**
- **`ysd8800_mmio_stub_v0_2.sv`** … **v0.2**（v0.1から改版）/ MMIOスタブ＋MMUレジスタ。PTR[16] @ `$FF00-$FF0F`、MCR @ `$FF10`。リセット値 PTR[n]=n（恒等写像）・MCR=`$00`。出力は **`ptr_flat_o` = packed 128bit**（★原則65: Icarusでunpacked array portはX固着する★）/ 検証: tb_mmio_mmureg **86/86 PASS**
- **`ysd8800_cdc_bridge_v0_2.sv`** … **v0.2**（v0.1から改版）/ CDCブリッジ＋`PHYS_AW` パラメタライズ。**v0.1は psram_addr が16bit固定だった**（★原則64★）/ 検証: tb_cdc_bridge_v02_poc **4/4 PASS**
- **`ysd8800_psram_ctrl_v0_2.sv`** … **v0.2**（v0.1から改版）/ PSRAMコントローラ＋`MEM_AW` パラメタライズ（1MB化）。**v0.1は内部で addr[15:0] に切り捨て・`mem[0:65535]` の64KB固定配列だった**（★原則64★）/ 検証: tb_bridge_psram_20bit **23/23 PASS**
- **`ysd8800_v35_membus_v0_1.sv`** … v0.1 / **新規** / V3.5統合ラッパー（`PHYS_AW=20` / `MEM_AW=20`）。**V3の `ysd8800_v3_membus_v0_1.sv` は削除せず残す**（回帰の退路確保）

**★CPUコアはV3.5で無改修★** … `ysd8800_cpu_v0_1_FIXED.sv`（v0.5.7）をそのまま使用。MMU統合にあたりCPUコアへの変更は一切不要であった。

### 10.2 V3.5 検証TB 現行版一覧

- **`tb_mmu_v0_1.sv`** … v0.1 / MMU単体（恒等写像・リマップ・境界） / **45/45 PASS**
- **`tb_mmio_mmureg_v0_1.sv`** … v0.1 / MMIOスタブv0.2のMMUレジスタ（PTR/MCR read-write） / **86/86 PASS**
- **`tb_bridge_psram_20bit_v0_1.sv`** … v0.1 / **20bit貫通・エイリアス非発生の恒久ガード**（物理 `$04000` vs `$14000`） / **23/23 PASS**
- **`tb_cdc_bridge_v02_poc.sv`** … PoC / CDCブリッジv0.2の回帰（`PHYS_AW=16` で v0.1 等価であることを確認） / **4/4 PASS**
- **`tb_cpu_v35regress_poc.sv`** … PoC / 実CPU＋V3.5（V2-a ALU系20ベクタ） / **20/20 PASS**
- **`tb_cpu_v35mem_poc.sv`** … PoC / 実CPU＋V3.5（V3メモリ系5ベクタ） / **5/5 PASS**
- **`tb_cpu_v35boundary_poc.sv`** … PoC / 実CPU＋V3.5（境界＋JSR/RET/BEQ） / **1/1 PASS**
- **`tb_cpu_v35mmu_v0_1.sv`** … **v0.1 / ★MMU有効時 emu23協調等価TB（6ベクタ）★ / 6/6 PASS**

上記のうち **PoC 3本（計26ベクタ）は MCR=0 における V3デグレ無しの証明**（S5）。
**`tb_cpu_v35mmu_v0_1.sv` は MMU有効時の外部観測等価**＝V3.5本来の検証対象（S7）。

### 10.3 V3.5 生成器・スクリプト・黄金値

- **`gen_v35_mmu_vectors.py`** … **v0.2**（v0.1から改版）/ MMUベクタ生成器（6本）。v0.1はMMU操作ヘルパがAレジスタを破壊するバグあり（§10.4-3）。v0.2で `mov(X,A)` 退避を追加し、ヘルパに「★A破壊★」警告コメントを付与
- **`mk_v35_regress_poc.py`** … PoC / V3のTBをV3.5構成へ機械的に差し替える変換スクリプト
- **`build_v35.sh`** … v0.1 / V3.5統合TBのビルドスクリプト（長い `iverilog` コマンド直書きによる書式崩れ防止に必須）

黄金値ファイル: `v35mmu/golden_v35mmu.txt` / `v35mmu/expected_v35mmu.hex` / `v35mmu/veclist_v35mmu.txt`

いずれも生成器が **emu23 v1.09（`--mmu`）の実行結果から自動生成**したもの。**手計算の期待値は一切使用していない**（＝偽合格防止）。

### 10.4 V3.5で検出・修正した実装バグ（4件）

1. **★物理アドレス上位ビット破棄（2箇所）★**
   - 検出: KY34（実ソース照合）
   - 内容: `cdc_bridge_v0_1` は psram_addr が16bit固定。加えて **`psram_ctrl_v0_1` は内部で addr[15:0] に切り捨て・`mem[0:65535]` の64KB固定配列**であった。「ポートが20bit」であっても「内部実装が20bit」ではなかった
   - 修正: 両者をパラメタライズし20bit・1MB化。`tb_bridge_psram_20bit` で恒久ガード
   - → **kaizen 原則64**（引継文書・HANDOVERの記述も実ソース照合の対象である）
2. **MMIOスタブ unpacked array port の X固着**
   - 検出: tb_mmio_mmureg（レジスタ本体は正しいのに出力ポートだけX）
   - 内容: `output logic [7:0] ptr_o [0:15]` が Icarus Verilog 12.0 でX固着。駆動記述をいくら変えても直らない
   - 修正: `output logic [127:0] ptr_flat_o` へフラット化（packed vector）。受け側で `[8*i +: 8]` スライス
   - → **kaizen 原則65**
3. **MMUベクタ生成器のAレジスタ破壊**
   - 検出: emu23黄金値がベクタ#5/#6で期待と不一致
   - 内容: MMU操作ヘルパ `set_ptr()` / `mmu_on()` / `mmu_off()` はいずれも `LDW A,#imm16` → `STB A,[MMIO]` の2命令列であり、**内部でAレジスタを破壊する**。`get_ptr(A,7)` の直後に呼ぶと読み値が上書きされる
   - 修正: `mov(X, A)` でXへ退避（XはMMU操作ヘルパで未使用）。生成器v0.2
4. **TB側MMIOカウンタ判定の偽FAIL**
   - 検出: S7初回実行（#3のみFAIL・CPUレジスタ値は全ベクタ黄金一致）
   - 内容: `dbg_mmio_access_count` は **cpu_rst_n でクリアされるベクタ毎の絶対カウンタ**（`ysd8800_mmio_stub_v0_2.sv` L134-141 実照合）。TB側で累積カウンタと誤解し前ベクタとの差分で判定したため誤FAIL
   - 修正: 判定を絶対値（`== 0` ならFAIL）へ変更。**RTL実装に誤りはなく、TBの判定ロジックのみが誤りだった**
   - → **kaizen 原則66**（診断信号を判定に使うときは、その信号のリセット条件・更新条件を実ソースで確認してから使う）

### 10.5 V3.5関連文書

- **`v3_5_design_memo_v0_3.md`** … **v0.3** / V3.5主設計メモ。§2 MMU挿入位置の確定根拠、§4.2 上位ビット破棄2箇所の訂正（KY41により旧記述は取り消し線で保持）、§10 レビュー結果、§12 実装・検証結果
- **`v3_5_design_review_reply_v1_0.docx`** … v1.0 / レビュー回答書。**総合判定＝承認（Mレベル指摘なし）**
- **`YSD8800_MMU_Design_v1_1_0.docx`** … v1.1.0 → **v1.2.0へ改版**（§5 物理メモリマップのI/O行是正・§7 MMU挿入位置を実装に整合）

### 10.6 V3.5の確定設計（レビュー承認済）

- **MMU挿入位置**: アドレスデコーダ（論理 `$FC80` 判定）が前段。**MMUはその後段・RAM側パスにのみ挿入**する。**MMIO（MMUレジスタ自身を含む）はアドレス変換を受けない**
- **MMUレジスタは常時アクセス可**（MCR.EN=0でも読み書き可）。でなければMCRに書けず有効化できない（鶏と卵）
- **リセット値 PTR[n]=n（恒等写像）/ MCR=`$00`** … MCRが誤って1でもベクタフェッチが破綻しない＝安全側。V7/V8のreset vector問題への布石
- **MMU無効時は V3 と bit-exact 等価**（S5で26/26 PASS実証）
- **ページ境界跨ぎ16bitアクセスは原理上発生しない**（4KB境界は偶数アドレス→跨ぎは奇数開始＝align例外）
- **自己ロックアウト回避（自己救済性）が実証済**（S7ベクタ#6）… PTR[15]=`$1F` で `$F000` 台をリマップした状態でも `$FF07` に到達でき、MCR（`$FF10`）でMMUを切り戻せた



---

## 11. V3.7（YSD8004 割込コントローラ）現行版一覧（★v1.3新設）

**工程**: Step 8 FPGA / V3.7（`fpga_impl_roadmap_v1_2.docx` にて新設）
**設計根拠**: `v3_7_design_memo_v0_3.md`（レビュー承認済・`v3_7_design_review_reply_v1_0.docx`）
**黄金リファレンス**: `emu23_v109.c`（v1.09）
**実装日**: 2026-07-12
**検証結果**: **全TB ALL PASS（32回帰 + 21単体 + CPU結合）・デグレゼロ**

### 11.1 新規RTL

| ファイル | 版数 | 内容 | 状態 |
| --- | --- | --- | --- |
| `ysd8800_ysd8004_v0_1.sv` | **v0.1** | **YSD8004 割込コントローラ本体（新規）**。IRQ_STAT(`$FCB2`) / IRQ_MASK(`$FCB4`) を保持。Write-to-Clear・マスク・3源集約・**IRQ1レベル出力**。★IRQ_MASK リセット値 = `0x04`★（emu23 L307） | ALL PASS |
| `ysd8800_v37_membus_v0_1.sv` | **v0.1** | **V3.7 メモリサブシステム統合ラッパー（新規）**。V3.5版（`ysd8800_v35_membus_v0_1.sv`）から最小差分。MMIOスタブをv0.3化し、割込I/F（`irq_src_*` / `irq1_o`）を外部ポートに出す | ALL PASS |

### 11.2 改版RTL

| ファイル | 旧版 | **新版** | 変更内容 |
| --- | --- | --- | --- |
| `ysd8800_mmio_stub_v0_3.sv` | v0.2 | **v0.3** | YSD8004（`ysd8800_ysd8004_v0_1`）をインスタンス化し、`$FCB2`-`$FCB5` をルーティング（**案B: スタブはルーティングに徹する**）。割込I/Fを外部ポートへ素通し。**MMUレジスタ部・スタブ挙動・診断出力は v0.2 から一切不変** |

### 11.3 ★CPUコアは無改修★

| ファイル | 版数 | 備考 |
| --- | --- | --- |
| `ysd8800_cpu_v0_1_FIXED.sv` | **v0.5.7（不変）** | **V3.5に続きV3.7でも無改修。** `irq_in[2:0]` は既に3bitで `vec = irq*2` を算術計算するため、IRQ1(=2) → `vec=$0004` がそのまま成立する（設計メモ §4.4） |

> **接続規約**: 上位で `irq_in = (irq1_o ? 3'd2 : 3'd0)` とする。`irq1_o` は**レベル信号**（`IRQ_STAT != 0`）。

### 11.4 検証TB

| ファイル | 版数 | 工程 | 内容 | 結果 |
| --- | --- | --- | --- | --- |
| `tb_ysd8004_v0_1.sv` | v0.1 | **S3** | YSD8004 単体TB。リセット値★KY-A★・R/W・W2C・マスク・集約・**レベル保持**・上位バイト`0x00`★KY-B★ の8項目 | **21/21 ALL PASS** |
| `tb_cpu_v37irq_v0_1.sv` | v0.1 | **S5** | **CPU結合TB（本丸）**。YSD8004 → IRQ1 → 受理 → `vec=$0004` → ハンドラ → W2C → IRET。emu23黄金照合 | **ALL PASS** |
| `tb_cpu_v37regress_poc.sv` | v0.1 | S4 | V2-a回帰（20ベクタ）。V3.5版から membus 差替のみ | 20/20 |
| `tb_cpu_v37mem_poc.sv` | v0.1 | S4 | V3メモリ命令（5ベクタ） | 5/5 |
| `tb_cpu_v37boundary_poc.sv` | v0.1 | S4 | 境界（1ベクタ） | 1/1 |
| `tb_cpu_v37mmu_v0_1.sv` | v0.1 | S4 | MMU（6ベクタ） | 6/6 |
| `tb_v37_w2c_diag_poc.sv` | v0.1 | 診断 | **BUG-1 究明用**（KY38 `_poc`）。YSD8004内部の `irq_stat_r` / `sel_i` / `addr_i` を直接観測 | （診断用） |

> S4のTB4本は、V3.5版TBを**機械的に複製**したもの（`mk_v37_tb.py` v1.0）。**V3.5版TBは温存**（KY38: 本番資産を壊さない）。

### 11.5 生成器・スクリプト

| ファイル | 版数 | 内容 |
| --- | --- | --- |
| `gen_v37_irq_vectors.py` | **v0.1** | **V3.7 黄金値生成器（新規）**。emu23 v1.09 でSYSCALL起点の割込受理を実行し、最終レジスタ状態を黄金として取得。ベクタ`$0004`(IRQ1)と`$0008`(SYSCALL)の両方を同一ハンドラ(`$0200`)に向けるイメージを生成 |
| `mk_v37_tb.py` | v0.1 | V3.5回帰TB4本をV3.7版へ複製（membusインスタンス差替のみ） |
| `build_v37.sh` | v0.1 | V3.7回帰TBのビルド・実行スクリプト |

### 11.6 黄金値

| ファイル | 内容 |
| --- | --- |
| `v37irq/golden_v37irq.txt` | emu23 v1.09 実行結果。**B=`1234` / X=`BEEF` / SP=`0400` / F=`81`**（★A は突合対象外★） |
| `v37irq/v37irq.hex` | RTL用イメージ（64KB） |

### 11.7 ★V3.7で検出・修正した実装バグ（1件）★

| # | バグ | 発覚工程 | 内容 | 対策 |
| --- | --- | --- | --- | --- |
| **BUG-1** | **IRQ_STAT と IRQ_MASK が入れ替わっていた** | **S5（結合）** | `addr_i` を2bit（`mmio_addr[1:0]`）にしたため、`$FCB2[1:0]=2'b10` / `$FCB4[1:0]=2'b00` となり STAT と MASK が逆にデコードされた。`mmio_addr[2]` でしか区別できず、2bitでは原理的に不可能だった。症状はハンドラW2Cが効かず**割込の無限ループ** | **`addr_i` を3bit（`mmio_addr[2:0]`）化**。`$FCB2`=`3'b010` … `$FCB5`=`3'b101` で一意にデコード |

> **★S3単体TBは 21/21 ALL PASS していたのに見逃した＝偽合格。★**
> 原因は「TBが `addr_i` に**自分が正しいと思う値**を直接与え、**DUTとTBが同じ誤った前提を共有**していた」こと。
> → **kaizen 原則67** として登録（「TBが与える定数の出所を疑え」）。
> → 修正版TBでは `localparam A_STAT_LO = ADDR_STAT[2:0];` と**実アドレスから機械的に導出**する形に改めた。
> → **V4（UART `$FC80`台）・V5（Timer）・V6（Storage `$FCA0`台）でも同じ轍に注意すること。**

### 11.8 所見

- **CPUコアは V3.5・V3.7 と2フェーズ連続で無改修。** `irq_in[2:0]` と `vec = irq*2` の算術設計が効いている（設計メモ §2.5(b)）。ISAのベクタ空き3枠（`$000A`/`$000C`/`$000E`）も温存されており、将来の専用ベクタ割込（案②）に対応可能
- **IRQ1をレベル出力にする設計判断（KY-D）が、emu23 の「IRQ_STAT再評価機構」をRTL側で自動成立させた**（設計メモ §4.4b【第2段】）。CPU L1224-1225 が `S_IRQCHK` のたびに `irq_in` を再ラッチするため。**パルス出力にしていたら V5 で取りこぼしていた**
- **多重割込の調停【第1段】（pending保護）は V5 の主要論点として申し送り済**（設計メモ §4.4b）

---

## 12. V4（YSD8001 UART）現行版一覧（★v1.4新設）

**工程**: Step 8 FPGA / V4（`fpga_impl_roadmap_v1_3.docx`）
**設計根拠**: `v4_design_memo_v0_2.md`（レビュー承認済）
**黄金リファレンス**: `emu23_v109.c`（v1.09）
**実装日**: 2026-07-12 〜 2026-07-13
**検証結果**: **S5 統合TB 23/23 ALL PASS ＋ V4回帰 32/32 ALL PASS・デグレゼロ**

### 12.1 新規RTL

| ファイル | 版数 | 内容 | 状態 |
| --- | --- | --- | --- |
| `ysd8800_ysd8001_v0_1.sv` | **v0.1** | **YSD8001 UART本体（新規）**。`UART_TX($FC80)` / `UART_RX($FC82)` / `UART_STAT($FC84)` / `UART_BAUD($FC86)`。★`UART_STAT` リセット値 = `0x01`（TX_READY=1）★（KY45）。TX完了 4167クロック。RX オーバーラン＝**先着優先**（`rx_accept = rx_valid_i & ~stat_r[BIT_RX_READY]`）。★`irq_tx_o` は**レベル**（`= stat_r[BIT_TX_READY]`）／`irq_rx_o` はパルス★ | ALL PASS |
| `ysd8800_v4_membus_v0_1.sv` | **v0.1** | **V4 メモリサブシステム統合ラッパー（新規）**。V3.7版（`ysd8800_v37_membus_v0_1.sv`）から最小差分。MMIOスタブをv0.4化し、UART物理層I/F（`uart_rx_valid_i`/`uart_rx_data_i`/`uart_tx_valid_o`/`uart_tx_data_o`）を外部ポートに出す | ALL PASS |

### 12.2 改版RTL

| ファイル | 旧版 | **新版** | 変更内容 |
| --- | --- | --- | --- |
| `ysd8800_mmio_stub_v0_4.sv` | v0.3 | **v0.4** | YSD8001（`ysd8800_ysd8001_v0_1`）をインスタンス化し、`$FC80`-`$FC87` をルーティング。`irq_src_uart_rx` / `irq_src_uart_tx` を YSD8004 へ接続。**YSD8004部・MMUレジスタ部・スタブ挙動は v0.3 から一切不変** |

### 12.3 ★CPUコアは無改修★

| ファイル | 版数 | 備考 |
| --- | --- | --- |
| `ysd8800_cpu_v0_1_FIXED.sv` | **v0.5.7（不変）** | **V3.5・V3.7 に続き V4 でも無改修。3フェーズ連続。** |

> **接続規約（§11.3 と同一）**: 上位で `irq_in = (irq1_o ? 3'd2 : 3'd0)` とする。
> `irq1_o` は**レベル信号**（`IRQ_STAT != 0`）。ベクタ = `irq*2` = `$0004`。

### 12.4 検証TB・生成器

| ファイル | 版数 | 工程 | 内容 | 結果 |
| --- | --- | --- | --- | --- |
| `tb_ysd8001_v0_1.sv` | v0.1 | **S3** | YSD8001 単体TB | ALL PASS |
| `tb_cpu_v4uart_v0_1.sv` | v0.1 | **S5(a)(b)** | CPU結合TB。(a) emu23協調等価7ベクタ ＋ (b) 物理層プロパティ2項目 | **9/9 ALL PASS** |
| **`tb_cpu_v4uart_v0_2.sv`** | **v0.2** | **S5(a)(b)(c)** | **★真のゲート★** v0.1 に **(c) RX/IRQ CPU結合検証14項目**を追加。`irq_en` ゲートを新設し `irq_in = (irq_en && irq1_o) ? 3'd2 : 3'd0` を結線（**v0.1 では `irq_in` が未結線で CPU に割込が入っていなかった**） | **23/23 ALL PASS** |
| `gen_v4_uart_vectors.py` | v1.0 | S5(a) | TX系・STAT系 emu23協調等価ベクタ生成（7本） | — |
| **`gen_v4_uart_c_vectors.py`** | **v1.0** | **S5(c)** | **RX系・IRQ系 プロパティ検証プログラム生成（6本）。** ベクタテーブル(`$0004`)・ハンドラ(`$0200`)・痕跡RAM(`$0300`)を含むイメージを生成 | — |
| `mk_v4_regress_tb.py` | v1.0 | S4 | V3.5回帰TB → V4 membus 用へ機械変換（KY48） | — |

### 12.5 S5(c) 検証項目（★真のゲート★）

**判定原則**: 判定は必ず**CPUが実命令で読んだ最終レジスタ値**で行う。TB は `rdata_o` / `stat_r` を直接覗かない（偽合格防止）。

| ID | 内容 | 期待値 | 結果 |
| --- | --- | --- | --- |
| c1 | RX注入 → CPU が `$FC82` を LDB | A=`0x005A` / STAT=`0x0003` | PASS |
| c2 | RX 2回読み → 読出に副作用なし | A=`0x005A` / RX_READY保持 | PASS |
| c3 | `$FC84` に `0x02` 書込 → RX_READY のみ W2C | W2C前`0x0003` → 後`0x0001` | PASS |
| c4 | オーバーラン → **先着優先**（`0xA5` 破棄） | A=`0x005A` | PASS |
| **c5-neg** | **★KY54 アンチ偽合格★** RX注入なし → ハンドラ未起動 | A=`0x0000` / B=`0x0000` | PASS |
| **c5** | **★真のゲート本体★** RX割込→`irq1_o`→`irq_in=2`→受理→`vec=$0004`→ハンドラ→`$FC82`読→UART W2C→YSD8004 W2C→IRET→メインが痕跡回収 | A=`0x00A5`（ハンドラ実行痕跡）<br>B=`0x005A`（ハンドラが読んだ値） | **PASS** |
| c6 | **TDRE = レベル**の識別テスト | cnt=`0x0002`（レベルの証明） | PASS |

### 12.6 ★S5(c)-ISSUE-1: IE 非依存ラッチとハンドラ冪等性要件★

**c6 の検証過程で判明した重要事実**（kaizen 原則69 として登録）:

`irq_pending` の**ラッチ**は `IE` を見ない。`IE` を見るのは**受理**のみである。

| 段階 | emu23 | FPGA RTL | IE参照 |
| --- | --- | --- | --- |
| ラッチ | `ysd8004_raise()` L314-329 | L1223-1225 `if (state == S_IRQCHK && irq_in != 3'd0) irq_pending <= irq_in;` | ★**しない**★ |
| 受理 | L1176 `if (cpu.irq_pending >= 0 && (cpu.flags & FL_IE))` | L590 `if (irq_pending != 3'd0 && flags_ie)` | する |

**帰結**: 「ネスト禁止（受理時 IE=0）」は**受理の抑止**であって**ラッチの抑止ではない**。ハンドラが割込源を沈黙させても、沈黙させる**前**にラッチ済みの pending が `IRET` 後に受理され、ハンドラが**もう1回だけ**起動する。

**これはバグではなく、emu23 と RTL が一致した正しい挙動である。** MC6809 の IRQ でも同じことが起きる（OS-9 の IRQ ポーリングルーチンが「仕事が無ければ即 RTI」と冪等に書かれている理由）。

> ★**YSD8800 上の全ての割込ハンドラは冪等（idempotent）に書かねばならない。**★
> 「ハンドラが必ず1回だけ呼ばれる」前提のコードを書いてはならない。
> → **YUI OS Ph.8（MMU連携・Level 2）のカーネル設計に直結する制約。**
> 文書化: `emu23_device_design_v1_5.docx` §3.2.2

### 12.7 検出・修正した実装上の問題

| # | 問題 | 対処 |
| --- | --- | --- |
| 1 | **`tb_cpu_v4uart_v0_1.sv` で `irq_in` が `irq1_o` から未結線**。S5(b) の「RX後に `irq1_o`=1」は割込コントローラ出力を覗いただけで、**CPU は割込を一切受けていなかった** | v0.2 で `irq_en` ゲート付きで結線。`irq_en=0` により (a)(b) の検証条件は従来と**構造的に同一**であることを保証（(a) 7/7 再現で実証） |
| 2 | **Icarus は `ref` ポート未サポート**（`sorry: Reference ports not supported yet`） | `task automatic run_to_halt()` を引数なしに変更（kaizen 原則70） |
| 3 | c6 の初期設計（「2回目にマスク」）で `cnt=3` となり FAIL | **期待値を3に書き換えるのは偽合格**（KY54）。「1回目でマスク（冪等）」に設計変更し、`cnt` が実装詳細に依存しない構造にした（レベル→必ず2 / パルス→必ず1） |

### 12.8 ★KY49 の実害2例目★

`HANDOVER_CHAT85.md` §6 に「✅完成」と記載されていた `tb_cpu_v4uart_v0_1.sv` / `gen_v4_uart_vectors.py` / `mk_v4_regress_tb.py` が**プロジェクトナレッジに未登録**であり、本チャット冒頭の実在確認（`ls`）で発覚。ユーザーに再アップロードを依頼して復旧した。

→ **kaizen 原則71 として登録**（HANDOVER 成果物はその場で登録する／次チャット冒頭で全件 `ls` 実在確認する／未登録なら「⚠未登録」と書く）。

### 12.9 所見

- **CPUコアは V3.5・V3.7・V4 と3フェーズ連続で無改修。** `irq_in[2:0]` と `vec = irq*2` の算術設計が引き続き効いている
- **KY44（BUG-1パターン）は V4 でも実際に危険だった**: `$FC80`(TX) と `$FC84`(STAT) は下位2bitが同一（`2'b00`）。2bitデコードなら TX に `0xFF` を書いた直後の STAT 読みが `0xFF` に化ける。→ S5(a) の `UART_TX_STAT_ALIAS` ベクタで**決定打として検証済**（PASS）
- **TX(TDRE) をレベルにした設計判断が正しかった**ことが c6 で実証された。ただしその副作用として §12.6 の冪等性要件が生じる。**V5（Timer）でも同じ論点が出る**（タイマー割込をレベルにするかパルスにするか）
- **V5 への申し送り**: `v3_7_design_memo_v0_3.md` §4.4b の「第1段（timer/align の pending 保護）」を RTL 実装する際、**保護ロジックを `flags_ie` で条件付けてはならない**（emu23 と乖離する）

---

## 13. V5（YSD8002 タイマー）現行版一覧（★v1.5新設）

**昇格記録**: S5 poc→正式版昇格。原則43レビュー承認済（2026-07-18）。「OR実装のまま昇格・TCR EN是正はV6以降」の条件付き。同一セッション内実体（`/home/claude/w/`）を正として台帳作成（ナレッジ登録の有無に依存しない・KY34）。

### 13.1 昇格RTL（新規／改版）

| ファイル | 旧版 | 新版 | md5（実測） | 内容 |
| --- | --- | --- | --- | --- |
| `ysd8800_ysd8002_v0_2.sv` | v0.2_poc | **v0.2（正式）** | `f3aa717023d6fd488ad847c8eccc2872` | YSD8002 タイマー本体。`TCR($FCA0)` = TIMER_EN(b0)/IRQ_EN(b1)/IRQ_ACK(b5)。`irq_timer_o` は**レベル**（`irq_req_r`：fireでset／TCR-ACK(bit5)でclear）。~~★現状 `fire_en=|tcr_r[1:0]` の OR 実装（§13.5負債）★~~ → **★2026-07-18 EN是正で `v0_3` へ改版・`fire_en=timer_en_r & irq_en_r`〈AND〉に是正済み（§14参照）★** |
| `ysd8800_mmio_stub_v0_5.sv` | v0.4 | **v0.5** | `9c8cd6e93fa84fd7f2a12f6c9b0bfe94` | YSD8002（`ysd8800_ysd8002_v0_2`）をインスタンス化。**`irq0_ack` 経路3点**（入力ポート／u_ysd8002をv0_1→v0_2差替／中継結線）を実装。★是正版 md5=`d2bef39c…`（poc段階）を昇格。旧sedリネーム版 `3058ffbb…` は3点欠落の誤登録だった★。YSD8004部・YSD8001部・MMUレジスタ部は v0.4 から不変 |
| `ysd8800_v5_membus_v0_1.sv` | (新規) | **v0.1** | `aec1839c115026a9c7839a4c93166f80` | V5メモリサブシステム統合ラッパー。MMU + YSD8004 + YSD8001 + **YSD8002**。下位 u_mmio_stub は正式版 `ysd8800_mmio_stub_v0_5` を参照。割込結線はmembus外＝TB内 |

### 13.2 ★CPUコアは無改修★

- V3.5・V3.7・V4 に続き **V5 でも CPUコア無改修**（**4フェーズ連続**）。使用は pending保護入りの **v0.5.8**（timer/align pending保護は `flags_ie` で条件付けない＝emu23と非乖離）。

### 13.3 昇格検証TB・テストベクタ

| ファイル | 版 | md5（実測） | 内容 |
| --- | --- | --- | --- |
| `tb_cpu_v5timer.sv` | v0.1（正式） | `4f41148cacba65aa38491da6f003ad16` | V5統合TB full版。module=`tb_cpu_v5timer`。下位 u_membus=`ysd8800_v5_membus_v0_1`。`v5t_ack.hex`/`v5t_noack.hex` 参照 |
| `tb_cpu_v5timer_short.sv` | v0.2（正式） | `d4c743d52b848946f91e78c48a8ce8e1` | 短縮版（psram_clk比8:1）。module=`tb_cpu_v5timer_short`。**ack側 T0〜T5 ALL PASS**（T0=ネガティブラン健全性/T1=IRQ到達/T2=IRET復帰/T3=レベルストーム無し/T4=周期再武装/T5=正常HALT）。HALT cyc=2903717, CNT=72, OUTC=200 |
| `v5t_ack_short.hex` | — | `12d86038856aedb2c6ed79e3a0f8c322` | ACKあり骨格。emu23実測 完走 |
| `v5t_noack_short.hex` | — | `be2ddc6c3b1bf69f8eb7b34b4483054c` | ACK無し対照（1回で自己沈黙） |

### 13.4 統合ビルド検証

- 昇格でモジュール名を4段連鎖変更（ysd8002→mmio_stub→v5_membus→TB）。**iverilog 12.0 統合ビルド rc=0・error/warning ゼロ**でモジュール名整合を実証。short版TB実行でack側 T0〜T5 全PASS＝機能面の正しさを実証。

### 13.5 ★設計負債: TCR EN=OR（~~V6以降で是正~~ → **2026-07-18 案Bで是正済み・§14参照**）★

- `fire_en = |tcr_r[1:0]`（OR）実装のため、**IRQ_EN(bit1) が名前どおり機能せず**、TIMER_EN(bit0) のみで割込発火する。デバイス契約（IRQ_EN でマスク可能）に対する違反。
- ~~**是正（OR→AND）は v5_design_memo §3.5.2 通り V6以降の独立工程**（ユーザ承認 2026-07-14/17）。~~
- ~~据置根拠: 「タイマーは回すが割込はマスク」を要するYUI OS実装の近接予定なし（ユーザ確認 2026-07-17）。負債は管理下で可視化（各RTLヘッダに明記）。**失念厳禁の常駐項目**。~~
- **→ ★2026-07-18 EN是正工程（案B）で是正完了★**: `fire_en = timer_en_r & irq_en_r`（AND）へ改修。ファイル名=モジュール名を **v0_3** に統一。負例 tb_cpu_v6mask（TCR=$0001→CNT=0）で IRQ_EN 割込マスクの機能回復を実証。詳細は本台帳 **§14** および ysd8002_timer_design v1.2 §3。
- **★残る「案A（TIMER_EN=0 でカウンタ歩進停止）」は将来課題**として V6以降へ先送り（§14.5・ysd8002_timer_design v1.2 §11.1）。案Bで割込マスク契約は回復済のため、案Aはカウンタ挙動の意味論改善の位置づけ。

### 13.6 ★irq_timer_o レベル化（案B-1a・MC6840 PTM作法）★

- 1クロックパルスは CPU が S_IRQCHK 状態でしかサンプルせず（実CPI≒18）★ほぼ確実に取りこぼす★。→ **レベル＋TCR-ACKハンドシェイク**を採用。★クロック非依存★でPSRAMウェイト・PLL比変更に頑健。
- 契約(D13-A): IRQ0_HANDLER は必ず TCR に `$0023` を書く（IRQ_ACK）。書かないと `irq_req_r` が落ちずIRET後即再受理でハング。これは**バグでなくACK義務の機械的強制**。
- emu23 は「1回で自己沈黙」（A=1）、RTL は「ACK無しでハング」。★意味論が異なる★が、`kernel_v12_8.asm`・`startup_harness23_v16.asm` は両者ともACK先行で契約充足済＝ソース改修不要。

### 13.7 ★poc昇格の教訓（KY34再確認）★

- 是正版 `ysd8800_mmio_stub_v0_5.sv`（md5=`d2bef39c…`）がセッション開始スナップショットに未反映で、`/mnt/project/` 側は旧版 `3058ffbb…`（irq0_ack 0件）が見えていた。記録「登録済み」を信じて昇格していたら、割込経路欠落版を正式版化する重大事故だった。
- **教訓**: ①スナップショットは信用せず実体md5を測る。②同一セッション内なら `/home/claude/w/`・`/mnt/user-data/outputs/` の実体が最も確かな真実で、ナレッジ登録の有無に関わらず台帳作成が可能。③KY対象の確認は作業開始直後の最初の1手で実測する。

### 13.8 所見

- **タイマー割込をレベルにするか論点（§12.9でV5に申し送られていた）** は案B-1a（レベル＋ACK）で決着。UART TX(TDRE)レベル化と同型の設計判断。
- **CPUコア4フェーズ連続無改修** は `irq_in[2:0]` と `vec=irq*2` の算術設計、および pending保護の非IE条件付けが効いている。
- **次工程**: v5_design_memo内部系列 S8/S9/S10（full版TB完走確認・全回帰・設計文書改版）。~~TCR EN是正はV6で独立実施。~~ → **★TCR EN是正（案B）は2026-07-18に前倒し実施・完了（§14）。V6は案A（カウンタ停止）を担当★**

---

## 14. EN是正工程（案B・TCR発火EN OR→AND）RTL・検証TB・生成器 現行版一覧

**工程位置**: V5完了後・V6の前に挿入（ユーザ承認 2026-07-18・ロードマップlatest反映は日報時に依頼）。
**目的**: TCR の発火許可条件を OR→AND へ是正し、IRQ_EN(bit1)=0 が割込マスクとして機能する契約を回復（案B）。

### 14.1 改版RTL一覧

| ファイル | 旧版 | 新版 | 変更内容 | 判断 |
|---|---|---|---|---|
| `ysd8800_ysd8002_v0_3.sv` | v0_2 | **v0_3** | `fire_en = \|tcr_r[1:0]`〈OR〉 → **`fire_en = timer_en_r & irq_en_r`〈AND〉**（L210）。IRQ_EN=0 で発火マスク。 | **★案X: ロジック変更ありゆえファイル名=モジュール名を v0_3 に統一★** |
| `ysd8800_mmio_stub_v0_6.sv` | v0_5 | **ファイル名v0_6** | u_ysd8002 参照先を v0_2→**v0_3** へ追従（L337）。 | **★案X-1: ロジック無変更・参照追従のみゆえモジュール名は v0_5 据え置き★**（membus/TB群は無改修） |

### 14.2 新規検証TB

| ファイル | 内容 | 結果 |
|---|---|---|
| `tb_cpu_v6mask.sv` | EN是正 負例統合TB。TCR=$0001（TIMER_EN=1/IRQ_EN=0）で割込が出ないこと（CNT=0）を検証。KY54準拠でN0にTB健全性チェック内蔵。 | **N0〜N3 ALL PASS（4/4）**。本丸N1=CNT0 で AND是正を直接実証。N2=OUTC200（主処理完走）・N3=HALT到達。 |

### 14.3 テストベクタ・スクリプト

| ファイル | 内容 |
|---|---|
| `v6t_mask.asm` | 負例テストプログラム（v5t_ack.asm 派生・TCR書込値のみ $03→$01 変更）。★内ループ #1000→**#100** に短縮（short版・RTLシムMAX_CYC=3M整合。§14.7）★ |
| `v6t_mask_short.hex` | 上記アセンブル結果（376バイト）。tb_cpu_v6mask が `v5timer/v6t_mask_short.hex` として参照。 |
| `build_v5en.sh` | EN是正版ビルドスクリプト（mmio_stub v0_6・membus v5・ysd8002 v0_3 を含む）。 |

### 14.4 CPUコア無改修（デグレゼロ）

CPUコアは V3.5・V3.7・V4・V5 に続き **EN是正でも無改修**（v0.5.8 のまま・**5フェーズ連続で論理不変**）。EN是正はYSD8002タイマーの発火条件のみの変更でCPUコアに非干渉。回帰実証: **V1/V2 全ベクタ ALL PASS**（V2a=20 / V2c=64 / V2d=75 / V2e=82・md5=`09de9678…`一致）。

### 14.5 ★将来課題: 案A（TIMER_EN=0 でカウンタ歩進停止）★

案Bは「発火許可条件のAND化」のみを対象とし、TIMER_EN=0 でもカウンタは歩進し続ける（発火はANDでfalse）。本来 TIMER_EN は「カウンタ稼働許可」であるべきで、TIMER_EN=0 でカウンタ歩進を停止する案Aが論理的に望ましい。**V6以降の独立工程へ先送り**（ユーザ承認 2026-07-17/18）。案Bで割込マスク契約は回復済のため、案Aはカウンタ挙動の意味論改善の位置づけ。正式記録=ysd8002_timer_design v1.2 §11.1。

### 14.6 ★案X/案X-1 の非対称判断（波及範囲最小化）★

- **YSD8002**: ロジック変更ありゆえ **ファイル名＝モジュール名を v0_3 に昇格**（下位モジュールの改版は上位に伝播させる正道）。
- **mmio_stub**: ロジック無変更・参照追従のみゆえ **ファイル名 v0_6・モジュール名 v0_5 据え置き**。これにより membus/TB群は無改修で済み、波及範囲を最小化。
- 判断根拠: 「実装が変わったモジュールだけ版を上げ、参照だけ変わったモジュールは名前を上げない」＝差分の局所化。

### 14.7 ★教訓: negTB ループ長は short版hex と揃える★

負例asm初版は内ループ #1000（正例フル版と同一長）だったが、RTLシムの MAX_CYC=3M で200回完走せず N2/N3 が FAIL していた。真因は **ループ長＞MAX_CYC** であり **AND化ロジックとは無関係**。#100 へ短縮（short版整合）で N1〜N3 全PASS。教訓: 負例作成時は既存 short版のループ定数を先に確認する（KY：真因の取り違えでAND化を疑って正しい是正を壊す危険の回避）。

---

## 15. V6-A ストレージコントローラ（YSD8003）RTL・検証TB 現行版一覧

**工程位置**: V5・EN是正完了後・V8統合前（CHAT109〜111）。
**目的**: SD SPIカードを介したセクタread（CMD17）契約をFPGAで実装・検証する。YUIFSファイルシステムの物理層基盤。

### 15.1 RTL一覧

| ファイル | 版 | md5(先頭10) | 変更内容 | 判断 |
|---|---|---|---|---|
| `ysd8800_ysd8003_v0_4.sv` | ファイル名v0_4 (モジュール名v0_1維持) | `639a4d6633…` | SPI CMD17実装。CS制御・レスポンス0x00・データトークン0xFE検出・512Bデータ・CRC無視。 | **★KY60運用: ファイル名v0_4／モジュール名v0_1★**（stub参照互換維持のため） |
| `ysd8800_v5_membus_v0_2.sv` | v0_2 | `202416c3b1…` | V5-membus改版：YSD8003結線対応。`$FCB6-$FCB9` を YSD8003 へルーティング。 | V5からの追従改版 |
| `ysd8800_mmio_stub_v0_7.sv` | v0_6→**v0_7** | `8464921597…` | YSD8003 ルーティング追加。V6-Aで正式版昇格。 | 内部追加のみ・波及最小 |

### 15.2 検証TB

| ファイル | 内容 | 結果 |
|---|---|---|
| `tb_ysd8003_v0_3.sv` | YSD8003単体TB。SPI CMD17一連の応答検証。 | ✅完了 |
| `tb_cpu_v6sdread.sv` | 上位結合TB：sd_read契約検証（sd_sample.c 由来）。 | ✅完了 |

### 15.3 CPUコア無改修（デグレゼロ）

CPUコアは V3.5・V3.7・V4・V5・EN是正 に続き **V6-A でも無改修**（v0.5.8 のまま・**6フェーズ連続で論理不変**）。

---

## 16. V8 YUI OS統合（cat/ls フル統合・選択肢A-1）RTL・検証TB・生成物 現行版一覧

**工程位置**: V6-A完了後（CHAT111選択肢B完了→CHAT112選択肢A設計・実装→**CHAT113 ALL PASS達成**）。
**目的**: mkfs_yuifs でYUIFSイメージ生成 → SD SPIモデル → **read専用cat/lsデモC** が実SPI経由で sd_read → YUIFS解釈 → ls/cat をUART出力 → TBが期待文字列と照合。**OS無改修**で cat/ls が動くことを実証する。

### 16.1 新規/改修ファイル一覧

| ファイル | 種別 | md5(先頭10) | 内容 |
|---|---|---|---|
| `tb_cpu_v8catls_poc.sv` | 新規TB | `597712e2d7…` | 統合TB v0.1。UARTキャプチャ＋期待列44B照合＋KY54負例枠(T0)。 |
| `v8_catls_demo_poc.c` | 新規デモC | `d816b882a8…` | read専用cat/lsデモC v0.1。sd_read/putchar は sd_sample.c 契約流用（ハードコード無し）。_main=$13be。 |
| `sd_spi_model_v0_3_poc.sv` | 改修 (v0.2→v0.3) | `b5d20cb0ea…` | 計算式返し→**$readmemh実イメージ返し**。範囲外LBAは8'hFF保護。KY41準拠改版。 |
| `v8t_catls.hex` | 生成物 | `7242221813…` | デモC最終ビルド（17408B, 17408行）。 |
| `sd_image.hex` | 生成物 | `2dc006b4b6…` | mkfs正例イメージ 8KB。HELLO.TXT="Hello, YUI OS!\n"。 |
| `sd_image_neg.hex` | 生成物 | `52426123fa…` | 内容差替負例。同dir同size、LBA4内容のみ "Byebye, YUIOS!\n"(15B)。 |
| `v8_catls_integ_design_memo_v0_2.md` | 文書改版 | — | 設計メモ。v0.1→v0.2（実行結果追記・KY41準拠）。 |

### 16.2 CPUコア無改修（デグレゼロ）

CPUコアは V3.5・V3.7・V4・V5・EN是正・V6-A に続き **V8でも無改修**（v0.5.8 のまま・**7フェーズ連続で論理不変**）。

### 16.3 無改修死守（KY38・統合の価値の核）

sd_sample.c / mkfs_yuifs_v1_1.py / RTL（CPU・YSD8003 v0.4・membus等）は**一切触っていない**。新規/改修は SDモデル(TB環境)とデモC(新規)、統合TB(新規)のみ。**cat/ls が既存無改修コードで動くことこそ「OS統合成功」の意味**。

### 16.4 検証設計思想（偽合格回避）

期待値 `"Hello, YUI OS!"` は mkfs 由来だが**偽合格ではない**：
- デモCは sd_read 経由でしかデータを得ず（ハードコード無し）
- SDモデルは img_mem の実バイトを返す
- superblock読→dir走査→data読の全経路を通さねば期待文字列に到達しない
- 「期待値=ディスク内容」の一致は「SD→FS→出力の往復が正しく機能した」証明

**KY54強化（内容差替負例）**: この因果をさらに強く縛る（唯一の直接的ハードコード検出手段）。

### 16.5 ★V8統合結果（2026-07-21・CHAT113）★

#### 負例ラン（KY54: 内容差替）
- **配置**: sd_image_neg.hex → sd_image.hex（md5=`52426123fa…`）
- **UART出力**: `"ls:\nHELLO.TXT\ncat HELLO.TXT:\nByebye, YUIOS!\n"` (44B)
- **結果**: T0_negative=PASS / S1_halt=PASS / S2_uart_len=PASS(44=44) / **S3_uart_match=FAIL(mism=11)** ← **期待通り**
- **意味**: 「デモCがSD実内容を読む（ハードコード無し）」を直接証明。FSパス（superblock→dir走査→data読）が実データを追従することを因果的に確認。

#### 正例ラン
- **配置**: sd_image_pos.hex → sd_image.hex（md5=`2dc006b4b6…`）
- **UART出力**: `"ls:\nHELLO.TXT\ncat HELLO.TXT:\nHello, YUI OS!\n"` (44B・完全一致)
- **サイクル**: HALT到達 cyc=**3,750,467** (PC=0x0025)
- **結果**: **PASS=4 FAIL=0 >>> V8 cat/ls INTEGRATION ALL PASS <<<**

| ケース | 結果 | got | exp | 意味 |
|---|---|---|---|---|
| T0_negative | PASS | 0 | ≠-1 | TB健全性 |
| S1_halt | PASS | 1 | 1 | プログラム完走 |
| S2_uart_len | PASS | 44 | 44 | 出力長一致 |
| S3_uart_match | PASS | 0 | 0 | 期待文字列完全一致 |

#### 到達点の意味
YSD8003(ストレージ) + YUIFS + YUI OS が実SPI経由で結合し、**OS無改修**で cat/ls が動作。V6-A から続くストレージ工程の到達点。YUI OSがFPGA実装上で実ファイルシステムを読み出せることの**初の実証**。

### 16.6 ★kaizen候補★

1. **統合TBビルドは decoder/regfile/alu 先頭必須**（idec_pkgパッケージ定義の前方参照）。
2. **期待文字列はPythonで実バイト列挙して確定**（40→44B欠落を自己検出・防止に有効）。
3. **正例hexは負例ラン前に必ず退避**（`sd_image_pos.hex`）＋**md5で二重確認**（原則79適用）。負例ラン前に上書きするsd_image.hexは正例名でもあるため。
4. モデル単体確認は `$readmemh` 独立チェックで代替可（SPI手駆動の深追いは省トークン観点で回避）。
