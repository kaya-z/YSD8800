# kernel v12.8 移行・yuios 絶対ゲート再設定 設計書 v1.0

- 版数: v1.0
- 発行日: 2026-07-28
- Status: Draft（レビュー待ち）
- 対象工程: **V8-b Pre**（V8-b 本番の前置工程）
- 対応関連: V5 TCR-ACK 対応（RTL 側は 2026-07-13 に凍結済）
- レビュー要否: **必要**（判定基準を動かす変更のため、原則87 系）
- 改版履歴:
  - v1.0 (2026-07-28) 初版起票。V8-b Pre-1（設計書先行）として作成。

---

## 0. 本設計書の位置付け（原則43 本質的動機）

本改訂の**本質的動機**は「RTL 実装との整合が取れているのは kernel v12.8 のみであり、
FPGA V8-b 工程（yuios.bin の RTL 実行）に着手するためには kernel v12.8 化と、
それに伴う yuios サイズ変化に応じた絶対ゲート再設定が不可避である」ことにある。

Makefile v1.1 は scc23 v2.04 対応で dhrystone 側の絶対ゲートを更新したが、
kernel 側は v12.7 のままに据え置かれていた。これは CHAT120〜122 の作業範囲外で
あったための「意図的な据え置き」ではあるが、V8-b 着手時点で顕在化する
**設計負債**（原則77）である。本設計書は、この負債を「直す前に記録」する
目的も兼ねる。

## 1. 設計負債の記録（原則77）

### 1.1 顕在化した経緯

| 時点 | 出来事 |
|---|---|
| 2026-07-13 | emu23 v1.10 でタイマー再武装契機を「IRET 命令」→「TCR bit5 書込」へ変更（kaizen 原則73 対応）。同日 kernel v12.8 実装完了。 |
| 2026-07-13 以降 | RTL 側（`ysd8800_ysd8002_v0_3.sv` 等）は V5 TCR-ACK 方式で凍結。 |
| CHAT117〜119 | V8-D 工程（Dhrystone RTL 動作確認）。kernel 非依存の C プログラム対象のため、kernel 版数差異は顕在化せず。 |
| CHAT120〜122 | scc23 v2.04（char ロード幅是正）・startup_harness23 v1.7（IRQ1 A/B/X 全退避）。**kernel は対象外**。Makefile v1.1 化はしたが KERN_SRC は v12.7 のまま。 |
| CHAT123（本チャット） | V8-b（yuios.bin RTL 実行）着手時、方針①検討により **RTL と kernel v12.7 の非整合が発覚**。 |

### 1.2 なぜ据え置きが継続したか（Why-Why 分析）

- Why-1: Makefile v1.1 改版時（CHAT121）に kernel 版数の同期が漏れた。
- Why-2: scc23 v2.04 対応の焦点が dhrystone 側（絶対ゲート 826→819）に絞られており、yuios 側は「対象範囲外」として意識から外れた。
- Why-3: V8-b 着手までは「yuios.bin を RTL で回す」場面が存在しなかったため、kernel v12.7/v12.8 差異が実害として顕在化する機会が無かった。
- Why-4: kernel v12.7→v12.8 移行の独立工程が明示的に立てられていなかった（暗黙に「emu23 動作確認完了 = 完了」と扱われた）。

### 1.3 再発防止（原則追加候補）

**候補原則**：ツール／サブシステム（RTL・emu23・kernel・scc・hasm・lnk）のいずれかで
「実装契機の変更」を行った場合、他サブシステムの追随状況を Makefile（版数・
KERN_SRC 参照・絶対ゲート）に **明示リスト**で書き留め、追随完了までは
Makefile ヘッダに「追随未完タグ」を残す。

（正式起票は本工程完了後に kaizen.txt へ提案。本設計書内では言及のみ）

## 2. 変更対象と変更内容

### 2.1 KERN_SRC 参照の切替

| 項目 | 変更前（Makefile v1.1） | 変更後（Makefile v1.2） |
|---|---|---|
| `KERN_SRC` | `kernel_v12_7.asm` | `kernel_v12_8.asm` |

**根拠**：
- kernel v12.8 は V5 TCR-ACK 方式（RTL 実装と整合）に対応（kernel_v12_8.asm ヘッダ §v0.12.8）。
- kernel v12.7 の IRQ0_HANDLER には TCR bit5 書込みが無く、RTL 上で走らせるとタイマーが二度と発火しない（永久停止）。
- V8-b（yuios.bin RTL 実行）の前提条件として v12.8 化は不可欠。

### 2.2 yuios 絶対ゲート値の再凍結

| 項目 | 変更前（Makefile v1.1） | 変更後（Makefile v1.2） |
|---|---|---|
| yuios 絶対ゲート | 56,416 バイト完全一致 | **v12.8 初回ビルド値**（本工程で確定） |
| リファレンス `REF_BIN` | `yuios_ref_road2_I3.bin`（v12.7 由来） | `yuios_ref_road2_I4.bin`（v12.8 由来・本工程で凍結） |

**予想サイズ変化**：
- v12.7 → v12.8 差分は「IRQ0_HANDLER 入口・B 退避直後に ACK 3 命令挿入」
- 3 命令の想定内訳：`LDA #TCR_IRQ_ACK` / `STA TCR` / （もう 1 命令、A 退避 or 相当）
- kernel_v12_8.asm ヘッダ §v0.12.8 の「(1) MMIO 定数追加 (2) ACK 3 命令挿入」から推定
- 命令サイズは ISA 2.3 に依存するが、概ね **+6〜+9 バイト** と予想（LDA immediate = 2〜3B、STA absolute = 3B、その他 1〜3B）
- 実測は Pre-3 で確定させる（想定値はレンジのみ記載）

### 2.3 Dhrystone 絶対ゲートは不変

| 項目 | 変更前（Makefile v1.1） | 変更後（Makefile v1.2） |
|---|---|---|
| Dhrystone 絶対ゲート | 819/48785/P:20 | **819/48785/P:20 変更なし** |

**根拠**：Dhrystone は kernel を積まない C 単体プログラム（`dhry_timer.c` +
`startup_harness23_v17.asm`）で構成される。kernel v12.7/v12.8 の差異は
Dhrystone の実行経路に影響しない。

### 2.4 HARNESS は変更なし

Makefile v1.1 で既に `startup_harness23_v17.asm` に更新済み（CHAT122 対応）。

## 3. 判定基準（Acceptance Criteria）

### 3.1 Pre 工程全体の合格判定

Pre-3〜Pre-6 の全ステップが以下を満たすこと：

- **AC-1**：Pre-3 で kernel v12.8 を組み込んだ yuios.bin が emu23 v1.11 上で
  「初回タイマー IRQ0 発火 → ACK 3 命令実行 → 次回タイマー IRQ0 発火」の
  再武装連鎖が観測できる（emu23 の -q --disk 実行完走で代替、詳細は Pre-4 で規定）
- **AC-2**：Pre-4 で emu23 上の yuios 動作ログが v12.7 版と機能的に等価
  （UART 出力列・完走時 cycle 数の許容範囲内一致）※ 詳細判定基準は Pre-4 で規定
- **AC-3**：Pre-5 で新リファレンス `yuios_ref_road2_I4.bin` が凍結され、
  ファイル名・ハッシュ値・ビルド時 KERN_SRC 版数が明記された凍結票が
  作成されること
- **AC-4**：Pre-6 で Makefile v1.2 の `regress` ターゲット実行が新絶対ゲート値で
  PASS すること

### 3.2 V8-b 着手条件（Pre 工程完了条件）

- Pre-7 のレビューが完了していること
- Pre 工程 Pre-1〜Pre-6 の全成果物（設計書 v1.1 化・Makefile v1.2・
  新リファレンス・凍結票）が揃っていること

## 4. 工程分割理由（原則87 系）

「kernel v12.7 → v12.8 化」と「V8-b（yuios.bin RTL 実行）」を **同一工程で
実施しない**のは、失敗時の弁別可能性を確保するため。

同時実施した場合：
- V8-b で失敗した際、「v12.8 実装バグ」／「RTL バグ」／「新絶対ゲート値誤設定」
  のいずれが原因か切り分け不能。

分割実施の場合：
- Pre 工程で emu23 上の v12.8 動作を確定 → RTL 側の凍結との整合が確認できた
  状態で V8-b に着手 → V8-b で失敗した場合は「RTL vs emu23 の差」に切り分けが
  絞られる（原則87：弁別能力のある負例）。

## 5. Pre 工程の分割

| Step | 内容 | 成果物 | 完了判定 |
|---|---|---|---|
| Pre-1 | 本設計書起票・レビュー承認取得 | `kernel_v12_8_migration_design_v1_0.md`（本書） | ユーザレビュー承認 |
| Pre-2 | Makefile v1.1 → v1.2 改版（`KERN_SRC` 切替のみ・絶対ゲート値は仮のまま） | `Makefile` v1.2（中間版） | 版数ヘッダ更新完了 |
| Pre-3 | v12.8 でクリーンビルド → 新 yuios.bin サイズ確定 | `yuios_road2.bin`（新サイズ） | ビルド完走・サイズ確定 |
| Pre-4 | emu23 v1.11 で動作確認（yuios 起動シーケンス確認） | 動作ログ | AC-1 / AC-2 満足 |
| Pre-5 | 新リファレンス凍結（`yuios_ref_road2_I4.bin`）＋凍結票 | 新リファレンス・凍結票 | AC-3 満足 |
| Pre-6 | Makefile v1.2 の `REF_BIN` 更新・絶対ゲート値更新 → `regress` 実行 | `Makefile` v1.2（最終版） | AC-4 満足 |
| Pre-7 | 設計書 v1.0 → v1.1 化（実測値・確定値の反映）＋レビュー | `kernel_v12_8_migration_design_v1_1.md` | レビュー完了 |

Pre-2〜Pre-6 は Pre-1 のレビュー承認後に着手する。

## 6. リスクと対策（活動KY 補足）

| リスク | 影響 | 対策 |
|---|---|---|
| R-1: v12.8 のビルド自体が失敗 | Pre-3 で足止め | HANDOVER §0.1 で「再ビルド想定」明示。ビルド手順書 v1.11（Makefile v1.1 対応）が既に存在するため、Makefile v1.2 化（KERN_SRC 切替 1 行）だけで手順書再改版は不要と判断（手順書自体の記述に KERN_SRC 具体名は現れないため）。ただし Pre-6 完了時に手順書再確認は必要。 |
| R-2: v12.8 の emu23 動作が v12.7 と機能的に異なる | Pre-4 で判定不能 | emu23 v1.10 以降は V5 TCR-ACK 前提のため、v12.8 が正常動作するのが本来の設計。v12.7 は「emu23 上でも本来は動かないはずが、動いていた」可能性（IRET 経路が emu23 側で残っていた等）を Pre-4 で調査。 |
| R-3: 新 yuios サイズがメモリマップに収まらない | Pre-3 完了不能 | v12.7 = 56,416B、+9B 想定でも 56,425B。yuios メモリマップの kernel 領域上限（現状は要確認）内に十分収まる想定。Pre-3 で実測時に上限を再確認。 |
| R-4: リファレンス凍結手順の記録漏れ | 再現性喪失 | Pre-5 で凍結票（ハッシュ・ビルド時ツール版数・KERN_SRC 版数）を明示的に作成。 |

## 7. 本設計書の版数改訂ルール

- Pre-3〜Pre-6 完了時点で **v1.0 → v1.1** に改訂（実測値の反映）
- 改訂時は本文と改訂履歴サマリの数値整合を必ずチェック（KY41 第5点）
- 前版の情報は欠落させない（改訂履歴に必ず経緯を残す）

## 8. 未確定事項（ユーザ相談事項）

- **Q-A**：v12.8 の実装は 2026-07-13 に emu23 上では動作確認済とのことだが、
  当時のログや凍結票は残存しているか？ 残存していれば Pre-4 の一部を省略可能。
- **Q-B**：`yuios_ref_road2_I3.bin`（v12.7 由来リファレンス）は「凍結退避品」
  として残しておくべきか、廃棄してよいか？（原則：前版情報を欠落させない
  =保持推奨）
- **Q-C**：Pre-3 のビルドは Makefile v1.2 中間版で実施予定だが、環境上
  `force` `hasm23` `lnk23` `scc23` `emu23` の各バイナリは
  プロジェクトナレッジに登録された状態で使えるか？（ソースからの再ビルドが
  必要な場合は Pre-2 前に追加ステップが必要）

以上
