# YUI OS メモリマップ再設計書（Ph.3.5-b 〜 Ph.5/Ph.6 高位再配置）

**Version:** 2.0（ドラフト）
**作成日:** 2026-05-14
**最終改版:** 2026-06-12
**作成者:** Claude
**対象フェーズ:** Ph.3.5-b カーネル基盤改訂（初版）／**Ph.5 ProcMgr・Ph.6 Shell 同居に向けたコード成長余地確保（v2.0 案A 高位集約）**
**前提文書:**
- `yuios_ipc4_pool_design_v1_1.md`（FIX 済）
- `yuios_tcb_design_v1_1.md`（FIX 済）
- `emu23_device_design_v1_2.docx`（MMIO アドレスマップ根拠）
- `yuios_design_v2_6.md`（Ph.5 ProcMgr・MemMgr 仕様の上位設計）【v2.0 追加】
- `yuios_cproc_layout_design_v1_1.md`（C-process 区画 $C040/$C380/$C680、承認済）【v2.0 追加】
- `ysd8800_kern.tgt`（DATA-START 実体の真値・KY34）【v2.0 追加】
- `HANDOVER_CHAT51.md`（v2.0 方向性=案A 確定の実測根拠）【v2.0 追加】
**ステータス:** **ドラフト（v2.0・有識者レビュー前）** ／ 旧 v1.7 までは確定

> **【v2.0 改版方針（KY41 厳守）】** 本書は v1.7 の内容を一切削除せず、追加・注記のみで改版する。旧情報は取り消し線（~~...~~）で保持し、是正・新値を併記する。v1.7 までの記述は Ph.3.5-b 時点の設計史として有効である。v2.0 で変更されるのは主に §4.1 全体マップ・§4.5 成長予約・§5.2 MemMgr 定数・§5.4/§5.5 バッファ/lnk23 セクション・§8 実装変更点であり、v2.0 固有の確定内容は新設の **§14（v2.0 案A 高位再配置）** に集約する。各旧セクションには §14 への参照を付す。

---

## 改版履歴

| 版 | 日付 | 内容 | 作成者 |
|---|---|---|---|
| v1.0 | 2026-05-08 | 初版作成（Ph.3.5-b、IPC4 Pool 設計 v1.1 + TCB 設計 v1.1 を前提） | Claude |
| v1.2 | 2026-05-14 | Step 3完了後の改版（動作確認後）：CALLSTK_TOP/DATASTK_TOPの計算値を$F07F/$F87F → $F07E/$F87Eに修正（奇数アドレスALIGNMENT例外防止・案1採用） | Claude |
| v1.1 | 2026-05-08 | レビュー指摘（review15.txt / review16.txt）反映：①MMIO サイズ修正（384B→896B）、②stack guard 領域新設（$FC00-$FC3F, 64 B）、③KERN_SP 配置確定（案 B：$4700-$477F 専用カーネルスタック、$4700 canary $A55A）、④Forth growth 不足可能性注記、⑤stack watermark 診断要件記載（実装は Ph.3.5）、⑥「ROM 性質」を「write 禁止前提領域」に表現変更、⑦カーネルワーク領域を frequently used / reserved growth に分割、⑧guard 初期化と診断ワード `CHK-GUARD` を設計記載 | Claude |
| v1.3 | 2026-05-20 | Ph.4 FileMgr 設計書（yuios_ph4_filemgr_design_v1_2.md）の改版要請 M1/M2 を反映：①カーネル成長予約を `$4800-$4FFF`（2 KB）→ `$4800-$50FF`（2304 B）へ拡張（FileMgr 専用領域 2144 B が 2 KB を 96 B 超過するため）、②Forth 辞書先頭を `$5000` → `$5100` へ繰り下げ、③FileMgr 専用領域（$4800-$505F、2144 B）をアドレス対照表に正式登録 | Claude |
| v1.7 | 2026-06-06 | **Ph.5 ProcMgr（案C）同期：** ①$C000-$DFFF（8 KB Forth 成長予約）を再分割 → ProcMgr ロード領域 $C000-$C7FF（2 KB）＋ MemMgr 固定ページプール $C800-$DFFF（6 KB/24 ページ）、②§4.5 Forth 成長予約を廃止し将来対処（§4.5.1・v2.0 再配置）を明記、③§5.2 に MemMgr ビットマップ案C 仕様を集約（PAGE-BMP-LO/HI=$FC40/$FC42・初期値 LO=$0000/HI=$FFF0・bit 割当 ユーザ20/予約4/無効8・POOL-BASE=$C800/TOTAL=24/USER-MAX=20）、④§5.5 DATA-START を实体 .tgt 準拠で $C000→$E000 に修正（v1.6 机上値の誤り訂正・KY34）、⑤K26 改訂＋K35/K36 新設。yuios_design v2.6 確定（v2_6_review_v1_0 条件付き承認）に対応 | Claude |
| v1.4 | 2026-05-24 | **Step 5-1 真因②修正反映：**Force コンパイラの 16bit 乗算ランタイム `_FMUL_A/B/R` を `$4232-$4236`（TCB[7] フィールドと衝突）から `$47B0-$47B5`（frequently used 領域内の空き 12B のうち先頭 6B）に正式登録。TCB プール拡張（16タスク化、$4000-$44FF）と Force ハードコードの整合性破綻による tid=7 TCB 破壊バグを恒久対策。Force v1.5 / codegen v1.5 にて実装済み。詳細は §3.5 および K35 参照 | Claude |
| **v1.5** | **2026-05-29** | **MMIO 表の欠落補完（Force コンパイラ メモリ使用契約書 v1.0 レビュー時に発見・M1）：**①§4.2 MMIO 表に YSD8002 タイマー（$FC90-$FC9E、TCR/PERIOD/CYCLE/SCORE/SW_RUNS）の行を追加。②既存の YSD8001 UART・YSD8003 ストレージ・YSD8004 IRQ コントローラの記載粒度を `emu23_device_design_v1_2.docx` MMIO アドレスマップに揃え、$FC80-$FFFF をデバイス別に完全分割。③$FF00-$FF10 MMU（特権モード専用）の行を追加。Force メモリ使用契約書 v1.1 §4.3 の MMIO 細分化と同期。本版で MMIO アドレスマップの設計書側 single source of truth が確立 | Claude |
| **v1.6** | **2026-06-01** | **コンテキストスイッチ A/Bレジスタ復元漏れ修正（kernel v0.12.7 / 設計書 yuios_ctxsw_abreg_design_v1_1.md）反映：**①frequently used 領域に `IRQ_WK_B`（$47B6・2B）を新設。IRQ0_HANDLER のプリエンプティブ・コンテキストスイッチで中断時 B レジスタを退避するために確保（従来 A/X のみ退避・B 非保護だったバグの恒久対策）。②§3.5 の Force ランタイム残余空きを `$47B6-$47BF`(10B) → `$47B8-$47BF`(8B) へ縮小。③ワーク変数オフセット対照表に IRQ_WK_B 行を追加。Force メモリ使用契約書 v1.2 §4.3 と同期 | Claude |
| **v2.0** | **2026-06-12** | **【案A 高位再配置・ドラフト】Ph.6 Shell 実装でのコード成長破綻を予防する全体再配置。**①§14 新設＝v2.0 確定内容を集約（案A 高位集約・実測値ベース）。②Forth コード天井を $C7FF→$DBFF へ引き上げ、コード余地 1690B→6810B(6.65KB) を確保。③DATA-START を **$E000→$DC00**（tgt 1 行・Force 再ビルドで VARIABLE 自動追従）。④MemMgr ページプールを **$C800-$DFFF/24pg → $DD00-$ECFF/16pg** へ高位集約縮小。⑤テストバッファ大を **$EC00-$EFFF → $ED00-$EFFF** へ縮小。⑥**PAGE-BMP-LO-INIT=$F000 を新設必須**と認定（16pg 構成では OS 予約4pg が LO 側 page12-15 に来るため、現行 BMP-INIT の LO=0 ハードコードを是正。CHAT51 §2.4 の見落とし事項を本版で明文化）。⑦MemMgr 定数 4→**5 定数**（PAGE-POOL-BASE=$DD00/PAGE-TOTAL=16/PAGE-USER-MAX=12/PAGE-BMP-LO-INIT=$F000/PAGE-BMP-HI-INIT=$FFFF）。⑧**v1.7 机上値の誤記是正（KY34）**：ワークスペース $E000-$EBFF は tgt 実体 $F000-$F7FF が真値。⑨実測値（コード終端 $C165・PAGE-POOL 常駐使用 0pg・DATA 実消費 84B）を設計入力として §14.2 に明記。⑩回帰計画（Dhrystone＋OS 全タスク／FileMgr 含む）を §14.6 に明記。⑪K38/K39 新設。`HANDOVER_CHAT51.md`（F55 検証成功・案A 確定）に対応。**※本版はドラフト＝有識者レビュー・承認前。承認まで実装着手しない（工程規律・原則43）** | Claude |

---

## 1. 文書の目的とスコープ

### 1.1 目的

本書は、Ph.3.5-c で確定した **MAX_TASKS = 16 / POOL_SIZE = 32** の前提に基づき、YUI OS の物理メモリ配置を再設計する。

### 1.2 解決すべき問題

`HANDOVER_CHAT22.docx` および現行 v1.2 メモリマップで露呈した以下の問題を恒久的に解決する：

1. **タスクスタック領域の物理衝突**（v0.11.3 で暫定修正済、本書で正式設計）
   - 旧設計：CALL 256 + DATA 256 = 512B 必要、GAP は $100 のみ
   - tid=N のデータ範囲と tid=N+2 のコール範囲が完全重なり
2. **TCB プール容量不足**（8 タスク×80B → 16 タスク×80B に拡大必要）
3. **MsgPool（IPC4 Pool 方式）の配置領域未確保**（512 B 新規必要）
4. **カーネル変数領域と Forth 領域の境界曖昧化**（review12.txt 指摘）
5. **MMIO 境界の暗黙化**（`$FC80-` 以降を「絶対 RAM 利用禁止」と明示する必要）

### 1.3 スコープ

**本書に含むもの：**
- アドレス空間全体（$0000-$FFFF）の領域分割と境界確定
- 各領域の用途・容量・成長余地
- 新旧アドレス対照表
- 既存コード／Forth 定数の修正箇所網羅
- スタック worst-case 解析

**本書のスコープ外：**
- TCB レイアウトの変更（→ TCB 設計 v1.1 で確定済）
- IPC4 アルゴリズム（→ IPC4 Pool 設計 v1.1 で確定済）
- Ph.4 以降の FileMgr/Shell/libc の具体実装（領域**予約のみ**確保）

### 1.4 設計上の前提

YSD8800 の現状から：
- **総アドレス空間：** $0000-$FFFF（64 KB）
- **MMIO：** $FC80-$FFFF（128 B）固定、これ以降は RAM 利用禁止
- **ベクタテーブル：** $0000-$000F（16 B、ROM 性質、変更不可）
- **カーネルコード（Force）：** ROM 領域として扱う（$0010 以降）

---

## 2. レビュー指摘の反映ポイント（review12.txt / review14.txt）

| # | レビュー指摘 | 反映箇所 |
|---|---|---|
| 1 | 論理領域でなく**最大消費量**で設計せよ | §4 各領域容量、§6 stack worst-case 解析 |
| 2 | 将来増殖領域を**予約**として確保せよ | §4 「kernel growth」「Forth growth」領域、§7 |
| 3 | MMIO 境界を**固定化**せよ（$FC80- 絶対 RAM 禁止） | §4.2、§9 KY |
| 4 | カーネル領域と Forth 領域を**明確分離** | §4.1 全体マップ |
| 5 | stack worst-case（IPC CALL depth、IRQ nest、Forth temporary、compiler runtime）込みで積算 | §6 |
| 6 | MsgPool 配置（512B）を明示 | §4.3、§5 |

---

## 3. 設計方針

### 3.1 全体方針

1. **「最大消費量」ベースで領域を確保する**（論理的な区切りでなく実消費量）
2. **将来増殖が確実な領域（Forth 辞書、カーネル変数）には成長予約を付与する**
3. **MMIO は $FC80- に固定、それ以降は絶対 RAM 利用禁止**（FPGA 実装時の制約とも整合）
4. **既存 API 互換性を最大限維持**（Forth `CONSTANT` の値は再配置時のみ変更）
5. **アドレス対照表で機械的な修正を可能にする**（網羅性の保証）

### 3.2 領域分離原則

レビュー指摘 #4 を反映し、以下の 3 ゾーンに分割：

| ゾーン | 範囲 | 性質 |
|---|---|---|
| **カーネルコード**（ROM 性質） | $0000-$3FFF | 実行コード、変更不可 |
| **カーネルデータ**（RAM、固定） | $4000-$50FF | TCB / カーネル変数 / MsgPool / カーネル予約 / FileMgr 専用領域【**v1.3拡張**】 |
| **Forth ゾーン**（RAM、可変） | **$5100**-$EFFF | Forth 辞書、Forth ワーク、テストバッファ【**v1.3 先頭 $5000→$5100**】 |
| **タスクスタック**（RAM、固定） | $F000-$FBFF | 16 タスク × 2 スタック |
| **OS 共有変数**（RAM、固定） | $FC00-$FC7F | ドライバ変数、IPC 共有等 |
| **MMIO**（I/O） | $FC80-$FFFF | UART / SD / IRQ コントローラ等 |

「カーネル」と「Forth」を**完全に別ゾーンに分離**することで、片方の成長が他方を侵食する事故を防ぐ。

### 3.5 Force コンパイラ ランタイムワーク領域（★v1.4 新設）

#### 3.5.1 背景

Force Forth Cross Compiler が出力するコードには、16bit 乗算（`*` 演算子）を実行時に処理するランタイムルーチン `_FORCE_MUL` が含まれる。このルーチンは作業用に 3 ワード（6 B）の固定アドレス領域 `_FMUL_A / _FMUL_B / _FMUL_R` を必要とする。

v1.3 以前（Force v1.3-v1.4）は、当時の旧メモリマップ（カーネルワーク変数 $4200-$4230 の直後が空き）を前提に **`$4232-$4236`** にハードコードされていた。

#### 3.5.2 問題（顕在化バグ：HANDOVER_CHAT32 真因②）

memmap v1.3 で TCB プールが **16 タスク化** され `$4000-$44FF`（1280 B）に拡張された結果、

| アドレス | Force ランタイム | TCB[7] = tid=7 フィールド |
|---|---|---|
| $4232 | _FMUL_A | **TCB[7].PC** |
| $4234 | _FMUL_B | **TCB[7].SP** |
| $4236 | _FMUL_R | **TCB[7].DSP** |

が **完全に衝突**。Forth プログラム中の `*` 実行で tid=7 の TCB が破壊され、スケジューラが READY 状態の不正 TCB[7] を選んで暴走する致命バグが Ph.4 Step 5-1 で顕在化した。

#### 3.5.3 v1.4 確定配置

Force ランタイム領域は frequently used カーネルワーク領域内に正式登録：

| シンボル | アドレス | サイズ | 用途 |
|---|---|---|---|
| `_FMUL_A` | **$47B0** | 2 B | `_FORCE_MUL` 引数 A（被乗数） |
| `_FMUL_B` | **$47B2** | 2 B | `_FORCE_MUL` 引数 B（乗数） |
| `_FMUL_R` | **$47B4** | 2 B | `_FORCE_MUL` 結果格納 |

合計 6 B。frequently used 領域 `$4780-$47BF` 内の従来の空き 12 B（$47AE-$47BF）のうち、$47AC/$47AE を MSG_POOL_FREE/IPC4_WK_IDX が既に使用済みのため、$47B0-$47B5 を Force ランタイム専用として確保。さらに v1.6（kernel v0.12.7）で $47B6 を IRQ_WK_B（コンテキストスイッチ B 退避用・カーネル占有）として確保。残余空きは $47B8-$47BF（8 B）。

#### 3.5.4 整合性運用ルール

**今後 Force コンパイラのランタイムワーク領域を追加・移動する際は、必ず本設計書のアドレス対照表を更新すること。** カーネルワーク領域 ($4780-$47BF) は Force ランタイムも含めて一元管理し、TCB プール領域 ($4000-$44FF) との衝突を避ける。

将来 Force に除算ランタイム等を追加する場合は、$47B8-$47BF（残 8 B）に配置するか、$47C0-$47FF（reserved growth）から確保すること。$47B6 は IRQ_WK_B（カーネル占有）のため Force は使用不可。

---

## 4. 新メモリマップ（v1.1 確定版）

### 4.1 全体マップ

**v1.1 で確定**：案 J（stack guard 64 B 新設）＋ KERN_SP 専用確保 ＋ MMIO サイズ修正。

| アドレス範囲 | サイズ | 用途 | 種別 | 成長方向 |
|---|---|---|---|---|
| $0000–$000F | 16 B | ベクタテーブル | write 禁止前提 | 固定 |
| $0010–$0FFF | 4 KB | カーネルコード本体（kernel.asm） | write 禁止前提 | 上方向（必要に応じ拡張） |
| $1000–$1FFF | 4 KB | カーネルコード拡張領域（IRQ ハンドラ等） | write 禁止前提 | 上方向 |
| $2000–$3FFF | 8 KB | **カーネルコード成長予約** | write 禁止前提 | (予約) |
| $4000–$44FF | 1280 B | **TCB プール（16 タスク × 80 B）** | RAM | 固定 |
| $4500–$46FF | 512 B | **MsgPool（32 entry × 16 B）** | RAM | 固定 |
| **$4700–$477F** | **128 B** | **カーネル専用スタック（KERN_SP）** ★v1.1 新設 | RAM | 下方向 |
| **$4780–$47BF** | **64 B** | **カーネルワーク変数（frequently used: L1_WK / IRQ_WK / IPC4_WK 等）** ★v1.1 細分 | RAM | 上方向 |
| **$47C0–$47FF** | **64 B** | **カーネルワーク予約（reserved growth）** ★v1.1 新設 | RAM | (予約) |
| $4800–$50FF | **2304 B** | **カーネル成長予約**（FileMgr 専用領域 $4800-$505F 含む）【**v1.3拡張**】 | RAM | (予約) |
| **$5100**–$BFFF | **27 KB** | Forth 辞書（Dictionary）【**v1.3 先頭 $5000→$5100**】 | RAM | 上方向 |
| $C000–$C7FF | 2 KB | **ProcMgr ロード領域**（Phase 1 単一プロセス・$C000 固定エントリ） | RAM | 固定 【**v1.7 案C新設**】【**v2.0: 不変**（C-process 区画 $C040/$C380/$C680 は yuios_cproc_layout_design v1.1 で確定。§14.3 参照）】 |
| ~~$C800–$DFFF~~ | ~~6 KB~~ | ~~**MemMgr 固定ページプール（24 ページ×256 B）**~~ | ~~RAM~~ | ~~固定 【**v1.7 案C：旧 Forth 成長予約 8 KB を縮小転用**】~~ → **【v2.0 案A】下記 §14 のとおり PAGE-POOL を $DD00-$ECFF/16pg へ高位集約。$C800-$DBFF はコード成長余地へ転用** |
| ~~$E000–$EBFF~~ | ~~3 KB~~ | ~~Forth ワークスペース（HERE 等）~~ | ~~RAM~~ | ~~上方向~~ **【v2.0 KY34 是正】この行は v1.7 の机上誤記。tgt 実体では $E000- は VARIABLE データ領域（DATA-START）であり、ワークスペース(HERE/stack)の実体は $F000-$F7FF。§14.2/§14.7 参照。v2.0 では DATA-START を $DC00 へ移動** |
| **【v2.0 案A 確定配置・以下を正とする】** | | （詳細は §14.3 配置表） | | |
| $5100–$DBFF | 約 35 KB | **Forth 辞書（Dictionary）天井を $DBFF へ引き上げ** | RAM | 上方向 【v2.0】 |
| $DC00–$DCFF | 256 B | **DATA（VARIABLE データ領域・DATA-START）** | RAM | 上方向 【v2.0: tgt $E000→$DC00】 |
| $DD00–$ECFF | 4 KB | **MemMgr 固定ページプール（16 ページ×256 B）** | RAM | 固定 【v2.0 案A】 |
| $ED00–$EFFF | 768 B | **テストバッファ（縮小）** | RAM | 固定 【v2.0: $EC00→$ED00】 |
| $F000–$F7FF | 2 KB | **コールスタック領域（16 タスク × 128 B）** | RAM | 下方向 |
| $F800–$FBFF | 2 KB | **データスタック領域（16 タスク × 128 B）** | RAM | 下方向 |
| **$FC00–$FC3F** | **64 B** | **stack guard 領域（$A55A 埋め、MMIO 突入防止）** ★v1.1 新設 | RAM | 固定 |
| **$FC40–$FC7F** | **64 B** | OS 共有変数（UART/STOR ドライバ変数等、scheduler 共有） ★v1.1 縮退 | RAM | 固定 |
| $FC80–$FFFF | **896 B** ★v1.1 修正 | MMIO（UART / SD / IRQ コントローラ等） | I/O | **絶対 RAM 禁止** |

**合計内訳（v1.3 修正）：**
- write 禁止前提領域: 16 KB（コード 8 KB + 予約 8 KB）
- カーネル RAM: **4.25 KB**（TCB 1280 B + MsgPool 512 B + KERN_SP 128 B + ワーク 64 B + 予約 64 B + カーネル成長予約 **2304 B**）【**v1.3: 2KB→2304B**】
- Forth RAM: **39.75 KB**（辞書 **27 KB** + 予約 8 KB + ワーク 3 KB + テスト 1 KB）【**v1.3: 28KB→27KB**】
- スタック RAM: 4 KB（コール 2 KB + データ 2 KB）
- stack guard: 64 B（v1.1 新設）
- OS 共有 RAM: 64 B（v1.1 縮退）
- MMIO: **896 B**（v1.1 修正、$FFFF - $FC80 + 1 = $0380 = 896）

**v1.1 重要設計判断：**

1. **「write 禁止前提領域」表現**（review16.txt 中程度④）：旧表記「ROM」は emu23 上では物理的に RAM として実装されているため誤解を生む。FPGA 化時には ROM 化候補だが、現状は「write 禁止のお約束」で扱う領域として表現を厳密化。
2. **stack guard 領域 $FC00-$FC3F**（review16.txt 重大②）：データスタックと OS 共有変数の間に 64 B のガードを設置。データスタックオーバーフロー時に即 MMIO 突入することを防ぐ。$A55A 埋めで初期化し、診断ワード `CHK-GUARD`（§7 参照）で破壊検出可能。
3. **KERN_SP 専用領域 $4700-$477F**（review16.txt 論点 2）：旧設計の「タスクスタック流用」を完全に排除。kernel context と task context の物理分離により、SYSCALL / nested IRQ / panic dump / VFS 等の将来拡張に備える。
4. **カーネルワーク領域の細分**（review16.txt 推奨）：frequently used (64 B) と reserved growth (64 B) に分けることで、将来のワーク変数追加時の整理が容易に。
5. **MMIO サイズ計算の正確化**：$FFFF - $FC80 + 1 = 896 B。FPGA 実装時のアドレスデコーダ設計の前提となるため、正確な値を記載。

### 4.2 MMIO 境界（$FC80-）の固定化

レビュー指摘 #3 を反映し、以下を**設計原則**として確定：

> **$FC80 以降は MMIO 専用領域。RAM として使用してはならない。**

具体的なデバイス配置（`emu23_device_design_v1_2.docx` MMIO アドレスマップに準拠、v1.5 で完全網羅化）：

| アドレス範囲 | サイズ | デバイス | 用途 |
|---|---|---|---|
| $FC80-$FC8F | 16 B | YSD8001 UART | UART_TX($FC80) / UART_RX($FC82) / UART_STAT($FC84) ほか |
| **$FC90-$FC9E** | **15 B** | **YSD8002 タイマー** | **TCR / PERIOD / CYCLE / SCORE / SW_RUNS（★v1.5 追加）** |
| $FCA0-$FCB1 | 18 B | YSD8003 ストレージ I/F | SD_CMD / SD_STAT / SD_LBA / SD_DATA ほか |
| $FCB2-$FCB5 | 4 B | YSD8004 割り込みコントローラ | IRQ_STAT($FCB2) / IRQ_MASK($FCB4) |
| $FCB6-$FEFF | 586 B | (予備) | 将来デバイス用 |
| **$FF00-$FF10** | **17 B** | **MMU（特権モード専用、★v1.5 追加）** | **ページテーブルレジスタ・MCR** |
| $FF11-$FFFF | 239 B | (予約) | MMIO 予約領域 |

**注**: 本表は `emu23_device_design_v1_2.docx` MMIO アドレスマップの設計書側 single source of truth である。Force コンパイラ メモリ使用契約書 v1.1 §4.3 の禁止領域表もこの表と同期している（KY19 対応）。

FPGA 実装時のアドレスデコーダもこの境界に整合させる。

### 4.3 MsgPool 専用領域の新設

IPC4 Pool 設計 v1.1 で確定した 32 entry × 16 B = 512 B のプールを、**TCB プール直後**に配置：

```
$4000  ┌──────────────────────────┐
       │ TCB プール (1280 B)      │  16 task × 80 B
$44FF  └──────────────────────────┘
$4500  ┌──────────────────────────┐
       │ MsgPool (512 B)          │  32 entry × 16 B
$46FF  └──────────────────────────┘
$4700  ┌──────────────────────────┐
       │ カーネルワーク変数 (256 B)│
$47FF  └──────────────────────────┘
```

設計判断：
- **TCB と MsgPool を隣接**：cache locality（YSD8800 にキャッシュは無いが、将来 FPGA 拡張時に有利）
- **MsgPool 先頭 = $4500**：16 B 境界に揃い、tid×16 のインデックス計算が簡素
- **境界 $46FF / $4700 は明確**：枯渇診断時のメモリダンプが見やすい

### 4.4 カーネル成長予約領域の意義

**【v1.3 更新】** カーネル成長予約を **`$4800-$50FF`（2304 B）** へ拡張した。Ph.4 FileMgr 設計書（yuios_ph4_filemgr_design_v1_2.md §5.2）で確定した FileMgr 専用領域 2144 B（`$4800-$505F`）が v1.2 の 2 KB 予約を 96 B 超過するため、案1（推奨）に従い 256 B 拡張する。Forth 辞書先頭を `$5000` → `$5100` へ繰り下げることで 256 B を吸収する。

`$4800-$50FF`（2304 B）を「カーネル成長予約」として確保。  
review12.txt / review14.txt で挙げられた将来需要（v1.2 より継続）と Ph.4 で確定した FileMgr 専用領域：

| 想定用途 | 推定サイズ | 状態 |
|---|---|---|
| **FileMgr 専用変数・バッファ（$4800-$505F）** | **2144 B** | **【v1.3 確定・正式登録（§5.1 追加）】** |
| Scheduler work（priority queue 等） | ~64 B | 予約 |
| Inode cache（将来） | ~256 B | 予約 |
| Block cache（将来） | ~512 B | 予約 |
| その他将来用途 | ~残余 | 予約 |

→ **v1.3 の確保 2304 B に対し FileMgr 使用 2144 B、残余 160 B**。将来の小規模追加には対応可能。本格的な inode cache 等は Ph.5 以降の設計書改版時に改めて検討する。

### 4.5 Forth 成長予約領域【v1.7 案C により廃止・$C000-$DFFF を再分割】

**【v1.7 重要変更・案C】** v1.6 まで `$C000-$DFFF`（8 KB）を「Forth コード成長予約」として確保していたが、Ph.5 ProcMgr 設計（yuios_design v2.6 §8.4・案C）に伴い**本領域を以下へ再分割し、Forth 成長予約は廃止**する：

| アドレス | サイズ | 新用途 |
|---|---|---|
| `$C000-$C7FF` | 2 KB | **ProcMgr ロード領域**（Phase 1 単一プロセス固定） |
| `$C800-$DFFF` | 6 KB | **MemMgr 固定ページプール（24 ページ×256 B）** |

**廃止可能の根拠：** 辞書本体は **$5100-$BFFF**（27 KB、v1.3 で先頭を $5000→$5100 に繰り下げ）で確保済みであり、$C000- の成長予約は**現時点で未使用**（lnk23 リンクログで HERE 値が $BFFF を超えていないことを確認・K26）。したがって本領域を ProcMgr/MemMgr へ転用しても現行辞書を侵食しない。

**成長予約消失（8 KB→0）への対処：** review12.txt が想定した FileMgr(~3KB)/Shell(~2KB)/libc(~2KB) 等の伸び代を本転用で失うため、辞書逼迫時は **memmap v2.0 で再配置**する（§4.5.1）。Ph.5/Ph.6 時点では辞書 $5100-$BFFF（27 KB）の余裕で吸収する想定。

#### 4.5.1 辞書逼迫時の将来対処（v2.0 予定）
辞書が $BFFF に接近した場合、(a) カーネル成長予約 $4800-$50FF の余剰移譲、(b) MMU 導入後のバンク切替、(c) ProcMgr ロード領域の Phase 2 仮想化による物理領域解放、のいずれかで対応する。lnk23 リンクログの HERE 監視（K26）を継続する。

> **【v2.0 で実施】** 本項が予告した「v2.0 再配置」を本書 §14（案A 高位再配置）で具体化した。採った手段は上記 (a)〜(c) のいずれでもなく、**DATA/PAGE-POOL/テストバッファを高位へ集約し、辞書天井を $C7FF→$DBFF へ引き上げる**方式である。理由は §14.1 参照（PAGE-POOL の起点を上げない限り辞書天井は上がらないため、領域の高位集約が本質的解決となる）。これによりコード余地は 1690B→6810B(6.65KB) に拡大し、Ph.6 Shell＋縮小版 FileMgr 試験の同居（目標 +6〜8KB の下限）に到達する。

**【v1.1 注記：将来不足可能性】**（review15.txt 中程度①）
本見積もり（合計 ~8 KB）は楽観的である可能性がある。Forth は辞書膨張しやすく、特に：
- debug ワード群（dump、trace、breakpoint 等）
- 文字列処理ワード（string、format、parse）
- パス処理ワード（path、glob、resolve）
- パーサ関連ワード

が増えると 8 KB 予約を超過する可能性がある。
Ph.4 以降の FileMgr / Shell 実装時に予約が逼迫したら、メモリマップ設計書 v2.0 で再配置を検討する。具体的には：
- カーネル成長予約（$4800-$4FFF, 2 KB）の余剰部分を Forth 側に移譲する
- テストバッファ（$EC00-$EFFF, 1 KB）を非検証時には Forth 領域として使用可能にする

辞書 HERE 値の監視（lnk23 リンクログ／Forth 起動時 HERE 出力）で枯渇を早期検出する（§9 K26 参照）。

---

## 5. アドレス対照表（旧 v1.2 → 新 v1.1）

### 5.1 カーネル変数

| 用途 | 旧（v1.2 / kernel_v11.3） | 新（v1.1） | 備考 |
|---|---|---|---|
| TCB プール | $4000-$427F | **$4000-$44FF** | 16 タスク化で拡大（+640 B） |
| **カーネル専用スタック（KERN_SP）** | （タスクスタック流用 $FBCE） | **$4700-$477F**（128 B） | ★v1.1 新設、$4700 に canary $A55A |
| カーネルワーク (L1_WK_A〜) | $4280-$42A1 | **$4780-$47BB**（frequently used 領域内） | KERN_SP と分離、領域明確化 |
| IRQ1 ワーク | $42A2-$42AB | **$47A0-$47A9**（frequently used 領域内） | 同上 |
| MSG_POOL_FREE | — | **$47AA**（frequently used 領域内） | ★v1.1 新設 |
| カーネルワーク予約 | — | **$47C0-$47FF**（reserved growth 64 B） | ★v1.1 新設 |
| **MsgPool（新設）** | — | **$4500-$46FF** | IPC4 Pool 設計 v1.1 |

カーネルワーク変数の具体オフセット（v1.1 確定版、frequently used 領域 $4780-$47BF 内）：

| 名称 | 旧 | 新（v1.1） | サイズ |
|---|---|---|---|
| L1_WK_A | $4280 | **$4780** | 2 |
| L1_WK_B | $4282 | **$4782** | 2 |
| L1_WK_C | $4284 | **$4784** | 2 |
| L1_WK_TMP | $4286 | **$4786** | 2 |
| IRQ_WK_X | $4288 | **$4788** | 2 |
| IRQ_WK_A | $428A | **$478A** | 2 |
| SLP_WK_DSP | $428C | **$478C** | 2 |
| SLP_WK_PC | $428E | **$478E** | 2 |
| MISC_WK_X | $4290 | **$4790** | 2 |
| CUR_TASK | $4292 | **$4792** | 2 |
| NEXT_TASK | $4294 | **$4794** | 2 |
| TASK_COUNT | $4296 | **$4796** | 2 |
| TC_WK_ENTRY | $4298 | **$4798** | 2 |
| TC_WK_TID | $429A | **$479A** | 2 |
| IPC4_WK_X | $429C | **$479C** | 2 |
| IPC4_WK_DST | $429E | **$479E** | 2 |
| IPC4_WK_SRCTCB | $42A0 | **$47A0** | 2 |
| IRQ1_WK_A | $42A2 | **$47A2** | 2 |
| IRQ1_WK_B | $42A4 | **$47A4** | 2 |
| IRQ1_WK_X | $42A6 | **$47A6** | 2 |
| IRQ1_WK_BYTE | $42A8 | **$47A8** | 2 |
| IRQ1_WK_STAT | $42AA | **$47AA** | 2 |
| **MSG_POOL_FREE** | — | **$47AC** | 2 |
| **IPC4_WK_IDX** | — | **$47AE** | 2 |
| **_FMUL_A** ★v1.4 新設 | — | **$47B0** | 2 |
| **_FMUL_B** ★v1.4 新設 | — | **$47B2** | 2 |
| **_FMUL_R** ★v1.4 新設 | — | **$47B4** | 2 |
| **IRQ_WK_B** ★v1.6 新設 | — | **$47B6** | 2 |（IRQ0用B退避・kernel v0.12.7）
| **FileMgr 専用領域（新設）** | — | **$4800-$505F**（2144 B） | ★v1.3 新設。yuios_ph4_filemgr_design_v1_2.md §5.2 を参照 |
| **（残余予約：$5060-$50FF、160 B）** | — | （空き） | FileMgr 拡張用・将来予約 |
| **GUARD_BASE（新設、定数）** | — | **$FC00** | (定数) |
| **KERN_SP_TOP（新設、定数）** | — | **$477E**（カーネルスタック頂上） | (定数) |
| (frequently used 領域、$47B8-$47BF が空き) | — | (8 B 余裕) | — |
| (reserved growth 領域、$47C0-$47FF) | — | 64 B 予約 | — |

**FileMgr 専用領域 詳細（v1.3 新設・yuios_ph4_filemgr_design_v1_2.md §5.2 確定）：**

| アドレス | サイズ | シンボル名（Forth） | 用途 |
|---|---|---|---|
| $4800 | 2 B | FILEMGR-TID | FileMgr タスク ID（=4） |
| $4802 | 2 B | FS-MOUNTED | FS マウント状態（0=未マウント, 1=マウント済） |
| $4804 | 2 B | FS-NEXT-FREE | next_free_sec のメモリキャッシュ |
| $4806 | 2 B | FS-FILE-COUNT | file_count のメモリキャッシュ |
| $4808 | 2 B | FS-TOTAL-SEC | total_sectors のメモリキャッシュ |
| $480A | 2 B | FS-WR-OLD-NEXT | FILE_WRITE ロールバック退避（old_next_free） |
| $480C | 2 B | FS-WR-OLD-COUNT | FILE_WRITE ロールバック退避（old_file_count） |
| $480E | 2 B | FS-FSVER | マウント済 FS バージョン（上位=major 下位=minor） |
| $4810 | 16 B | (予約) | FS メタ情報拡張用 |
| $4820 | 64 B | FS-OPENTAB | オープンファイルテーブル（16B × 4 スロット） |
| $4860 | 512 B | FS-SECBUF | 汎用セクタバッファ（1 セクタ。READ/WRITE 中継用） |
| $4A60 | 1536 B | FS-DIRBUF | ディレクトリキャッシュ（3 セクタ = 全ディレクトリ） |
| **合計** | **2144 B** | **$4800-$505F** | **残余 $5060-$50FF（160 B）は将来予約** |

### 5.2 Forth 関連変数（OS 共有変数領域 $FC40-$FC7F、v1.1 で 64 B に縮退）

| 用途 | 旧 | 新（v1.1） | 備考 |
|---|---|---|---|
| PAGE-BMP-LO | $F010 | **$FC40** | OS 共有変数領域へ。**【v1.7 案C】bit0-15＝page0-15。初期値 $0000** |
| PAGE-BMP-HI | $F012 | **$FC42** | 同上。**【v1.7 案C】bit16-31。bit16-19＝page16-19（ユーザ）、bit20-23＝OS予約、bit24-31＝無効ページ。初期値 $FFF0** |
| MEM-TID-ADDR | $F014 | **$FC44** | 同上 |

**【v1.7 案C：MemMgr ページビットマップ仕様（集約）】** MemMgr プール（$C800-$DFFF・24 ページ×256 B）を `PAGE-BMP-LO`($FC40)＋`PAGE-BMP-HI`($FC42) の 16bit×2 変数（合成 32bit）で管理する。bit$n$ ↔ page$n$（page0=$C800〜page23=$DF00）。bit 割当: **bit0-19=ユーザ20 ページ／bit20-23=OS 予約4 ページ／bit24-31=無効ページ（恒久使用中マーク）**。初期値: **LO=$0000 / HI=$FFF0**。ページアドレス計算: `base = $C800 + page_no×256`。実装定数（kernel_forth・Ph.5 工程で変更）: `PAGE-POOL-BASE=$C800` / `PAGE-TOTAL=24` / `PAGE-USER-MAX=20` / `PAGE-BMP-HI-INIT=$FFF0`。（yuios_design v2.6 §2.2/§5.3/§8.4 と一致）

> **【v2.0 案A 改版・上記を以下に更新】** PAGE-POOL を高位集約縮小し **$DD00-$ECFF・16 ページ×256 B** とする。bit$n$ ↔ page$n$（page0=$DD00〜page15=$EC00）。bit 割当: **bit0-11=ユーザ12 ページ／bit12-15=OS 予約4 ページ／bit16-31=無効ページ（恒久使用中マーク）**。初期値: **LO=$F000 / HI=$FFFF**。ページアドレス計算: `base = $DD00 + page_no×256`。実装定数（5 定数）: `PAGE-POOL-BASE=$DD00` / `PAGE-TOTAL=16` / `PAGE-USER-MAX=12` / **`PAGE-BMP-LO-INIT=$F000`（★v2.0 新設必須）** / `PAGE-BMP-HI-INIT=$FFFF`。
>
> **★PAGE-BMP-LO-INIT 新設の根拠（v2.0 重要・CHAT51 §2.4 の見落とし是正）：** 24pg 構成では OS 予約4pg が page20-23（＝HI 側）に来たため LO 初期値は $0000 のままでよく、現行 BMP-INIT は `0 PAGE-BMP-LO !` とハードコードしていた。しかし 16pg 構成では OS 予約4pg が **page12-15（＝LO 側、page<16）** に来るため、LO 初期値も $F000（bit12-15=1）にしなければ予約ページが空きと誤認される。したがって定数 `PAGE-BMP-LO-INIT=$F000` を新設し、`BMP-INIT` を `PAGE-BMP-LO-INIT PAGE-BMP-LO !` へ変更すること（§14.4・§14.5・K39 参照）。
>
> **ALLOC ロジック整合（実測確認済）：** `MEM-ALLOC-PAGES` は `max_start = PAGE-USER-MAX - n`（=12-n）で start=0 から First Fit 走査し、`addr = PAGE-POOL-BASE + start×256`。ユーザを page0 始まり（bit0-11）に置けば走査は予約 page12-15 に到達しない（start 最大=11、n=1 時）。BMP-BIT-SET/CLR/@ の page≷16 分岐・`256×PAGE-POOL-BASE+`・`addr-PAGE-POOL-BASE` はすべて定数追従で**ロジック改変ゼロ**（v0.10.13b 実体 §855-1010 で確認）。変更は 5 定数と BMP-INIT の LO 代入のみ。
| UART-RX-RING-BUF | $F020-$F02F | **$FC46-$FC55** | 同上 |
| UART-RX-HEAD | $F030 | **$FC56** | 同上 |
| UART-RX-TAIL | $F032 | **$FC58** | 同上 |
| UART-RX-COUNT | $F034 | **$FC5A** | 同上 |
| UART-DRV-TID | $F036 | **$FC5C** | 同上 |
| UART-WAIT-TID | $F038 | **$FC5E** | 同上 |
| BC-STR | $F040 | **$FC60** | 同上 |
| STOR-DRV-TID | $F050 | **$FC64** | 同上 |
| STOR-WAIT-TID | $F052 | **$FC66** | 同上 |
| STOR-LAST-STAT | $F054 | **$FC68** | 同上 |
| (OS 共有領域、$FC6A-$FC7F が空き) | — | (22 B 余裕) | 将来のドライバ変数追加用 |

**重要：** 旧 $F0xx 系を $FCxx 系に集約することで、F000-FBFF を**全てタスクスタック専用**にできる。

**v1.1 領域分担原則：** OS 共有変数（$FC40-$FC7F）には scheduler 共有 / current_tid / flags / IRQ 共有 / ドライバ共有変数のみを置き、カーネル内部ワーク（L1_WK 等）は混入させない。これは review16.txt の「shared 変数と kernel work を混同しない」推奨に従う。

### 5.3 タスクスタック領域

§6 で詳細解析したとおり、**コール領域とデータ領域を完全分離**して配置する：

| tid | CALLSTK_TOP | DATASTK_TOP | コールスタック範囲 | データスタック範囲 |
|---|---|---|---|---|
| 0  | **$F07E** | **$F87E** | $F000-$F07E (128 B) | $F800-$F87E (128 B) |
| 1  | $F0FF | $F8FF | $F080-$F0FF (128 B) | $F880-$F8FF (128 B) |
| 2  | $F17F | $F97F | $F100-$F17F (128 B) | $F900-$F97F (128 B) |
| ... | ... | ... | ... | ... |
| 15 | $F7FF | $FBFF | $F780-$F7FF (128 B) | $FB80-$FBFF (128 B) |

旧設計との比較：

| 項目 | 旧（v1.2 / v0.11.3） | 新（v1.0） |
|---|---|---|
| CALLSTK_BASE (tid=0) | $FBCE | **$F07E**（v1.2修正、奇数$F07F→偶数$F07E）|
| DATASTK_BASE (tid=0) | $FB4E（暫定）/ $F9CE（旧） | **$F87E**（v1.2修正、奇数$F87F→偶数$F87E）|
| TASK_STK_GAP | $0100（衝突あり） | $0080（コールのみ／データのみ別領域） |
| 領域配置 | コール／データ混在 | **完全分離**（衝突不可能）|
| 1 タスクサイズ | 256 B（暫定 128 B 制限） | コール 128 B + データ 128 B = 256 B |
| 合計サイズ | 2 KB（衝突）／実 1.5 KB | **4 KB（独立領域）** |

### 5.4 テストバッファ

| 用途 | 旧 | 新 | 備考 |
|---|---|---|---|
| TEST_SRC_BUF | $E800 | **$EC00** | テスト領域へ移動（512 B） |
| TEST_DST_BUF | $EA00 | **$EE00** | 同上（512 B） |

> **【v2.0 案A 改版】** テストバッファ領域を $EC00-$EFFF(1KB) → **$ED00-$EFFF(768B)** へ縮小し、$EC00-$ECFF をPAGE-POOL の最上位ページ（page15＝予約）に充てる。kernel_forth の CONSTANT 群を以下へ再配置する（§14.4）。実体 v0.10.13b の該当 CONSTANT（§499-600）:
> - `TEST-SRC-BUF $EC00` / `TEST-DST-BUF $EE00`
> - `FT-NAME-BUF $EC00` / `FT-SRC-BUF $EC10` / `FT-DST-BUF $EE00` / `FT-STAT-BUF $EF00` / `FT-RW-BUF $EC00`
>
> 縮小後の配置案（768B 枠 $ED00-$EFFF に収める。詳細割付は §14.4 で確定）。なお過去に FT-NAME-BUF を $EC00→$5060、FT-STAT-BUF を $EF00→$5070（FileMgr 残余 $5060-$50FF）へ退避した前例があり、小バッファは同手段で $5060-$50FF へ退避可能（当該領域は現ビルドで全ゼロ＝実行時未使用を確認済）。

### 5.5 lnk23 セクション

| 項目 | 旧 | 新 | 備考 |
|---|---|---|---|
| CODE-START（Forceマシン） | $4400 | **$5100** | Forth ゾーン先頭へ【**v1.3: $5000→$5100**】 |
| DATA-START | $C000 | **$E000** | **【v1.7 修正】実体 ysd8800_kern.tgt は `DATA-START $E000`（Force VARIABLE データ領域）。v1.6 の「$C000 兼用」は机上値で実体と不一致だった（KY34: .tgt が真値）。案C で $C000-$C7FF は ProcMgr ロード領域となるため、DATA-START を実体どおり $E000 と明記し衝突を解消 ／ **【v2.0 案A 再改版】** DATA-START を **$E000→$DC00** へ移動（tgt 1 行変更）。Force 再ビルドで VARIABLE データ部が $DC00 起点へ自動追従（kernel_forth.fs 内に DATA-START 直接参照なし＝$533 FS-DATA-START は FS 用 LBA で無関係。§14.4 で確認済）。$DC00-$DCFF(256B) に実消費 84B(VAR 42個×2B) を収容（余裕 172B）。これにより旧 $E000-$EBFF の死蔵空きを解消し、辞書天井を $DBFF まで引き上げる |

---

## 6. スタック worst-case 解析

レビュー指摘 #5（review14.txt）に応えるため、各タスクのスタック消費を解析する。

### 6.1 コールスタック（return アドレススタック）の worst-case

#### 6.1.1 通常タスクの IPC4 CALL 経路

```
USER: IPC4-CALL
  ├─ Forthレベル：DOCOL の RSP push   = +2 (return addr)
  ├─ アセンブラ JSR 多段：典型 2-3 段  = +6
  ├─ DI/EI 区間内の関数呼出         = +4
  └─ スケジューラ JSR チェイン        = +6
合計： ~18 B（worst case）
```

#### 6.1.2 IRQ ハンドラのネスト

```
IRQ0 (timer):
  ├─ ハードウェア push: PC + FLAGS    = +4
  ├─ ハンドラ内で A/B/X 退避        = +6
  └─ scheduler 呼出までの JSR       = +4
IRQ1 (UART) が IRQ0 直後に発火する場合（マスク許可時）:
  └─ IRQ1 ハンドラがさらに +14
合計： ~28 B（双方発火 worst case）
```

#### 6.1.3 IPC4-CALL ＋ IRQ の合成 worst case

通常 CALL 中に IRQ が両方発火するシナリオ：
- 通常 18 B + IRQ 28 B = **46 B**

#### 6.1.4 nested JSR の制約

- Forth カーネル内では IPC4 や TASK プリミティブが多段の JSR チェインを構成
- Force コンパイラの実測：DOCOL+ INNER + 共有ワード呼出で典型 8 段（16 B）
- worst case で 16 段 ≈ 32 B

#### 6.1.5 コールスタック合計 worst case

| 項目 | サイズ |
|---|---|
| 通常 IPC4-CALL チェイン | 18 B |
| IRQ0 + IRQ1 ネスト | 28 B |
| Forth ワード深いネスト | 32 B |
| 安全マージン | 20 B |
| **合計** | **~98 B** |

→ **コールスタック 1 タスク 128 B**（$80）あれば余裕。

### 6.2 データスタック（パラメータスタック）の worst-case

#### 6.2.1 通常 Forth 演算

- DSP は X レジスタ管理、ワード境界で 2 B push/pop
- 典型 Forth ワードは 4-8 個のスタック深度
- `R> >R` 等で一時退避するパターンあり

#### 6.2.2 IPC4 メッセージ受け渡し

- IPC4-CALL: msg[4] + tid = 5 word push（pre）／ reply 4 word push（post）
- IPC4-RECV: 4 word push
- 一時的に 10 word（20 B）程度

#### 6.2.3 Force コンパイラ実行時

- compile 中に locals/temp で 16 word 程度（32 B）が見込まれる

#### 6.2.4 Dhrystone 等のベンチマーク

- 構造体ローカル展開で 20 word 程度（40 B）

#### 6.2.5 データスタック合計 worst case

| 項目 | サイズ |
|---|---|
| 通常 Forth | 16 B |
| IPC4 ピーク | 20 B |
| Compiler runtime | 32 B |
| Dhrystone | 40 B |
| 安全マージン | 20 B |
| **合計** | **~128 B** |

→ **データスタック 1 タスク 128 B**（$80）あれば余裕。

### 6.3 1 タスクあたりのスタック容量決定

| 項目 | 容量 |
|---|---|
| コールスタック | 128 B（$80）+ 余裕 64 B = **192 B（$C0）** |
| データスタック | 128 B（$80）+ 余裕 64 B = **192 B（$C0）** |
| GAP（衝突防止） | **0 B**（192 + 192 = 384 B = $180、ぴったり配置） |

### 6.3 1 タスクあたりのスタック容量決定

§6.1 と §6.2 の worst-case 解析より：

| 項目 | worst-case | 余裕込み |
|---|---|---|
| コールスタック | 98 B | **128 B（$80）** |
| データスタック | 128 B | **128 B（$80）** |
| 1 タスク合計 | 226 B | **256 B（$100）** |

**TASK_STK_GAP = $100**（コール $80 + データ $80）と確定する。

### 6.4 16 タスク全体のスタック領域配置

#### 6.4.1 配置方針：コール／データの**完全領域分離**

旧設計の根本欠陥は「同一領域内にコールスタックとデータスタックを交互配置」していたことである：

```
[旧設計（衝突あり）]
$F800  ┌─────────┐
       │ tid=N データ │
       │ tid=N コール │
       │ tid=N+1 データ│ ← tid=N コールと衝突しうる
       │ tid=N+1 コール│
       │ ...           │
$FBCF  └─────────┘
```

新設計では**コール領域とデータ領域を物理的に独立**させる：

```
[新設計（衝突不可能）]
$F000  ┌──────────────────────────────┐
       │ コールスタック領域 ($800=2KB)│ 16 タスク × $80
$F7FF  └──────────────────────────────┘
$F800  ┌──────────────────────────────┐
       │ データスタック領域 ($800=2KB)│ 16 タスク × $80
$FBFF  └──────────────────────────────┘
```

#### 6.4.2 配置サイズの正当性

- 16 タスク × $80（コール）= $800 = 2 KB
- 16 タスク × $80（データ）= $800 = 2 KB
- 合計 $1000 = 4 KB
- 配置：$F000-$FBFF（4 KB）にちょうど収まる

#### 6.4.3 アドレス計算式

```
CALLSTK_TOP(tid) = $F000 + (tid + 1) × $80 - 2   （= $F07E when tid=0）
DATASTK_TOP(tid) = $F800 + (tid + 1) × $80 - 2   （= $F87E when tid=0）
```

アセンブリ実装：`tid << 7` で 1 命令計算可能（hasm23 の SHL 対応）。

#### 6.4.4 旧設計との決定的違い

> **旧：「コール頂上から下に $100 下げてデータ頂上」という相対配置**
> → 隣接 tid のスタックが重なるリスク常時あり
>
> **新：コール領域とデータ領域が完全に独立した連続ブロック**
> → 物理的に重なれない構造

**衝突可能性をゼロにする設計上の不変条件**：
- `(tid+1) × $80 ≤ $800` であること（コール側）→ tid ≤ 15 で OK
- `(tid+1) × $80 ≤ $800` であること（データ側）→ tid ≤ 15 で OK
- → **MAX_TASKS = 16 まで確実に衝突しない**

#### 6.4.5 タスクごとの配置詳細

| tid | CALLSTK_TOP | DATASTK_TOP | コールスタック範囲 | データスタック範囲 |
|---|---|---|---|---|
| 0  | **$F07E** | **$F87E** | $F000-$F07E (128 B) | $F800-$F87E (128 B) |
| 1  | $F0FF | $F8FF | $F080-$F0FF (128 B) | $F880-$F8FF (128 B) |
| 2  | $F17F | $F97F | $F100-$F17F (128 B) | $F900-$F97F (128 B) |
| ... | ... | ... | ... | ... |
| 15 | $F7FF | $FBFF | $F780-$F7FF (128 B) | $FB80-$FBFF (128 B) |

### 6.5 スタック設計の最終確定

| 項目 | 値 |
|---|---|
| コールスタック領域 | $F000-$F7FF（2 KB） |
| データスタック領域 | $F800-$FBFF（2 KB） |
| 1 タスクあたりコール容量 | 128 B（$80） |
| 1 タスクあたりデータ容量 | 128 B（$80） |
| アドレス計算 | `BASE + (tid+1) × $80 - 2`（偶数化、tid << 7 で 1 命令）★v1.2修正 |
| **衝突可能性** | **なし**（領域が物理的に分離されている） |

**Ph.3.5-b の最重要設計判断：** 旧設計で根本欠陥だった「コール／データ混在配置」を**完全分離**することで、HANDOVER_CHAT22 で発生したスタック衝突バグを**構造的に再発不能**にする。これは単なるアドレス変更ではなく、設計の不変条件を変える本質的修正である。

### 6.6 スタック watermark 診断要件（v1.1 新設）

レビュー指摘（review15.txt 中程度②、review16.txt 論点 3）に応え、**設計要件**を明記する（実装は Ph.3.5 実装フェーズに持ち越し、案 A）。

**要件：**
- スタック領域および guard 領域は**起動時に既知値で初期化可能な配置**とする
- コール／データスタック未使用部：**$0000 で初期化**（push されると上書きされる）
- guard 領域 ($FC00-$FC3F)：**$A55A で初期化**（破壊検出用 canary）
- カーネル専用スタック ($4700-$477F)：**$0000 で初期化**、$4700 に canary $A55A を配置

**watermark 診断の概要（実装は Ph.3.5）：**
```
診断ワード `STACK-WATERMARK ( tid -- depth )`
  指定 tid のスタックを下方向から走査し、$0000 でない最初の位置を返す
  → タスクごとの最大スタック使用量が判明

診断ワード `CHK-GUARD ( -- ok? )`
  guard 領域 ($FC00-$FC3F) を全走査し、全要素が $A55A であれば true
  KERN_SP の canary ($4700) もチェック
  → スタックオーバーフロー／カーネルスタック破壊の検出
```

**運用方針：**
- Ph.3.5 実装後、Dhrystone・回帰テスト後に `STACK-WATERMARK` を全 tid で実行
- 想定（コール 98 B / データ 128 B）と実測の差分を確認、設計マージンの妥当性を検証
- `CHK-GUARD` はデバッグビルドで scheduler 周期ごとに実行（リリースビルドでは無効化）

**初期化コードの設計上の前提：**
カーネル `_kstart` で以下を実装する（実装は Ph.3.5）：
1. TCB プール、MsgPool、カーネル専用スタックを $0000 でクリア
2. guard 領域 $FC00-$FC3F を $A55A で埋める
3. KERN_SP の canary $4700 = $A55A
4. （オプション）コール／データスタック領域も $0000 でクリア

---

## 7. 領域成長予約の運用ルール

### 7.1 「予約」の定義

本書で「予約」と記した領域は：
- **現フェーズで配置・初期化を行わない**
- **将来の使用時まで実コードからの参照を禁止する**
- **アセンブラ／リンカで `.org` ジャンプの目印として使う**

### 7.2 成長時の手続き

予約領域に新規データを置く際は：
1. メモリマップ設計書（本書）を改版
2. 配置アドレスと用途を表に追加
3. アセンブラ EQU 定義／Forth CONSTANT を新規追加（既存変更禁止）
4. アドレス対照表を更新

これにより**メモリマップ管理が一元化**される。

### 7.3 予約消尽時の対応

予約 8 KB が枯渇した場合：
- まず**未使用 EQU 定義の整理**（廃止された変数の削除）
- 次に**Forth 辞書の縮減**（未使用ワードの削除）
- それでも足りなければ**メモリマップ全体の見直し**（→ 設計書 v2.0 改版）

### 7.4 stack guard 領域の運用（v1.1 新設）

レビュー指摘（review15.txt 重大②、review16.txt 推奨）に応え、guard 領域の運用ルールを定義する。

**目的：**
- データスタックオーバーフロー時に MMIO レジスタを破壊しないための「緩衝地帯」
- 破壊検出によるデバッグ支援

**配置：**
- $FC00-$FC3F（64 B）
- データスタック領域 ($F800-$FBFF) と OS 共有変数領域 ($FC40-$FC7F) の間

**初期化：**
- カーネル `_kstart` で起動直後に **$A55A** で全 64 B を埋める
- $A55A はビットパターンが目立ち、デバッグ時のメモリダンプで識別しやすい

**診断ワード `CHK-GUARD ( -- ok? )`（設計のみ、実装は Ph.3.5）：**
```forth
\ guard 領域全 32 ワード ($FC00-$FC3E) が $A55A であるかチェック
\ さらに KERN_SP の canary ($4700) も同様にチェック
\ どこかが破壊されていたら false、すべて健全なら true
\ 破壊位置を UART に出力する版（CHK-GUARD-VERBOSE）も推奨
```

**運用方針：**
- デバッグビルドでは scheduler 周期ごとに `CHK-GUARD` を実行（割込ハンドラから呼出）
- リリースビルドでは無効化（オーバーヘッド削減）
- `CHK-GUARD` 失敗時は UART にエラー出力後、無限ループ HALT で異常終了

**guard 64 B が十分か：**
レビュー指摘（review16.txt）どおり、guard の目的は「MMIO 即突入の防止」であり「オーバーフロー完全防止」ではない。64 B あれば暴走を 32 ワード分遅らせられ、その間に IRQ ハンドラが `CHK-GUARD` で検出可能。Ph.3.5 実装後の実測で不足が判明したら拡張を検討する。

---

## 8. 実装変更点まとめ

### 8.1 kernel.asm（v0.11.3 → v0.12.0）

| 変更カテゴリ | 内容 | 規模 |
|---|---|---|
| EQU 定義変更 | 全 RAM アドレス定数を新値に更新（カーネルワーク $4280→$4780、OS 共有 $F0xx→$FC4x） | 30 行修正 |
| EQU 新規追加 | MSG_POOL_BASE, MSG_POOL_FREE, MAX_TASKS, POOL_SIZE, KERN_SP_TOP, GUARD_BASE 等 | 8 行追加 |
| TCB プール容量 | $4000-$427F → $4000-$44FF（init_tcb ループ上限変更） | 1 行修正 |
| MsgPool 初期化 | init_msg_pool ルーチン新設、_kstart から呼出 | 50 行追加 |
| カーネル専用スタック確保 | $4700-$477F（128 B）、$4700 に canary $A55A | 5 行追加 |
| guard 領域初期化 | $FC00-$FC3F を $A55A で埋める _kstart コード | 10 行追加 |
| カーネルワーク領域 | $4280-$42A1 → $4780-$47AC（frequently used 領域内） | 全参照箇所修正 |
| TASK_CREATE スタック計算 | CALLSTK_BASE = **$F07E**、DATASTK_BASE = **$F87E**、tid<<7 計算（v1.2修正） | 8 行修正 |
| KERN_SP | $FBCE → **$477E**（カーネル専用スタック頂上、案 B 確定） | 1 行修正 |
| TEST_SRC_BUF/TEST_DST_BUF | $E800/$EA00 → $EC00/$EE00 | 2 行修正 |
| OS 共有変数領域参照 | UART_*/STOR_* 系 を $F0xx → $FC4x に変更 | 全 EQU 修正 |

合計：約 **100 行修正 / 73 行追加**

### 8.2 kernel_forth.fs（v0.7.2 → v0.8.0）

| 変更カテゴリ | 内容 |
|---|---|
| `CONSTANT` 値変更 | 全 $F0xx → $FC4x 系（OS 共有変数領域へ移動） |
| `BC-STR` | $F040 → **$FC60** |
| `STOR-*` | $F050-$F054 → **$FC64-$FC68** |
| `UART-*` | $F020-$F038 → **$FC46-$FC5E** |
| `PAGE-BMP-*` / `MEM-TID-ADDR` | $F010-$F014 → **$FC40-$FC44** |
| `CALLSTK-BASE` | $FBCE → **$F07E**（tid=0 コールスタック頂上、v1.2修正）|
| `DATASTK-BASE` | $FB4E → **$F87E**（tid=0 データスタック頂上、v1.2修正）|
| `TASK-STK-GAP` | $0100 → **$0080**（領域分離設計のため、コール／データそれぞれの GAP） |
| **新規 `CONSTANT`** | `GUARD-BASE = $FC00`、`GUARD-SIZE = $40`、`KERN-SP-TOP = $477E` |
| **新規ワード（設計のみ、実装 Ph.3.5）** | `CHK-GUARD`、`STACK-WATERMARK` |

### 8.3 ysd8800_kern.tgt（v0.2 → v0.3 ／ **v2.0: v0.5→v0.6 予定**）

| 項目 | 旧 | 新 |
|---|---|---|
| CODE-START | $4400 | **$5100**【v1.3: $5000→$5100】 |
| DATA-START | $C000 | **$E000**（実体 .tgt 準拠・v1.7 確定） |
| **DATA-START【v2.0 案A】** | **$E000** | **$DC00**（VARIABLE データ領域を高位集約。tgt v0.5→v0.6 で 1 行変更。Force 再ビルドで自動追従） |

> **【v2.0 KY19 同期】** tgt のメモリマップコメント（$F000-$F7FF Forth Workspace ほか）も v2.0 マップへ更新すること。tgt v0.5 のコメントは memmap v1.6 を参照しているため、v2.0 改版時に依存設計書バージョンを `yuios_memmap_design_v2_0.md` へ同期改版する。

### 8.4 lnk23 SECTION

新メモリマップに整合するよう SECTION 配置を調整：

```
SECTION code 0x0000 kernel.asm.bin kernel.asm.sym
SECTION forth $5100 forth.asm.bin forth.asm.sym
SECTION data  $E000 ...   ; v1.7: 実体 .tgt DATA-START=$E000 に整合
```

> **【v2.0 案A】** data セクションを **$DC00** に整合させる（DATA-START 移動に追従）。
> ```
> SECTION data  $DC00 ...  ; v2.0: 案A DATA-START=$DC00 に整合
> ```
> なお実ビルドは build_v0_10_13.sh（Force→sed→hasm23 2パス→lnk23）であり、Force の DATA-START は tgt から取得されるため、tgt 1 行変更で連動する。lnk23 SECTION 記述を併用する場合は本値へ更新すること。

---

## 9. KY 項目（v1.1）

| # | 危険 | 防止策 |
|---|---|---|
| K21 | アドレス変更箇所の修正漏れで「動作するように見えて別アドレスを叩く」事故 | §5 アドレス対照表ベースで grep を使い**機械的に**全箇所抽出。レビュー前にチェックリスト化 |
| K22 | $F0xx → $FCxx への変更が Forth `CONSTANT` の **新規定義のみ追加**で旧定義が残ると、参照先が二重化 | 旧 `CONSTANT` 定義は**完全削除**する。新値で再定義禁止（同名変数の値だけ変更） |
| K23 | タスクスタック領域分離の実装誤りで再び衝突 | 領域 $F000-$F7FF（コール）と $F800-$FBFF（データ）の独立性をアサート。`tid << 7` 計算式を全箇所で統一 |
| K24 | $FC80- 以降の RAM 利用が混入 | コードレビューで `LDW/STW` の destination アドレスをチェック。$FC80 以降への書込は MMIO レジスタ以外禁止 |
| K25 | カーネル成長予約 $4800-$4FFF への意図しない write | 起動直後に予約領域を 0xFF 等の sentinel で埋め、定期チェック（デバッグビルドのみ） |
| K26 | **【v1.7 改訂】** Forth 辞書が $BFFF を超えて伸び、ProcMgr ロード領域($C000-)/MemMgr プール($C800-) を侵食 | lnk23 のリンクログで HERE 値が **$BFFF** を超えたら警告。Forth 起動時の HERE 値もコンソール出力。成長予約は v1.7 で廃止したため、超過は即・領域侵食を意味する（旧版より重大度上昇） |
| **K35** | **【v1.7 新設・案C】** ProcMgr ロード領域($C000-$C7FF) と MemMgr プール($C800-$DFFF) の境界跨ぎ。C バイナリが 2 KB 超で MemMgr プールを破壊 | ProcMgr の PROC_EXEC で FILE_STAT サイズ > 2 KB を -1 で弾く（design v2.6 §8.2.1）。Ph.5 実装時に dhry_timer 等で実測（R5） |
| **K36** | **【v1.7 新設・案C】** kernel_forth の MemMgr 定数が案C 前の旧値（POOL-BASE=$C000/BMP-HI-INIT=$F000）のまま実装され、新マップ($C800/$FFF0)と不一致 | Ph.5 実装着手時に 4 定数（POOL-BASE/TOTAL/USER-MAX/BMP-HI-INIT）変更を 1変更1検証 で確実に拾う。変更後 ALLOC が bit0-23 のみ正走査を実検証（R1） |
| K27 | スタック容量 128 B が不足してオーバーラン | Dhrystone 等のベンチマークで実測。Ph.3.5 で `STACK-WATERMARK` 診断ワード実装、回帰テスト後に全 tid で実行（§6.6 設計済） |
| K28 | MsgPool 配置 $4500-$46FF と TCB プール $4000-$44FF の境界 1 byte ずれで参照混乱 | EQU 定数 `MSG_POOL_BASE EQU $4500` を**唯一の真値**とし、即値ハードコード禁止 |
| K29 | テストバッファ $EC00/$EE00 への移動で既存テストが失敗 | 移動後にテストバッファ参照箇所を grep で全件確認。最低限 Dhrystone 回帰テスト実施 |
| K30 | カーネルコード成長予約 $2000-$3FFF まで実コードが侵入する想定外事態 | hasm23 のリンクマップで先頭・末尾アドレスを確認。$1FFF を超えたら警告 |
| **K31** | **stack guard 領域の初期化忘れ／破壊検出機構欠落**（v1.1 新設） | `_kstart` で $FC00-$FC3F を $A55A で埋める処理を必須化。デバッグビルドで `CHK-GUARD` を scheduler 周期で実行 |
| **K32** | **KERN_SP の canary ($4700) 上書きで kernel stack 破壊が検出できなくなる**（v1.1 新設） | `_kstart` で $4700 = $A55A 設定を必須化。`CHK-GUARD` で監視 |
| **K33** | **OS 共有変数領域 64 B（$FC40-$FC7F）の容量不足**（v1.1 新設） | 新規ドライバ追加時は対照表 §5.2 を更新し、$FC7F を超えないことを確認。超過時はカーネル成長予約への移動を検討 |
| **K34** | **kernel work と shared 変数の混在**（v1.1 新設、review16.txt 推奨） | コードレビューで「カーネル内部状態」（CUR_TASK 等）は $4780- 側、「ドライバ間共有」は $FC40- 側に置くことを徹底 |
| **K35** | **「ROM 性質」表記の誤解で write 禁止前提領域を誤って初期化**（v1.1 新設） | 表記を「write 禁止前提」に統一。$0000-$3FFF への STW 命令は実装上禁止 |
| **K36** | **v1.3 変更：Forth 辞書先頭 $5100 変更の実装漏れ**（v1.3 新設）：ysd8800_kern.tgt の CODE-START や lnk23 スクリプトの SECTION forth が旧値 $5000 のまま残存する | §8.3・§8.4。ysd8800_kern.tgt と lnk23 スクリプトを grep で確認し `$5000` を `$5100` に変更。FileMgr 実装着手時に必ず確認する |
| **K37** | **Force コンパイラのランタイムワーク領域とカーネルデータ領域の衝突**（★v1.4 新設、HANDOVER_CHAT33 真因②） | Force ランタイム（`_FMUL_A/B/R` 等）を本設計書 §3.5 / アドレス対照表で一元管理する。Force 改版時は本設計書を真値とし、ハードコード値は禁止。コードレビューで Force 出力 ASM 中の EQU 定義アドレスが $4780-$47BF 領域に収まっていることを確認 |
| **K38** | **【v2.0 新設・案A】DATA-START 移動（$E000→$DC00）後、Force VARIABLE データ部が辞書末尾（$DBFF 想定）と衝突／DATA 枠 256B を超過する** | tgt 1 行変更後にフルビルドし、辞書実終端（非ゼロ最終アドレス）が $DBFF を超えないこと、VARIABLE データ部（実消費 84B）が $DC00-$DCFF に収まることを .sym/.bin 実測で確認（§14.2）。コード成長余地 vs 次領域($DC00)の余裕を数値でレビュー必須項目化（CHAT48 §5.2 踏襲・§14.6） |
| **K39** | **【v2.0 新設・案A】16pg 構成で OS 予約4pg が LO 側(page12-15)へ移動するのに、BMP-INIT の LO=0 ハードコードが残置され、予約ページが空きと誤認されて ALLOC が予約領域を払い出す** | 定数 `PAGE-BMP-LO-INIT=$F000` を新設し、`BMP-INIT` を `PAGE-BMP-LO-INIT PAGE-BMP-LO !` へ変更（§5.2・§14.4・§14.5）。変更後、`BMP-FREE-COUNT` が **12**（=USER-MAX）を返すこと、ALLOC が page0-11 のみ払い出し page12-15 に触れないことを emu23 で実検証（1 変更 1 検証） |

---

## 10. 検証計画

### 10.1 メモリマップ検証

| 項目 | 方法 |
|---|---|
| EQU 定数の整合 | hasm23 アセンブル成功＋ sym ファイルで全シンボルアドレスを目視確認 |
| Forth `CONSTANT` の整合 | yuios_v12.bin リンク後、emu23 でカーネル起動成功 |
| アドレス境界の独立性 | TCB ($4000-$44FF) / MsgPool ($4500-$46FF) / カーネルワーク ($4700-$47FF) のマジックパターン書込でリーク検出 |

### 10.2 機能回帰テスト

| 項目 | 期待結果 |
|---|---|
| Ph.1 IPC4 Hello/Echo | 全テスト合格 |
| Ph.2 メモリマネージャ | 全テスト合格 |
| Ph.3-A UART ドライバ | "ABCXD" 出力 |
| Ph.3-B ストレージドライバ | 既知バグの再現を確認、Ph.3.5 統合後に再検証 |
| **Dhrystone (基準値 826 DPS)** | **回帰チェック必須** |

### 10.3 スタック分離検証（最重要）

複数タスクを同時動作させ、コール／データスタックの領域に**マジック値**を書き込み、相互侵入が起きないことを確認。

検証手順：

```
1. 全タスク（tid=0..15）を起動し、各 tid のコール／データスタックの未使用部に
   固有のマジック値を書込む：
   - tid=N のコールスタック未使用部 = $A500 + N
   - tid=N のデータスタック未使用部 = $5A00 + N

2. 全タスクを複数周期動作させる（IPC4-CALL や TASK-SLEEP 等で切替）。

3. 各 tid のスタック領域を再走査し、マジック値が保持されていることを確認。
   （実際に push/pop した範囲は上書きされるため、未使用部のみ検証）

4. 隣接 tid の領域（例：tid=0 のコール末尾と tid=1 のコール先頭）を境界として、
   一方への書込が他方に侵入していないことを確認。
```

特に確認すべき境界（§6.4.5 表より）：

| 境界 | アドレス | 意味 |
|---|---|---|
| tid=0/1 コール境界 | **$F07E** / $F080 | 隣接 tid 間の独立性 |
| tid=15 コール末尾 | $F7FF | コールスタック領域全体の上限 |
| tid=0 コール / tid=0 データ | **$F07E** / $F800 | コール領域とデータ領域の独立性（最重要）|
| tid=15 データ末尾 | $FBFF | データスタック領域全体の上限。直後 $FC00 が guard 領域 |

### 10.4 guard 領域・KERN_SP canary 検証（v1.1 新設）

| 項目 | 方法 |
|---|---|
| guard 初期化 | `_kstart` 実行直後に $FC00-$FC3F の全 64 B が $A55A であることを emu23 で確認 |
| KERN_SP canary | `_kstart` 実行直後に $4700 が $A55A であることを emu23 で確認 |
| `CHK-GUARD` 動作 | 正常時 true、guard 領域に故意に異常値を書込んだ場合 false が返ることを確認 |
| カーネル動作中の guard 健全性 | Dhrystone 完走後に `CHK-GUARD` が true を返すことを確認 |
| **MMIO 突入防止の確認** | デバッグ用に故意のスタックオーバーフローを発生させ、guard で停止することを確認（destructive test、別ビルドで実施） |

---

## 11. 設計レビュー観点

レビュー時に確認していただきたい項目：

- [ ] **全体マップの妥当性**（§4.1）：8 ゾーン分割の論理的一貫性（v1.1 で guard / kernel stack 追加）
- [ ] **MMIO サイズの正確性**（§4.1, §4.2）：$FC80-$FFFF = 896 B の計算妥当性（v1.1 修正）
- [ ] **MMIO 境界 $FC80- 固定**（§4.2）：絶対 RAM 禁止の宣言が明確か
- [ ] **stack guard 領域 $FC00-$FC3F**（§4.1, §7.4）：MMIO 突入防止の妥当性、64 B サイズの十分性（v1.1 新設）
- [ ] **KERN_SP 専用配置 $4700-$477F**（§4.1, §5.1）：案 B 採用の妥当性、128 B 容量（v1.1 確定）
- [ ] **canary 配置 $4700 = $A55A**（§4.1, §7.4）：kernel stack 破壊検出の妥当性（v1.1 新設）
- [ ] **カーネルワーク細分**（§4.1, §5.1）：frequently used 64 B + reserved growth 64 B の分割（v1.1 新設）
- [ ] **OS 共有変数領域 64 B 縮退**（§4.1, §5.2）：将来のドライバ追加余裕として 22 B 空きで十分か（v1.1）
- [ ] **MsgPool 配置 $4500-$46FF**（§4.3）：TCB 隣接の妥当性
- [ ] **カーネル成長予約 2 KB**（§4.4）：将来需要見積もりが現実的か
- [ ] **【v1.7 案C で廃止】** ~~Forth 成長予約 8 KB（§4.5）~~ → $C000-$DFFF は ProcMgr ロード領域＋MemMgr プールへ再分割。辞書逼迫時は §4.5.1 / v2.0 で対処（HERE 監視 K26 継続）
- [ ] **アドレス対照表の網羅性**（§5）：修正漏れの可能性が残っていないか
- [ ] **スタック worst-case 解析**（§6）：見積もり方法とマージンの妥当性
- [ ] **スタック領域完全分離**（§6.5）：旧設計の根本欠陥が再発しない構造になっているか
- [ ] **stack watermark 診断要件**（§6.6）：設計のみ記載、実装 Ph.3.5 持ち越しの妥当性（v1.1 新設）
- [ ] **領域成長予約の運用ルール**（§7）：実用的か
- [ ] **`CHK-GUARD` ワード設計**（§7.4）：診断機能として十分か（v1.1 新設）
- [ ] **「write 禁止前提」表現**（§4.1）：旧「ROM」表記からの移行が明確か（v1.1）
- [ ] **既存コード変更規模**（§8）：100 行修正 / 73 行追加の見積もり妥当性
- [ ] **KY 項目**（§9）：実装時の注意点として十分か（v1.1 で K31〜K35 追加）

---

## 12. オープン項目

### 12.1 v1.1 で確定した論点

v1.0 で残されていた論点は、レビュー（review15.txt / review16.txt）と本書 v1.1 で全て確定済：

| 論点 | v1.0 状態 | v1.1 確定内容 |
|---|---|---|
| KERN_SP 配置 | 案 A/B/C 提示、推奨案 B | **案 B 確定**：$4700-$477F 専用カーネルスタック、$4700 canary $A55A |
| MMIO サイズ | 384 B 誤記 | **896 B に修正**（$FFFF - $FC80 + 1 = $0380） |
| stack guard 領域 | なし | **$FC00-$FC3F（64 B）新設**、$A55A 埋め初期化 |
| カーネルワーク領域構造 | 256 B 単一領域 | **frequently used 64 B + reserved growth 64 B + KERN_SP 128 B に細分** |
| stack watermark 診断 | 未言及 | **§6.6 に設計のみ記載、実装は Ph.3.5**（案 A） |
| 「ROM」表現 | 「ROM」表記 | **「write 禁止前提」に変更** |

### 12.2 後続フェーズへ持ち越し

| 項目 | 持ち越し先 | 備考 |
|---|---|---|
| 実装後のスタック実消費量計測 | Ph.3.5 実装フェーズ | `STACK-WATERMARK` ワード実装、Dhrystone 後に全 tid で実行 |
| `CHK-GUARD` ワード実装 | Ph.3.5 実装フェーズ | 設計は §7.4 確定済 |
| `_kstart` での guard 初期化 | Ph.3.5 実装フェーズ | $FC00-$FC3F = $A55A、$4700 = $A55A の書込み |
| カーネル成長予約の具体配置 | Ph.4 以降（FileMgr 設計時） | 本書 §4.4 の予約サイズ内で確定 |
| Forth 成長予約の具体配置 | Ph.4 以降（FileMgr/Shell/libc 設計時） | 本書 §4.5 の予約サイズ内で確定。楽観的見積もりのため監視必須 |
| カーネルコード write 禁止前提領域 → ROM 化 | Ph.6 以降 | FPGA 実装時に ROM 配置 |
| ベンチマーク用スタック計測ツール | Ph.5 以降 | Dhrystone 等の実測精度向上 |

---

## 13. 関連文書

- `yuios_ipc4_pool_design_v1_1.md`：IPC4 共通メッセージプール方式（FIX 済、本書の前提）
- `yuios_tcb_design_v1_1.md`：TCB レイアウト改版（FIX 済、本書の前提）
- `yuios_kernel_memmap_v1_2.md`：旧メモリマップ仕様書（本書で代替）
- `HANDOVER_CHAT22.docx`：Ph.3.5 引継ぎ
- `review11.txt`〜`review14.txt`：IPC4 Pool / TCB 設計のレビュー履歴
- `review15.txt`：本書 v1.0 のレビュー指摘
- `review16.txt`：本書 v1.0 オープン論点（KERN_SP 等）への有識者推奨
- `kernel_v11_3.asm`：現行カーネル実装（v0.11.3）
- `kernel_forth_v0_7_2.fs`：現行 Forth カーネル（v0.7.2）
- `yuios_design_v2_6.md`：Ph.5 ProcMgr/MemMgr 上位設計（v2.0 の前提）【v2.0 追加】
- `yuios_cproc_layout_design_v1_1.md`：C-process 区画 $C040/$C380/$C680（承認済）【v2.0 追加】
- `ysd8800_kern.tgt`：DATA-START 実体の真値（KY34）【v2.0 追加】
- `kernel_forth_v0_10_13b.fs`：MemMgr 定数・BMP ロジックの実体（§855-1010 / §499-662）【v2.0 追加】
- `HANDOVER_CHAT51.md`：F55 検証成功・案A 方向性確定の実測根拠【v2.0 追加】
- `kaizen.txt`：原則43（再設計タスクは本質的動機を実アーティファクトで確認）【v2.0 追加】

---

## 14. v2.0 案A：高位再配置（コード成長余地確保）

> 本章は v2.0 の確定内容を集約する。旧章（§4.1/§4.5/§5.2/§5.4/§5.5/§8.3/§8.4）は KY41 に従い削除せず注記のみとし、矛盾時は **本章を正**とする。本章はドラフトであり、有識者レビュー・承認をもって確定する。

### 14.1 解決すべき本質（kaizen 原則43 の契機）

Forth カーネルコードは `CODE-START=$5100` から**連続成長**する（辞書は HERE 単純連続で、途中に他用途領域を挟めない）。コードの真上を PAGE-POOL($C800)・DATA($E000) が塞ぐため、**コードの天井 = PAGE-POOL 手前の $C7FF** に固定される。Ph.6 Shell を Forth 実装するとコードが増え $C800 に激突して**破綻**する（CHAT48「Ph.6 で再発必至」）。

**コードを増やす唯一の方法は PAGE-POOL の起点($C800)を上げること**である。PAGE-POOL を $C800 起点に残したまま縮小しても天井は $C800 のままで、コードは増えない。したがって DATA/PAGE-POOL/テストバッファを高位へ集約し、PAGE-POOL を高位へ動かす案A が不可避となる。これが v1.7 §4.5.1 が予告した「v2.0 再配置」の本質である。

### 14.2 設計入力＝実測値（v0.10.13b 現物計測・KY34）

CHAT51 でフルビルド（errors=0 / W001=0）し現物計測した値を設計入力とする。机上の v1.7 値ではなく実測値を正とする。

| 項目 | 実測値 | 計測方法 |
|---|---|---|
| **Forth コード実終端** | **$C165** | yuios_v0_10_13.bin 非ゼロ最終（$5100-$C165=28774B） |
| WORD_OS_START | $C150 | .sym（※OS_START はコード末尾ではない。真終端=$C165） |
| コード現空き | $C166-$C7FF = 1690B | PAGE-POOL 手前まで |
| PAGE-POOL（現） | $C800-$DFFF/24page/6KB | 定数 PAGE-POOL-BASE=$C800 |
| └ PAGE-POOL 常駐使用 | **0page** | 唯一の要求元 MEM-TEST-TASK は確保即 FREE 型。常駐要求ゼロ |
| DATA（VARIABLE） | $E000- 実消費 **84B** | .sym VAR_* 42個×2B（$E000-$E053） |
| └ DATA 死蔵空き | $E054-$EBFF = 2988B | 3KB 枠に対し実消費 84B のみ |
| テストバッファ | $EC00-$EFFF/1KB | TEST-SRC/DST・FT-* 系 |
| Workspace/stack/MMIO | $F000-$FFFF | **移動不可**（CALLSTK-BASE=$F07E/DATASTK-BASE=$F87E/MMIO） |

**KY34 是正事項：** v1.7 §4.1 は「$E000-$EBFF＝Forth ワークスペース」と記載するが、tgt 実体では $E000- は VARIABLE データ領域（DATA-START）であり、ワークスペース(HERE/スタック)の実体は **$F000-$F7FF**（ysd8800_kern.tgt コメント $F000-$F7FF Forth Workspace）。v1.7 机上値の誤記として記録し、本書では tgt 実体を正とする。

### 14.3 v2.0 確定配置（案A）

| 領域 | 現行(v1.7) | v2.0 確定 | サイズ | 変更手段 |
|---|---|---|---|---|
| Forth コード | $5100-$C165 | **$5100-$DBFF** | 天井 +5.0KB | 自動追従（辞書連続成長） |
| DATA（VARIABLE） | $E000 | **$DC00-$DCFF** | 256B(実84B+余裕172B) | **tgt: DATA-START 変更** |
| PAGE-POOL | $C800-$DFFF/24pg | **$DD00-$ECFF/16pg** | 4KB | kernel_forth 定数 |
| テストバッファ(大) | $EC00-$EFFF | **$ED00-$EFFF** 縮小 | 768B | CONSTANT 再配置 |
| テストバッファ(小・退避) | — | **$5060-$50FF 活用** | 160B | 前例踏襲（FileMgr 残余） |
| ProcMgr ロード領域 | $C000-$C7FF | **不変** | 2KB | — |
| Workspace/stack/MMIO | $F000-$FFFF | **不変** | — | — |

**配置の連続性（隙間・重複ゼロを検算済）：**
```
$5100 ┬ Forth 辞書（天井 $DBFF）
$DC00 ┼ DATA(VARIABLE) 256B  ($DC00-$DCFF)
$DD00 ┼ PAGE-POOL 16pg*256=4KB  ($DD00-$ECFF)
$ED00 ┼ テストバッファ 768B  ($ED00-$EFFF)
$F000 ┴ Workspace/stack/MMIO（不変）
```
- PAGE-POOL: page0=$DD00 … page11(user末)=$E800 … page12-15(予約)=$E900-$ECFF（終端 $ECFF）
- **結果: コード天井 $C7FF→$DBFF、コード余地 1690B→6810B(6.65KB)（+5.0KB）**

C-process 区画（ProcMgr ロード領域 $C000-$C7FF 内）は yuios_cproc_layout_design v1.1 で確定済の **crt0=$C000 / CODE=$C040 / RUNTIME=$C380 / DATA=$C680** を維持する（CHAT51 F55 検証で実証）。本領域は案A で不変。

### 14.4 案A 実現に必要な変更（実測確認済）

**(1) ysd8800_kern.tgt（v0.5→v0.6）：1 行**
- `DATA-START $E000 → $DC00`
- kernel_forth.fs 内に DATA-START 直接参照なし（$533 FS-DATA-START は FS 用 LBA で無関係）。Force 再ビルドで VARIABLE データ部が新アドレスへ**自動追従**（ソース側手当て不要）。
- tgt コメントのメモリマップ・依存設計書バージョンを v2.0 へ同期（KY19）。

**(2) kernel_forth.fs 定数（5 定数＋BMP-INIT）：**

| 定数/ワード | 現行(v0.10.13b) | v2.0 | 行（実体） |
|---|---|---|---|
| `PAGE-POOL-BASE` | $C800 | **$DD00** | 659 |
| `PAGE-TOTAL` | 24 | **16** | 660 |
| `PAGE-USER-MAX` | 20 | **12** | 661 |
| `PAGE-BMP-HI-INIT` | $FFF0 | **$FFFF** | 662 |
| **`PAGE-BMP-LO-INIT`（★新設）** | （無・LO=0 ハードコード） | **$F000** | 661-662 付近に追加 |
| `BMP-INIT` の LO 代入 | `0 PAGE-BMP-LO !` | **`PAGE-BMP-LO-INIT PAGE-BMP-LO !`** | 861 |

**(3) テストバッファ CONSTANT 再配置（768B 枠 $ED00-$EFFF＋退避 $5060-$50FF）：**

| CONSTANT | 現行 | v2.0 案 | 行 |
|---|---|---|---|
| `TEST-SRC-BUF` | $EC00 | **$ED00** | 499 |
| `TEST-DST-BUF` | $EE00 | **$EE00**（枠内・不変可） | 500 |
| `FT-NAME-BUF` | $EC00 | **$5060**（退避・前例あり） | 591 |
| `FT-SRC-BUF` | $EC10 | **$ED10** | 592 |
| `FT-DST-BUF` | $EE00 | **$EE00**（不変可） | 593 |
| `FT-STAT-BUF` | $EF00 | **$EF00**（枠内・不変可） | 594 |
| `FT-RW-BUF` | $EC00 | **$ED00** | 600 |

> 上表は配置「案」。768B 枠内で TEST-SRC/FT-RW(共用1KB→要縮小)・FT-DST(512B)・FT-STAT(256B) が衝突しないよう、実装着手時に最終割付を 1 変更 1 検証で確定する（FT-RW-BUF が src/dst 共用 1024B のため、768B 枠では縮小か退避併用が要る点に注意）。退避先 $5060-$50FF(160B) は FileMgr 残余で現ビルド全ゼロ＝実行時未使用を確認済。

### 14.5 PAGE-BMP-INIT 16pg 構成 再計算（K39 対応）

16pg 構成では page0-15 が LO 側、page16-31 が HI 側に入る。OS 予約4pg は **page12-15（LO 側）** に来るため、LO 初期値の手当てが必須（CHAT51 §2.4 では HI のみ言及で見落とし）。

| 項目 | 値 | 根拠 |
|---|---|---|
| ユーザ | page0-11（bit0-11） | ALLOC は start=0 始まり前提。空き=0 |
| OS 予約 | page12-15（bit12-15） | 使用中=1。LO に来る |
| 無効 | page16-31（bit16-31） | 恒久使用中=1 |
| **PAGE-BMP-LO-INIT** | **$F000** | bit12-15=1（予約）、bit0-11=0（user 空き） |
| **PAGE-BMP-HI-INIT** | **$FFFF** | bit16-31=1（無効16pg） |
| 検算: 空き(bit=0)数 | **12** | =PAGE-USER-MAX。32bit 値 $FFFFF000 |

**ALLOC ロジック整合：** `max_start = PAGE-USER-MAX - n`(=12-n)、`addr = PAGE-POOL-BASE + start*256`。ユーザ page0 始まりにより予約 page12-15 に到達しない。BMP-BIT-SET/CLR/@ の page≷16 分岐は 16pg でも正常（page12-15<16→LO 側、page16+→HI 側）。**ロジック改変ゼロ**（v0.10.13b §855-1010 で確認）。

### 14.6 回帰計画

tgt 変更 → Force 再ビルド → 全シンボル再配置となるため、回帰は広い。

| 区分 | 内容 | 必須度 |
|---|---|---|
| ツール回帰 | **Dhrystone**（ツールチェーン改修時の標準回帰。kaizen 規定） | 必須 |
| OS 機能回帰 | IPC4 Hello/Echo・MemMgr 全 API・UART・ストレージ・**FileMgr 全 API** | 必須 |
| MemMgr 専用 | `BMP-FREE-COUNT`=12 確認、ALLOC が page0-11 のみ払い出し・page12-15 不可侵を emu23 で実検証（K39） | 必須 |
| 境界検査 | 辞書実終端 ≦ $DBFF、DATA 部 ⊆ $DC00-$DCFF、PAGE-POOL ⊆ $DD00-$ECFF を .sym/.bin 実測（K38） | 必須 |
| 範囲外 | $F000 以降（スタック/MMIO）は不変のため回帰範囲外 | — |

**レビュー必須項目（CHAT48 §5.2 踏襲）：** 辞書終端 vs 次領域($DC00)の余裕を**数値で明記**し、コード成長余地を設計レビューの必須確認項目とする。

### 14.7 設計目標と適用範囲（かやぬま指示）

- Dhrystone(21KB) は MMU 対応(Ph.8)の将来対応。当面は 2KB 枠に収まるプログラムのロードで対処（C-process 区画 $C000-$C7FF）。
- FILEMGR-TEST 全量(8.3KB)復活までは求めないが、**FileMgr 健全性をある程度担保できる縮小版試験**は載せたい。v2.0 のコード余地 +6.6KB で Shell＋縮小版 FileMgr 試験の同居（目標 +6〜8KB の下限）を狙う。FILEMGR-TEST のサブセット設計は別タスク。

### 14.8 工程上の位置づけ

- 本書はドラフト。**有識者レビュー → 承認 → 実装**の順を厳守する（設計書承認前の実装禁止・原則43）。
- 承認後の実装順序（案）：① tgt DATA-START 変更 → Force 再ビルドで DATA 自動追従を確認 → ② kernel_forth 5 定数＋BMP-INIT 変更（1 変更 1 検証）→ ③ テストバッファ再配置 → ④ フルビルド・境界実測（K38）→ ⑤ 回帰（§14.6）→ ⑥ memmap v2.0 を「確定」へ。
- v2.0 確定後に Ph.5 Step4 本体（C バイナリ PROC_EXEC/PROC_WAIT 経由ロード・実行）へ進む。

---

*--- 以上 ---*
